---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alertenrichmentv1beta1s.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AlertenrichmentV1Beta1
    listKind: AlertenrichmentV1Beta1List
    plural: alertenrichmentv1beta1s
    singular: alertenrichmentv1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertenrichmentV1Beta1 is the Schema for the AlertenrichmentV1Beta1s
          API. Manages Grafana Cloud Alert Enrichment https://grafana.com/docs/grafana-cloud/alerting-and-irm/alerting/configure-notifications/alert-enrichment/.
          Alert enrichment is currently in public preview. Grafana Labs offers limited
          support, and breaking changes might occur prior to the feature being made
          generally available.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertenrichmentV1Beta1Spec defines the desired state of AlertenrichmentV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: AlertenrichmentV1Beta1Status defines the observed state of
              AlertenrichmentV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alertrulev0alpha1s.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AlertruleV0Alpha1
    listKind: AlertruleV0Alpha1List
    plural: alertrulev0alpha1s
    singular: alertrulev0alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertruleV0Alpha1 is the Schema for the AlertruleV0Alpha1s API.
          Manages Grafana Alert Rules. This resource is currently in alpha and is
          subject to change. Grafana 12.4+ users must enable the kubernetesAlertingRules
          feature toggle https://grafana.com/docs/grafana/latest/setup-grafana/configure-grafana/feature-toggles/.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertruleV0Alpha1Spec defines the desired state of AlertruleV0Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: AlertruleV0Alpha1Status defines the observed state of AlertruleV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: contactpoints.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ContactPoint
    listKind: ContactPointList
    plural: contactpoints
    singular: contactpoint
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ContactPoint is the Schema for the ContactPoints API. Manages
          Grafana Alerting contact points. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#contact-points
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContactPointSpec defines the desired state of ContactPoint
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The password component of the basic auth credentials to use.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The DingDing webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The discord webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The Google Chat webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            Personal Access Token that is used as a bearer authorization header.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            Password to use for Jira authentication.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        userSecretRef:
                          description: |-
                            (String, Sensitive) Username to use for Jira authentication.
                            Username to use for Jira authentication.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            The password to use when making a call to the Kafka REST Proxy
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        restProxyUrlSecretRef:
                          description: |-
                            (String, Sensitive) The URL of the Kafka REST proxy to send requests to.
                            The URL of the Kafka REST proxy to send requests to.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        oncallIntegrationRef:
                          description: Reference to a Integration in oncall to populate
                            url.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        oncallIntegrationSelector:
                          description: Selector for a Integration in oncall to populate
                            url.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                        urlSecretRef:
                          description: The OnCall webhook URL (from secret).
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The OpsGenie API key to use.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        integrationKeySecretRef:
                          description: |-
                            (String, Sensitive) The PagerDuty API key.
                            The PagerDuty API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            The Pushover API token.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                        userKeySecretRef:
                          description: |-
                            (String, Sensitive) The Pushover user key.
                            The Pushover user key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The SensuGo API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            A Slack API token,for sending messages directly without the webhook method.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Slack webhook URL,for sending messages via the webhook method.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS access key ID used to authenticate with Amazon SNS.
                            AWS access key ID used to authenticate with Amazon SNS.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS secret access key used to authenticate with Amazon SNS.
                            AWS secret access key used to authenticate with Amazon SNS.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Teams webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The Telegram bot token.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        apiSecretSecretRef:
                          description: |-
                            (String, Sensitive) The Threema API key.
                            The Threema API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The VictorOps webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) The secret key used to generate the HMAC signature.
                                  The secret key used to generate the HMAC signature.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    clientSecretSecretRef:
                                      description: |-
                                        (String, Sensitive) Client secret to use when authenticating.
                                        Client secret to use when authenticating.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          caCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Certificate in PEM format to use when verifying the server's certificate chain.
                                              Certificate in PEM format to use when verifying the server's certificate chain.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          clientCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Client certificate in PEM format to use when connecting to the server.
                                              Client certificate in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          clientKeySecretRef:
                                            description: |-
                                              (String, Sensitive) Client key in PEM format to use when connecting to the server.
                                              Client key in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tlsConfigSecretRef:
                          description: |-
                            (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                            Allows configuring TLS for the webhook notifier.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        secretSecretRef:
                          description: |-
                            (String, Sensitive) The secret key used to generate the HMAC signature.
                            The secret key required to obtain access token when using APIAPP. See https://work.weixin.qq.com/wework_admin/frame#apps to create APIAPP.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - name
                          - namespace
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The WeCom webhook URL. Required if using GroupRobot.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The password component of the basic auth credentials to use.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The DingDing webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The discord webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The Google Chat webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            Personal Access Token that is used as a bearer authorization header.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            Password to use for Jira authentication.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        userSecretRef:
                          description: |-
                            (String, Sensitive) Username to use for Jira authentication.
                            Username to use for Jira authentication.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            The password to use when making a call to the Kafka REST Proxy
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        restProxyUrlSecretRef:
                          description: |-
                            (String, Sensitive) The URL of the Kafka REST proxy to send requests to.
                            The URL of the Kafka REST proxy to send requests to.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      required:
                      - restProxyUrlSecretRef
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        oncallIntegrationRef:
                          description: Reference to a Integration in oncall to populate
                            url.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        oncallIntegrationSelector:
                          description: Selector for a Integration in oncall to populate
                            url.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                        urlSecretRef:
                          description: The OnCall webhook URL (from secret).
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The OpsGenie API key to use.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      required:
                      - apiKeySecretRef
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        integrationKeySecretRef:
                          description: |-
                            (String, Sensitive) The PagerDuty API key.
                            The PagerDuty API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      required:
                      - integrationKeySecretRef
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            The Pushover API token.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                        userKeySecretRef:
                          description: |-
                            (String, Sensitive) The Pushover user key.
                            The Pushover user key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - apiTokenSecretRef
                      - userKeySecretRef
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The SensuGo API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      required:
                      - apiKeySecretRef
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            A Slack API token,for sending messages directly without the webhook method.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Slack webhook URL,for sending messages via the webhook method.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS access key ID used to authenticate with Amazon SNS.
                            AWS access key ID used to authenticate with Amazon SNS.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS secret access key used to authenticate with Amazon SNS.
                            AWS secret access key used to authenticate with Amazon SNS.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Teams webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The Telegram bot token.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        apiSecretSecretRef:
                          description: |-
                            (String, Sensitive) The Threema API key.
                            The Threema API key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                      required:
                      - apiSecretSecretRef
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The VictorOps webhook URL.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) The secret key used to generate the HMAC signature.
                                  The secret key used to generate the HMAC signature.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            required:
                            - secretSecretRef
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    clientSecretSecretRef:
                                      description: |-
                                        (String, Sensitive) Client secret to use when authenticating.
                                        Client secret to use when authenticating.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          caCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Certificate in PEM format to use when verifying the server's certificate chain.
                                              Certificate in PEM format to use when verifying the server's certificate chain.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          clientCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Client certificate in PEM format to use when connecting to the server.
                                              Client certificate in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          clientKeySecretRef:
                                            description: |-
                                              (String, Sensitive) Client key in PEM format to use when connecting to the server.
                                              Client key in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                description: The key to select.
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                              namespace:
                                                description: Namespace of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            - namespace
                                            type: object
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  required:
                                  - clientSecretSecretRef
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tlsConfigSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        secretSecretRef:
                          description: |-
                            (String, Sensitive) The secret key used to generate the HMAC signature.
                            The secret key required to obtain access token when using APIAPP. See https://work.weixin.qq.com/wework_admin/frame#apps to create APIAPP.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The WeCom webhook URL. Required if using GroupRobot.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ContactPointStatus defines the observed state of ContactPoint.
            properties:
              atProvider:
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: inhibitionrulev1beta1s.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: InhibitionruleV1Beta1
    listKind: InhibitionruleV1Beta1List
    plural: inhibitionrulev1beta1s
    singular: inhibitionrulev1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InhibitionruleV1Beta1 is the Schema for the InhibitionruleV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InhibitionruleV1Beta1Spec defines the desired state of InhibitionruleV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: InhibitionruleV1Beta1Status defines the observed state of
              InhibitionruleV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: messagetemplates.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MessageTemplate
    listKind: MessageTemplateList
    plural: messagetemplates
    singular: messagetemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MessageTemplate is the Schema for the MessageTemplates API. Manages
          Grafana Alerting notification template groups, including notification templates.
          Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#notification-template-groups
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MessageTemplateSpec defines the desired state of MessageTemplate
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.template is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.template)
                || (has(self.initProvider) && has(self.initProvider.template))'
          status:
            description: MessageTemplateStatus defines the observed state of MessageTemplate.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mutetimings.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MuteTiming
    listKind: MuteTimingList
    plural: mutetimings
    singular: mutetiming
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MuteTiming is the Schema for the MuteTimings API. Manages Grafana
          Alerting mute timings. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#mute-timings
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MuteTimingSpec defines the desired state of MuteTiming
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: MuteTimingStatus defines the observed state of MuteTiming.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: notificationpolicies.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: NotificationPolicy
    listKind: NotificationPolicyList
    plural: notificationpolicies
    singular: notificationpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotificationPolicy is the Schema for the NotificationPolicys
          API. Sets the global notification policy for Grafana. !> This resource manages
          the entire notification policy tree and overwrites its policies. However,
          it does not overwrite internal policies created when alert rules directly
          set a contact point for notifications. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#notification-policies
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationPolicySpec defines the desired state of NotificationPolicy
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  contactPointRef:
                    description: Reference to a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  contactPointSelector:
                    description: Selector for a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        contactPointRef:
                          description: Reference to a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        contactPointSelector:
                          description: Selector for a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimingRef:
                          description: References to MuteTiming in alerting to populate
                            muteTimings.
                          items:
                            description: A Reference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        muteTimingSelector:
                          description: Selector for a list of MuteTiming in alerting
                            to populate muteTimings.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimingRef:
                                description: References to MuteTiming in alerting
                                  to populate muteTimings.
                                items:
                                  description: A Reference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              muteTimingSelector:
                                description: Selector for a list of MuteTiming in
                                  alerting to populate muteTimings.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    contactPointRef:
                                      description: Reference to a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        name:
                                          description: Name of the referenced object.
                                          type: string
                                        policy:
                                          description: Policies for referencing.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      required:
                                      - name
                                      type: object
                                    contactPointSelector:
                                      description: Selector for a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimingRef:
                                      description: References to MuteTiming in alerting
                                        to populate muteTimings.
                                      items:
                                        description: A Reference to a named object.
                                        properties:
                                          name:
                                            description: Name of the referenced object.
                                            type: string
                                          policy:
                                            description: Policies for referencing.
                                            properties:
                                              resolution:
                                                default: Required
                                                description: |-
                                                  Resolution specifies whether resolution of this reference is required.
                                                  The default is 'Required', which means the reconcile will fail if the
                                                  reference cannot be resolved. 'Optional' means this reference will be
                                                  a no-op if it cannot be resolved.
                                                enum:
                                                - Required
                                                - Optional
                                                type: string
                                              resolve:
                                                description: |-
                                                  Resolve specifies when this reference should be resolved. The default
                                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                                  the corresponding field is not present. Use 'Always' to resolve the
                                                  reference on every reconcile.
                                                enum:
                                                - Always
                                                - IfNotPresent
                                                type: string
                                            type: object
                                        required:
                                        - name
                                        type: object
                                      type: array
                                    muteTimingSelector:
                                      description: Selector for a list of MuteTiming
                                        in alerting to populate muteTimings.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          contactPointRef:
                                            description: Reference to a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              name:
                                                description: Name of the referenced
                                                  object.
                                                type: string
                                              policy:
                                                description: Policies for referencing.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            required:
                                            - name
                                            type: object
                                          contactPointSelector:
                                            description: Selector for a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimingRef:
                                            description: References to MuteTiming
                                              in alerting to populate muteTimings.
                                            items:
                                              description: A Reference to a named
                                                object.
                                              properties:
                                                name:
                                                  description: Name of the referenced
                                                    object.
                                                  type: string
                                                policy:
                                                  description: Policies for referencing.
                                                  properties:
                                                    resolution:
                                                      default: Required
                                                      description: |-
                                                        Resolution specifies whether resolution of this reference is required.
                                                        The default is 'Required', which means the reconcile will fail if the
                                                        reference cannot be resolved. 'Optional' means this reference will be
                                                        a no-op if it cannot be resolved.
                                                      enum:
                                                      - Required
                                                      - Optional
                                                      type: string
                                                    resolve:
                                                      description: |-
                                                        Resolve specifies when this reference should be resolved. The default
                                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                                        the corresponding field is not present. Use 'Always' to resolve the
                                                        reference on every reconcile.
                                                      enum:
                                                      - Always
                                                      - IfNotPresent
                                                      type: string
                                                  type: object
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          muteTimingSelector:
                                            description: Selector for a list of MuteTiming
                                              in alerting to populate muteTimings.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  contactPointRef:
                    description: Reference to a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  contactPointSelector:
                    description: Selector for a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        contactPointRef:
                          description: Reference to a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        contactPointSelector:
                          description: Selector for a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimingRef:
                          description: References to MuteTiming in alerting to populate
                            muteTimings.
                          items:
                            description: A Reference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        muteTimingSelector:
                          description: Selector for a list of MuteTiming in alerting
                            to populate muteTimings.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimingRef:
                                description: References to MuteTiming in alerting
                                  to populate muteTimings.
                                items:
                                  description: A Reference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              muteTimingSelector:
                                description: Selector for a list of MuteTiming in
                                  alerting to populate muteTimings.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    contactPointRef:
                                      description: Reference to a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        name:
                                          description: Name of the referenced object.
                                          type: string
                                        policy:
                                          description: Policies for referencing.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      required:
                                      - name
                                      type: object
                                    contactPointSelector:
                                      description: Selector for a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimingRef:
                                      description: References to MuteTiming in alerting
                                        to populate muteTimings.
                                      items:
                                        description: A Reference to a named object.
                                        properties:
                                          name:
                                            description: Name of the referenced object.
                                            type: string
                                          policy:
                                            description: Policies for referencing.
                                            properties:
                                              resolution:
                                                default: Required
                                                description: |-
                                                  Resolution specifies whether resolution of this reference is required.
                                                  The default is 'Required', which means the reconcile will fail if the
                                                  reference cannot be resolved. 'Optional' means this reference will be
                                                  a no-op if it cannot be resolved.
                                                enum:
                                                - Required
                                                - Optional
                                                type: string
                                              resolve:
                                                description: |-
                                                  Resolve specifies when this reference should be resolved. The default
                                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                                  the corresponding field is not present. Use 'Always' to resolve the
                                                  reference on every reconcile.
                                                enum:
                                                - Always
                                                - IfNotPresent
                                                type: string
                                            type: object
                                        required:
                                        - name
                                        type: object
                                      type: array
                                    muteTimingSelector:
                                      description: Selector for a list of MuteTiming
                                        in alerting to populate muteTimings.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          contactPointRef:
                                            description: Reference to a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              name:
                                                description: Name of the referenced
                                                  object.
                                                type: string
                                              policy:
                                                description: Policies for referencing.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            required:
                                            - name
                                            type: object
                                          contactPointSelector:
                                            description: Selector for a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimingRef:
                                            description: References to MuteTiming
                                              in alerting to populate muteTimings.
                                            items:
                                              description: A Reference to a named
                                                object.
                                              properties:
                                                name:
                                                  description: Name of the referenced
                                                    object.
                                                  type: string
                                                policy:
                                                  description: Policies for referencing.
                                                  properties:
                                                    resolution:
                                                      default: Required
                                                      description: |-
                                                        Resolution specifies whether resolution of this reference is required.
                                                        The default is 'Required', which means the reconcile will fail if the
                                                        reference cannot be resolved. 'Optional' means this reference will be
                                                        a no-op if it cannot be resolved.
                                                      enum:
                                                      - Required
                                                      - Optional
                                                      type: string
                                                    resolve:
                                                      description: |-
                                                        Resolve specifies when this reference should be resolved. The default
                                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                                        the corresponding field is not present. Use 'Always' to resolve the
                                                        reference on every reconcile.
                                                      enum:
                                                      - Always
                                                      - IfNotPresent
                                                      type: string
                                                  type: object
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          muteTimingSelector:
                                            description: Selector for a list of MuteTiming
                                              in alerting to populate muteTimings.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.groupBy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.groupBy)
                || (has(self.initProvider) && has(self.initProvider.groupBy))'
          status:
            description: NotificationPolicyStatus defines the observed state of NotificationPolicy.
            properties:
              atProvider:
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: recordingrulev0alpha1s.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RecordingruleV0Alpha1
    listKind: RecordingruleV0Alpha1List
    plural: recordingrulev0alpha1s
    singular: recordingrulev0alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RecordingruleV0Alpha1 is the Schema for the RecordingruleV0Alpha1s
          API. Manages Grafana Recording Rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RecordingruleV0Alpha1Spec defines the desired state of RecordingruleV0Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: RecordingruleV0Alpha1Status defines the observed state of
              RecordingruleV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rulegroups.alerting.grafana.crossplane.io
spec:
  group: alerting.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RuleGroup
    listKind: RuleGroupList
    plural: rulegroups
    singular: rulegroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleGroup is the Schema for the RuleGroups API. Manages Grafana
          Alerting rule groups. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#alert-rules
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleGroupSpec defines the desired state of RuleGroup
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.intervalSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.intervalSeconds)
                || (has(self.initProvider) && has(self.initProvider.intervalSeconds))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rule is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rule)
                || (has(self.initProvider) && has(self.initProvider.rule))'
          status:
            description: RuleGroupStatus defines the observed state of RuleGroup.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alertenrichmentv1beta1s.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AlertenrichmentV1Beta1
    listKind: AlertenrichmentV1Beta1List
    plural: alertenrichmentv1beta1s
    singular: alertenrichmentv1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertenrichmentV1Beta1 is the Schema for the AlertenrichmentV1Beta1s
          API. Manages Grafana Cloud Alert Enrichment https://grafana.com/docs/grafana-cloud/alerting-and-irm/alerting/configure-notifications/alert-enrichment/.
          Alert enrichment is currently in public preview. Grafana Labs offers limited
          support, and breaking changes might occur prior to the feature being made
          generally available.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertenrichmentV1Beta1Spec defines the desired state of AlertenrichmentV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: AlertenrichmentV1Beta1Status defines the observed state of
              AlertenrichmentV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      alertRuleUids:
                        description: |-
                          (List of String) UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                          UIDs of alert rules this enrichment applies to. If empty, applies to all alert rules.
                        items:
                          type: string
                        type: array
                      annotationMatchers:
                        description: |-
                          (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      description:
                        description: |-
                          (String) Description of the alert enrichment.
                          Description of the alert enrichment.
                        type: string
                      disableProvenance:
                        description: |-
                          (boolean) allow modifying alert enrichment outside of provider
                          allow modifying alert enrichment outside of provider
                        type: boolean
                      labelMatchers:
                        description: |-
                          (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                          Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators).
                        items:
                          properties:
                            name:
                              description: (String)
                              type: string
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      receivers:
                        description: |-
                          (List of String) Receiver names to match. If empty, applies to all receivers.
                          Receiver names to match. If empty, applies to all receivers.
                        items:
                          type: string
                        type: array
                      step:
                        description: |-
                          (Block List) Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block. (see below for nested schema)
                          Enrichment step. Can be repeated multiple times to define a sequence of steps. Each step must contain exactly one enrichment block.
                        items:
                          properties:
                            asserts:
                              description: |-
                                (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                Integrate with Grafana Asserts for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assign:
                              description: |-
                                (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                Assign annotations to an alert.
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    (Map of String) Annotations of the resource.
                                    Map of annotation names to values to set on matching alerts.
                                  type: object
                                  x-kubernetes-map-type: granular
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            assistantInvestigations:
                              description: |-
                                (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                Use AI assistant to investigate alerts and add insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            conditional:
                              description: |-
                                (Block, Optional) Conditional step with if/then/else. (see below for nested schema)
                                Conditional step with if/then/else.
                              properties:
                                else:
                                  description: |-
                                    (Block, Optional) Steps when condition is false. (see below for nested schema)
                                    Steps when condition is false.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                if:
                                  description: |-
                                    (Block, Optional) Condition to evaluate. (see below for nested schema)
                                    Condition to evaluate.
                                  properties:
                                    annotationMatchers:
                                      description: |-
                                        (List of Object) Annotation matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, annotation key to match), 'value' (string, annotation value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Annotation matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                    dataSourceCondition:
                                      description: |-
                                        (Block, Optional) Data source condition. (see below for nested schema)
                                        Data source condition.
                                      properties:
                                        request:
                                          description: |-
                                            (String) Raw request payload for the data source query.
                                            Data source request payload.
                                          type: string
                                      type: object
                                    labelMatchers:
                                      description: |-
                                        (List of Object) Label matchers that an alert must satisfy for this enrichment to apply. Each matcher is an object with: 'type' (string, one of: =, !=, =~, !~), 'name' (string, label key to match), 'value' (string, label value to compare against, supports regex for =~/!~ operators). (see below for nested schema)
                                        Label matchers for the condition.
                                      items:
                                        properties:
                                          name:
                                            description: (String)
                                            type: string
                                          type:
                                            description: (String)
                                            type: string
                                          value:
                                            description: (String)
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                then:
                                  description: |-
                                    (Block, Optional) Steps when condition is true. (see below for nested schema)
                                    Steps when condition is true.
                                  properties:
                                    step:
                                      description: (Block List) Enrichment step. Can
                                        be repeated multiple times to define a sequence
                                        of steps. Each step must contain exactly one
                                        enrichment block. (see below for nested schema)
                                      items:
                                        properties:
                                          asserts:
                                            description: |-
                                              (Block, Optional) Integrate with Grafana Asserts for enrichment. (see below for nested schema)
                                              Integrate with Grafana Asserts for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assign:
                                            description: |-
                                              (Block, Optional) Assign annotations to an alert. (see below for nested schema)
                                              Assign annotations to an alert.
                                            properties:
                                              annotations:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  (Map of String) Annotations of the resource.
                                                  Map of annotation names to values to set on matching alerts.
                                                type: object
                                                x-kubernetes-map-type: granular
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          assistantInvestigations:
                                            description: |-
                                              (Block, Optional) Use AI assistant to investigate alerts and add insights. (see below for nested schema)
                                              Use AI assistant to investigate alerts and add insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          dataSource:
                                            description: |-
                                              (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                              Query Grafana data sources and add results to alerts.
                                            properties:
                                              logsQuery:
                                                description: |-
                                                  (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                                  Logs query configuration for querying log data sources.
                                                properties:
                                                  dataSourceType:
                                                    description: |-
                                                      (String) Data source type (e.g., 'loki').
                                                      Data source type (e.g., 'loki').
                                                    type: string
                                                  dataSourceUid:
                                                    description: |-
                                                      (String) UID of the data source to query.
                                                      UID of the data source to query.
                                                    type: string
                                                  expr:
                                                    description: |-
                                                      (String) Log query expression to execute.
                                                      Log query expression to execute.
                                                    type: string
                                                  maxLines:
                                                    description: |-
                                                      (Number) Maximum number of log lines to include. Defaults to 3.
                                                      Maximum number of log lines to include. Defaults to 3.
                                                    type: number
                                                type: object
                                              rawQuery:
                                                description: |-
                                                  (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                                  Raw query configuration for advanced data source queries.
                                                properties:
                                                  refId:
                                                    description: |-
                                                      (String) Reference ID for correlating queries.
                                                      Reference ID for correlating queries.
                                                    type: string
                                                  request:
                                                    description: |-
                                                      (String) Raw request payload for the data source query.
                                                      Raw request payload for the data source query.
                                                    type: string
                                                type: object
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          explain:
                                            description: |-
                                              (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                              Generate AI explanation and store in an annotation.
                                            properties:
                                              annotation:
                                                description: |-
                                                  (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                  Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                                type: string
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                          external:
                                            description: |-
                                              (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                              Call an external HTTP service for enrichment.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                              url:
                                                description: |-
                                                  (String) The full URL of the resource.
                                                  HTTP endpoint URL to call for enrichment
                                                type: string
                                            type: object
                                          sift:
                                            description: |-
                                              (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                              Analyze alerts for patterns and insights.
                                            properties:
                                              timeout:
                                                description: |-
                                                  (String) Maximum execution time (e.g., '30s', '1m')
                                                  Maximum execution time (e.g., '30s', '1m')
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            dataSource:
                              description: |-
                                (Block, Optional) Query Grafana data sources and add results to alerts. (see below for nested schema)
                                Query Grafana data sources and add results to alerts.
                              properties:
                                logsQuery:
                                  description: |-
                                    (Block, Optional) Logs query configuration for querying log data sources. (see below for nested schema)
                                    Logs query configuration for querying log data sources.
                                  properties:
                                    dataSourceType:
                                      description: |-
                                        (String) Data source type (e.g., 'loki').
                                        Data source type (e.g., 'loki').
                                      type: string
                                    dataSourceUid:
                                      description: |-
                                        (String) UID of the data source to query.
                                        UID of the data source to query.
                                      type: string
                                    expr:
                                      description: |-
                                        (String) Log query expression to execute.
                                        Log query expression to execute.
                                      type: string
                                    maxLines:
                                      description: |-
                                        (Number) Maximum number of log lines to include. Defaults to 3.
                                        Maximum number of log lines to include. Defaults to 3.
                                      type: number
                                  type: object
                                rawQuery:
                                  description: |-
                                    (Block, Optional) Raw query configuration for advanced data source queries. (see below for nested schema)
                                    Raw query configuration for advanced data source queries.
                                  properties:
                                    refId:
                                      description: |-
                                        (String) Reference ID for correlating queries.
                                        Reference ID for correlating queries.
                                      type: string
                                    request:
                                      description: |-
                                        (String) Raw request payload for the data source query.
                                        Raw request payload for the data source query.
                                      type: string
                                  type: object
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            explain:
                              description: |-
                                (Block, Optional) Generate AI explanation and store in an annotation. (see below for nested schema)
                                Generate AI explanation and store in an annotation.
                              properties:
                                annotation:
                                  description: |-
                                    (String) Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                    Annotation name to set the explanation in. Defaults to 'ai_explanation'.
                                  type: string
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                            external:
                              description: |-
                                (Block, Optional) Call an external HTTP service for enrichment. (see below for nested schema)
                                Call an external HTTP service for enrichment.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                                url:
                                  description: |-
                                    (String) The full URL of the resource.
                                    HTTP endpoint URL to call for enrichment
                                  type: string
                              type: object
                            sift:
                              description: |-
                                (Block, Optional) Analyze alerts for patterns and insights. (see below for nested schema)
                                Analyze alerts for patterns and insights.
                              properties:
                                timeout:
                                  description: |-
                                    (String) Maximum execution time (e.g., '30s', '1m')
                                    Maximum execution time (e.g., '30s', '1m')
                                  type: string
                              type: object
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the alert enrichment.
                          The title of the alert enrichment.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alertrulev0alpha1s.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AlertruleV0Alpha1
    listKind: AlertruleV0Alpha1List
    plural: alertrulev0alpha1s
    singular: alertrulev0alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertruleV0Alpha1 is the Schema for the AlertruleV0Alpha1s API.
          Manages Grafana Alert Rules. This resource is currently in alpha and is
          subject to change. Grafana 12.4+ users must enable the kubernetesAlertingRules
          feature toggle https://grafana.com/docs/grafana/latest/setup-grafana/configure-grafana/feature-toggles/.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertruleV0Alpha1Spec defines the desired state of AlertruleV0Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: AlertruleV0Alpha1Status defines the observed state of AlertruleV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts.
                        type: object
                        x-kubernetes-map-type: granular
                      execErrState:
                        description: |-
                          (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.
                        type: string
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      for:
                        description: |-
                          (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                          The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending.
                        type: string
                      keepFiringFor:
                        description: |-
                          (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                          Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing.
                        type: object
                        x-kubernetes-map-type: granular
                      missingSeriesEvalsToResolve:
                        description: |-
                          (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          The number of missing series evaluations that must occur before the rule is considered to be resolved.
                        type: number
                      noDataState:
                        description: |-
                          (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                          Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting.
                        type: string
                      notificationSettings:
                        description: |-
                          (Block, Optional) Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing (see below for nested schema)
                          Notification settings for the rule. If specified, it overrides the notification policies. The flat configuration is deprecated, please specify one of named_routing_tree or simplified_routing
                        properties:
                          activeTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                              Deprecated. A list of time interval names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          contactPoint:
                            description: |-
                              (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                              Deprecated. The contact point to route notifications that match this rule to.
                            type: string
                          groupBy:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                              Deprecated. A list of alert labels to group alerts into notifications by.
                            items:
                              type: string
                            type: array
                          groupInterval:
                            description: |-
                              (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                              Deprecated. Minimum time interval between two notifications for the same group.
                            type: string
                          groupWait:
                            description: |-
                              (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                              Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                            type: string
                          muteTimings:
                            description: |-
                              (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                              Deprecated. A list of mute timing names to apply to alerts that match this policy.
                            items:
                              type: string
                            type: array
                          namedRoutingTree:
                            description: |-
                              (Block, Optional) Route notifications to a specific routing tree. (see below for nested schema)
                              Route notifications to a specific routing tree.
                            properties:
                              routingTree:
                                description: |-
                                  (String) The name of the routing tree to use.
                                  The name of the routing tree to use.
                                type: string
                            type: object
                          repeatInterval:
                            description: |-
                              sending a notification if an alert is still firing.
                              Deprecated. Minimum time interval for re-sending a notification if an alert is still firing.
                            type: string
                          simplifiedRouting:
                            description: |-
                              (Block, Optional) Simplified routing to a contact point with optional grouping and timing overrides. (see below for nested schema)
                              Simplified routing to a contact point with optional grouping and timing overrides.
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of time interval names to apply to alerts that match this policy.
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String, Deprecated) Deprecated. The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of alert labels to group alerts into notifications by.
                                  A list of alert labels to group alerts into notifications by.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String, Deprecated) Deprecated. Minimum time interval between two notifications for the same group.
                                  Minimum time interval between two notifications for the same group.
                                type: string
                              groupWait:
                                description: |-
                                  (String, Deprecated) Deprecated. Time to wait to buffer alerts of the same group before sending a notification.
                                  Time to wait to buffer alerts of the same group before sending a notification.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String, Deprecated) Deprecated. A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing.
                                  Minimum time interval for re-sending a notification if an alert is still firing.
                                type: string
                            type: object
                        type: object
                      panelRef:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                          Reference to a panel that this alert rule is associated with. Should be an object with 'dashboard_uid' (string) and 'panel_id' (number) fields.
                        type: object
                        x-kubernetes-map-type: granular
                      paused:
                        description: |-
                          (Boolean) Sets whether the rule should be paused or not.
                          Sets whether the rule should be paused or not.
                        type: boolean
                      title:
                        description: |-
                          (String) The title of the alert rule.
                          The title of the alert rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the alert rule. (see below for nested schema)
                          The trigger configuration for the alert rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the alert rule should be evaluated.
                              The interval at which the alert rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: contactpoints.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ContactPoint
    listKind: ContactPointList
    plural: contactpoints
    singular: contactpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ContactPoint is the Schema for the ContactPoints API. Manages
          Grafana Alerting contact points. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#contact-points
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContactPointSpec defines the desired state of ContactPoint
            properties:
              forProvider:
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The password component of the basic auth credentials to use.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The DingDing webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The discord webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The Google Chat webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            Personal Access Token that is used as a bearer authorization header.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            Password to use for Jira authentication.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        userSecretRef:
                          description: |-
                            (String, Sensitive) Username to use for Jira authentication.
                            Username to use for Jira authentication.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            The password to use when making a call to the Kafka REST Proxy
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        restProxyUrlSecretRef:
                          description: |-
                            (String, Sensitive) The URL of the Kafka REST proxy to send requests to.
                            The URL of the Kafka REST proxy to send requests to.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        oncallIntegrationRef:
                          description: Reference to a Integration in oncall to populate
                            url.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        oncallIntegrationSelector:
                          description: Selector for a Integration in oncall to populate
                            url.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                        urlSecretRef:
                          description: The OnCall webhook URL (from secret).
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The OpsGenie API key to use.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        integrationKeySecretRef:
                          description: |-
                            (String, Sensitive) The PagerDuty API key.
                            The PagerDuty API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            The Pushover API token.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                        userKeySecretRef:
                          description: |-
                            (String, Sensitive) The Pushover user key.
                            The Pushover user key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The SensuGo API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            A Slack API token,for sending messages directly without the webhook method.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Slack webhook URL,for sending messages via the webhook method.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS access key ID used to authenticate with Amazon SNS.
                            AWS access key ID used to authenticate with Amazon SNS.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS secret access key used to authenticate with Amazon SNS.
                            AWS secret access key used to authenticate with Amazon SNS.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Teams webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The Telegram bot token.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        apiSecretSecretRef:
                          description: |-
                            (String, Sensitive) The Threema API key.
                            The Threema API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The VictorOps webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) The secret key used to generate the HMAC signature.
                                  The secret key used to generate the HMAC signature.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    clientSecretSecretRef:
                                      description: |-
                                        (String, Sensitive) Client secret to use when authenticating.
                                        Client secret to use when authenticating.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          caCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Certificate in PEM format to use when verifying the server's certificate chain.
                                              Certificate in PEM format to use when verifying the server's certificate chain.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          clientCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Client certificate in PEM format to use when connecting to the server.
                                              Client certificate in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          clientKeySecretRef:
                                            description: |-
                                              (String, Sensitive) Client key in PEM format to use when connecting to the server.
                                              Client key in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tlsConfigSecretRef:
                          description: |-
                            (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                            Allows configuring TLS for the webhook notifier.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        secretSecretRef:
                          description: |-
                            (String, Sensitive) The secret key used to generate the HMAC signature.
                            The secret key required to obtain access token when using APIAPP. See https://work.weixin.qq.com/wework_admin/frame#apps to create APIAPP.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          description: |-
                            (Map of String, Sensitive) Additional custom properties to attach to the notifier. Defaults to map[].
                            Additional custom properties to attach to the notifier. Defaults to `map[]`.
                          properties:
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - name
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The WeCom webhook URL. Required if using GroupRobot.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The password component of the basic auth credentials to use.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The DingDing webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The discord webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The Google Chat webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            Personal Access Token that is used as a bearer authorization header.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            Password to use for Jira authentication.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        userSecretRef:
                          description: |-
                            (String, Sensitive) Username to use for Jira authentication.
                            Username to use for Jira authentication.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password to use for Jira authentication.
                            The password to use when making a call to the Kafka REST Proxy
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        restProxyUrlSecretRef:
                          description: |-
                            (String, Sensitive) The URL of the Kafka REST proxy to send requests to.
                            The URL of the Kafka REST proxy to send requests to.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      required:
                      - restProxyUrlSecretRef
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        oncallIntegrationRef:
                          description: Reference to a Integration in oncall to populate
                            url.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        oncallIntegrationSelector:
                          description: Selector for a Integration in oncall to populate
                            url.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                        urlSecretRef:
                          description: The OnCall webhook URL (from secret).
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The OpsGenie API key to use.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      required:
                      - apiKeySecretRef
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        integrationKeySecretRef:
                          description: |-
                            (String, Sensitive) The PagerDuty API key.
                            The PagerDuty API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      required:
                      - integrationKeySecretRef
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            (String, Sensitive) Personal Access Token that is used as a bearer authorization header.
                            The Pushover API token.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                        userKeySecretRef:
                          description: |-
                            (String, Sensitive) The Pushover user key.
                            The Pushover user key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - apiTokenSecretRef
                      - userKeySecretRef
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        apiKeySecretRef:
                          description: |-
                            (String, Sensitive) The OpsGenie API key to use.
                            The SensuGo API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      required:
                      - apiKeySecretRef
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            A Slack API token,for sending messages directly without the webhook method.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Slack webhook URL,for sending messages via the webhook method.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS access key ID used to authenticate with Amazon SNS.
                            AWS access key ID used to authenticate with Amazon SNS.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            (String, Sensitive) AWS secret access key used to authenticate with Amazon SNS.
                            AWS secret access key used to authenticate with Amazon SNS.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            A Teams webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The Telegram bot token.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        apiSecretSecretRef:
                          description: |-
                            (String, Sensitive) The Threema API key.
                            The Threema API key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                      required:
                      - apiSecretSecretRef
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The VictorOps webhook URL.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - urlSecretRef
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        tokenSecretRef:
                          description: |-
                            (String, Sensitive) The bearer token used to authorize the client.
                            The bearer token used to authorize the client.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - tokenSecretRef
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationCredentialsSecretRef:
                          description: |-
                            attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this value. Do not use in conjunction with basic auth parameters.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthPasswordSecretRef:
                          description: |-
                            (String, Sensitive) The password component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              secretSecretRef:
                                description: |-
                                  (String, Sensitive) The secret key used to generate the HMAC signature.
                                  The secret key used to generate the HMAC signature.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            required:
                            - secretSecretRef
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    clientSecretSecretRef:
                                      description: |-
                                        (String, Sensitive) Client secret to use when authenticating.
                                        Client secret to use when authenticating.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          caCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Certificate in PEM format to use when verifying the server's certificate chain.
                                              Certificate in PEM format to use when verifying the server's certificate chain.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          clientCertificateSecretRef:
                                            description: |-
                                              (String, Sensitive) Client certificate in PEM format to use when connecting to the server.
                                              Client certificate in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          clientKeySecretRef:
                                            description: |-
                                              (String, Sensitive) Client key in PEM format to use when connecting to the server.
                                              Client key in PEM format to use when connecting to the server.
                                            properties:
                                              key:
                                                type: string
                                              name:
                                                description: Name of the secret.
                                                type: string
                                            required:
                                            - key
                                            - name
                                            type: object
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  required:
                                  - clientSecretSecretRef
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        tlsConfigSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        secretSecretRef:
                          description: |-
                            (String, Sensitive) The secret key used to generate the HMAC signature.
                            The secret key required to obtain access token when using APIAPP. See https://work.weixin.qq.com/wework_admin/frame#apps to create APIAPP.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        settingsSecretRef:
                          additionalProperties:
                            type: string
                          type: object
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        urlSecretRef:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The WeCom webhook URL. Required if using GroupRobot.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ContactPointStatus defines the observed state of ContactPoint.
            properties:
              atProvider:
                properties:
                  alertmanager:
                    description: |-
                      (Block Set) A contact point that sends notifications to other Alertmanager instances. (see below for nested schema)
                      A contact point that sends notifications to other Alertmanager instances.
                    items:
                      properties:
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username component of the basic auth credentials to use.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL of the Alertmanager instance.
                          type: string
                      type: object
                    type: array
                  dingding:
                    description: |-
                      (Block Set) A contact point that sends notifications to DingDing. (see below for nested schema)
                      A contact point that sends notifications to DingDing.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The format of message to send - either 'link' or 'actionCard'
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  discord:
                    description: |-
                      (Block Set) A contact point that sends notifications as Discord messages (see below for nested schema)
                      A contact point that sends notifications as Discord messages
                    items:
                      properties:
                        avatarUrl:
                          description: |-
                            (String) The URL of a custom avatar image to use. Defaults to “.
                            The URL of a custom avatar image to use. Defaults to “.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message. Defaults to “.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        useDiscordUsername:
                          description: |-
                            (Boolean) Whether to use the bot account's plain username instead of "Grafana." Defaults to false.
                            Whether to use the bot account's plain username instead of "Grafana." Defaults to `false`.
                          type: boolean
                      type: object
                    type: array
                  email:
                    description: |-
                      (Block Set) A contact point that sends notifications to an email address. (see below for nested schema)
                      A contact point that sends notifications to an email address.
                    items:
                      properties:
                        addresses:
                          description: |-
                            (List of String) The addresses to send emails to.
                            The addresses to send emails to.
                          items:
                            type: string
                          type: array
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the email. Defaults to “.
                          type: string
                        singleEmail:
                          description: |-
                            (Boolean) Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to false.
                            Whether to send a single email CC'ing all addresses, rather than a separate email to each address. Defaults to `false`.
                          type: boolean
                        subject:
                          description: |-
                            (String) The templated subject line of the email. Defaults to “.
                            The templated subject line of the email. Defaults to “.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  googlechat:
                    description: |-
                      (Block Set) A contact point that sends notifications to Google Chat. (see below for nested schema)
                      A contact point that sends notifications to Google Chat.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated content of the title.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  jira:
                    description: |-
                      (Block Set) A contact point that sends notifications to Jira. (see below for nested schema)
                      A contact point that sends notifications to Jira.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL of the Jira REST API (v2 or v3).
                          type: string
                        dedupKeyField:
                          description: |-
                            (String) Custom field ID for storing deduplication keys. Must be numeric.
                            Custom field ID for storing deduplication keys. Must be numeric.
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Jira issue. Maximum length is 32767 characters.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        fields:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom Jira issue fields.
                            Custom Jira issue fields.
                          type: object
                          x-kubernetes-map-type: granular
                        issueType:
                          description: |-
                            (String) The type of issue to create (e.g., Bug, Task, Story).
                            The type of issue to create (e.g., Bug, Task, Story).
                          type: string
                        labels:
                          description: |-
                            (List of String) Labels to assign to the Jira issue.
                            Labels to assign to the Jira issue.
                          items:
                            type: string
                          type: array
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the issue (e.g., High, Medium, Low).
                          type: string
                        project:
                          description: |-
                            (String) The project key in Jira.
                            The project key in Jira.
                          type: string
                        reopenDuration:
                          description: |-
                            (String) Duration to consider reopening issues (e.g., '10m').
                            Duration to consider reopening issues (e.g., '10m').
                          type: string
                        reopenTransition:
                          description: |-
                            (String) The name of the workflow transition to reopen an issue.
                            The name of the workflow transition to reopen an issue.
                          type: string
                        resolveTransition:
                          description: |-
                            (String) The name of the workflow transition to resolve an issue.
                            The name of the workflow transition to resolve an issue.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary of the Jira issue. Maximum length is 255 characters.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        wontFixResolution:
                          description: |-
                            (String) Resolution status to exclude from reopening/updating.
                            Resolution status to exclude from reopening/updating.
                          type: string
                      type: object
                    type: array
                  kafka:
                    description: |-
                      (Block Set) A contact point that publishes notifications to Apache Kafka topics. (see below for nested schema)
                      A contact point that publishes notifications to Apache Kafka topics.
                    items:
                      properties:
                        apiVersion:
                          description: |-
                            (String) The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to v2.
                            The API version to use when contacting the Kafka REST Server. Supported: v2 (default) and v3. Defaults to `v2`.
                          type: string
                        clusterId:
                          description: |-
                            (String) The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                            The Id of cluster to use when contacting the Kafka REST Server. Required api_version to be 'v3'
                          type: string
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the Kafka message.
                          type: string
                        details:
                          description: |-
                            (String) The templated details to include with the message.
                            The templated details to include with the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The name of the Kafka topic to publish to.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            The user name to use when making a call to the Kafka REST Proxy
                          type: string
                      type: object
                    type: array
                  line:
                    description: |-
                      (Block Set) A contact point that sends notifications to LINE.me. (see below for nested schema)
                      A contact point that sends notifications to LINE.me.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the contact point.
                      The name of the contact point.
                    type: string
                  oncall:
                    description: |-
                      Call. (see below for nested schema)
                      A contact point that sends notifications to Grafana On-Call.
                    items:
                      properties:
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  opsgenie:
                    description: |-
                      (Block Set) A contact point that sends notifications to OpsGenie. (see below for nested schema)
                      A contact point that sends notifications to OpsGenie.
                    items:
                      properties:
                        autoClose:
                          description: |-
                            close alerts in OpsGenie when they resolve in the Alertmanager.
                            Whether to auto-close alerts in OpsGenie when they resolve in the Alertmanager.
                          type: boolean
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            A templated high-level description to use for the alert.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        overridePriority:
                          description: |-
                            (Boolean) Whether to allow the alert priority to be configured via the value of the og_priority annotation on the alert.
                            Whether to allow the alert priority to be configured via the value of the `og_priority` annotation on the alert.
                          type: boolean
                        responders:
                          description: |-
                            (Block List) Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+. (see below for nested schema)
                            Teams, users, escalations and schedules that the alert will be routed to send notifications. If the API Key belongs to a team integration, this field will be overwritten with the owner team. This feature is available from Grafana 10.3+.
                          items:
                            properties:
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  ID of the responder. Must be specified if name and username are empty.
                                type: string
                              name:
                                description: |-
                                  (String) The name of the contact point.
                                  Name of the responder. Must be specified if username and id are empty.
                                type: string
                              type:
                                description: |-
                                  (String) Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                  Type of the responder. Supported: team, teams, user, escalation, schedule or a template that is expanded to one of these values.
                                type: string
                              username:
                                description: |-
                                  (String) The user name to use when making a call to the Kafka REST Proxy
                                  User name of the responder. Must be specified if name and id are empty.
                                type: string
                            type: object
                          type: array
                        sendTagsAs:
                          description: |-
                            (String) Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are tags, details, both, or empty to use the default behavior of Tags.
                            Whether to send annotations to OpsGenie as Tags, Details, or both. Supported values are `tags`, `details`, `both`, or empty to use the default behavior of Tags.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            Allows customization of the OpsGenie API URL.
                          type: string
                      type: object
                    type: array
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  pagerduty:
                    description: |-
                      (Block Set) A contact point that sends notifications to PagerDuty. (see below for nested schema)
                      A contact point that sends notifications to PagerDuty.
                    items:
                      properties:
                        class:
                          description: |-
                            (String) The class or type of event, for example ping failure.
                            The class or type of event, for example `ping failure`.
                          type: string
                        client:
                          description: |-
                            (String) The name of the monitoring client that is triggering this event.
                            The name of the monitoring client that is triggering this event.
                          type: string
                        clientUrl:
                          description: |-
                            (String) The URL of the monitoring client that is triggering this event.
                            The URL of the monitoring client that is triggering this event.
                          type: string
                        component:
                          description: |-
                            (String) The component being affected by the event.
                            The component being affected by the event.
                          type: string
                        details:
                          additionalProperties:
                            type: string
                          description: |-
                            (String) The templated details to include with the message.
                            A set of arbitrary key/value pairs that provide further detail about the incident.
                          type: object
                          x-kubernetes-map-type: granular
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        group:
                          description: |-
                            (String) The group to which the provided component belongs to.
                            The group to which the provided component belongs to.
                          type: string
                        severity:
                          description: |-
                            (String) The PagerDuty event severity level. Default is critical.
                            The PagerDuty event severity level. Default is `critical`.
                          type: string
                        source:
                          description: |-
                            (String) The unique location of the affected system.
                            The unique location of the affected system.
                          type: string
                        summary:
                          description: |-
                            (String) The templated summary of the Jira issue. Maximum length is 255 characters.
                            The templated summary message of the event.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send API requests to
                          type: string
                      type: object
                    type: array
                  pushover:
                    description: |-
                      (Block Set) A contact point that sends notifications to Pushover. (see below for nested schema)
                      A contact point that sends notifications to Pushover.
                    items:
                      properties:
                        device:
                          description: |-
                            separated list of devices to which the event is associated.
                            Comma-separated list of devices to which the event is associated.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        expire:
                          description: |-
                            (Number) How many seconds for which the notification will continue to be retried by Pushover.
                            How many seconds for which the notification will continue to be retried by Pushover.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated notification message content.
                          type: string
                        okPriority:
                          description: |-
                            (Number) The priority level of the resolved event.
                            The priority level of the resolved event.
                          type: number
                        okSound:
                          description: |-
                            (String) The sound associated with the resolved notification.
                            The sound associated with the resolved notification.
                          type: string
                        priority:
                          description: |-
                            (String) The priority level of the issue (e.g., High, Medium, Low).
                            The priority level of the event.
                          type: number
                        retry:
                          description: |-
                            (Number) How often, in seconds, the Pushover servers will send the same notification to the user.
                            How often, in seconds, the Pushover servers will send the same notification to the user.
                          type: number
                        sound:
                          description: |-
                            (String) The sound associated with the notification.
                            The sound associated with the notification.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        uploadImage:
                          description: |-
                            (Boolean) Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                            Whether to send images in the notification or not. Default is true. Requires Grafana to be configured to send images in notifications.
                          type: boolean
                      type: object
                    type: array
                  sensugo:
                    description: |-
                      (Block Set) A contact point that sends notifications to SensuGo. (see below for nested schema)
                      A contact point that sends notifications to SensuGo.
                    items:
                      properties:
                        check:
                          description: |-
                            (String) The SensuGo check to which the event should be routed.
                            The SensuGo check to which the event should be routed.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        entity:
                          description: |-
                            (String) The entity being monitored.
                            The entity being monitored.
                          type: string
                        handler:
                          description: |-
                            (String) A custom handler to execute in addition to the check.
                            A custom handler to execute in addition to the check.
                          type: string
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Templated message content describing the alert.
                          type: string
                        namespace:
                          description: |-
                            (String) The namespace in which the check resides.
                            The namespace in which the check resides.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The SensuGo URL to send requests to.
                          type: string
                      type: object
                    type: array
                  slack:
                    description: |-
                      (Block Set) A contact point that sends notifications to Slack. (see below for nested schema)
                      A contact point that sends notifications to Slack.
                    items:
                      properties:
                        color:
                          description: |-
                            (String) Templated color of the slack message.
                            Templated color of the slack message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        endpointUrl:
                          description: |-
                            (String) Use this to override the Slack API endpoint URL to send requests to.
                            Use this to override the Slack API endpoint URL to send requests to.
                          type: string
                        iconEmoji:
                          description: |-
                            (String) The name of a Slack workspace emoji to use as the bot icon.
                            The name of a Slack workspace emoji to use as the bot icon.
                          type: string
                        iconUrl:
                          description: |-
                            (String) A URL of an image to use as the bot icon.
                            A URL of an image to use as the bot icon.
                          type: string
                        mentionChannel:
                          description: |-
                            (String) Describes how to ping the slack channel that messages are being sent to. Options are here for an @here ping, channel for @channel, or empty for no ping.
                            Describes how to ping the slack channel that messages are being sent to. Options are `here` for an @here ping, `channel` for @channel, or empty for no ping.
                          type: string
                        mentionGroups:
                          description: |-
                            separated list of groups to mention in the message.
                            Comma-separated list of groups to mention in the message.
                          type: string
                        mentionUsers:
                          description: |-
                            separated list of users to mention in the message.
                            Comma-separated list of users to mention in the message.
                          type: string
                        recipient:
                          description: |-
                            (String) Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                            Channel, private group, or IM channel (can be an encoded ID or a name) to send messages to.
                          type: string
                        text:
                          description: |-
                            (String) Templated content of the message.
                            Templated content of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        username:
                          description: |-
                            (String) The user name to use when making a call to the Kafka REST Proxy
                            Username for the bot to use.
                          type: string
                      type: object
                    type: array
                  sns:
                    description: |-
                      (Block Set) A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana. (see below for nested schema)
                      A contact point that sends notifications to Amazon SNS. Requires Amazon Managed Grafana.
                    items:
                      properties:
                        assumeRoleArn:
                          description: |-
                            (String) The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                            The Amazon Resource Name (ARN) of the role to assume to send notifications to Amazon SNS.
                          type: string
                        authProvider:
                          description: |-
                            (String) The authentication provider to use. Valid values are default, arn and keys. Default is default. Defaults to default.
                            The authentication provider to use. Valid values are `default`, `arn` and `keys`. Default is `default`. Defaults to `default`.
                          type: string
                        body:
                          description: (String)
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        externalId:
                          description: |-
                            (String) The external ID to use when assuming the role.
                            The external ID to use when assuming the role.
                          type: string
                        messageFormat:
                          description: |-
                            (String) The format of the message to send. Valid values are text, body and json. Default is text. Defaults to text.
                            The format of the message to send. Valid values are `text`, `body` and `json`. Default is `text`. Defaults to `text`.
                          type: string
                        subject:
                          description: (String) The templated subject line of the
                            email. Defaults to “.
                          type: string
                        topic:
                          description: |-
                            (String) The name of the Kafka topic to publish to.
                            The Amazon SNS topic to send notifications to.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  teams:
                    description: |-
                      (Block Set) A contact point that sends notifications to Microsoft Teams. (see below for nested schema)
                      A contact point that sends notifications to Microsoft Teams.
                    items:
                      properties:
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated message content to send.
                          type: string
                        sectionTitle:
                          description: |-
                            (String) The templated subtitle for each message section.
                            The templated subtitle for each message section.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  telegram:
                    description: |-
                      (Block Set) A contact point that sends notifications to Telegram. (see below for nested schema)
                      A contact point that sends notifications to Telegram.
                    items:
                      properties:
                        chatId:
                          description: |-
                            (String) The chat ID to send messages to.
                            The chat ID to send messages to.
                          type: string
                        disableNotifications:
                          description: |-
                            (Boolean) When set users will receive a notification with no sound.
                            When set users will receive a notification with no sound.
                          type: boolean
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        disableWebPagePreview:
                          description: |-
                            (Boolean) When set it disables link previews for links in the message.
                            When set it disables link previews for links in the message.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message.
                          type: string
                        messageThreadId:
                          description: |-
                            (String) The ID of the message thread to send the message to.
                            The ID of the message thread to send the message to.
                          type: string
                        parseMode:
                          description: |-
                            (String) Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                            Mode for parsing entities in the message text. Supported: None, Markdown, MarkdownV2, and HTML. HTML is the default.
                          type: string
                        protectContent:
                          description: |-
                            (Boolean) When set it protects the contents of the message from forwarding and saving.
                            When set it protects the contents of the message from forwarding and saving.
                          type: boolean
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  threema:
                    description: |-
                      (Block Set) A contact point that sends notifications to Threema. (see below for nested schema)
                      A contact point that sends notifications to Threema.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            The templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        gatewayId:
                          description: |-
                            (String) The Threema gateway ID.
                            The Threema gateway ID.
                          type: string
                        recipientId:
                          description: |-
                            (String) The ID of the recipient of the message.
                            The ID of the recipient of the message.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  victorops:
                    description: |-
                      (Block Set) A contact point that sends notifications to VictorOps (now known as Splunk OnCall). (see below for nested schema)
                      A contact point that sends notifications to VictorOps (now known as Splunk OnCall).
                    items:
                      properties:
                        description:
                          description: |-
                            (String) The templated description of the Jira issue. Maximum length is 32767 characters.
                            Templated description of the message.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        messageType:
                          description: |-
                            either 'link' or 'actionCard'
                            The VictorOps alert state - typically either `CRITICAL` or `RECOVERY`.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title to display.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  webex:
                    description: |-
                      (Block Set) A contact point that sends notifications to Cisco Webex. (see below for nested schema)
                      A contact point that sends notifications to Cisco Webex.
                    items:
                      properties:
                        apiUrl:
                          description: |-
                            (String) The URL of the Jira REST API (v2 or v3).
                            The URL to send webhook requests to.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated title of the message to send.
                          type: string
                        roomId:
                          description: |-
                            (String) ID of the Webex Teams room where to send the messages.
                            ID of the Webex Teams room where to send the messages.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                  webhook:
                    description: |-
                      (Block Set) A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config (see below for nested schema)
                      A contact point that sends notifications to an arbitrary webhook, using the Prometheus webhook format defined here: https://prometheus.io/docs/alerting/latest/configuration/#webhook_config
                    items:
                      properties:
                        authorizationScheme:
                          description: |-
                            attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                            Allows a custom authorization scheme - attaches an auth header with this name. Do not use in conjunction with basic auth parameters.
                          type: string
                        basicAuthUser:
                          description: |-
                            (String) The username component of the basic auth credentials to use.
                            The username to use in basic auth headers attached to the request. If omitted, basic auth will not be used.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom headers to attach to the request.
                            Custom headers to attach to the request.
                          type: object
                          x-kubernetes-map-type: granular
                        hmacConfig:
                          description: |-
                            (Block Set, Max: 1) HMAC signature configuration options. (see below for nested schema)
                            HMAC signature configuration options.
                          items:
                            properties:
                              header:
                                description: |-
                                  Grafana-Alerting-Signature.
                                  The header in which the HMAC signature will be included. Defaults to `X-Grafana-Alerting-Signature`.
                                type: string
                              timestampHeader:
                                description: |-
                                  (String) If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                  If set, the timestamp will be included in the HMAC signature. The value should be the name of the header to use.
                                type: string
                            type: object
                          type: array
                        httpConfig:
                          description: |-
                            (Block Set, Max: 1) Common HTTP client options. (see below for nested schema)
                            Common HTTP client options.
                          items:
                            properties:
                              oauth2:
                                description: |-
                                  (Block Set, Max: 1) OAuth2 configuration options. (see below for nested schema)
                                  OAuth2 configuration options.
                                items:
                                  properties:
                                    clientId:
                                      description: |-
                                        (String) Client ID to use when authenticating.
                                        Client ID to use when authenticating.
                                      type: string
                                    endpointParams:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Optional parameters to append to the access token request.
                                        Optional parameters to append to the access token request.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    proxyConfig:
                                      description: |-
                                        (Block Set, Max: 1) Optional proxy configuration for OAuth2 requests. (see below for nested schema)
                                        Optional proxy configuration for OAuth2 requests.
                                      items:
                                        properties:
                                          noProxy:
                                            description: |-
                                              separated list of addresses that should not use a proxy.
                                              Comma-separated list of addresses that should not use a proxy.
                                            type: string
                                          proxyConnectHeader:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              (Map of String) Optional headers to send to proxies during CONNECT requests.
                                              Optional headers to send to proxies during CONNECT requests.
                                            type: object
                                            x-kubernetes-map-type: granular
                                          proxyFromEnvironment:
                                            description: |-
                                              (Boolean) Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to false.
                                              Use environment HTTP_PROXY, HTTPS_PROXY and NO_PROXY to determine proxies. Defaults to `false`.
                                            type: boolean
                                          proxyUrl:
                                            description: |-
                                              (String) HTTP proxy server to use to connect to the targets.
                                              HTTP proxy server to use to connect to the targets.
                                            type: string
                                        type: object
                                      type: array
                                    scopes:
                                      description: |-
                                        (List of String) Optional scopes to request when obtaining an access token.
                                        Optional scopes to request when obtaining an access token.
                                      items:
                                        type: string
                                      type: array
                                    tlsConfig:
                                      description: |-
                                        (Map of String, Sensitive) Allows configuring TLS for the webhook notifier.
                                        Optional TLS configuration options for OAuth2 requests.
                                      items:
                                        properties:
                                          insecureSkipVerify:
                                            description: |-
                                              (Boolean) Do not verify the server's certificate chain and host name. Defaults to false.
                                              Do not verify the server's certificate chain and host name. Defaults to `false`.
                                            type: boolean
                                        type: object
                                      type: array
                                    tokenUrl:
                                      description: |-
                                        (String) URL for the access token endpoint.
                                        URL for the access token endpoint.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        httpMethod:
                          description: |-
                            (String) The HTTP method to use in the request. Defaults to POST.
                            The HTTP method to use in the request. Defaults to `POST`.
                          type: string
                        maxAlerts:
                          description: |-
                            (Number) The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                            The maximum number of alerts to send in a single request. This can be helpful in limiting the size of the request body. The default is 0, which indicates no limit.
                          type: number
                        message:
                          description: |-
                            (String) The templated content of the message.
                            Custom message. You can use template variables.
                          type: string
                        payload:
                          description: |-
                            (Block Set, Max: 1) Optionally provide a templated payload. Overrides 'Message' and 'Title' field. (see below for nested schema)
                            Optionally provide a templated payload. Overrides 'Message' and 'Title' field.
                          items:
                            properties:
                              template:
                                description: |-
                                  (String) Custom payload template.
                                  Custom payload template.
                                type: string
                              vars:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Optionally provide a variables to be used in the payload template. They will be available in the template as .Vars.<variable_name>.
                                  Optionally provide a variables to be used in the payload template. They will be available in the template as `.Vars.<variable_name>`.
                                type: object
                                x-kubernetes-map-type: granular
                            type: object
                          type: array
                        title:
                          description: |-
                            (String) The templated title of the message.
                            Templated title of the message.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                        url:
                          description: |-
                            (String) The URL of the Alertmanager instance.
                            The URL to send webhook requests to.
                          type: string
                      type: object
                    type: array
                  wecom:
                    description: |-
                      (Block Set) A contact point that sends notifications to WeCom. (see below for nested schema)
                      A contact point that sends notifications to WeCom.
                    items:
                      properties:
                        agentId:
                          description: |-
                            (String) Agent ID added to the request payload when using APIAPP.
                            Agent ID added to the request payload when using APIAPP.
                          type: string
                        corpId:
                          description: |-
                            (String) Corp ID used to get token when using APIAPP.
                            Corp ID used to get token when using APIAPP.
                          type: string
                        disableResolveMessage:
                          description: |-
                            (Boolean) Whether to disable sending resolve messages. Defaults to false.
                            Whether to disable sending resolve messages. Defaults to `false`.
                          type: boolean
                        message:
                          description: |-
                            (String) The templated content of the message.
                            The templated content of the message to send.
                          type: string
                        msgType:
                          description: |-
                            (String) The type of them message. Supported: markdown, text. Default: text.
                            The type of them message. Supported: markdown, text. Default: text.
                          type: string
                        title:
                          description: |-
                            (String) The templated title of the message.
                            The templated title of the message to send.
                          type: string
                        toUser:
                          description: |-
                            (String) The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                            The ID of user that should receive the message. Multiple entries should be separated by '|'. Default: @all.
                          type: string
                        uid:
                          description: |-
                            (String) The UID of the contact point.
                            The UID of the contact point.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: inhibitionrulev1beta1s.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: InhibitionruleV1Beta1
    listKind: InhibitionruleV1Beta1List
    plural: inhibitionrulev1beta1s
    singular: inhibitionrulev1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InhibitionruleV1Beta1 is the Schema for the InhibitionruleV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InhibitionruleV1Beta1Spec defines the desired state of InhibitionruleV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: InhibitionruleV1Beta1Status defines the observed state of
              InhibitionruleV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      equal:
                        description: Labels that must have equal values in source
                          and target alerts for the inhibition to take effect.
                        items:
                          type: string
                        type: array
                      sourceMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be a source of inhibition.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                      targetMatchers:
                        description: Matchers that must be satisfied for an alert
                          to be inhibited.
                        items:
                          properties:
                            label:
                              type: string
                            type:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: messagetemplates.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MessageTemplate
    listKind: MessageTemplateList
    plural: messagetemplates
    singular: messagetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MessageTemplate is the Schema for the MessageTemplates API. Manages
          Grafana Alerting notification template groups, including notification templates.
          Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#notification-template-groups
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MessageTemplateSpec defines the desired state of MessageTemplate
            properties:
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.template is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.template)
                || (has(self.initProvider) && has(self.initProvider.template))'
          status:
            description: MessageTemplateStatus defines the observed state of MessageTemplate.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the notification template group.
                      The name of the notification template group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  template:
                    description: |-
                      (String) The content of the notification template group.
                      The content of the notification template group.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: mutetimings.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MuteTiming
    listKind: MuteTimingList
    plural: mutetimings
    singular: mutetiming
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MuteTiming is the Schema for the MuteTimings API. Manages Grafana
          Alerting mute timings. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#mute-timings
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MuteTimingSpec defines the desired state of MuteTiming
            properties:
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: MuteTimingStatus defines the observed state of MuteTiming.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  intervals:
                    description: |-
                      (Block List) The time intervals at which to mute notifications. Use an empty block to mute all the time. (see below for nested schema)
                      The time intervals at which to mute notifications. Use an empty block to mute all the time.
                    items:
                      properties:
                        daysOfMonth:
                          description: |-
                            31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                            An inclusive range of days, 1-31, within a month, e.g. "1" or "14:16". Negative values can be used to represent days counting from the end of a month, e.g. "-1".
                          items:
                            type: string
                          type: array
                        location:
                          description: |-
                            (String) Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                            Provides the time zone for the time interval. Must be a location in the IANA time zone database, e.g "America/New_York"
                          type: string
                        months:
                          description: |-
                            (List of String) An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                            An inclusive range of months, either numerical or full calendar month, e.g. "1:3", "december", or "may:august".
                          items:
                            type: string
                          type: array
                        times:
                          description: |-
                            (Block List) The time ranges, represented in minutes, during which to mute in a given day. (see below for nested schema)
                            The time ranges, represented in minutes, during which to mute in a given day.
                          items:
                            properties:
                              end:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should end exclusively.
                                  The time, in hh:mm format, of when the interval should end exclusively.
                                type: string
                              start:
                                description: |-
                                  (String) The time, in hh:mm format, of when the interval should begin inclusively.
                                  The time, in hh:mm format, of when the interval should begin inclusively.
                                type: string
                            type: object
                          type: array
                        weekdays:
                          description: |-
                            (List of String) An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                            An inclusive range of weekdays, e.g. "monday" or "tuesday:thursday".
                          items:
                            type: string
                          type: array
                        years:
                          description: |-
                            (List of String) A positive inclusive range of years, e.g. "2030" or "2025:2026".
                            A positive inclusive range of years, e.g. "2030" or "2025:2026".
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the mute timing.
                      The name of the mute timing.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: notificationpolicies.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: NotificationPolicy
    listKind: NotificationPolicyList
    plural: notificationpolicies
    singular: notificationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotificationPolicy is the Schema for the NotificationPolicys
          API. Sets the global notification policy for Grafana. !> This resource manages
          the entire notification policy tree and overwrites its policies. However,
          it does not overwrite internal policies created when alert rules directly
          set a contact point for notifications. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#notification-policies
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationPolicySpec defines the desired state of NotificationPolicy
            properties:
              forProvider:
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  contactPointRef:
                    description: Reference to a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  contactPointSelector:
                    description: Selector for a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        contactPointRef:
                          description: Reference to a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        contactPointSelector:
                          description: Selector for a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimingRef:
                          description: References to MuteTiming in alerting to populate
                            muteTimings.
                          items:
                            description: A NamespacedReference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              namespace:
                                description: Namespace of the referenced object
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        muteTimingSelector:
                          description: Selector for a list of MuteTiming in alerting
                            to populate muteTimings.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimingRef:
                                description: References to MuteTiming in alerting
                                  to populate muteTimings.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              muteTimingSelector:
                                description: Selector for a list of MuteTiming in
                                  alerting to populate muteTimings.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    contactPointRef:
                                      description: Reference to a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        name:
                                          description: Name of the referenced object.
                                          type: string
                                        namespace:
                                          description: Namespace of the referenced
                                            object
                                          type: string
                                        policy:
                                          description: Policies for referencing.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      required:
                                      - name
                                      type: object
                                    contactPointSelector:
                                      description: Selector for a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        namespace:
                                          description: Namespace for the selector
                                          type: string
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimingRef:
                                      description: References to MuteTiming in alerting
                                        to populate muteTimings.
                                      items:
                                        description: A NamespacedReference to a named
                                          object.
                                        properties:
                                          name:
                                            description: Name of the referenced object.
                                            type: string
                                          namespace:
                                            description: Namespace of the referenced
                                              object
                                            type: string
                                          policy:
                                            description: Policies for referencing.
                                            properties:
                                              resolution:
                                                default: Required
                                                description: |-
                                                  Resolution specifies whether resolution of this reference is required.
                                                  The default is 'Required', which means the reconcile will fail if the
                                                  reference cannot be resolved. 'Optional' means this reference will be
                                                  a no-op if it cannot be resolved.
                                                enum:
                                                - Required
                                                - Optional
                                                type: string
                                              resolve:
                                                description: |-
                                                  Resolve specifies when this reference should be resolved. The default
                                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                                  the corresponding field is not present. Use 'Always' to resolve the
                                                  reference on every reconcile.
                                                enum:
                                                - Always
                                                - IfNotPresent
                                                type: string
                                            type: object
                                        required:
                                        - name
                                        type: object
                                      type: array
                                    muteTimingSelector:
                                      description: Selector for a list of MuteTiming
                                        in alerting to populate muteTimings.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        namespace:
                                          description: Namespace for the selector
                                          type: string
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          contactPointRef:
                                            description: Reference to a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              name:
                                                description: Name of the referenced
                                                  object.
                                                type: string
                                              namespace:
                                                description: Namespace of the referenced
                                                  object
                                                type: string
                                              policy:
                                                description: Policies for referencing.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            required:
                                            - name
                                            type: object
                                          contactPointSelector:
                                            description: Selector for a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              namespace:
                                                description: Namespace for the selector
                                                type: string
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimingRef:
                                            description: References to MuteTiming
                                              in alerting to populate muteTimings.
                                            items:
                                              description: A NamespacedReference to
                                                a named object.
                                              properties:
                                                name:
                                                  description: Name of the referenced
                                                    object.
                                                  type: string
                                                namespace:
                                                  description: Namespace of the referenced
                                                    object
                                                  type: string
                                                policy:
                                                  description: Policies for referencing.
                                                  properties:
                                                    resolution:
                                                      default: Required
                                                      description: |-
                                                        Resolution specifies whether resolution of this reference is required.
                                                        The default is 'Required', which means the reconcile will fail if the
                                                        reference cannot be resolved. 'Optional' means this reference will be
                                                        a no-op if it cannot be resolved.
                                                      enum:
                                                      - Required
                                                      - Optional
                                                      type: string
                                                    resolve:
                                                      description: |-
                                                        Resolve specifies when this reference should be resolved. The default
                                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                                        the corresponding field is not present. Use 'Always' to resolve the
                                                        reference on every reconcile.
                                                      enum:
                                                      - Always
                                                      - IfNotPresent
                                                      type: string
                                                  type: object
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          muteTimingSelector:
                                            description: Selector for a list of MuteTiming
                                              in alerting to populate muteTimings.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              namespace:
                                                description: Namespace for the selector
                                                type: string
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  contactPointRef:
                    description: Reference to a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  contactPointSelector:
                    description: Selector for a ContactPoint in alerting to populate
                      contactPoint.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        contactPointRef:
                          description: Reference to a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        contactPointSelector:
                          description: Selector for a ContactPoint in alerting to
                            populate contactPoint.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimingRef:
                          description: References to MuteTiming in alerting to populate
                            muteTimings.
                          items:
                            description: A NamespacedReference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              namespace:
                                description: Namespace of the referenced object
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        muteTimingSelector:
                          description: Selector for a list of MuteTiming in alerting
                            to populate muteTimings.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimingRef:
                                description: References to MuteTiming in alerting
                                  to populate muteTimings.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              muteTimingSelector:
                                description: Selector for a list of MuteTiming in
                                  alerting to populate muteTimings.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    contactPointRef:
                                      description: Reference to a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        name:
                                          description: Name of the referenced object.
                                          type: string
                                        namespace:
                                          description: Namespace of the referenced
                                            object
                                          type: string
                                        policy:
                                          description: Policies for referencing.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      required:
                                      - name
                                      type: object
                                    contactPointSelector:
                                      description: Selector for a ContactPoint in
                                        alerting to populate contactPoint.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        namespace:
                                          description: Namespace for the selector
                                          type: string
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimingRef:
                                      description: References to MuteTiming in alerting
                                        to populate muteTimings.
                                      items:
                                        description: A NamespacedReference to a named
                                          object.
                                        properties:
                                          name:
                                            description: Name of the referenced object.
                                            type: string
                                          namespace:
                                            description: Namespace of the referenced
                                              object
                                            type: string
                                          policy:
                                            description: Policies for referencing.
                                            properties:
                                              resolution:
                                                default: Required
                                                description: |-
                                                  Resolution specifies whether resolution of this reference is required.
                                                  The default is 'Required', which means the reconcile will fail if the
                                                  reference cannot be resolved. 'Optional' means this reference will be
                                                  a no-op if it cannot be resolved.
                                                enum:
                                                - Required
                                                - Optional
                                                type: string
                                              resolve:
                                                description: |-
                                                  Resolve specifies when this reference should be resolved. The default
                                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                                  the corresponding field is not present. Use 'Always' to resolve the
                                                  reference on every reconcile.
                                                enum:
                                                - Always
                                                - IfNotPresent
                                                type: string
                                            type: object
                                        required:
                                        - name
                                        type: object
                                      type: array
                                    muteTimingSelector:
                                      description: Selector for a list of MuteTiming
                                        in alerting to populate muteTimings.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        namespace:
                                          description: Namespace for the selector
                                          type: string
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          contactPointRef:
                                            description: Reference to a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              name:
                                                description: Name of the referenced
                                                  object.
                                                type: string
                                              namespace:
                                                description: Namespace of the referenced
                                                  object
                                                type: string
                                              policy:
                                                description: Policies for referencing.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            required:
                                            - name
                                            type: object
                                          contactPointSelector:
                                            description: Selector for a ContactPoint
                                              in alerting to populate contactPoint.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              namespace:
                                                description: Namespace for the selector
                                                type: string
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimingRef:
                                            description: References to MuteTiming
                                              in alerting to populate muteTimings.
                                            items:
                                              description: A NamespacedReference to
                                                a named object.
                                              properties:
                                                name:
                                                  description: Name of the referenced
                                                    object.
                                                  type: string
                                                namespace:
                                                  description: Namespace of the referenced
                                                    object
                                                  type: string
                                                policy:
                                                  description: Policies for referencing.
                                                  properties:
                                                    resolution:
                                                      default: Required
                                                      description: |-
                                                        Resolution specifies whether resolution of this reference is required.
                                                        The default is 'Required', which means the reconcile will fail if the
                                                        reference cannot be resolved. 'Optional' means this reference will be
                                                        a no-op if it cannot be resolved.
                                                      enum:
                                                      - Required
                                                      - Optional
                                                      type: string
                                                    resolve:
                                                      description: |-
                                                        Resolve specifies when this reference should be resolved. The default
                                                        is 'IfNotPresent', which will attempt to resolve the reference only when
                                                        the corresponding field is not present. Use 'Always' to resolve the
                                                        reference on every reconcile.
                                                      enum:
                                                      - Always
                                                      - IfNotPresent
                                                      type: string
                                                  type: object
                                              required:
                                              - name
                                              type: object
                                            type: array
                                          muteTimingSelector:
                                            description: Selector for a list of MuteTiming
                                              in alerting to populate muteTimings.
                                            properties:
                                              matchControllerRef:
                                                description: |-
                                                  MatchControllerRef ensures an object with the same controller reference
                                                  as the selecting object is selected.
                                                type: boolean
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: MatchLabels ensures an
                                                  object with matching labels is selected.
                                                type: object
                                              namespace:
                                                description: Namespace for the selector
                                                type: string
                                              policy:
                                                description: Policies for selection.
                                                properties:
                                                  resolution:
                                                    default: Required
                                                    description: |-
                                                      Resolution specifies whether resolution of this reference is required.
                                                      The default is 'Required', which means the reconcile will fail if the
                                                      reference cannot be resolved. 'Optional' means this reference will be
                                                      a no-op if it cannot be resolved.
                                                    enum:
                                                    - Required
                                                    - Optional
                                                    type: string
                                                  resolve:
                                                    description: |-
                                                      Resolve specifies when this reference should be resolved. The default
                                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                                      the corresponding field is not present. Use 'Always' to resolve the
                                                      reference on every reconcile.
                                                    enum:
                                                    - Always
                                                    - IfNotPresent
                                                    type: string
                                                type: object
                                            type: object
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.groupBy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.groupBy)
                || (has(self.initProvider) && has(self.initProvider.groupBy))'
          status:
            description: NotificationPolicyStatus defines the observed state of NotificationPolicy.
            properties:
              atProvider:
                properties:
                  contactPoint:
                    description: |-
                      (String) The default contact point to route all unmatched notifications to.
                      The default contact point to route all unmatched notifications to.
                    type: string
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  groupBy:
                    description: |-
                      (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                      A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                      Minimum time interval between two notifications for the same group. Default is 5 minutes.
                    type: string
                  groupWait:
                    description: |-
                      (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                      Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  policy:
                    description: |-
                      (Block List) Routing rules for specific label sets. (see below for nested schema)
                      Routing rules for specific label sets.
                    items:
                      properties:
                        activeTimings:
                          description: |-
                            (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                            A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: |-
                            (String) The default contact point to route all unmatched notifications to.
                            The contact point to route notifications that match this rule to.
                          type: string
                        continue:
                          description: |-
                            (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                            Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                          type: boolean
                        groupBy:
                          description: |-
                            (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                            A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: |-
                            (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                            Minimum time interval between two notifications for the same group. Default is 5 minutes.
                          type: string
                        groupWait:
                          description: |-
                            (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                            Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                          type: string
                        matcher:
                          description: |-
                            (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                            Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                          items:
                            properties:
                              label:
                                description: |-
                                  (String) The name of the label to match against.
                                  The name of the label to match against.
                                type: string
                              match:
                                description: |-
                                  (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                  The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                type: string
                              value:
                                description: |-
                                  (String) The label value to match against.
                                  The label value to match against.
                                type: string
                            type: object
                          type: array
                        muteTimings:
                          description: |-
                            (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                            A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            (Block List) Routing rules for specific label sets. (see below for nested schema)
                            Routing rules for specific label sets.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The default contact point to route all unmatched notifications to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              continue:
                                description: |-
                                  (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                  Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                type: boolean
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              matcher:
                                description: |-
                                  (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                  Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                items:
                                  properties:
                                    label:
                                      description: |-
                                        (String) The name of the label to match against.
                                        The name of the label to match against.
                                      type: string
                                    match:
                                      description: |-
                                        (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                        The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                      type: string
                                    value:
                                      description: |-
                                        (String) The label value to match against.
                                        The label value to match against.
                                      type: string
                                  type: object
                                type: array
                              muteTimings:
                                description: |-
                                  (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                  A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  (Block List) Routing rules for specific label sets. (see below for nested schema)
                                  Routing rules for specific label sets.
                                items:
                                  properties:
                                    activeTimings:
                                      description: |-
                                        (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                        A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: |-
                                        (String) The default contact point to route all unmatched notifications to.
                                        The contact point to route notifications that match this rule to.
                                      type: string
                                    continue:
                                      description: |-
                                        (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                        Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                      type: boolean
                                    groupBy:
                                      description: |-
                                        (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                        A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: |-
                                        (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                        Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                      type: string
                                    groupWait:
                                      description: |-
                                        (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                        Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                      type: string
                                    matcher:
                                      description: |-
                                        (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                        Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                      items:
                                        properties:
                                          label:
                                            description: |-
                                              (String) The name of the label to match against.
                                              The name of the label to match against.
                                            type: string
                                          match:
                                            description: |-
                                              (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                              The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                            type: string
                                          value:
                                            description: |-
                                              (String) The label value to match against.
                                              The label value to match against.
                                            type: string
                                        type: object
                                      type: array
                                    muteTimings:
                                      description: |-
                                        (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                        A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                      items:
                                        type: string
                                      type: array
                                    policy:
                                      description: |-
                                        (Block List) Routing rules for specific label sets. (see below for nested schema)
                                        Routing rules for specific label sets.
                                      items:
                                        properties:
                                          activeTimings:
                                            description: |-
                                              (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                              A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                            items:
                                              type: string
                                            type: array
                                          contactPoint:
                                            description: |-
                                              (String) The default contact point to route all unmatched notifications to.
                                              The contact point to route notifications that match this rule to.
                                            type: string
                                          continue:
                                            description: |-
                                              (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                              Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                            type: boolean
                                          groupBy:
                                            description: |-
                                              (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                              A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                            items:
                                              type: string
                                            type: array
                                          groupInterval:
                                            description: |-
                                              (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                              Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                            type: string
                                          groupWait:
                                            description: |-
                                              (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                              Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                            type: string
                                          matcher:
                                            description: |-
                                              (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                              Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                            items:
                                              properties:
                                                label:
                                                  description: |-
                                                    (String) The name of the label to match against.
                                                    The name of the label to match against.
                                                  type: string
                                                match:
                                                  description: |-
                                                    (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                    The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The label value to match against.
                                                    The label value to match against.
                                                  type: string
                                              type: object
                                            type: array
                                          muteTimings:
                                            description: |-
                                              (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                              A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                            items:
                                              type: string
                                            type: array
                                          policy:
                                            description: |-
                                              (Block List) Routing rules for specific label sets. (see below for nested schema)
                                              Routing rules for specific label sets.
                                            items:
                                              properties:
                                                activeTimings:
                                                  description: |-
                                                    (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                    A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                                  items:
                                                    type: string
                                                  type: array
                                                contactPoint:
                                                  description: |-
                                                    (String) The default contact point to route all unmatched notifications to.
                                                    The contact point to route notifications that match this rule to.
                                                  type: string
                                                continue:
                                                  description: |-
                                                    (Boolean) Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                    Whether to continue matching subsequent rules if an alert matches the current rule. Otherwise, the rule will be 'consumed' by the first policy to match it.
                                                  type: boolean
                                                groupBy:
                                                  description: |-
                                                    (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping.
                                                    A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. Required for root policy only. If empty, the parent grouping is used.
                                                  items:
                                                    type: string
                                                  type: array
                                                groupInterval:
                                                  description: |-
                                                    (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                    Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                                  type: string
                                                groupWait:
                                                  description: |-
                                                    (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                    Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                                  type: string
                                                matcher:
                                                  description: |-
                                                    (Block Set) Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances. (see below for nested schema)
                                                    Describes which labels this rule should match. When multiple matchers are supplied, an alert must match ALL matchers to be accepted by this policy. When no matchers are supplied, the rule will match all alert instances.
                                                  items:
                                                    properties:
                                                      label:
                                                        description: |-
                                                          (String) The name of the label to match against.
                                                          The name of the label to match against.
                                                        type: string
                                                      match:
                                                        description: |-
                                                          (String) The operator to apply when matching values of the given label. Allowed operators are = for equality, != for negated equality, =~ for regex equality, and !~ for negated regex equality.
                                                          The operator to apply when matching values of the given label. Allowed operators are `=` for equality, `!=` for negated equality, `=~` for regex equality, and `!~` for negated regex equality.
                                                        type: string
                                                      value:
                                                        description: |-
                                                          (String) The label value to match against.
                                                          The label value to match against.
                                                        type: string
                                                    type: object
                                                  type: array
                                                muteTimings:
                                                  description: |-
                                                    (List of String) A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                    A list of time intervals to apply to alerts that match this policy to mute them for the specified time.
                                                  items:
                                                    type: string
                                                  type: array
                                                repeatInterval:
                                                  description: |-
                                                    sending a notification if an alert is still firing. Default is 4 hours.
                                                    Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                                  type: string
                                              type: object
                                            type: array
                                          repeatInterval:
                                            description: |-
                                              sending a notification if an alert is still firing. Default is 4 hours.
                                              Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                            type: string
                                        type: object
                                      type: array
                                    repeatInterval:
                                      description: |-
                                        sending a notification if an alert is still firing. Default is 4 hours.
                                        Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: |-
                            sending a notification if an alert is still firing. Default is 4 hours.
                            Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      sending a notification if an alert is still firing. Default is 4 hours.
                      Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: recordingrulev0alpha1s.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RecordingruleV0Alpha1
    listKind: RecordingruleV0Alpha1List
    plural: recordingrulev0alpha1s
    singular: recordingrulev0alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RecordingruleV0Alpha1 is the Schema for the RecordingruleV0Alpha1s
          API. Manages Grafana Recording Rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RecordingruleV0Alpha1Spec defines the desired state of RecordingruleV0Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: RecordingruleV0Alpha1Status defines the observed state of
              RecordingruleV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      expressions:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                          A sequence of stages that describe the contents of the rule. Each value is a JSON string representing an expression object.
                        type: object
                        x-kubernetes-map-type: granular
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          value pairs to attach to the recorded metric.
                          Key-value pairs to attach to the recorded metric.
                        type: object
                        x-kubernetes-map-type: granular
                      metric:
                        description: |-
                          (String) The name of the metric to write to.
                          The name of the metric to write to.
                        type: string
                      paused:
                        description: |-
                          (Boolean) Sets whether the recording rule should be paused or not.
                          Sets whether the recording rule should be paused or not.
                        type: boolean
                      targetDatasourceUid:
                        description: |-
                          (String) The UID of the datasource to write the metric to.
                          The UID of the datasource to write the metric to.
                        type: string
                      title:
                        description: |-
                          (String) The title of the recording rule.
                          The title of the recording rule.
                        type: string
                      trigger:
                        description: |-
                          (Block, Optional) The trigger configuration for the recording rule. (see below for nested schema)
                          The trigger configuration for the recording rule.
                        properties:
                          interval:
                            description: |-
                              (String) The interval at which the recording rule should be evaluated.
                              The interval at which the recording rule should be evaluated.
                            type: string
                        type: object
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rulegroups.alerting.grafana.m.crossplane.io
spec:
  group: alerting.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RuleGroup
    listKind: RuleGroupList
    plural: rulegroups
    singular: rulegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleGroup is the Schema for the RuleGroups API. Manages Grafana
          Alerting rule groups. Official documentation https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/alerting_provisioning/#alert-rules
          This resource requires Grafana 9.1.0 or later.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleGroupSpec defines the desired state of RuleGroup
            properties:
              forProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              contactPointRef:
                                description: Reference to a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: Selector for a ContactPoint in alerting
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.intervalSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.intervalSeconds)
                || (has(self.initProvider) && has(self.initProvider.intervalSeconds))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rule is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rule)
                || (has(self.initProvider) && has(self.initProvider.rule))'
          status:
            description: RuleGroupStatus defines the observed state of RuleGroup.
            properties:
              atProvider:
                properties:
                  disableProvenance:
                    description: Defaults to false. Defaults to `false`.
                    type: boolean
                  folderUid:
                    description: |-
                      (String) The UID of the folder that the group belongs to.
                      The UID of the folder that the group belongs to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  intervalSeconds:
                    description: |-
                      (Number) The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                      The interval, in seconds, at which all rules in the group are evaluated. If a group contains many rules, the rules are evaluated sequentially.
                    type: number
                  name:
                    description: |-
                      (String) The name of the rule group.
                      The name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  rule:
                    description: |-
                      (Block List, Min: 1) The rules within the group. (see below for nested schema)
                      The rules within the group.
                    items:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs of metadata to attach to the alert rule. They add additional information, such as a summary or runbook_url, to help identify and investigate alerts. The __dashboardUid__ and __panelId__ annotations, which link alerts to a panel, must be set together. Defaults to map[].
                            Key-value pairs of metadata to attach to the alert rule. They add additional information, such as a `summary` or `runbook_url`, to help identify and investigate alerts. The `__dashboardUid__` and `__panelId__` annotations, which link alerts to a panel, must be set together. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        condition:
                          description: |-
                            (String) The ref_id of the query node in the data field to use as the alert condition.
                            The `ref_id` of the query node in the `data` field to use as the alert condition.
                          type: string
                        data:
                          description: |-
                            (Block List, Min: 1) A sequence of stages that describe the contents of the rule. (see below for nested schema)
                            A sequence of stages that describe the contents of the rule.
                          items:
                            properties:
                              datasourceUid:
                                description: |-
                                  100" if this stage is an expression stage.
                                  The UID of the datasource being queried, or "-100" if this stage is an expression stage.
                                type: string
                              model:
                                description: |-
                                  (String) Custom JSON data to send to the specified datasource when querying.
                                  Custom JSON data to send to the specified datasource when querying.
                                type: string
                              queryType:
                                description: |-
                                  (String) An optional identifier for the type of query being executed. Defaults to “.
                                  An optional identifier for the type of query being executed. Defaults to “.
                                type: string
                              refId:
                                description: |-
                                  (String) A unique string to identify this query stage within a rule.
                                  A unique string to identify this query stage within a rule.
                                type: string
                              relativeTimeRange:
                                description: |-
                                  (Block List, Min: 1, Max: 1) The time range, relative to when the query is executed, across which to query. (see below for nested schema)
                                  The time range, relative to when the query is executed, across which to query.
                                items:
                                  properties:
                                    from:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                      type: number
                                    to:
                                      description: |-
                                        (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                        The number of seconds in the past, relative to when the rule is evaluated, at which the time range ends.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        execErrState:
                          description: |-
                            (String) Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                            Describes what state to enter when the rule's query is invalid and the rule cannot be executed. Options are OK, Error, KeepLast, and Alerting.  Defaults to Alerting if not set.
                          type: string
                        for:
                          description: |-
                            (String) The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to 0.
                            The amount of time for which the rule must be breached for the rule to be considered to be Firing. Before this time has elapsed, the rule is only considered to be Pending. Defaults to `0`.
                          type: string
                        isPaused:
                          description: |-
                            (Boolean) Sets whether the alert should be paused or not. Defaults to false.
                            Sets whether the alert should be paused or not. Defaults to `false`.
                          type: boolean
                        keepFiringFor:
                          description: |-
                            (String) The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                            The amount of time for which the rule will considered to be Recovering after initially Firing. Before this time has elapsed, the rule will continue to fire once it's been triggered.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to map[].
                            Key-value pairs to attach to the alert rule that can be used in matching, grouping, and routing. Defaults to `map[]`.
                          type: object
                          x-kubernetes-map-type: granular
                        missingSeriesEvalsToResolve:
                          description: |-
                            (Number) The number of missing series evaluations that must occur before the rule is considered to be resolved.
                            The number of missing series evaluations that must occur before the rule is considered to be resolved.
                          type: number
                        name:
                          description: |-
                            (String) The name of the rule group.
                            The name of the alert rule.
                          type: string
                        noDataState:
                          description: |-
                            (String) Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                            Describes what state to enter when the rule's query returns No Data. Options are OK, NoData, KeepLast, and Alerting. Defaults to NoData if not set.
                          type: string
                        notificationSettings:
                          description: |-
                            (Block List, Max: 1) Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled. (see below for nested schema)
                            Notification settings for the rule. If specified, it overrides the notification policies. Available since Grafana 10.4, requires feature flag 'alertingSimplifiedRouting' to be enabled.
                          items:
                            properties:
                              activeTimings:
                                description: |-
                                  (List of String) A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                  A list of time interval names to apply to alerts that match this policy to suppress them unless they are sent at the specified time. Supported in Grafana 12.1.0 and later
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: |-
                                  (String) The contact point to route notifications that match this rule to.
                                  The contact point to route notifications that match this rule to.
                                type: string
                              groupBy:
                                description: |-
                                  (List of String) A list of alert labels to group alerts into notifications by. Use the special label ... to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                  A list of alert labels to group alerts into notifications by. Use the special label `...` to group alerts by all labels, effectively disabling grouping. If empty, no grouping is used. If specified, requires labels 'alertname' and 'grafana_folder' to be included.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: |-
                                  (String) Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                  Minimum time interval between two notifications for the same group. Default is 5 minutes.
                                type: string
                              groupWait:
                                description: |-
                                  (String) Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                  Time to wait to buffer alerts of the same group before sending a notification. Default is 30 seconds.
                                type: string
                              muteTimings:
                                description: |-
                                  (List of String) A list of mute timing names to apply to alerts that match this policy.
                                  A list of mute timing names to apply to alerts that match this policy.
                                items:
                                  type: string
                                type: array
                              repeatInterval:
                                description: |-
                                  sending a notification if an alert is still firing. Default is 4 hours.
                                  Minimum time interval for re-sending a notification if an alert is still firing. Default is 4 hours.
                                type: string
                            type: object
                          type: array
                        record:
                          description: |-
                            (Block List, Max: 1) Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled. (see below for nested schema)
                            Settings for a recording rule. Available since Grafana 11.2, requires feature flag 'grafanaManagedRecordingRules' to be enabled.
                          items:
                            properties:
                              from:
                                description: |-
                                  (Number) The number of seconds in the past, relative to when the rule is evaluated, at which the time range begins.
                                  The ref id of the query node in the data field to use as the source of the metric.
                                type: string
                              metric:
                                description: |-
                                  (String) The name of the metric to write to.
                                  The name of the metric to write to.
                                type: string
                              targetDatasourceUid:
                                description: |-
                                  (String) The UID of the datasource to write the metric to.
                                  The UID of the datasource to write the metric to.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) The unique identifier of the alert rule.
                            The unique identifier of the alert rule.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: custommodelrules.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CustomModelRules
    listKind: CustomModelRulesList
    plural: custommodelrules
    singular: custommodelrules
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CustomModelRules is the Schema for the CustomModelRuless API.
          Manages Knowledge Graph Custom Model Rules through the Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomModelRulesSpec defines the desired state of CustomModelRules
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rules)
                || (has(self.initProvider) && has(self.initProvider.rules))'
          status:
            description: CustomModelRulesStatus defines the observed state of CustomModelRules.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: logconfigs.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LogConfig
    listKind: LogConfigList
    plural: logconfigs
    singular: logconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogConfig is the Schema for the LogConfigs API. Manages Knowledge
          Graph Log Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogConfigSpec defines the desired state of LogConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: LogConfigStatus defines the observed state of LogConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: notificationalertsconfigs.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: NotificationAlertsConfig
    listKind: NotificationAlertsConfigList
    plural: notificationalertsconfigs
    singular: notificationalertsconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotificationAlertsConfig is the Schema for the NotificationAlertsConfigs
          API. Manages Knowledge Graph Alert Configurations through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationAlertsConfigSpec defines the desired state of
              NotificationAlertsConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: NotificationAlertsConfigStatus defines the observed state
              of NotificationAlertsConfig.
            properties:
              atProvider:
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: profileconfigs.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProfileConfig
    listKind: ProfileConfigList
    plural: profileconfigs
    singular: profileconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProfileConfig is the Schema for the ProfileConfigs API. Manages
          Knowledge Graph Profile Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProfileConfigSpec defines the desired state of ProfileConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: ProfileConfigStatus defines the observed state of ProfileConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: promrulefiles.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PromRuleFile
    listKind: PromRuleFileList
    plural: promrulefiles
    singular: promrulefile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PromRuleFile is the Schema for the PromRuleFiles API. Manages
          Prometheus Rules configurations through Grafana Asserts API. Allows creation
          and management of custom Prometheus recording and alerting rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PromRuleFileSpec defines the desired state of PromRuleFile
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.group is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.group)
                || (has(self.initProvider) && has(self.initProvider.group))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PromRuleFileStatus defines the observed state of PromRuleFile.
            properties:
              atProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Stack is the Schema for the Stacks API. Manages the Asserts
          Stack configuration. This resource configures the Asserts stack with the
          required API tokens for integration with Grafana Cloud services. It performs
          the full onboarding flow: Provisions API tokensConfigures datasets (auto-detected
          or manually specified)Enables the stack By default, datasets are auto-configured
          based on detected metrics. To manually configure datasets (e.g., when using
          non-standard label names), use the dataset block. The cloud_access_policy_token
          is used internally for GCom API access, Mimir metrics authentication, and
          assertion detector webhook authentication. Create a Cloud Access Policy
          with the following scopes: stacks:read, metrics:read, metrics:write. The
          grafana_token is a Grafana Service Account token used for installing dashboards
          and Grafana Managed Alerts.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - cloudAccessPolicyTokenSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudAccessPolicyTokenSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudAccessPolicyTokenSecretRef)'
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              atProvider:
                properties:
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the stack is currently enabled.
                      Whether the stack is currently enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  status:
                    description: |-
                      (String) Current onboarding status of the stack.
                      Current onboarding status of the stack.
                    type: string
                  version:
                    description: |-
                      (Number) Configuration version number.
                      Configuration version number.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: suppressedassertionsconfigs.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SuppressedAssertionsConfig
    listKind: SuppressedAssertionsConfigList
    plural: suppressedassertionsconfigs
    singular: suppressedassertionsconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SuppressedAssertionsConfig is the Schema for the SuppressedAssertionsConfigs
          API. Manages Knowledge Graph Disabled Alert Configurations through Grafana
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SuppressedAssertionsConfigSpec defines the desired state
              of SuppressedAssertionsConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SuppressedAssertionsConfigStatus defines the observed state
              of SuppressedAssertionsConfig.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: thresholds.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Thresholds
    listKind: ThresholdsList
    plural: thresholds
    singular: thresholds
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Thresholds is the Schema for the Thresholdss API. Manages Knowledge
          Graph Thresholds configuration (request, resource, health) via bulk endpoints.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThresholdsSpec defines the desired state of Thresholds
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ThresholdsStatus defines the observed state of Thresholds.
            properties:
              atProvider:
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: traceconfigs.asserts.grafana.crossplane.io
spec:
  group: asserts.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: TraceConfig
    listKind: TraceConfigList
    plural: traceconfigs
    singular: traceconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TraceConfig is the Schema for the TraceConfigs API. Manages Knowledge
          Graph Trace Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TraceConfigSpec defines the desired state of TraceConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: TraceConfigStatus defines the observed state of TraceConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: custommodelrules.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CustomModelRules
    listKind: CustomModelRulesList
    plural: custommodelrules
    singular: custommodelrules
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CustomModelRules is the Schema for the CustomModelRuless API.
          Manages Knowledge Graph Custom Model Rules through the Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomModelRulesSpec defines the desired state of CustomModelRules
            properties:
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.rules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rules)
                || (has(self.initProvider) && has(self.initProvider.rules))'
          status:
            description: CustomModelRulesStatus defines the observed state of CustomModelRules.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the custom model rules.
                      The name of the custom model rules.
                    type: string
                  rules:
                    description: |-
                      (Block List, Min: 1, Max: 1) The rules configuration for the custom model rules. (see below for nested schema)
                      The rules configuration for the custom model rules.
                    items:
                      properties:
                        entity:
                          description: |-
                            (Block List, Min: 1) List of entities to define in the custom model rules. (see below for nested schema)
                            List of entities to define in the custom model rules.
                          items:
                            properties:
                              definedBy:
                                description: |-
                                  (Block List, Min: 1) List of queries that define this entity. (see below for nested schema)
                                  List of queries that define this entity.
                                items:
                                  properties:
                                    disabled:
                                      description: |-
                                        (Boolean) Whether this entity is disabled.
                                        Whether this rule is disabled. When true, only the 'query' field is used to match an existing rule to disable; other fields are ignored.
                                      type: boolean
                                    labelValues:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Label value mappings for the query.
                                        Label value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    literals:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) Literal value mappings for the query.
                                        Literal value mappings for the query.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    metricValue:
                                      description: |-
                                        (String) Metric value for the query.
                                        Metric value for the query.
                                      type: string
                                    query:
                                      description: |-
                                        (String) The Prometheus query that defines this entity.
                                        The Prometheus query that defines this entity.
                                      type: string
                                  type: object
                                type: array
                              disabled:
                                description: |-
                                  (Boolean) Whether this entity is disabled.
                                  Whether this entity is disabled.
                                type: boolean
                              enrichedBy:
                                description: |-
                                  (List of String) List of enrichment sources for the entity.
                                  List of enrichment sources for the entity.
                                items:
                                  type: string
                                type: array
                              lookup:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Lookup mappings for the entity.
                                  Lookup mappings for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              name:
                                description: |-
                                  (String) The name of the custom model rules.
                                  The name of the entity.
                                type: string
                              scope:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Scope labels for the entity.
                                  Scope labels for the entity.
                                type: object
                                x-kubernetes-map-type: granular
                              type:
                                description: |-
                                  (String) The type of the entity (e.g., Service, Pod, Namespace).
                                  The type of the entity (e.g., Service, Pod, Namespace).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: logconfigs.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LogConfig
    listKind: LogConfigList
    plural: logconfigs
    singular: logconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LogConfig is the Schema for the LogConfigs API. Manages Knowledge
          Graph Log Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LogConfigSpec defines the desired state of LogConfig
            properties:
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: LogConfigStatus defines the observed state of LogConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Loki instance).
                      DataSource to be queried (e.g., a Loki instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToLogLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to log labels.
                      Mapping of entity properties to log labels.
                    type: object
                    x-kubernetes-map-type: granular
                  errorLabel:
                    description: |-
                      (String) Error label to filter logs.
                      Error label to filter logs.
                    type: string
                  filterBySpanId:
                    description: |-
                      (Boolean) Filter logs by span ID.
                      Filter logs by span ID.
                    type: boolean
                  filterByTraceId:
                    description: |-
                      (Boolean) Filter logs by trace ID.
                      Filter logs by trace ID.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the log configuration.
                      The name of the log configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the log configuration. A lower number means a higher priority.
                      Priority of the log configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: notificationalertsconfigs.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: NotificationAlertsConfig
    listKind: NotificationAlertsConfigList
    plural: notificationalertsconfigs
    singular: notificationalertsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NotificationAlertsConfig is the Schema for the NotificationAlertsConfigs
          API. Manages Knowledge Graph Alert Configurations through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationAlertsConfigSpec defines the desired state of
              NotificationAlertsConfig
            properties:
              forProvider:
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: NotificationAlertsConfigStatus defines the observed state
              of NotificationAlertsConfig.
            properties:
              atProvider:
                properties:
                  alertLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to add to alerts generated by this configuration.
                      Labels to add to alerts generated by this configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  duration:
                    description: |-
                      (String) Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                      Duration for which the condition must be true before firing (e.g., '5m', '30s'). Maps to 'for' in Asserts API.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this alert configuration.
                      Labels to match for this alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the alert configuration.
                      The name of the alert configuration.
                    type: string
                  silenced:
                    description: |-
                      (Boolean) Whether this alert configuration is silenced. Defaults to false.
                      Whether this alert configuration is silenced. Defaults to `false`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: profileconfigs.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProfileConfig
    listKind: ProfileConfigList
    plural: profileconfigs
    singular: profileconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProfileConfig is the Schema for the ProfileConfigs API. Manages
          Knowledge Graph Profile Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProfileConfigSpec defines the desired state of ProfileConfig
            properties:
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: ProfileConfigStatus defines the observed state of ProfileConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Pyroscope instance).
                      DataSource to be queried (e.g., a Pyroscope instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToProfileLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to profile labels.
                      Mapping of entity properties to profile labels.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the profile configuration.
                      The name of the profile configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the profile configuration. A lower number means a higher priority.
                      Priority of the profile configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: promrulefiles.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PromRuleFile
    listKind: PromRuleFileList
    plural: promrulefiles
    singular: promrulefile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PromRuleFile is the Schema for the PromRuleFiles API. Manages
          Prometheus Rules configurations through Grafana Asserts API. Allows creation
          and management of custom Prometheus recording and alerting rules.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PromRuleFileSpec defines the desired state of PromRuleFile
            properties:
              forProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.group is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.group)
                || (has(self.initProvider) && has(self.initProvider.group))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PromRuleFileStatus defines the observed state of PromRuleFile.
            properties:
              atProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                      Whether the rules file is active. Inactive rules are not evaluated. Defaults to `true`.
                    type: boolean
                  group:
                    description: |-
                      (Block List, Min: 1) List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval. (see below for nested schema)
                      List of Prometheus rule groups. Each group contains one or more rules and can have its own evaluation interval.
                    items:
                      properties:
                        interval:
                          description: |-
                            (String) Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                            Evaluation interval for this group (e.g., '30s', '1m'). If not specified, uses the global evaluation interval.
                          type: string
                        name:
                          description: |-
                            (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                            The name of the rule group (e.g., 'latency_monitoring').
                          type: string
                        rule:
                          description: |-
                            (Block List, Min: 1) List of Prometheus rules in this group. (see below for nested schema)
                            List of Prometheus rules in this group.
                          items:
                            properties:
                              active:
                                description: |-
                                  (Boolean) Whether the rules file is active. Inactive rules are not evaluated. Defaults to true.
                                  Whether this specific rule is active. This field is read-only and controlled by the API.
                                type: boolean
                              alert:
                                description: |-
                                  (String) The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the alert for alerting rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Annotations to add to alerts (e.g., summary, description).
                                  Annotations to add to alerts (e.g., summary, description).
                                type: object
                                x-kubernetes-map-type: granular
                              disableInGroups:
                                description: |-
                                  (Set of String) List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                  List of group names where this rule should be disabled. Useful for conditional rule enablement.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              duration:
                                description: |-
                                  (String) How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                  How long the condition must be true before firing the alert (e.g., '5m'). Only applicable for alerting rules. Maps to 'for' in Prometheus.
                                type: string
                              expr:
                                description: |-
                                  (String) The PromQL expression to evaluate.
                                  The PromQL expression to evaluate.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  (Map of String) Labels to attach to the resulting time series or alert.
                                  Labels to attach to the resulting time series or alert.
                                type: object
                                x-kubernetes-map-type: granular
                              record:
                                description: |-
                                  (String) The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                  The name of the time series to output for recording rules. Either 'record' or 'alert' must be specified, but not both.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                      The name of the Prometheus rules file. This will be stored with a .custom extension. Must follow naming validation rules (alphanumeric, hyphens, underscores).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Stack is the Schema for the Stacks API. Manages the Asserts
          Stack configuration. This resource configures the Asserts stack with the
          required API tokens for integration with Grafana Cloud services. It performs
          the full onboarding flow: Provisions API tokensConfigures datasets (auto-detected
          or manually specified)Enables the stack By default, datasets are auto-configured
          based on detected metrics. To manually configure datasets (e.g., when using
          non-standard label names), use the dataset block. The cloud_access_policy_token
          is used internally for GCom API access, Mimir metrics authentication, and
          assertion detector webhook authentication. Create a Cloud Access Policy
          with the following scopes: stacks:read, metrics:read, metrics:write. The
          grafana_token is a Grafana Service Account token used for installing dashboards
          and Grafana Managed Alerts.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              forProvider:
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Cloud Access Policy token with the following scopes: stacks:read, metrics:read, metrics:write. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                      A Grafana Cloud Access Policy token with the following scopes: `stacks:read`, `metrics:read`, `metrics:write`. This token is used for GCom API access, Mimir authentication, and assertion detector webhook authentication.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  grafanaTokenSecretRef:
                    description: |-
                      (String, Sensitive) A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: dashboards:create, dashboards:write, dashboards:read, folders:create, folders:write, folders:read, folders:delete, datasources:read, datasources:query, alert.provisioning:write, alert.notifications.provisioning:write, alert.notifications:write, alert.rules:read, alert.rules:create, alert.rules:delete. Create using grafana_cloud_stack_service_account_token resource.
                      A Grafana Service Account token for installing dashboards and Grafana Managed Alerts. Required permissions: `dashboards:create`, `dashboards:write`, `dashboards:read`, `folders:create`, `folders:write`, `folders:read`, `folders:delete`, `datasources:read`, `datasources:query`, `alert.provisioning:write`, `alert.notifications.provisioning:write`, `alert.notifications:write`, `alert.rules:read`, `alert.rules:create`, `alert.rules:delete`. Create using `grafana_cloud_stack_service_account_token` resource.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - cloudAccessPolicyTokenSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudAccessPolicyTokenSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudAccessPolicyTokenSecretRef)'
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              atProvider:
                properties:
                  dataset:
                    description: |-
                      detection. Use this when your metrics use non-standard label names (e.g., a custom environment label). (see below for nested schema)
                      Manual dataset configuration. When specified, datasets are configured manually instead of using auto-detection. Use this when your metrics use non-standard label names (e.g., a custom environment label).
                    items:
                      properties:
                        disabledVendors:
                          description: |-
                            (List of String) List of vendors to disable for this dataset.
                            List of vendors to disable for this dataset.
                          items:
                            type: string
                          type: array
                        filterGroup:
                          description: |-
                            (Block List) Filter groups for this dataset. Use when you need custom label mappings. (see below for nested schema)
                            Filter groups for this dataset. Use when you need custom label mappings.
                          items:
                            properties:
                              envLabel:
                                description: |-
                                  (String) The metric label name used for environment (e.g., env, environment, deployment_environment). Defaults to standard labels if not set.
                                  The metric label name used for environment (e.g., `env`, `environment`, `deployment_environment`). Defaults to standard labels if not set.
                                type: string
                              envLabelValues:
                                description: |-
                                  (List of String) Specific values of the environment label to match.
                                  Specific values of the environment label to match.
                                items:
                                  type: string
                                type: array
                              envName:
                                description: |-
                                  (String) A friendly name for the environment.
                                  A friendly name for the environment.
                                type: string
                              filter:
                                description: |-
                                  (Block List) Additional metric filters. (see below for nested schema)
                                  Additional metric filters.
                                items:
                                  properties:
                                    name:
                                      description: |-
                                        (String) The label name to filter on.
                                        The label name to filter on.
                                      type: string
                                    operator:
                                      description: |-
                                        (String) The filter operator (e.g., =, !=, =~, !~).
                                        The filter operator (e.g., `=`, `!=`, `=~`, `!~`).
                                      type: string
                                    values:
                                      description: |-
                                        (List of String) The values to match.
                                        The values to match.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              siteLabel:
                                description: |-
                                  (String) The metric label name used for site/cluster.
                                  The metric label name used for site/cluster.
                                type: string
                              siteLabelValues:
                                description: |-
                                  (List of String) Specific values of the site label to match.
                                  Specific values of the site label to match.
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) The dataset type. Available types: kubernetes, otel (App O11y), prometheus, aws. Note: kubernetes requires K8s Monitoring to be enabled, and otel requires Application Observability to be enabled on the stack.
                            The dataset type. Available types: `kubernetes`, `otel` (App O11y), `prometheus`, `aws`. Note: `kubernetes` requires K8s Monitoring to be enabled, and `otel` requires Application Observability to be enabled on the stack.
                          type: string
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the stack is currently enabled.
                      Whether the stack is currently enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  status:
                    description: |-
                      (String) Current onboarding status of the stack.
                      Current onboarding status of the stack.
                    type: string
                  version:
                    description: |-
                      (Number) Configuration version number.
                      Configuration version number.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: suppressedassertionsconfigs.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SuppressedAssertionsConfig
    listKind: SuppressedAssertionsConfigList
    plural: suppressedassertionsconfigs
    singular: suppressedassertionsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SuppressedAssertionsConfig is the Schema for the SuppressedAssertionsConfigs
          API. Manages Knowledge Graph Disabled Alert Configurations through Grafana
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SuppressedAssertionsConfigSpec defines the desired state
              of SuppressedAssertionsConfig
            properties:
              forProvider:
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SuppressedAssertionsConfigStatus defines the observed state
              of SuppressedAssertionsConfig.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels to match for this disabled alert configuration.
                      Labels to match for this disabled alert configuration.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of the disabled alert configuration.
                      The name of the disabled alert configuration.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: thresholds.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Thresholds
    listKind: ThresholdsList
    plural: thresholds
    singular: thresholds
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Thresholds is the Schema for the Thresholdss API. Manages Knowledge
          Graph Thresholds configuration (request, resource, health) via bulk endpoints.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThresholdsSpec defines the desired state of Thresholds
            properties:
              forProvider:
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ThresholdsStatus defines the observed state of Thresholds.
            properties:
              atProvider:
                properties:
                  healthThresholds:
                    description: |-
                      (Block List) List of health thresholds. (see below for nested schema)
                      List of health thresholds.
                    items:
                      properties:
                        alertCategory:
                          description: |-
                            (String) Optional alert category label for the health threshold.
                            Optional alert category label for the health threshold.
                          type: string
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name.
                          type: string
                        entityType:
                          description: |-
                            (String) Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                            Entity type for the health threshold (e.g., Service, Pod, Namespace, Volume).
                          type: string
                        expression:
                          description: |-
                            (String) Prometheus expression.
                            Prometheus expression.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  requestThresholds:
                    description: |-
                      (Block List) List of request thresholds. (see below for nested schema)
                      List of request thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., RequestRateAnomaly, ErrorRatioBreach).
                          type: string
                        entityName:
                          description: |-
                            (String) Entity name the threshold applies to.
                            Entity name the threshold applies to.
                          type: string
                        requestContext:
                          description: |-
                            (String) Request context (e.g., path or context identifier).
                            Request context (e.g., path or context identifier).
                          type: string
                        requestType:
                          description: |-
                            (String) Request type (e.g., inbound/outbound).
                            Request type (e.g., inbound/outbound).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                  resourceThresholds:
                    description: |-
                      (Block List) List of resource thresholds. (see below for nested schema)
                      List of resource thresholds.
                    items:
                      properties:
                        assertionName:
                          description: |-
                            (String) Assertion name.
                            Assertion name (e.g., Saturation, ResourceRateBreach).
                          type: string
                        containerName:
                          description: |-
                            (String) Container name.
                            Container name.
                          type: string
                        resourceType:
                          description: |-
                            (String) Resource type (e.g., container/pod/node).
                            Resource type (e.g., container/pod/node).
                          type: string
                        severity:
                          description: |-
                            (String) Severity (warning or critical).
                            Severity (warning or critical).
                          type: string
                        source:
                          description: |-
                            (String) Data source for the threshold (e.g., metrics/logs).
                            Data source for the threshold (e.g., metrics/logs).
                          type: string
                        value:
                          description: |-
                            (Number) Threshold value.
                            Threshold value.
                          type: number
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: traceconfigs.asserts.grafana.m.crossplane.io
spec:
  group: asserts.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: TraceConfig
    listKind: TraceConfigList
    plural: traceconfigs
    singular: traceconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TraceConfig is the Schema for the TraceConfigs API. Manages Knowledge
          Graph Trace Configuration through Grafana API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TraceConfigSpec defines the desired state of TraceConfig
            properties:
              forProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dataSourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dataSourceUid)
                || (has(self.initProvider) && has(self.initProvider.dataSourceUid))'
            - message: spec.forProvider.defaultConfig is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultConfig)
                || (has(self.initProvider) && has(self.initProvider.defaultConfig))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.priority is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.priority)
                || (has(self.initProvider) && has(self.initProvider.priority))'
          status:
            description: TraceConfigStatus defines the observed state of TraceConfig.
            properties:
              atProvider:
                properties:
                  dataSourceUid:
                    description: |-
                      (String) DataSource to be queried (e.g., a Tempo instance).
                      DataSource to be queried (e.g., a Tempo instance).
                    type: string
                  defaultConfig:
                    description: |-
                      (Boolean) Is it the default config, therefore undeletable?
                      Is it the default config, therefore undeletable?
                    type: boolean
                  entityPropertyToTraceLabelMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Mapping of entity properties to trace labels.
                      Mapping of entity properties to trace labels.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  match:
                    description: |-
                      (Block List) List of match rules for entity properties. (see below for nested schema)
                      List of match rules for entity properties.
                    items:
                      properties:
                        op:
                          description: |-
                            (String) Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                            Operation to use for matching. One of: =, <>, <, >, <=, >=, IS NULL, IS NOT NULL, STARTS WITH, CONTAINS.
                          type: string
                        property:
                          description: |-
                            (String) Entity property to match.
                            Entity property to match.
                          type: string
                        values:
                          description: |-
                            (List of String) Values to match against.
                            Values to match against.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the trace configuration.
                      The name of the trace configuration.
                    type: string
                  priority:
                    description: |-
                      (Number) Priority of the trace configuration. A lower number means a higher priority.
                      Priority of the trace configuration. A lower number means a higher priority.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicies.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicy
    listKind: AccessPolicyList
    plural: accesspolicies
    singular: accesspolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicy is the Schema for the AccessPolicys API. Official
          documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-an-access-policy
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicySpec defines the desired state of AccessPolicy
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        stackRef:
                          description: Reference to a Stack in cloud to populate identifier.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        stackSelector:
                          description: Selector for a Stack in cloud to populate identifier.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        stackRef:
                          description: Reference to a Stack in cloud to populate identifier.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        stackSelector:
                          description: Selector for a Stack in cloud to populate identifier.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.realm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.realm)
                || (has(self.initProvider) && has(self.initProvider.realm))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.scopes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scopes)
                || (has(self.initProvider) && has(self.initProvider.scopes))'
          status:
            description: AccessPolicyStatus defines the observed state of AccessPolicy.
            properties:
              atProvider:
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy.
                      Creation date of the access policy.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  policyId:
                    description: |-
                      (String) ID of the access policy.
                      ID of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy.
                      Last update date of the access policy.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicyrotatingtokens.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicyRotatingToken
    listKind: AccessPolicyRotatingTokenList
    plural: accesspolicyrotatingtokens
    singular: accesspolicyrotatingtoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicyRotatingToken is the Schema for the AccessPolicyRotatingTokens
          API. Official documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete
          This is similar to the grafana_cloud_access_policy_token resource, but it
          represents a token that will be rotated automatically over time.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicyRotatingTokenSpec defines the desired state of
              AccessPolicyRotatingToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.accessPolicyId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.accessPolicyId)
                || (has(self.initProvider) && has(self.initProvider.accessPolicyId))'
            - message: spec.forProvider.earlyRotationWindow is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindow)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindow))'
            - message: spec.forProvider.expireAfter is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.expireAfter)
                || (has(self.initProvider) && has(self.initProvider.expireAfter))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: AccessPolicyRotatingTokenStatus defines the observed state
              of AccessPolicyRotatingToken.
            properties:
              atProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy token.
                      Creation date of the access policy token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token.
                      Expiration date of the access policy token.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the token is either expired or within the period to be early rotated.
                      Signals that the token is either expired or within the period to be early rotated.
                    type: boolean
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy token.
                      Last update date of the access policy token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicytokens.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicyToken
    listKind: AccessPolicyTokenList
    plural: accesspolicytokens
    singular: accesspolicytoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicyToken is the Schema for the AccessPolicyTokens API.
          Official documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicyTokenSpec defines the desired state of AccessPolicyToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  accessPolicyRef:
                    description: Reference to a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessPolicySelector:
                    description: Selector for a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  accessPolicyRef:
                    description: Reference to a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessPolicySelector:
                    description: Selector for a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: AccessPolicyTokenStatus defines the observed state of AccessPolicyToken.
            properties:
              atProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy token.
                      Creation date of the access policy token.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy token.
                      Last update date of the access policy token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: appo11yconfigv1alpha1s.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Appo11YconfigV1Alpha1
    listKind: Appo11YconfigV1Alpha1List
    plural: appo11yconfigv1alpha1s
    singular: appo11yconfigv1alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Appo11YconfigV1Alpha1 is the Schema for the Appo11YconfigV1Alpha1s
          API. Manages Grafana Application Observability configurations using the
          Grafana APIs. This resource allows you to enable or disable application
          observability features. Note: This is a singleton resource. The UID is automatically
          set to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Appo11YconfigV1Alpha1Spec defines the desired state of Appo11YconfigV1Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: Appo11YconfigV1Alpha1Status defines the observed state of
              Appo11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dbo11yconfigv1alpha1s.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Dbo11YconfigV1Alpha1
    listKind: Dbo11YconfigV1Alpha1List
    plural: dbo11yconfigv1alpha1s
    singular: dbo11yconfigv1alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Dbo11YconfigV1Alpha1 is the Schema for the Dbo11YconfigV1Alpha1s
          API. Manages Grafana Database Observability configurations using the Grafana
          APIs. This resource allows you to enable or disable database observability
          features. Note: This is a singleton resource. The UID is automatically set
          to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Dbo11YconfigV1Alpha1Spec defines the desired state of Dbo11YconfigV1Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: Dbo11YconfigV1Alpha1Status defines the observed state of
              Dbo11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: k8so11yconfigv1alpha1s.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: K8So11YconfigV1Alpha1
    listKind: K8So11YconfigV1Alpha1List
    plural: k8so11yconfigv1alpha1s
    singular: k8so11yconfigv1alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'K8So11YconfigV1Alpha1 is the Schema for the K8So11YconfigV1Alpha1s
          API. Manages Grafana Kubernetes Observability configurations using the Grafana
          APIs. This resource allows you to enable or disable Kubernetes observability
          features. Note: This is a singleton resource. The UID is automatically set
          to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: K8So11YconfigV1Alpha1Spec defines the desired state of K8So11YconfigV1Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: K8So11YconfigV1Alpha1Status defines the observed state of
              K8So11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: orgmembers.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OrgMember
    listKind: OrgMemberList
    plural: orgmembers
    singular: orgmember
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrgMember is the Schema for the OrgMembers API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrgMemberSpec defines the desired state of OrgMember
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.org is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.org)
                || (has(self.initProvider) && has(self.initProvider.org))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
            - message: spec.forProvider.user is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.user)
                || (has(self.initProvider) && has(self.initProvider.user))'
          status:
            description: OrgMemberStatus defines the observed state of OrgMember.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: plugininstallations.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PluginInstallation
    listKind: PluginInstallationList
    plural: plugininstallations
    singular: plugininstallation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PluginInstallation is the Schema for the PluginInstallations
          API. Manages Grafana Cloud Plugin Installations. Plugin management https://grafana.com/docs/grafana/latest/administration/plugin-management/
          Required access policy scopes: stack-plugins:readstack-plugins:writestack-plugins:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PluginInstallationSpec defines the desired state of PluginInstallation
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: PluginInstallationStatus defines the observed state of PluginInstallation.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: privatedatasourceconnectnetworks.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PrivateDataSourceConnectNetwork
    listKind: PrivateDataSourceConnectNetworkList
    plural: privatedatasourceconnectnetworks
    singular: privatedatasourceconnectnetwork
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PrivateDataSourceConnectNetwork is the Schema for the PrivateDataSourceConnectNetworks
          API. Official documentation https://grafana.com/docs/grafana-cloud/connect-externally-hosted/private-data-source-connect/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-an-access-policy
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrivateDataSourceConnectNetworkSpec defines the desired state
              of PrivateDataSourceConnectNetwork
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.stackIdentifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackIdentifier)
                || (has(self.initProvider) && has(self.initProvider.stackIdentifier))'
          status:
            description: PrivateDataSourceConnectNetworkStatus defines the observed
              state of PrivateDataSourceConnectNetwork.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) Creation date of the private data source connect network.
                      Creation date of the private data source connect network.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source connect network.
                      ID of the private data source connect network.
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the private data source connect network.
                      Last update date of the private data source connect network.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: privatedatasourceconnectnetworktokens.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PrivateDataSourceConnectNetworkToken
    listKind: PrivateDataSourceConnectNetworkTokenList
    plural: privatedatasourceconnectnetworktokens
    singular: privatedatasourceconnectnetworktoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PrivateDataSourceConnectNetworkToken is the Schema for the PrivateDataSourceConnectNetworkTokens
          API. Official documentation https://grafana.com/docs/grafana-cloud/connect-externally-hosted/private-data-source-connect/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrivateDataSourceConnectNetworkTokenSpec defines the desired
              state of PrivateDataSourceConnectNetworkToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.pdcNetworkId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.pdcNetworkId)
                || (has(self.initProvider) && has(self.initProvider.pdcNetworkId))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: PrivateDataSourceConnectNetworkTokenStatus defines the observed
              state of PrivateDataSourceConnectNetworkToken.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) Creation date of the private data source network token.
                      Creation date of the private data source network token.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the private data source network token.
                      Last update date of the private data source network token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Stack is the Schema for the Stacks API. Official documentation
          https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#stacks/
          Required access policy scopes: stacks:readstacks:writestacks:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              atProvider:
                properties:
                  alertmanagerIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Alertmanager instances
                      Comma-separated list of CNAMEs that can be whitelisted to access the Alertmanager instances (Optional)
                    type: string
                  alertmanagerName:
                    description: |-
                      (String) Name of the Alertmanager instance configured for this stack.
                      Name of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerStatus:
                    description: |-
                      (String) Status of the Alertmanager instance configured for this stack.
                      Status of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerUrl:
                    description: |-
                      (String) Base URL of the Alertmanager instance configured for this stack.
                      Base URL of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerUserId:
                    description: |-
                      (Number) User ID of the Alertmanager instance configured for this stack.
                      User ID of the Alertmanager instance configured for this stack.
                    type: number
                  cloudProviderUrl:
                    description: |-
                      (String) Base URL of the Cloud Provider API for this stack's cluster. This can be used with the cloud_provider_url provider config option to manage Cloud Provider resources for this stack.
                      Base URL of the Cloud Provider API for this stack's cluster. This can be used with the `cloud_provider_url` provider config option to manage Cloud Provider resources for this stack.
                    type: string
                  clusterName:
                    description: |-
                      (String) Name of the cluster where this stack resides.
                      Name of the cluster where this stack resides.
                    type: string
                  clusterSlug:
                    description: |-
                      (String) Slug of the cluster where this stack resides.
                      Slug of the cluster where this stack resides.
                    type: string
                  connectionsApiUrl:
                    description: |-
                      (String) Base URL of the Connections API for this stack's cluster. This can be used with the connections_api_url provider config option to manage Connections resources for this stack.
                      Base URL of the Connections API for this stack's cluster. This can be used with the `connections_api_url` provider config option to manage Connections resources for this stack.
                    type: string
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  fleetManagementName:
                    description: |-
                      (String) Name of the Fleet Management instance configured for this stack.
                      Name of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Regions for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementStatus:
                    description: |-
                      (String) Status of the Fleet Management instance configured for this stack.
                      Status of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementUrl:
                    description: |-
                      (String) Base URL of the Fleet Management instance configured for this stack.
                      Base URL of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementUserId:
                    description: |-
                      (Number) User ID of the Fleet Management instance configured for this stack.
                      User ID of the Fleet Management instance configured for this stack.
                    type: number
                  grafanasIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the grafana instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the grafana instance (Optional)
                    type: string
                  graphiteIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Graphite instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Graphite instance (Optional)
                    type: string
                  graphiteName:
                    description: (String)
                    type: string
                  graphitePrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Graphite when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  graphitePrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Regions for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Graphite when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  graphiteStatus:
                    description: (String)
                    type: string
                  graphiteUrl:
                    description: (String)
                    type: string
                  graphiteUserId:
                    description: (Number)
                    type: number
                  id:
                    description: (String) The stack id assigned to this stack by Grafana.
                    type: string
                  influxUrl:
                    description: |-
                      cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/ for docs on how to use this.
                      Base URL of the InfluxDB instance configured for this stack. The username is the same as the metrics' (`prometheus_user_id` attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/ for docs on how to use this.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  logsIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Logs instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Logs instance (Optional)
                    type: string
                  logsName:
                    description: (String)
                    type: string
                  logsPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Logs when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Logs when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Logs when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Logs when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  logsPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Logs when using AWS PrivateLink (only for AWS stacks)
                      Regions for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Logs when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Logs when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  logsStatus:
                    description: (String)
                    type: string
                  logsUrl:
                    description: (String)
                    type: string
                  logsUserId:
                    description: (Number)
                    type: number
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  oncallApiUrl:
                    description: |-
                      (String) Base URL of the OnCall API instance configured for this stack.
                      Base URL of the OnCall API instance configured for this stack.
                    type: string
                  orgId:
                    description: |-
                      (Number) Organization id to assign to this stack.
                      Organization id to assign to this stack.
                    type: number
                  orgName:
                    description: |-
                      (String) Organization name to assign to this stack.
                      Organization name to assign to this stack.
                    type: string
                  orgSlug:
                    description: |-
                      (String) Organization slug to assign to this stack.
                      Organization slug to assign to this stack.
                    type: string
                  otlpPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for OTLP when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  otlpPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Regions for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Service Name for OTLP when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  otlpUrl:
                    description: |-
                      cloud/send-data/otlp/send-data-otlp/ for docs on how to use this.
                      Base URL of the OTLP instance configured for this stack. The username is the stack's ID (`id` attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/otlp/send-data-otlp/ for docs on how to use this.
                    type: string
                  pdcApiPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcApiPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Regions for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Service Name for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Regions for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Service Name for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Profiles instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Profiles instance (Optional)
                    type: string
                  profilesName:
                    description: (String)
                    type: string
                  profilesPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Profiles when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Regions for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Profiles when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesStatus:
                    description: (String)
                    type: string
                  profilesUrl:
                    description: (String)
                    type: string
                  profilesUserId:
                    description: (Number)
                    type: number
                  prometheusIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Prometheus instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Prometheus instance (Optional)
                    type: string
                  prometheusName:
                    description: |-
                      (String) Prometheus name for this instance.
                      Prometheus name for this instance.
                    type: string
                  prometheusPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  prometheusPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Regions for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  prometheusRemoteEndpoint:
                    description: |-
                      (String) Use this URL to query hosted metrics data e.g. Prometheus data source in Grafana
                      Use this URL to query hosted metrics data e.g. Prometheus data source in Grafana
                    type: string
                  prometheusRemoteWriteEndpoint:
                    description: |-
                      (String) Use this URL to send prometheus metrics to Grafana cloud
                      Use this URL to send prometheus metrics to Grafana cloud
                    type: string
                  prometheusStatus:
                    description: |-
                      (String) Prometheus status for this instance.
                      Prometheus status for this instance.
                    type: string
                  prometheusUrl:
                    description: |-
                      (String) Prometheus url for this instance.
                      Prometheus url for this instance.
                    type: string
                  prometheusUserId:
                    description: |-
                      (Number) Prometheus user ID. Used for e.g. remote_write.
                      Prometheus user ID. Used for e.g. remote_write.
                    type: number
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  smUrl:
                    description: |-
                      (String) Base URL of the Synthetic Monitoring API for this stack's region. This can be used with the sm_url provider config option. Note: Synthetic Monitoring requires activation either via the grafana_synthetic_monitoring_installation resource or manually in the Grafana Cloud UI before it can be used.
                      Base URL of the Synthetic Monitoring API for this stack's region. This can be used with the `sm_url` provider config option. Note: Synthetic Monitoring requires activation either via the `grafana_synthetic_monitoring_installation` resource or manually in the Grafana Cloud UI before it can be used.
                    type: string
                  status:
                    description: |-
                      (String) Status of the stack.
                      Status of the stack.
                    type: string
                  tracesIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Traces instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Traces instance (Optional)
                    type: string
                  tracesName:
                    description: (String)
                    type: string
                  tracesPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Traces when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Traces when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Traces when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Traces when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  tracesPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Traces when using AWS PrivateLink (only for AWS stacks)
                      Regions for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Traces when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Traces when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  tracesStatus:
                    description: (String)
                    type: string
                  tracesUrl:
                    description: |-
                      (String) Base URL of the Traces instance configured for this stack. To use this in the Tempo data source in Grafana, append /tempo to the URL.
                      Base URL of the Traces instance configured for this stack. To use this in the Tempo data source in Grafana, append `/tempo` to the URL.
                    type: string
                  tracesUserId:
                    description: (Number)
                    type: number
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccountrotatingtokens.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccountRotatingToken
    listKind: StackServiceAccountRotatingTokenList
    plural: stackserviceaccountrotatingtokens
    singular: stackserviceaccountrotatingtoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccountRotatingToken is the Schema for the StackServiceAccountRotatingTokens
          API. Manages and rotates service account tokens of a Grafana Cloud stack
          using the Cloud API This can be used to bootstrap a management service account
          token for a new stack Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountRotatingTokenSpec defines the desired
              state of StackServiceAccountRotatingToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.earlyRotationWindowSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindowSeconds)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindowSeconds))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.secondsToLive is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.secondsToLive)
                || (has(self.initProvider) && has(self.initProvider.secondsToLive))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
            - message: spec.forProvider.stackSlug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackSlug)
                || (has(self.initProvider) && has(self.initProvider.stackSlug))'
          status:
            description: StackServiceAccountRotatingTokenStatus defines the observed
              state of StackServiceAccountRotatingToken.
            properties:
              atProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      and will have characters appended to it to make the name unique.
                      The name of the service account token. It will start with `<name_prefix>-` and will have characters appended to it to make the name unique.
                    type: string
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the service account token is expired or within the period to be early rotated.
                      Signals that the service account token is expired or within the period to be early rotated.
                    type: boolean
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccounts.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccount
    listKind: StackServiceAccountList
    plural: stackserviceaccounts
    singular: stackserviceaccount
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccount is the Schema for the StackServiceAccounts
          API. Manages service accounts of a Grafana Cloud stack using the Cloud API
          This can be used to bootstrap a management service account for a new stack
          Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stacks:readstack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountSpec defines the desired state of StackServiceAccount
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
          status:
            description: StackServiceAccountStatus defines the observed state of StackServiceAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccounttokens.cloud.grafana.crossplane.io
spec:
  group: cloud.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccountToken
    listKind: StackServiceAccountTokenList
    plural: stackserviceaccounttokens
    singular: stackserviceaccounttoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccountToken is the Schema for the StackServiceAccountTokens
          API. Manages service account tokens of a Grafana Cloud stack using the Cloud
          API This can be used to bootstrap a management service account token for
          a new stack Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountTokenSpec defines the desired state of
              StackServiceAccountToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: StackServiceAccountTokenStatus defines the observed state
              of StackServiceAccountToken.
            properties:
              atProvider:
                properties:
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicies.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicy
    listKind: AccessPolicyList
    plural: accesspolicies
    singular: accesspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicy is the Schema for the AccessPolicys API. Official
          documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-an-access-policy
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicySpec defines the desired state of AccessPolicy
            properties:
              forProvider:
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        stackRef:
                          description: Reference to a Stack in cloud to populate identifier.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        stackSelector:
                          description: Selector for a Stack in cloud to populate identifier.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        stackRef:
                          description: Reference to a Stack in cloud to populate identifier.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        stackSelector:
                          description: Selector for a Stack in cloud to populate identifier.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.realm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.realm)
                || (has(self.initProvider) && has(self.initProvider.realm))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.scopes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scopes)
                || (has(self.initProvider) && has(self.initProvider.scopes))'
          status:
            description: AccessPolicyStatus defines the observed state of AccessPolicy.
            properties:
              atProvider:
                properties:
                  conditions:
                    description: |-
                      (Block Set) Conditions for the access policy. (see below for nested schema)
                      Conditions for the access policy.
                    items:
                      properties:
                        allowedSubnets:
                          description: |-
                            (Set of String) Conditions that apply to the access policy,such as IP Allow lists.
                            Conditions that apply to the access policy,such as IP Allow lists.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy.
                      Creation date of the access policy.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the access policy. Defaults to the name.
                      Display name of the access policy. Defaults to the name.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy.
                      Name of the access policy.
                    type: string
                  policyId:
                    description: |-
                      (String) ID of the access policy.
                      ID of the access policy.
                    type: string
                  realm:
                    description: '(Block Set, Min: 1) (see below for nested schema)'
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                            The identifier of the org or stack. For orgs, this is the slug, for stacks, this is the stack ID.
                          type: string
                        labelPolicy:
                          description: (Block Set) (see below for nested schema)
                          items:
                            properties:
                              selector:
                                description: |-
                                  (String) The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                  The label selector to match in metrics or logs query. Should be in PromQL or LogQL format.
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Whether a policy applies to a Cloud org or a specific stack. Should be one of org or stack.
                            Whether a policy applies to a Cloud org or a specific stack. Should be one of `org` or `stack`.
                          type: string
                      type: object
                    type: array
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region where the API is deployed. Generally where the stack is deployed. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  scopes:
                    description: |-
                      cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                      Scopes of the access policy. See https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/#scopes for possible values.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy.
                      Last update date of the access policy.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicyrotatingtokens.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicyRotatingToken
    listKind: AccessPolicyRotatingTokenList
    plural: accesspolicyrotatingtokens
    singular: accesspolicyrotatingtoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicyRotatingToken is the Schema for the AccessPolicyRotatingTokens
          API. Official documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete
          This is similar to the grafana_cloud_access_policy_token resource, but it
          represents a token that will be rotated automatically over time.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicyRotatingTokenSpec defines the desired state of
              AccessPolicyRotatingToken
            properties:
              forProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.accessPolicyId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.accessPolicyId)
                || (has(self.initProvider) && has(self.initProvider.accessPolicyId))'
            - message: spec.forProvider.earlyRotationWindow is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindow)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindow))'
            - message: spec.forProvider.expireAfter is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.expireAfter)
                || (has(self.initProvider) && has(self.initProvider.expireAfter))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: AccessPolicyRotatingTokenStatus defines the observed state
              of AccessPolicyRotatingToken.
            properties:
              atProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy token.
                      Creation date of the access policy token.
                    type: string
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  earlyRotationWindow:
                    description: |-
                      (String) Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration of the window before expiring where the token can be rotated (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expireAfter:
                    description: |-
                      (String) Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                      Duration after which the token will expire (e.g. '24h', '30m', '1h30m'). Valid units are 's' (seconds), 'm' (minutes) and 'h' (hours).
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token.
                      Expiration date of the access policy token.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  namePrefix:
                    description: |-
                      <expiration_timestamp>'
                      Prefix for the name of the access policy token. The actual name will be stored in the computed field `name`, which will be in the format '<name_prefix>-<expiration_timestamp>'
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the token is either expired or within the period to be early rotated.
                      Signals that the token is either expired or within the period to be early rotated.
                    type: boolean
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy token.
                      Last update date of the access policy token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicytokens.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AccessPolicyToken
    listKind: AccessPolicyTokenList
    plural: accesspolicytokens
    singular: accesspolicytoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'AccessPolicyToken is the Schema for the AccessPolicyTokens API.
          Official documentation https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccessPolicyTokenSpec defines the desired state of AccessPolicyToken
            properties:
              forProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  accessPolicyRef:
                    description: Reference to a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessPolicySelector:
                    description: Selector for a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  accessPolicyRef:
                    description: Reference to a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessPolicySelector:
                    description: Selector for a AccessPolicy in cloud to populate
                      accessPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: AccessPolicyTokenStatus defines the observed state of AccessPolicyToken.
            properties:
              atProvider:
                properties:
                  accessPolicyId:
                    description: |-
                      (String) ID of the access policy for which to create a token.
                      ID of the access policy for which to create a token.
                    type: string
                  createdAt:
                    description: |-
                      (String) Creation date of the access policy token.
                      Creation date of the access policy token.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the access policy token. Defaults to the name.
                      Display name of the access policy token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the access policy token. Does not expire by default.
                      Expiration date of the access policy token. Does not expire by default.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the access policy token.
                      Name of the access policy token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the access policy. Should be set to the same region as the access policy. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the access policy token.
                      Last update date of the access policy token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: appo11yconfigv1alpha1s.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Appo11YconfigV1Alpha1
    listKind: Appo11YconfigV1Alpha1List
    plural: appo11yconfigv1alpha1s
    singular: appo11yconfigv1alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Appo11YconfigV1Alpha1 is the Schema for the Appo11YconfigV1Alpha1s
          API. Manages Grafana Application Observability configurations using the
          Grafana APIs. This resource allows you to enable or disable application
          observability features. Note: This is a singleton resource. The UID is automatically
          set to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Appo11YconfigV1Alpha1Spec defines the desired state of Appo11YconfigV1Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: Appo11YconfigV1Alpha1Status defines the observed state of
              Appo11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether application observability is enabled.
                          Whether application observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dbo11yconfigv1alpha1s.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Dbo11YconfigV1Alpha1
    listKind: Dbo11YconfigV1Alpha1List
    plural: dbo11yconfigv1alpha1s
    singular: dbo11yconfigv1alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Dbo11YconfigV1Alpha1 is the Schema for the Dbo11YconfigV1Alpha1s
          API. Manages Grafana Database Observability configurations using the Grafana
          APIs. This resource allows you to enable or disable database observability
          features. Note: This is a singleton resource. The UID is automatically set
          to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Dbo11YconfigV1Alpha1Spec defines the desired state of Dbo11YconfigV1Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: Dbo11YconfigV1Alpha1Status defines the observed state of
              Dbo11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether database observability is enabled.
                          Whether database observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: k8so11yconfigv1alpha1s.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: K8So11YconfigV1Alpha1
    listKind: K8So11YconfigV1Alpha1List
    plural: k8so11yconfigv1alpha1s
    singular: k8so11yconfigv1alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'K8So11YconfigV1Alpha1 is the Schema for the K8So11YconfigV1Alpha1s
          API. Manages Grafana Kubernetes Observability configurations using the Grafana
          APIs. This resource allows you to enable or disable Kubernetes observability
          features. Note: This is a singleton resource. The UID is automatically set
          to "global" and there can only be one per namespace.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: K8So11YconfigV1Alpha1Spec defines the desired state of K8So11YconfigV1Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: K8So11YconfigV1Alpha1Status defines the observed state of
              K8So11YconfigV1Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      enabled:
                        description: |-
                          (Boolean) Whether Kubernetes observability is enabled.
                          Whether Kubernetes observability is enabled.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: orgmembers.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OrgMember
    listKind: OrgMemberList
    plural: orgmembers
    singular: orgmember
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrgMember is the Schema for the OrgMembers API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrgMemberSpec defines the desired state of OrgMember
            properties:
              forProvider:
                properties:
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.org is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.org)
                || (has(self.initProvider) && has(self.initProvider.org))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
            - message: spec.forProvider.user is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.user)
                || (has(self.initProvider) && has(self.initProvider.user))'
          status:
            description: OrgMemberStatus defines the observed state of OrgMember.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  org:
                    description: The slug or ID of the organization.
                    type: string
                  receiveBillingEmails:
                    description: Whether the user should receive billing emails.
                    type: boolean
                  role:
                    description: The role to assign to the user in the organization.
                    type: string
                  user:
                    description: Username or ID of the user to add to the org's members.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: plugininstallations.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PluginInstallation
    listKind: PluginInstallationList
    plural: plugininstallations
    singular: plugininstallation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PluginInstallation is the Schema for the PluginInstallations
          API. Manages Grafana Cloud Plugin Installations. Plugin management https://grafana.com/docs/grafana/latest/administration/plugin-management/
          Required access policy scopes: stack-plugins:readstack-plugins:writestack-plugins:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PluginInstallationSpec defines the desired state of PluginInstallation
            properties:
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: PluginInstallationStatus defines the observed state of PluginInstallation.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  slug:
                    description: |-
                      (String) Slug of the plugin to be installed.
                      Slug of the plugin to be installed.
                    type: string
                  stackSlug:
                    description: |-
                      (String) The stack id to which the plugin should be installed.
                      The stack id to which the plugin should be installed.
                    type: string
                  version:
                    description: |-
                      (String) Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to latest.
                      Version of the plugin to be installed. Defaults to 'latest' and installs the most recent version. Defaults to `latest`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: privatedatasourceconnectnetworks.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PrivateDataSourceConnectNetwork
    listKind: PrivateDataSourceConnectNetworkList
    plural: privatedatasourceconnectnetworks
    singular: privatedatasourceconnectnetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PrivateDataSourceConnectNetwork is the Schema for the PrivateDataSourceConnectNetworks
          API. Official documentation https://grafana.com/docs/grafana-cloud/connect-externally-hosted/private-data-source-connect/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-an-access-policy
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrivateDataSourceConnectNetworkSpec defines the desired state
              of PrivateDataSourceConnectNetwork
            properties:
              forProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.stackIdentifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackIdentifier)
                || (has(self.initProvider) && has(self.initProvider.stackIdentifier))'
          status:
            description: PrivateDataSourceConnectNetworkStatus defines the observed
              state of PrivateDataSourceConnectNetwork.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) Creation date of the private data source connect network.
                      Creation date of the private data source connect network.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the PDC network. Defaults to the name.
                      Display name of the PDC network. Defaults to the name.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#request-body
                      Name of the PDC network.**Note:** The name must be lowercase and can contain hyphens or underscores. See full requirements here: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#request-body
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source connect network.
                      ID of the private data source connect network.
                    type: string
                  region:
                    description: |-
                      use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                      The region where your stack is deployed. Use the instances list API to get the region for your instance - use the regionSlug property: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-stacks
                    type: string
                  stackIdentifier:
                    description: |-
                      (String) The identifier of the stack.
                      The identifier of the stack.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the private data source connect network.
                      Last update date of the private data source connect network.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: privatedatasourceconnectnetworktokens.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PrivateDataSourceConnectNetworkToken
    listKind: PrivateDataSourceConnectNetworkTokenList
    plural: privatedatasourceconnectnetworktokens
    singular: privatedatasourceconnectnetworktoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'PrivateDataSourceConnectNetworkToken is the Schema for the PrivateDataSourceConnectNetworkTokens
          API. Official documentation https://grafana.com/docs/grafana-cloud/connect-externally-hosted/private-data-source-connect/API
          documentation https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#create-a-token
          Required access policy scopes: accesspolicies:readaccesspolicies:writeaccesspolicies:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrivateDataSourceConnectNetworkTokenSpec defines the desired
              state of PrivateDataSourceConnectNetworkToken
            properties:
              forProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.pdcNetworkId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.pdcNetworkId)
                || (has(self.initProvider) && has(self.initProvider.pdcNetworkId))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: PrivateDataSourceConnectNetworkTokenStatus defines the observed
              state of PrivateDataSourceConnectNetworkToken.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: |-
                      (String) Creation date of the private data source network token.
                      Creation date of the private data source network token.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the private data source network token. Defaults to the name.
                      Display name of the private data source network token. Defaults to the name.
                    type: string
                  expiresAt:
                    description: |-
                      (String) Expiration date of the private data source network token. Does not expire by default.
                      Expiration date of the private data source network token. Does not expire by default.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the private data source network token.
                      Name of the private data source network token.
                    type: string
                  pdcNetworkId:
                    description: |-
                      (String) ID of the private data source network for which to create a token.
                      ID of the private data source network for which to create a token.
                    type: string
                  region:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region of the private data source network. Should be set to the same region as the private data source network. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Last update date of the private data source network token.
                      Last update date of the private data source network token.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Stack is the Schema for the Stacks API. Official documentation
          https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#stacks/
          Required access policy scopes: stacks:readstacks:writestacks:delete'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack
            properties:
              forProvider:
                properties:
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              atProvider:
                properties:
                  alertmanagerIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Alertmanager instances
                      Comma-separated list of CNAMEs that can be whitelisted to access the Alertmanager instances (Optional)
                    type: string
                  alertmanagerName:
                    description: |-
                      (String) Name of the Alertmanager instance configured for this stack.
                      Name of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerStatus:
                    description: |-
                      (String) Status of the Alertmanager instance configured for this stack.
                      Status of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerUrl:
                    description: |-
                      (String) Base URL of the Alertmanager instance configured for this stack.
                      Base URL of the Alertmanager instance configured for this stack.
                    type: string
                  alertmanagerUserId:
                    description: |-
                      (Number) User ID of the Alertmanager instance configured for this stack.
                      User ID of the Alertmanager instance configured for this stack.
                    type: number
                  cloudProviderUrl:
                    description: |-
                      (String) Base URL of the Cloud Provider API for this stack's cluster. This can be used with the cloud_provider_url provider config option to manage Cloud Provider resources for this stack.
                      Base URL of the Cloud Provider API for this stack's cluster. This can be used with the `cloud_provider_url` provider config option to manage Cloud Provider resources for this stack.
                    type: string
                  clusterName:
                    description: |-
                      (String) Name of the cluster where this stack resides.
                      Name of the cluster where this stack resides.
                    type: string
                  clusterSlug:
                    description: |-
                      (String) Slug of the cluster where this stack resides.
                      Slug of the cluster where this stack resides.
                    type: string
                  connectionsApiUrl:
                    description: |-
                      (String) Base URL of the Connections API for this stack's cluster. This can be used with the connections_api_url provider config option to manage Connections resources for this stack.
                      Base URL of the Connections API for this stack's cluster. This can be used with the `connections_api_url` provider config option to manage Connections resources for this stack.
                    type: string
                  deleteProtection:
                    description: |-
                      (Boolean) Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to true.
                      Whether to enable delete protection for the stack, preventing accidental deletion. Defaults to `true`.
                    type: boolean
                  description:
                    description: |-
                      (String) Description of stack.
                      Description of stack.
                    type: string
                  fleetManagementName:
                    description: |-
                      (String) Name of the Fleet Management instance configured for this stack.
                      Name of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Regions for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Fleet Management when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementStatus:
                    description: |-
                      (String) Status of the Fleet Management instance configured for this stack.
                      Status of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementUrl:
                    description: |-
                      (String) Base URL of the Fleet Management instance configured for this stack.
                      Base URL of the Fleet Management instance configured for this stack.
                    type: string
                  fleetManagementUserId:
                    description: |-
                      (Number) User ID of the Fleet Management instance configured for this stack.
                      User ID of the Fleet Management instance configured for this stack.
                    type: number
                  grafanasIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the grafana instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the grafana instance (Optional)
                    type: string
                  graphiteIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Graphite instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Graphite instance (Optional)
                    type: string
                  graphiteName:
                    description: (String)
                    type: string
                  graphitePrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Graphite when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  graphitePrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Regions for Graphite when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Graphite when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Graphite when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  graphiteStatus:
                    description: (String)
                    type: string
                  graphiteUrl:
                    description: (String)
                    type: string
                  graphiteUserId:
                    description: (Number)
                    type: number
                  id:
                    description: (String) The stack id assigned to this stack by Grafana.
                    type: string
                  influxUrl:
                    description: |-
                      cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/ for docs on how to use this.
                      Base URL of the InfluxDB instance configured for this stack. The username is the same as the metrics' (`prometheus_user_id` attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/ for docs on how to use this.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      zA-Z0-9/\-._]+$" and stacks cannot have more than 10 labels.
                      A map of labels to assign to the stack. Label keys and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$" and stacks cannot have more than 10 labels.
                    type: object
                    x-kubernetes-map-type: granular
                  logsIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Logs instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Logs instance (Optional)
                    type: string
                  logsName:
                    description: (String)
                    type: string
                  logsPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Logs when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Logs when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Logs when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Logs when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  logsPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Logs when using AWS PrivateLink (only for AWS stacks)
                      Regions for Logs when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Logs when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Logs when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  logsStatus:
                    description: (String)
                    type: string
                  logsUrl:
                    description: (String)
                    type: string
                  logsUserId:
                    description: (Number)
                    type: number
                  name:
                    description: |-
                      (String) Name of stack. Conventionally matches the url of the instance (e.g. <stack_slug>.grafana.net).
                      Name of stack. Conventionally matches the url of the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  oncallApiUrl:
                    description: |-
                      (String) Base URL of the OnCall API instance configured for this stack.
                      Base URL of the OnCall API instance configured for this stack.
                    type: string
                  orgId:
                    description: |-
                      (Number) Organization id to assign to this stack.
                      Organization id to assign to this stack.
                    type: number
                  orgName:
                    description: |-
                      (String) Organization name to assign to this stack.
                      Organization name to assign to this stack.
                    type: string
                  orgSlug:
                    description: |-
                      (String) Organization slug to assign to this stack.
                      Organization slug to assign to this stack.
                    type: string
                  otlpPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for OTLP when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  otlpPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Regions for OTLP when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for OTLP when using AWS PrivateLink (only for AWS stacks)
                      Service Name for OTLP when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  otlpUrl:
                    description: |-
                      cloud/send-data/otlp/send-data-otlp/ for docs on how to use this.
                      Base URL of the OTLP instance configured for this stack. The username is the stack's ID (`id` attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/otlp/send-data-otlp/ for docs on how to use this.
                    type: string
                  pdcApiPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcApiPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Regions for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcApiPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for PDC's API when using AWS PrivateLink (only for AWS stacks)
                      Service Name for PDC's API when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Regions for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                      Service Name for PDC's Gateway when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Profiles instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Profiles instance (Optional)
                    type: string
                  profilesName:
                    description: (String)
                    type: string
                  profilesPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Profiles when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Regions for Profiles when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Profiles when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Profiles when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  profilesStatus:
                    description: (String)
                    type: string
                  profilesUrl:
                    description: (String)
                    type: string
                  profilesUserId:
                    description: (Number)
                    type: number
                  prometheusIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Prometheus instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Prometheus instance (Optional)
                    type: string
                  prometheusName:
                    description: |-
                      (String) Prometheus name for this instance.
                      Prometheus name for this instance.
                    type: string
                  prometheusPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  prometheusPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Regions for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Prometheus when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Prometheus when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  prometheusRemoteEndpoint:
                    description: |-
                      (String) Use this URL to query hosted metrics data e.g. Prometheus data source in Grafana
                      Use this URL to query hosted metrics data e.g. Prometheus data source in Grafana
                    type: string
                  prometheusRemoteWriteEndpoint:
                    description: |-
                      (String) Use this URL to send prometheus metrics to Grafana cloud
                      Use this URL to send prometheus metrics to Grafana cloud
                    type: string
                  prometheusStatus:
                    description: |-
                      (String) Prometheus status for this instance.
                      Prometheus status for this instance.
                    type: string
                  prometheusUrl:
                    description: |-
                      (String) Prometheus url for this instance.
                      Prometheus url for this instance.
                    type: string
                  prometheusUserId:
                    description: |-
                      (Number) Prometheus user ID. Used for e.g. remote_write.
                      Prometheus user ID. Used for e.g. remote_write.
                    type: number
                  regionSlug:
                    description: |-
                      cloud/developer-resources/api-reference/cloud-api/#list-regions.
                      Region slug to assign to this stack. Changing region will destroy the existing stack and create a new one in the desired region. Use the region list API to get the list of available regions: https://grafana.com/docs/grafana-cloud/developer-resources/api-reference/cloud-api/#list-regions.
                    type: string
                  slug:
                    description: |-
                      (String) Subdomain that the Grafana instance will be available at. Setting slug to <stack_slug> will make the instance available at https://<stack_slug>.grafana.net.
                      Subdomain that the Grafana instance will be available at. Setting slug to `<stack_slug>` will make the instance available at `https://<stack_slug>.grafana.net`.
                    type: string
                  smUrl:
                    description: |-
                      (String) Base URL of the Synthetic Monitoring API for this stack's region. This can be used with the sm_url provider config option. Note: Synthetic Monitoring requires activation either via the grafana_synthetic_monitoring_installation resource or manually in the Grafana Cloud UI before it can be used.
                      Base URL of the Synthetic Monitoring API for this stack's region. This can be used with the `sm_url` provider config option. Note: Synthetic Monitoring requires activation either via the `grafana_synthetic_monitoring_installation` resource or manually in the Grafana Cloud UI before it can be used.
                    type: string
                  status:
                    description: |-
                      (String) Status of the stack.
                      Status of the stack.
                    type: string
                  tracesIpAllowListCname:
                    description: |-
                      separated list of CNAMEs that can be whitelisted to access the Traces instance
                      Comma-separated list of CNAMEs that can be whitelisted to access the Traces instance (Optional)
                    type: string
                  tracesName:
                    description: (String)
                    type: string
                  tracesPrivateConnectivityInfoAvailabilityZoneIds:
                    description: |-
                      (List of String) Availability Zone IDs for Traces when using AWS PrivateLink (only for AWS stacks)
                      Availability Zone IDs for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoAvailabilityZones:
                    description: |-
                      (List of String) Availability Zones for Traces when using AWS PrivateLink (only for AWS stacks)
                      Availability Zones for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoPrivateDns:
                    description: |-
                      (String) Private DNS for Traces when using AWS PrivateLink (only for AWS stacks)
                      Private DNS for Traces when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  tracesPrivateConnectivityInfoRegions:
                    description: |-
                      (List of String) Regions for Traces when using AWS PrivateLink (only for AWS stacks)
                      Regions for Traces when using AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoServiceName:
                    description: |-
                      (String) Service Name for Traces when using AWS PrivateLink (only for AWS stacks)
                      Service Name for Traces when using AWS PrivateLink (only for AWS stacks)
                    type: string
                  tracesStatus:
                    description: (String)
                    type: string
                  tracesUrl:
                    description: |-
                      (String) Base URL of the Traces instance configured for this stack. To use this in the Tempo data source in Grafana, append /tempo to the URL.
                      Base URL of the Traces instance configured for this stack. To use this in the Tempo data source in Grafana, append `/tempo` to the URL.
                    type: string
                  tracesUserId:
                    description: (Number)
                    type: number
                  url:
                    description: |-
                      (String) Custom URL for the Grafana instance. Must have a CNAME setup to point to .grafana.net before creating the stack
                      Custom URL for the Grafana instance. Must have a CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                  waitForReadiness:
                    description: |-
                      (Boolean) Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to true.
                      Whether to wait for readiness of the stack after creating it. The check is a HEAD request to the stack URL (Grafana instance). Defaults to `true`.
                    type: boolean
                  waitForReadinessTimeout:
                    description: |-
                      (String) How long to wait for readiness (if enabled). Defaults to 5m0s.
                      How long to wait for readiness (if enabled). Defaults to `5m0s`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccountrotatingtokens.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccountRotatingToken
    listKind: StackServiceAccountRotatingTokenList
    plural: stackserviceaccountrotatingtokens
    singular: stackserviceaccountrotatingtoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccountRotatingToken is the Schema for the StackServiceAccountRotatingTokens
          API. Manages and rotates service account tokens of a Grafana Cloud stack
          using the Cloud API This can be used to bootstrap a management service account
          token for a new stack Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountRotatingTokenSpec defines the desired
              state of StackServiceAccountRotatingToken
            properties:
              forProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.earlyRotationWindowSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindowSeconds)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindowSeconds))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.secondsToLive is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.secondsToLive)
                || (has(self.initProvider) && has(self.initProvider.secondsToLive))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
            - message: spec.forProvider.stackSlug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackSlug)
                || (has(self.initProvider) && has(self.initProvider.stackSlug))'
          status:
            description: StackServiceAccountRotatingTokenStatus defines the observed
              state of StackServiceAccountRotatingToken.
            properties:
              atProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      and will have characters appended to it to make the name unique.
                      The name of the service account token. It will start with `<name_prefix>-` and will have characters appended to it to make the name unique.
                    type: string
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the service account token is expired or within the period to be early rotated.
                      Signals that the service account token is expired or within the period to be early rotated.
                    type: boolean
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccounts.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccount
    listKind: StackServiceAccountList
    plural: stackserviceaccounts
    singular: stackserviceaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccount is the Schema for the StackServiceAccounts
          API. Manages service accounts of a Grafana Cloud stack using the Cloud API
          This can be used to bootstrap a management service account for a new stack
          Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stacks:readstack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountSpec defines the desired state of StackServiceAccount
            properties:
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
          status:
            description: StackServiceAccountStatus defines the observed state of StackServiceAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stackserviceaccounttokens.cloud.grafana.m.crossplane.io
spec:
  group: cloud.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: StackServiceAccountToken
    listKind: StackServiceAccountTokenList
    plural: stackserviceaccounttokens
    singular: stackserviceaccounttoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'StackServiceAccountToken is the Schema for the StackServiceAccountTokens
          API. Manages service account tokens of a Grafana Cloud stack using the Cloud
          API This can be used to bootstrap a management service account token for
          a new stack Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api
          Required access policy scopes: stack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackServiceAccountTokenSpec defines the desired state of
              StackServiceAccountToken
            properties:
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackSlug.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackSlug.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a StackServiceAccount in cloud to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: StackServiceAccountTokenStatus defines the observed state
              of StackServiceAccountToken.
            properties:
              atProvider:
                properties:
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  stackSlug:
                    description: (String)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: accesspolicies.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: AccessPolicies
    listKind: AccessPoliciesList
    plural: accesspolicies
    singular: accesspolicies
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessPolicies is an observe-only resource backed by the grafana_cloud_access_policies
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AccessPoliciesParameters defines the input parameters
                  for the grafana_cloud_access_policies data source.
                properties:
                  nameFilter:
                    description: If set, only access policies with the specified name
                      will be returned. This is faster than filtering in Terraform.
                    type: string
                  regionFilter:
                    description: If set, only access policies in the specified region
                      will be returned. Otherwise, fetches from all available regions
                      (more resource intensive).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AccessPoliciesObservation holds the observed (computed)
                  fields from the grafana_cloud_access_policies data source.
                properties:
                  accessPolicies:
                    items:
                      type: string
                    type: array
                  nameFilter:
                    description: If set, only access policies with the specified name
                      will be returned. This is faster than filtering in Terraform.
                    type: string
                  regionFilter:
                    description: If set, only access policies in the specified region
                      will be returned. Otherwise, fetches from all available regions
                      (more resource intensive).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ipses.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: IPS
    listKind: IPSList
    plural: ipses
    singular: ips
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPS is an observe-only resource backed by the grafana_cloud_ips
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: IPSParameters defines the input parameters for the grafana_cloud_ips
                  data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: IPSObservation holds the observed (computed) fields from
                  the grafana_cloud_ips data source.
                properties:
                  hostedAlerts:
                    description: Set of IP addresses that are used for hosted alerts.
                    items:
                      type: string
                    type: array
                  hostedGrafana:
                    description: Set of IP addresses that are used for hosted Grafana.
                    items:
                      type: string
                    type: array
                  hostedLogs:
                    description: Set of IP addresses that are used for hosted logs.
                    items:
                      type: string
                    type: array
                  hostedMetrics:
                    description: Set of IP addresses that are used for hosted metrics.
                    items:
                      type: string
                    type: array
                  hostedOtlp:
                    description: Set of IP addresses that are used for the OTLP Gateway.
                    items:
                      type: string
                    type: array
                  hostedProfiles:
                    description: Set of IP addresses that are used for hosted profiles.
                    items:
                      type: string
                    type: array
                  hostedTraces:
                    description: Set of IP addresses that are used for hosted traces.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizations.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Organization
    listKind: OrganizationList
    plural: organizations
    singular: organization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Organization is an observe-only resource backed by the grafana_cloud_organization
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: OrganizationParameters defines the input parameters for
                  the grafana_cloud_organization data source.
                properties:
                  slug:
                    description: The organization slug.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: OrganizationObservation holds the observed (computed)
                  fields from the grafana_cloud_organization data source.
                properties:
                  createdAt:
                    description: The date and time the organization was created.
                    type: string
                  name:
                    description: The organization name.
                    type: string
                  slug:
                    description: The organization slug.
                    type: string
                  updatedAt:
                    description: The date and time the organization was last updated.
                    type: string
                  url:
                    description: The organization URL.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: privatedatasourceconnectnetworks.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: PrivateDataSourceConnectNetworks
    listKind: PrivateDataSourceConnectNetworksList
    plural: privatedatasourceconnectnetworks
    singular: privatedatasourceconnectnetworks
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrivateDataSourceConnectNetworks is an observe-only resource
          backed by the grafana_cloud_private_data_source_connect_networks Terraform
          data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: PrivateDataSourceConnectNetworksParameters defines the
                  input parameters for the grafana_cloud_private_data_source_connect_networks
                  data source.
                properties:
                  nameFilter:
                    description: If set, only private data source connect networks
                      with the specified name will be returned. This is faster than
                      filtering in Terraform.
                    type: string
                  regionFilter:
                    description: If set, only private data source connect networks
                      in the specified region will be returned. This is faster than
                      filtering in Terraform.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: PrivateDataSourceConnectNetworksObservation holds the
                  observed (computed) fields from the grafana_cloud_private_data_source_connect_networks
                  data source.
                properties:
                  nameFilter:
                    description: If set, only private data source connect networks
                      with the specified name will be returned. This is faster than
                      filtering in Terraform.
                    type: string
                  privateDataSourceConnectNetworks:
                    items:
                      type: string
                    type: array
                  regionFilter:
                    description: If set, only private data source connect networks
                      in the specified region will be returned. This is faster than
                      filtering in Terraform.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stacks.cloud.grafana.o.crossplane.io
spec:
  group: cloud.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Stack is an observe-only resource backed by the grafana_cloud_stack
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: StackParameters defines the input parameters for the
                  grafana_cloud_stack data source.
                properties:
                  slug:
                    description: |-
                      Subdomain that the Grafana instance will be available at (i.e. setting slug to “<stack_slug>” will make the instance
                      available at “https://<stack_slug>.grafana.net".
                    type: string
                required:
                - slug
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: StackObservation holds the observed (computed) fields
                  from the grafana_cloud_stack data source.
                properties:
                  alertmanagerIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Alertmanager instances (Optional)
                    type: string
                  alertmanagerName:
                    description: Name of the Alertmanager instance configured for
                      this stack.
                    type: string
                  alertmanagerStatus:
                    description: Status of the Alertmanager instance configured for
                      this stack.
                    type: string
                  alertmanagerURL:
                    description: Base URL of the Alertmanager instance configured
                      for this stack.
                    type: string
                  alertmanagerUserID:
                    description: User ID of the Alertmanager instance configured for
                      this stack.
                    format: int64
                    type: integer
                  cloudProviderURL:
                    description: Base URL of the Cloud Provider API for this stack's
                      cluster. This can be used with the `cloud_provider_url` provider
                      config option to manage Cloud Provider resources for this stack.
                    type: string
                  clusterName:
                    description: Name of the cluster where this stack resides.
                    type: string
                  clusterSlug:
                    description: Slug of the cluster where this stack resides.
                    type: string
                  connectionsAPIURL:
                    description: Base URL of the Connections API for this stack's
                      cluster. This can be used with the `connections_api_url` provider
                      config option to manage Connections resources for this stack.
                    type: string
                  deleteProtection:
                    description: Whether to enable delete protection for the stack,
                      preventing accidental deletion.
                    type: boolean
                  description:
                    description: Description of stack.
                    type: string
                  fleetManagementName:
                    description: Name of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Fleet Management when using
                      AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Fleet Management when using
                      AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Fleet Management when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  fleetManagementPrivateConnectivityInfoRegions:
                    description: Regions for Fleet Management when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  fleetManagementPrivateConnectivityInfoServiceName:
                    description: Service Name for Fleet Management when using AWS
                      PrivateLink (only for AWS stacks)
                    type: string
                  fleetManagementStatus:
                    description: Status of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementURL:
                    description: Base URL of the Fleet Management instance configured
                      for this stack.
                    type: string
                  fleetManagementUserID:
                    description: User ID of the Fleet Management instance configured
                      for this stack.
                    format: int64
                    type: integer
                  grafanasIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the grafana instance (Optional)
                    type: string
                  graphiteIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Graphite instance (Optional)
                    type: string
                  graphiteName:
                    type: string
                  graphitePrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Graphite when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  graphitePrivateConnectivityInfoRegions:
                    description: Regions for Graphite when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  graphitePrivateConnectivityInfoServiceName:
                    description: Service Name for Graphite when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  graphiteStatus:
                    type: string
                  graphiteURL:
                    type: string
                  graphiteUserID:
                    format: int64
                    type: integer
                  influxURL:
                    description: Base URL of the InfluxDB instance configured for
                      this stack. The username is the same as the metrics' (`prometheus_user_id`
                      attribute of this resource). See https://grafana.com/docs/grafana-cloud/send-data/metrics/metrics-influxdb/push-from-telegraf/
                      for docs on how to use this.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: 'A map of labels to assign to the stack. Label keys
                      and values must match the following regexp: "^[a-zA-Z0-9/\\-._]+$"
                      and stacks cannot have more than 10 labels.'
                    type: object
                  logsIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Logs instance (Optional)
                    type: string
                  logsName:
                    type: string
                  logsPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Logs when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Logs when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Logs when using AWS PrivateLink (only
                      for AWS stacks)
                    type: string
                  logsPrivateConnectivityInfoRegions:
                    description: Regions for Logs when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  logsPrivateConnectivityInfoServiceName:
                    description: Service Name for Logs when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  logsStatus:
                    type: string
                  logsURL:
                    type: string
                  logsUserID:
                    format: int64
                    type: integer
                  name:
                    description: Name of stack. Conventionally matches the url of
                      the instance (e.g. `<stack_slug>.grafana.net`).
                    type: string
                  oncallAPIURL:
                    description: Base URL of the OnCall API instance configured for
                      this stack.
                    type: string
                  orgID:
                    description: Organization id to assign to this stack.
                    format: int64
                    type: integer
                  orgName:
                    description: Organization name to assign to this stack.
                    type: string
                  orgSlug:
                    description: Organization slug to assign to this stack.
                    type: string
                  otlpPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for OTLP when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for OTLP when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for OTLP when using AWS PrivateLink (only
                      for AWS stacks)
                    type: string
                  otlpPrivateConnectivityInfoRegions:
                    description: Regions for OTLP when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  otlpPrivateConnectivityInfoServiceName:
                    description: Service Name for OTLP when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  otlpURL:
                    description: Base URL of the OTLP instance configured for this
                      stack. The username is the stack's ID (`id` attribute of this
                      resource). See https://grafana.com/docs/grafana-cloud/send-data/otlp/send-data-otlp/
                      for docs on how to use this.
                    type: string
                  pdcAPIPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for PDC's API when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcAPIPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcAPIPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcAPIPrivateConnectivityInfoRegions:
                    description: Regions for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcAPIPrivateConnectivityInfoServiceName:
                    description: Service Name for PDC's API when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for PDC's Gateway when using
                      AWS PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for PDC's Gateway when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  pdcGatewayPrivateConnectivityInfoRegions:
                    description: Regions for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  pdcGatewayPrivateConnectivityInfoServiceName:
                    description: Service Name for PDC's Gateway when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Profiles instance (Optional)
                    type: string
                  profilesName:
                    type: string
                  profilesPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Profiles when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesPrivateConnectivityInfoRegions:
                    description: Regions for Profiles when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  profilesPrivateConnectivityInfoServiceName:
                    description: Service Name for Profiles when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  profilesStatus:
                    type: string
                  profilesURL:
                    type: string
                  profilesUserID:
                    format: int64
                    type: integer
                  prometheusIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Prometheus instance (Optional)
                    type: string
                  prometheusName:
                    description: Prometheus name for this instance.
                    type: string
                  prometheusPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Prometheus when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Prometheus when using AWS
                      PrivateLink (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  prometheusPrivateConnectivityInfoRegions:
                    description: Regions for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  prometheusPrivateConnectivityInfoServiceName:
                    description: Service Name for Prometheus when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  prometheusRemoteEndpoint:
                    description: Use this URL to query hosted metrics data e.g. Prometheus
                      data source in Grafana
                    type: string
                  prometheusRemoteWriteEndpoint:
                    description: Use this URL to send prometheus metrics to Grafana
                      cloud
                    type: string
                  prometheusStatus:
                    description: Prometheus status for this instance.
                    type: string
                  prometheusURL:
                    description: Prometheus url for this instance.
                    type: string
                  prometheusUserID:
                    description: Prometheus user ID. Used for e.g. remote_write.
                    format: int64
                    type: integer
                  regionSlug:
                    description: The region this stack is deployed to.
                    type: string
                  smURL:
                    description: 'Base URL of the Synthetic Monitoring API for this
                      stack''s region. This can be used with the `sm_url` provider
                      config option. Note: Synthetic Monitoring requires activation
                      either via the `grafana_synthetic_monitoring_installation` resource
                      or manually in the Grafana Cloud UI before it can be used.'
                    type: string
                  status:
                    description: Status of the stack.
                    type: string
                  tracesIPAllowListCname:
                    description: Comma-separated list of CNAMEs that can be whitelisted
                      to access the Traces instance (Optional)
                    type: string
                  tracesName:
                    type: string
                  tracesPrivateConnectivityInfoAvailabilityZoneIDs:
                    description: Availability Zone IDs for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoAvailabilityZones:
                    description: Availability Zones for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoPrivateDNS:
                    description: Private DNS for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  tracesPrivateConnectivityInfoRegions:
                    description: Regions for Traces when using AWS PrivateLink (only
                      for AWS stacks)
                    items:
                      type: string
                    type: array
                  tracesPrivateConnectivityInfoServiceName:
                    description: Service Name for Traces when using AWS PrivateLink
                      (only for AWS stacks)
                    type: string
                  tracesStatus:
                    type: string
                  tracesURL:
                    description: Base URL of the Traces instance configured for this
                      stack. To use this in the Tempo data source in Grafana, append
                      `/tempo` to the URL.
                    type: string
                  tracesUserID:
                    format: int64
                    type: integer
                  url:
                    description: Custom URL for the Grafana instance. Must have a
                      CNAME setup to point to `.grafana.net` before creating the stack
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cloudintegrations.cloudintegrations.grafana.crossplane.io
spec:
  group: cloudintegrations.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CloudIntegration
    listKind: CloudIntegrationList
    plural: cloudintegrations
    singular: cloudintegration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'CloudIntegration is the Schema for the CloudIntegrations API.
          Manages Grafana Cloud integrations. Official documentation https://grafana.com/docs/grafana-cloud/data-configuration/integrations/
          This provider lets you manage Grafana Cloud Integrations. Alerts can optionally
          be disabled. Please note: Grafana Cloud Integrations do not support in-place
          upgrades, and require a teardown and reapply to resolve version drift. As
          such it is recommended to have a separate TF plan for integrations to cleanly
          destroy them as needed. Update, only triggered on config change, is implemented
          as a complete uninstall, then reinstall of the integration in question.
          Required access policy scopes: folders:readfolders:writedashboards:readdashboards:writerules:readrules:write
          Based on: https://grafana.com/docs/grafana/latest/alerting/alerting-rules/alerting-migration/#import-rules-with-grafana-alerting
          Note: This resource creates folders and dashboards as part of the integration
          installation process, which requires additional permissions beyond the basic
          integration scopes.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudIntegrationSpec defines the desired state of CloudIntegration
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: CloudIntegrationStatus defines the observed state of CloudIntegration.
            properties:
              atProvider:
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  dashboardFolder:
                    description: |-
                      (String) The dashboard folder associated with this integration.
                      The dashboard folder associated with this integration.
                    type: string
                  id:
                    description: Set to the integration slug.
                    type: string
                  installedVersion:
                    description: |-
                      (String) The version of the installed integration.
                      The version of the installed integration.
                    type: string
                  latestVersion:
                    description: |-
                      (String) The latest version available for this integration.
                      The latest version available for this integration.
                    type: string
                  name:
                    description: |-
                      (String) The display name of the integration.
                      The display name of the integration.
                    type: string
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cloudintegrations.cloudintegrations.grafana.m.crossplane.io
spec:
  group: cloudintegrations.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CloudIntegration
    listKind: CloudIntegrationList
    plural: cloudintegrations
    singular: cloudintegration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'CloudIntegration is the Schema for the CloudIntegrations API.
          Manages Grafana Cloud integrations. Official documentation https://grafana.com/docs/grafana-cloud/data-configuration/integrations/
          This provider lets you manage Grafana Cloud Integrations. Alerts can optionally
          be disabled. Please note: Grafana Cloud Integrations do not support in-place
          upgrades, and require a teardown and reapply to resolve version drift. As
          such it is recommended to have a separate TF plan for integrations to cleanly
          destroy them as needed. Update, only triggered on config change, is implemented
          as a complete uninstall, then reinstall of the integration in question.
          Required access policy scopes: folders:readfolders:writedashboards:readdashboards:writerules:readrules:write
          Based on: https://grafana.com/docs/grafana/latest/alerting/alerting-rules/alerting-migration/#import-rules-with-grafana-alerting
          Note: This resource creates folders and dashboards as part of the integration
          installation process, which requires additional permissions beyond the basic
          integration scopes.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudIntegrationSpec defines the desired state of CloudIntegration
            properties:
              forProvider:
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: CloudIntegrationStatus defines the observed state of CloudIntegration.
            properties:
              atProvider:
                properties:
                  alertsEnabled:
                    description: |-
                      (Boolean) Whether alerts are enabled for this integration.
                      Whether alerts are enabled for this integration.
                    type: boolean
                  dashboardFolder:
                    description: |-
                      (String) The dashboard folder associated with this integration.
                      The dashboard folder associated with this integration.
                    type: string
                  id:
                    description: Set to the integration slug.
                    type: string
                  installedVersion:
                    description: |-
                      (String) The version of the installed integration.
                      The version of the installed integration.
                    type: string
                  latestVersion:
                    description: |-
                      (String) The latest version available for this integration.
                      The latest version available for this integration.
                    type: string
                  name:
                    description: |-
                      (String) The display name of the integration.
                      The display name of the integration.
                    type: string
                  slug:
                    description: |-
                      node').
                      The slug of the integration to install (e.g., 'docker', 'linux-node').
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awsaccounts.cloudprovider.grafana.crossplane.io
spec:
  group: cloudprovider.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsAccount
    listKind: AwsAccountList
    plural: awsaccounts
    singular: awsaccount
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsAccount is the Schema for the AwsAccounts API. This resource
          allows you to link your AWS Account to Grafana Cloud for use in creating
          Cloud Provider resources. See the Grafana Provider configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsAccountSpec defines the desired state of AwsAccount
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.regions is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.regions)
                || (has(self.initProvider) && has(self.initProvider.regions))'
            - message: spec.forProvider.roleArn is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.roleArn)
                || (has(self.initProvider) && has(self.initProvider.roleArn))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsAccountStatus defines the observed state of AwsAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: This has the format "{{ stack_id }}:{{ resource_id
                      }}".
                    type: string
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  resourceId:
                    description: |-
                      (String) The ID given by the Grafana Cloud Provider API to this AWS Account resource.
                      The ID given by the Grafana Cloud Provider API to this AWS Account resource.
                    type: string
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awscloudwatchscrapejobs.cloudprovider.grafana.crossplane.io
spec:
  group: cloudprovider.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsCloudwatchScrapeJob
    listKind: AwsCloudwatchScrapeJobList
    plural: awscloudwatchscrapejobs
    singular: awscloudwatchscrapejob
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsCloudwatchScrapeJob is the Schema for the AwsCloudwatchScrapeJobs
          API. This resource allows you to scrape AWS CloudWatch metrics in Grafana
          Cloud without needing to run your own infrastructure. See the Grafana Provider
          configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsCloudwatchScrapeJobSpec defines the desired state of AwsCloudwatchScrapeJob
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.awsAccountResourceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsAccountResourceId)
                || (has(self.initProvider) && has(self.initProvider.awsAccountResourceId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsCloudwatchScrapeJobStatus defines the observed state of
              AwsCloudwatchScrapeJob.
            properties:
              atProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  disabledReason:
                    description: |-
                      (String) When the AWS CloudWatch Scrape Job is disabled, this will show the reason that it is in that state.
                      When the AWS CloudWatch Scrape Job is disabled, this will show the reason that it is in that state.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awsresourcemetadatascrapejobs.cloudprovider.grafana.crossplane.io
spec:
  group: cloudprovider.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsResourceMetadataScrapeJob
    listKind: AwsResourceMetadataScrapeJobList
    plural: awsresourcemetadatascrapejobs
    singular: awsresourcemetadatascrapejob
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsResourceMetadataScrapeJob is the Schema for the AwsResourceMetadataScrapeJobs
          API. This resource allows you to scrape AWS resource metadata such as ARN
          and tags as info metrics in Grafana Cloud without needing to run your own
          infrastructure. Use this resource if you aren't using grafana_cloud_provider_aws_cloudwatch_scrape_job,
          but still want to have AWS resource metadata available in Grafana Cloud,
          for example for use with our AWS Metrics Streams integration and/or Knowledge
          Graph features. See the Grafana Provider configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsResourceMetadataScrapeJobSpec defines the desired state
              of AwsResourceMetadataScrapeJob
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.awsAccountResourceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsAccountResourceId)
                || (has(self.initProvider) && has(self.initProvider.awsAccountResourceId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsResourceMetadataScrapeJobStatus defines the observed state
              of AwsResourceMetadataScrapeJob.
            properties:
              atProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  disabledReason:
                    description: |-
                      (String) When the AWS Resource Metadata Scrape Job is disabled, this will show the reason that it is in that state.
                      When the AWS Resource Metadata Scrape Job is disabled, this will show the reason that it is in that state.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: azurecredentials.cloudprovider.grafana.crossplane.io
spec:
  group: cloudprovider.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AzureCredential
    listKind: AzureCredentialList
    plural: azurecredentials
    singular: azurecredential
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureCredential is the Schema for the AzureCredentials API. This
          resource allows you to autodiscover resources in your Azure tenant and scrape
          Azure Monitor metrics for those resources in Grafana Cloud without needing
          to run your own infrastructure. See the Grafana Provider configuration docs
          https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/azure/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureCredentialSpec defines the desired state of AzureCredential
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      (String, Sensitive) The client secret of the Azure Credential.
                      The client secret of the Azure Credential.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      (String, Sensitive) The client secret of the Azure Credential.
                      The client secret of the Azure Credential.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                required:
                - clientSecretSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clientId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientId)
                || (has(self.initProvider) && has(self.initProvider.clientId))'
            - message: spec.forProvider.clientSecretSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientSecretSecretRef)'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
            - message: spec.forProvider.tenantId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tenantId)
                || (has(self.initProvider) && has(self.initProvider.tenantId))'
          status:
            description: AzureCredentialStatus defines the observed state of AzureCredential.
            properties:
              atProvider:
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  id:
                    description: This has the format "{{ stack_id }}:{{ resource_id
                      }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceId:
                    description: |-
                      (String) The ID given by the Grafana Cloud Provider API to this Azure Credential resource.
                      The ID given by the Grafana Cloud Provider API to this Azure Credential resource.
                    type: string
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awsaccounts.cloudprovider.grafana.m.crossplane.io
spec:
  group: cloudprovider.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsAccount
    listKind: AwsAccountList
    plural: awsaccounts
    singular: awsaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsAccount is the Schema for the AwsAccounts API. This resource
          allows you to link your AWS Account to Grafana Cloud for use in creating
          Cloud Provider resources. See the Grafana Provider configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsAccountSpec defines the desired state of AwsAccount
            properties:
              forProvider:
                properties:
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.regions is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.regions)
                || (has(self.initProvider) && has(self.initProvider.regions))'
            - message: spec.forProvider.roleArn is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.roleArn)
                || (has(self.initProvider) && has(self.initProvider.roleArn))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsAccountStatus defines the observed state of AwsAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: This has the format "{{ stack_id }}:{{ resource_id
                      }}".
                    type: string
                  name:
                    description: |-
                      readable name for this AWS Account resource.
                      An optional human-readable name for this AWS Account resource.
                    type: string
                  regions:
                    description: |-
                      (Set of String) A set of regions that this AWS Account resource applies to.
                      A set of regions that this AWS Account resource applies to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  resourceId:
                    description: |-
                      (String) The ID given by the Grafana Cloud Provider API to this AWS Account resource.
                      The ID given by the Grafana Cloud Provider API to this AWS Account resource.
                    type: string
                  roleArn:
                    description: |-
                      (String) An IAM Role ARN string to represent with this AWS Account resource.
                      An IAM Role ARN string to represent with this AWS Account resource.
                    type: string
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awscloudwatchscrapejobs.cloudprovider.grafana.m.crossplane.io
spec:
  group: cloudprovider.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsCloudwatchScrapeJob
    listKind: AwsCloudwatchScrapeJobList
    plural: awscloudwatchscrapejobs
    singular: awscloudwatchscrapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsCloudwatchScrapeJob is the Schema for the AwsCloudwatchScrapeJobs
          API. This resource allows you to scrape AWS CloudWatch metrics in Grafana
          Cloud without needing to run your own infrastructure. See the Grafana Provider
          configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsCloudwatchScrapeJobSpec defines the desired state of AwsCloudwatchScrapeJob
            properties:
              forProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.awsAccountResourceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsAccountResourceId)
                || (has(self.initProvider) && has(self.initProvider.awsAccountResourceId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsCloudwatchScrapeJobStatus defines the observed state of
              AwsCloudwatchScrapeJob.
            properties:
              atProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this CloudWatch Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  customNamespace:
                    description: |-
                      (Block List) Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      Zero or more configuration blocks to configure custom namespaces for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the custom namespace to scrape.
                          type: string
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the custom namespace. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  disabledReason:
                    description: |-
                      (String) When the AWS CloudWatch Scrape Job is disabled, this will show the reason that it is in that state.
                      When the AWS CloudWatch Scrape Job is disabled, this will show the reason that it is in that state.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS CloudWatch Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  exportTags:
                    description: |-
                      (Boolean) When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as aws_<service_name>_info. Defaults to true.
                      When enabled, AWS resource tags are exported as Prometheus labels to metrics formatted as `aws_<service_name>_info`. Defaults to `true`.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the AWS CloudWatch Scrape Job.
                      The name of the AWS CloudWatch Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the AWS CloudWatch Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        metric:
                          description: |-
                            (Block List) One or more configuration blocks to configure metrics and their statistics to scrape. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure metrics and their statistics to scrape. Please note that AWS metric names must be supplied, and not their PromQL counterparts. Each block must represent a distinct metric name. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              name:
                                description: |-
                                  (String) The name of the AWS CloudWatch Scrape Job.
                                  The name of the metric to scrape.
                                type: string
                              statistics:
                                description: |-
                                  (Set of String) A set of statistics to scrape.
                                  A set of statistics to scrape.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) The name of the AWS CloudWatch Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            (Number) The interval in seconds to scrape the custom namespace. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                        tagsToAddToMetrics:
                          description: |-
                            (Set of String) A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                            A set of tags to add to all metrics exported by this scrape job, for use in PromQL queries.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awsresourcemetadatascrapejobs.cloudprovider.grafana.m.crossplane.io
spec:
  group: cloudprovider.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AwsResourceMetadataScrapeJob
    listKind: AwsResourceMetadataScrapeJobList
    plural: awsresourcemetadatascrapejobs
    singular: awsresourcemetadatascrapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsResourceMetadataScrapeJob is the Schema for the AwsResourceMetadataScrapeJobs
          API. This resource allows you to scrape AWS resource metadata such as ARN
          and tags as info metrics in Grafana Cloud without needing to run your own
          infrastructure. Use this resource if you aren't using grafana_cloud_provider_aws_cloudwatch_scrape_job,
          but still want to have AWS resource metadata available in Grafana Cloud,
          for example for use with our AWS Metrics Streams integration and/or Knowledge
          Graph features. See the Grafana Provider configuration docs https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AwsResourceMetadataScrapeJobSpec defines the desired state
              of AwsResourceMetadataScrapeJob
            properties:
              forProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.awsAccountResourceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.awsAccountResourceId)
                || (has(self.initProvider) && has(self.initProvider.awsAccountResourceId))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AwsResourceMetadataScrapeJobStatus defines the observed state
              of AwsResourceMetadataScrapeJob.
            properties:
              atProvider:
                properties:
                  awsAccountResourceId:
                    description: |-
                      (String) The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the resource_id attribute of the grafana_cloud_provider_aws_account resource.
                      The ID assigned by the Grafana Cloud Provider API to an AWS Account resource that should be associated with this Resource Metadata Scrape Job. This can be provided by the `resource_id` attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  disabledReason:
                    description: |-
                      (String) When the AWS Resource Metadata Scrape Job is disabled, this will show the reason that it is in that state.
                      When the AWS Resource Metadata Scrape Job is disabled, this will show the reason that it is in that state.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to true.
                      Whether the AWS Resource Metadata Scrape Job is enabled or not. Defaults to `true`.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the AWS Resource Metadata Scrape Job.
                      The name of the AWS Resource Metadata Scrape Job.
                    type: string
                  regionsSubsetOverride:
                    description: |-
                      (Set of String) A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                      A subset of the regions that are configured in the associated AWS Account resource to apply to this scrape job. If not set or empty, all of the Account resource's regions are scraped.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  service:
                    description: |-
                      (Block List) One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct name attribute. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                      One or more configuration blocks to configure AWS services for the Resource Metadata Scrape Job to scrape. Each block must have a distinct `name` attribute. When accessing this as an attribute reference, it is a list of objects.
                    items:
                      properties:
                        name:
                          description: |-
                            (String) The name of the AWS Resource Metadata Scrape Job.
                            The name of the service to scrape. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported services.
                          type: string
                        resourceDiscoveryTagFilter:
                          description: |-
                            (Block List) One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects. (see below for nested schema)
                            One or more configuration blocks to configure tag filters applied to discovery of resource entities in the associated AWS account. When accessing this as an attribute reference, it is a list of objects.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The key of the tag filter.
                                  The key of the tag filter.
                                type: string
                              value:
                                description: |-
                                  (String) The value of the tag filter.
                                  The value of the tag filter.
                                type: string
                            type: object
                          type: array
                        scrapeIntervalSeconds:
                          description: |-
                            cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to 300.
                            The interval in seconds to scrape the service. See https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/aws/cloudwatch-metrics/services/ for supported scrape intervals. Defaults to `300`.
                          type: number
                      type: object
                    type: array
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A set of static labels to add to all metrics exported by this scrape job.
                      A set of static labels to add to all metrics exported by this scrape job.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: azurecredentials.cloudprovider.grafana.m.crossplane.io
spec:
  group: cloudprovider.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: AzureCredential
    listKind: AzureCredentialList
    plural: azurecredentials
    singular: azurecredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureCredential is the Schema for the AzureCredentials API. This
          resource allows you to autodiscover resources in your Azure tenant and scrape
          Azure Monitor metrics for those resources in Grafana Cloud without needing
          to run your own infrastructure. See the Grafana Provider configuration docs
          https://registry.io/providers/grafana/grafana/latest/docs#managing-cloud-provider
          for information on authentication and required access policy scopes. Official
          Grafana Cloud documentation https://grafana.com/docs/grafana-cloud/monitor-infrastructure/monitor-cloud-provider/azure/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzureCredentialSpec defines the desired state of AzureCredential
            properties:
              forProvider:
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      (String, Sensitive) The client secret of the Azure Credential.
                      The client secret of the Azure Credential.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  clientSecretSecretRef:
                    description: |-
                      (String, Sensitive) The client secret of the Azure Credential.
                      The client secret of the Azure Credential.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                required:
                - clientSecretSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clientId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientId)
                || (has(self.initProvider) && has(self.initProvider.clientId))'
            - message: spec.forProvider.clientSecretSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientSecretSecretRef)'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
            - message: spec.forProvider.tenantId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tenantId)
                || (has(self.initProvider) && has(self.initProvider.tenantId))'
          status:
            description: AzureCredentialStatus defines the observed state of AzureCredential.
            properties:
              atProvider:
                properties:
                  autoDiscoveryConfiguration:
                    description: |-
                      (Block List) The list of auto discovery configurations. (see below for nested schema)
                      The list of auto discovery configurations.
                    items:
                      properties:
                        resourceTypeConfigurations:
                          description: |-
                            (List of Object) The list of resource type configurations. (see below for nested schema)
                            The list of resource type configurations.
                          items:
                            properties:
                              metricConfiguration:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    aggregations:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    dimensions:
                                      description: (List of String)
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: (String) The name of the Azure
                                        Credential.
                                      type: string
                                  type: object
                                type: array
                              resourceTypeName:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        subscriptionId:
                          description: |-
                            (String) The subscription ID of the Azure account.
                            The subscription ID of the Azure account.
                          type: string
                      type: object
                    type: array
                  clientId:
                    description: |-
                      (String) The client ID of the Azure Credential.
                      The client ID of the Azure Credential.
                    type: string
                  id:
                    description: This has the format "{{ stack_id }}:{{ resource_id
                      }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the Azure Credential.
                      The name of the Azure Credential.
                    type: string
                  resourceDiscoveryTagFilter:
                    description: |-
                      (Block List) The list of tag filters to apply to resources. (see below for nested schema)
                      The list of tag filters to apply to resources.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the tag filter.
                            The key of the tag filter.
                          type: string
                        value:
                          description: |-
                            (String) The value of the tag filter.
                            The value of the tag filter.
                          type: string
                      type: object
                    type: array
                  resourceId:
                    description: |-
                      (String) The ID given by the Grafana Cloud Provider API to this Azure Credential resource.
                      The ID given by the Grafana Cloud Provider API to this Azure Credential resource.
                    type: string
                  resourceTagsToAddToMetrics:
                    description: |-
                      (Set of String) The list of resource tags to add to metrics.
                      The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  stackId:
                    description: |-
                      (String) The StackID of the Grafana Cloud instance.
                      The StackID of the Grafana Cloud instance.
                    type: string
                  tenantId:
                    description: |-
                      (String) The tenant ID of the Azure Credential.
                      The tenant ID of the Azure Credential.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awsaccounts.cloudprovider.grafana.o.crossplane.io
spec:
  group: cloudprovider.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: AwsAccount
    listKind: AwsAccountList
    plural: awsaccounts
    singular: awsaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsAccount is an observe-only resource backed by the grafana_cloud_provider_aws_account
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AwsAccountParameters defines the input parameters for
                  the grafana_cloud_provider_aws_account data source.
                properties:
                  resourceID:
                    description: The ID given by the Grafana Cloud Provider API to
                      this AWS Account resource.
                    type: string
                  stackID:
                    description: The StackID of the Grafana Cloud instance. Part of
                      the Terraform Resource ID.
                    type: string
                required:
                - resourceID
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AwsAccountObservation holds the observed (computed) fields
                  from the grafana_cloud_provider_aws_account data source.
                properties:
                  name:
                    description: An optional human-readable name for this AWS Account
                      resource.
                    type: string
                  regions:
                    description: A set of regions that this AWS Account resource applies
                      to.
                    items:
                      type: string
                    type: array
                  roleArn:
                    description: An IAM Role ARN string to represent with this AWS
                      Account resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awscloudwatchscrapejobs.cloudprovider.grafana.o.crossplane.io
spec:
  group: cloudprovider.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: AwsCloudwatchScrapeJob
    listKind: AwsCloudwatchScrapeJobList
    plural: awscloudwatchscrapejobs
    singular: awscloudwatchscrapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsCloudwatchScrapeJob is an observe-only resource backed by
          the grafana_cloud_provider_aws_cloudwatch_scrape_job Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AwsCloudwatchScrapeJobParameters defines the input parameters
                  for the grafana_cloud_provider_aws_cloudwatch_scrape_job data source.
                properties:
                  name:
                    description: The name of the AWS CloudWatch Scrape Job. Part of
                      the Terraform Resource ID.
                    type: string
                  stackID:
                    description: The Stack ID of the Grafana Cloud instance. Part
                      of the Terraform Resource ID.
                    type: string
                required:
                - name
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AwsCloudwatchScrapeJobObservation holds the observed
                  (computed) fields from the grafana_cloud_provider_aws_cloudwatch_scrape_job
                  data source.
                properties:
                  awsAccountResourceID:
                    description: The ID assigned by the Grafana Cloud Provider API
                      to an AWS Account resource that should be associated with this
                      AWS CloudWatch Scrape Job. This can be provided by the `resource_id`
                      attribute of the `grafana_cloud_provider_aws_account` resource.
                    type: string
                  disabledReason:
                    description: When the AWS CloudWatch Scrape Job is disabled, this
                      will show the reason that it is in that state.
                    type: string
                  enabled:
                    description: Whether the AWS CloudWatch Scrape Job is enabled
                      or not.
                    type: boolean
                  exportTags:
                    description: When enabled, AWS resource tags are exported as Prometheus
                      labels to metrics formatted as `aws_<service_name>_info`.
                    type: boolean
                  regions:
                    description: The set of AWS region names that this AWS CloudWatch
                      Scrape Job is configured to scrape.
                    items:
                      type: string
                    type: array
                  regionsSubsetOverrideUsed:
                    description: When true, the `regions` attribute will be the set
                      of regions configured in the override. When false, the `regions`
                      attribute will be the set of regions belonging to the AWS Account
                      resource that is associated with this AWS CloudWatch Scrape
                      Job.
                    type: boolean
                  roleArn:
                    description: The AWS ARN of the IAM role associated with the AWS
                      Account resource that is being used by this AWS CloudWatch Scrape
                      Job.
                    type: string
                  staticLabels:
                    additionalProperties:
                      type: string
                    description: A set of static labels to add to all metrics exported
                      by this scrape job.
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: awscloudwatchscrapejobsets.cloudprovider.grafana.o.crossplane.io
spec:
  group: cloudprovider.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: AwsCloudwatchScrapeJobSet
    listKind: AwsCloudwatchScrapeJobSetList
    plural: awscloudwatchscrapejobsets
    singular: awscloudwatchscrapejobset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AwsCloudwatchScrapeJobSet is an observe-only resource backed
          by the grafana_cloud_provider_aws_cloudwatch_scrape_jobs Terraform data
          source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AwsCloudwatchScrapeJobSetParameters defines the input
                  parameters for the grafana_cloud_provider_aws_cloudwatch_scrape_jobs
                  data source.
                properties:
                  stackID:
                    description: The Stack ID of the Grafana Cloud instance. Part
                      of the Terraform Resource ID.
                    type: string
                required:
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AwsCloudwatchScrapeJobSetObservation holds the observed
                  (computed) fields from the grafana_cloud_provider_aws_cloudwatch_scrape_jobs
                  data source.
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: azurecredentials.cloudprovider.grafana.o.crossplane.io
spec:
  group: cloudprovider.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: AzureCredential
    listKind: AzureCredentialList
    plural: azurecredentials
    singular: azurecredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzureCredential is an observe-only resource backed by the grafana_cloud_provider_azure_credential
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AzureCredentialParameters defines the input parameters
                  for the grafana_cloud_provider_azure_credential data source.
                properties:
                  resourceID:
                    description: The ID given by the Grafana Cloud Provider API to
                      this Azure Credential resource.
                    type: string
                  stackID:
                    description: The StackID of the Grafana Cloud instance. Part of
                      the Terraform Resource ID.
                    type: string
                required:
                - resourceID
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AzureCredentialObservation holds the observed (computed)
                  fields from the grafana_cloud_provider_azure_credential data source.
                properties:
                  clientID:
                    description: The client ID of the Azure Credential.
                    type: string
                  clientSecret:
                    description: The client secret of the Azure Credential.
                    type: string
                  name:
                    description: The name of the Azure Credential.
                    type: string
                  resourceTagsToAddToMetrics:
                    description: The list of resource tags to add to metrics.
                    items:
                      type: string
                    type: array
                  tenantID:
                    description: The tenant ID of the Azure Credential.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: metricsendpointscrapejobs.connections.grafana.crossplane.io
spec:
  group: connections.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MetricsEndpointScrapeJob
    listKind: MetricsEndpointScrapeJobList
    plural: metricsendpointscrapejobs
    singular: metricsendpointscrapejob
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsEndpointScrapeJob is the Schema for the MetricsEndpointScrapeJobs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MetricsEndpointScrapeJobSpec defines the desired state of
              MetricsEndpointScrapeJob
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  authenticationBasicPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Password for basic authentication, use if scrape job is using basic authentication method
                      Password for basic authentication, use if scrape job is using basic authentication method
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationBearerTokenSecretRef:
                    description: |-
                      (String, Sensitive) Bearer token used for authentication, use if scrape job is using bearer authentication method
                      Bearer token used for authentication, use if scrape job is using bearer authentication method
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationBasicPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Password for basic authentication, use if scrape job is using basic authentication method
                      Password for basic authentication, use if scrape job is using basic authentication method
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationBearerTokenSecretRef:
                    description: |-
                      (String, Sensitive) Bearer token used for authentication, use if scrape job is using bearer authentication method
                      Bearer token used for authentication, use if scrape job is using bearer authentication method
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.authenticationMethod is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.authenticationMethod)
                || (has(self.initProvider) && has(self.initProvider.authenticationMethod))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
            - message: spec.forProvider.url is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.url)
                || (has(self.initProvider) && has(self.initProvider.url))'
          status:
            description: MetricsEndpointScrapeJobStatus defines the observed state
              of MetricsEndpointScrapeJob.
            properties:
              atProvider:
                properties:
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: metricsendpointscrapejobs.connections.grafana.m.crossplane.io
spec:
  group: connections.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: MetricsEndpointScrapeJob
    listKind: MetricsEndpointScrapeJobList
    plural: metricsendpointscrapejobs
    singular: metricsendpointscrapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsEndpointScrapeJob is the Schema for the MetricsEndpointScrapeJobs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MetricsEndpointScrapeJobSpec defines the desired state of
              MetricsEndpointScrapeJob
            properties:
              forProvider:
                properties:
                  authenticationBasicPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Password for basic authentication, use if scrape job is using basic authentication method
                      Password for basic authentication, use if scrape job is using basic authentication method
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationBearerTokenSecretRef:
                    description: |-
                      (String, Sensitive) Bearer token used for authentication, use if scrape job is using bearer authentication method
                      Bearer token used for authentication, use if scrape job is using bearer authentication method
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationBasicPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Password for basic authentication, use if scrape job is using basic authentication method
                      Password for basic authentication, use if scrape job is using basic authentication method
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationBearerTokenSecretRef:
                    description: |-
                      (String, Sensitive) Bearer token used for authentication, use if scrape job is using bearer authentication method
                      Bearer token used for authentication, use if scrape job is using bearer authentication method
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.authenticationMethod is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.authenticationMethod)
                || (has(self.initProvider) && has(self.initProvider.authenticationMethod))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
            - message: spec.forProvider.url is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.url)
                || (has(self.initProvider) && has(self.initProvider.url))'
          status:
            description: MetricsEndpointScrapeJobStatus defines the observed state
              of MetricsEndpointScrapeJob.
            properties:
              atProvider:
                properties:
                  authenticationBasicUsername:
                    description: |-
                      (String) Username for basic authentication, use if scrape job is using basic authentication method
                      Username for basic authentication, use if scrape job is using basic authentication method
                    type: string
                  authenticationMethod:
                    description: |-
                      (String) Method to pass authentication credentials: basic or bearer.
                      Method to pass authentication credentials: basic or bearer.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the metrics endpoint scrape job is enabled or not.
                      Whether the metrics endpoint scrape job is enabled or not.
                    type: boolean
                  id:
                    description: This has the format "{{ stack_id }}:{{ name }}".
                    type: string
                  name:
                    description: |-
                      (String) The name of the metrics endpoint scrape job.
                      The name of the metrics endpoint scrape job.
                    type: string
                  scrapeIntervalSeconds:
                    description: |-
                      (Number) Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                      Frequency for scraping the metrics endpoint: 30, 60, or 120 seconds.
                    type: number
                  stackId:
                    description: |-
                      (String) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: string
                  url:
                    description: |-
                      (String) The url to scrape metrics from; a valid HTTPs URL is required.
                      The url to scrape metrics from; a valid HTTPs URL is required.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: metricsendpointscrapejobs.connections.grafana.o.crossplane.io
spec:
  group: connections.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: MetricsEndpointScrapeJob
    listKind: MetricsEndpointScrapeJobList
    plural: metricsendpointscrapejobs
    singular: metricsendpointscrapejob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsEndpointScrapeJob is an observe-only resource backed by
          the grafana_connections_metrics_endpoint_scrape_job Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: MetricsEndpointScrapeJobParameters defines the input
                  parameters for the grafana_connections_metrics_endpoint_scrape_job
                  data source.
                properties:
                  name:
                    description: The name of the Metrics Endpoint Scrape Job. Part
                      of the Terraform Resource ID.
                    type: string
                  stackID:
                    description: The Stack ID of the Grafana Cloud instance. Part
                      of the Terraform Resource ID.
                    type: string
                required:
                - name
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: MetricsEndpointScrapeJobObservation holds the observed
                  (computed) fields from the grafana_connections_metrics_endpoint_scrape_job
                  data source.
                properties:
                  authenticationBasicPassword:
                    description: Password for basic authentication.
                    type: string
                  authenticationBasicUsername:
                    description: Username for basic authentication.
                    type: string
                  authenticationBearerToken:
                    description: Token for authentication bearer.
                    type: string
                  authenticationMethod:
                    description: 'Method to pass authentication credentials: basic
                      or bearer.'
                    type: string
                  enabled:
                    description: Whether the metrics endpoint scrape job is enabled
                      or not.
                    type: boolean
                  scrapeIntervalSeconds:
                    description: 'Frequency for scraping the metrics endpoint: 30,
                      60, or 120 seconds.'
                    format: int64
                    type: integer
                  url:
                    description: The url to scrape metrics.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcecacheconfigs.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceCacheConfig
    listKind: DataSourceCacheConfigList
    plural: datasourcecacheconfigs
    singular: datasourcecacheconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourceCacheConfig is the Schema for the DataSourceCacheConfigs
          API. Manages cache configuration for a data source (Grafana Enterprise).
          Use this resource to enable or disable caching for a particular data source.
          You can also tune the TTL settings for the cache behaviour, or choose to
          use defaults. Deleting this resource will cause the cache to be disabled
          for the target data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceCacheConfigSpec defines the desired state of DataSourceCacheConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
          status:
            description: DataSourceCacheConfigStatus defines the observed state of
              DataSourceCacheConfig.
            properties:
              atProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourceconfiglbacrules.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceConfigLbacRules
    listKind: DataSourceConfigLbacRulesList
    plural: datasourceconfiglbacrules
    singular: datasourceconfiglbacrules
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'DataSourceConfigLbacRules is the Schema for the DataSourceConfigLbacRuless
          API. Manages LBAC rules for a data source. !> Warning: The resource is experimental
          and will be subject to change. This resource manages the entire LBAC rules
          tree, and will overwrite any existing rules. Official documentation https://grafana.com/docs/grafana/latest/administration/data-source-management/teamlbac/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/datasource_lbac_rules/
          This resource requires Grafana >=11.5.0.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceConfigLbacRulesSpec defines the desired state of
              DataSourceConfigLbacRules
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
            - message: spec.forProvider.rules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rules)
                || (has(self.initProvider) && has(self.initProvider.rules))'
          status:
            description: DataSourceConfigLbacRulesStatus defines the observed state
              of DataSourceConfigLbacRules.
            properties:
              atProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcepermissionitems.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourcePermissionItem
    listKind: DataSourcePermissionItemList
    plural: datasourcepermissionitems
    singular: datasourcepermissionitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourcePermissionItem is the Schema for the DataSourcePermissionItems
          API. Manages a single permission item for a datasource. Conflicts with the
          "grafana_data_source_permission" resource which manages the entire set of
          permissions for a datasource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourcePermissionItemSpec defines the desired state of
              DataSourcePermissionItem
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: DataSourcePermissionItemStatus defines the observed state
              of DataSourcePermissionItem.
            properties:
              atProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcepermissions.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourcePermission
    listKind: DataSourcePermissionList
    plural: datasourcepermissions
    singular: datasourcepermission
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourcePermission is the Schema for the DataSourcePermissions
          API. Manages the entire set of permissions for a datasource. Permissions
          that aren't specified when applying this resource will be removed. HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/datasource_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourcePermissionSpec defines the desired state of DataSourcePermission
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DataSourcePermissionStatus defines the observed state of
              DataSourcePermission.
            properties:
              atProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: keeperactivationv1beta1s.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: KeeperActivationV1Beta1
    listKind: KeeperActivationV1Beta1List
    plural: keeperactivationv1beta1s
    singular: keeperactivationv1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeeperActivationV1Beta1 is the Schema for the KeeperActivationV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeeperActivationV1Beta1Spec defines the desired state of
              KeeperActivationV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: KeeperActivationV1Beta1Status defines the observed state
              of KeeperActivationV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: keeperv1beta1s.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: KeeperV1Beta1
    listKind: KeeperV1Beta1List
    plural: keeperv1beta1s
    singular: keeperv1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeeperV1Beta1 is the Schema for the KeeperV1Beta1s API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeeperV1Beta1Spec defines the desired state of KeeperV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: KeeperV1Beta1Status defines the observed state of KeeperV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: reports.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Report
    listKind: ReportList
    plural: reports
    singular: report
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Report is the Schema for the Reports API. Note: This resource
          is available only with Grafana Enterprise 7.+. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-reports/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/reporting/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReportSpec defines the desired state of Report
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.recipients is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.recipients)
                || (has(self.initProvider) && has(self.initProvider.recipients))'
          status:
            description: ReportStatus defines the observed state of Report.
            properties:
              atProvider:
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) Generated identifier of the report.
                    type: string
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roleassignmentitems.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RoleAssignmentItem
    listKind: RoleAssignmentItemList
    plural: roleassignmentitems
    singular: roleassignmentitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RoleAssignmentItem is the Schema for the RoleAssignmentItems
          API. Manages a single assignment for a role. Conflicts with the "grafana_role_assignment"
          resource which manages the entire set of assignments for a role.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleAssignmentItemSpec defines the desired state of RoleAssignmentItem
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                  userRef:
                    description: Reference to a User in oss to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  userSelector:
                    description: Selector for a User in oss to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                  userRef:
                    description: Reference to a User in oss to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  userSelector:
                    description: Selector for a User in oss to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RoleAssignmentItemStatus defines the observed state of RoleAssignmentItem.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roleassignments.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RoleAssignment
    listKind: RoleAssignmentList
    plural: roleassignments
    singular: roleassignment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'RoleAssignment is the Schema for the RoleAssignments API. Manages
          the entire set of assignments for a role. Assignments that aren''t specified
          when applying this resource will be removed. Note: This resource is available
          only with Grafana Enterprise 9.2+. Official documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/access_control/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleAssignmentSpec defines the desired state of RoleAssignment
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccountRefs:
                    description: References to ServiceAccount in oss to populate serviceAccounts.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceAccountSelector:
                    description: Selector for a list of ServiceAccount in oss to populate
                      serviceAccounts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamRefs:
                    description: References to Team in oss to populate teams.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  teamSelector:
                    description: Selector for a list of Team in oss to populate teams.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  userRefs:
                    description: References to User in oss to populate users.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  userSelector:
                    description: Selector for a list of User in oss to populate users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccountRefs:
                    description: References to ServiceAccount in oss to populate serviceAccounts.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceAccountSelector:
                    description: Selector for a list of ServiceAccount in oss to populate
                      serviceAccounts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamRefs:
                    description: References to Team in oss to populate teams.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  teamSelector:
                    description: Selector for a list of Team in oss to populate teams.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  userRefs:
                    description: References to User in oss to populate users.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  userSelector:
                    description: Selector for a list of User in oss to populate users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RoleAssignmentStatus defines the observed state of RoleAssignment.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roles.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Role
    listKind: RoleList
    plural: roles
    singular: role
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Role is the Schema for the Roles API. Note: This resource is
          available only with Grafana Enterprise 8.+. Official documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/access_control/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleSpec defines the desired state of Role
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: RoleStatus defines the observed state of Role.
            properties:
              atProvider:
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: scimconfigs.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ScimConfig
    listKind: ScimConfigList
    plural: scimconfigs
    singular: scimconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ScimConfig is the Schema for the ScimConfigs API. Note: Available
          in Grafana Enterprise https://grafana.com/docs/grafana/latest/introduction/grafana-enterprise/
          and Grafana Cloud https://grafana.com/docs/grafana-cloud/. Official documentation
          https://grafana.com/docs/grafana/latest/setup-grafana/configure-access/configure-scim-provisioning/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScimConfigSpec defines the desired state of ScimConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enableGroupSync is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enableGroupSync)
                || (has(self.initProvider) && has(self.initProvider.enableGroupSync))'
            - message: spec.forProvider.enableUserSync is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enableUserSync)
                || (has(self.initProvider) && has(self.initProvider.enableUserSync))'
            - message: spec.forProvider.rejectNonProvisionedUsers is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rejectNonProvisionedUsers)
                || (has(self.initProvider) && has(self.initProvider.rejectNonProvisionedUsers))'
          status:
            description: ScimConfigStatus defines the observed state of ScimConfig.
            properties:
              atProvider:
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: securevaluev1beta1s.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SecurevalueV1Beta1
    listKind: SecurevalueV1Beta1List
    plural: securevaluev1beta1s
    singular: securevaluev1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecurevalueV1Beta1 is the Schema for the SecurevalueV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurevalueV1Beta1Spec defines the desired state of SecurevalueV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                      valueSecretRef:
                        description: Plaintext value to store. This value is write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                      valueSecretRef:
                        description: Plaintext value to store. This value is write-only.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: SecurevalueV1Beta1Status defines the observed state of SecurevalueV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teamexternalgroups.enterprise.grafana.crossplane.io
spec:
  group: enterprise.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: TeamExternalGroup
    listKind: TeamExternalGroupList
    plural: teamexternalgroups
    singular: teamexternalgroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamExternalGroup is the Schema for the TeamExternalGroups API.
          Equivalent to the the team_sync attribute of the grafana_team resource.
          Use one or the other to configure a team's external groups syncing config.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamExternalGroupSpec defines the desired state of TeamExternalGroup
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.groups is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.groups)
                || (has(self.initProvider) && has(self.initProvider.groups))'
          status:
            description: TeamExternalGroupStatus defines the observed state of TeamExternalGroup.
            properties:
              atProvider:
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcecacheconfigs.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceCacheConfig
    listKind: DataSourceCacheConfigList
    plural: datasourcecacheconfigs
    singular: datasourcecacheconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourceCacheConfig is the Schema for the DataSourceCacheConfigs
          API. Manages cache configuration for a data source (Grafana Enterprise).
          Use this resource to enable or disable caching for a particular data source.
          You can also tune the TTL settings for the cache behaviour, or choose to
          use defaults. Deleting this resource will cause the cache to be disabled
          for the target data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceCacheConfigSpec defines the desired state of DataSourceCacheConfig
            properties:
              forProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
          status:
            description: DataSourceCacheConfigStatus defines the observed state of
              DataSourceCacheConfig.
            properties:
              atProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) UID of the data source to configure.
                      UID of the data source to configure.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether caching is enabled for this data source.
                      Whether caching is enabled for this data source.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  ttlQueriesMs:
                    description: |-
                      (Number) TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for query caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  ttlResourcesMs:
                    description: |-
                      (Number) TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                      TTL for resource caching, in milliseconds. Ignored if use_default_ttl is true.
                    type: number
                  useDefaultTtl:
                    description: |-
                      (Boolean) If true, use Grafana's default TTLs instead of custom values. Defaults to true.
                      If true, use Grafana's default TTLs instead of custom values. Defaults to `true`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourceconfiglbacrules.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceConfigLbacRules
    listKind: DataSourceConfigLbacRulesList
    plural: datasourceconfiglbacrules
    singular: datasourceconfiglbacrules
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'DataSourceConfigLbacRules is the Schema for the DataSourceConfigLbacRuless
          API. Manages LBAC rules for a data source. !> Warning: The resource is experimental
          and will be subject to change. This resource manages the entire LBAC rules
          tree, and will overwrite any existing rules. Official documentation https://grafana.com/docs/grafana/latest/administration/data-source-management/teamlbac/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/datasource_lbac_rules/
          This resource requires Grafana >=11.5.0.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceConfigLbacRulesSpec defines the desired state of
              DataSourceConfigLbacRules
            properties:
              forProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
            - message: spec.forProvider.rules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rules)
                || (has(self.initProvider) && has(self.initProvider.rules))'
          status:
            description: DataSourceConfigLbacRulesStatus defines the observed state
              of DataSourceConfigLbacRules.
            properties:
              atProvider:
                properties:
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  rules:
                    description: |-
                      encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                      JSON-encoded LBAC rules for the data source. Map of team UIDs to lists of rule strings.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcepermissionitems.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourcePermissionItem
    listKind: DataSourcePermissionItemList
    plural: datasourcepermissionitems
    singular: datasourcepermissionitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourcePermissionItem is the Schema for the DataSourcePermissionItems
          API. Manages a single permission item for a datasource. Conflicts with the
          "grafana_data_source_permission" resource which manages the entire set of
          permissions for a datasource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourcePermissionItemSpec defines the desired state of
              DataSourcePermissionItem
            properties:
              forProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceUid)
                || (has(self.initProvider) && has(self.initProvider.datasourceUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: DataSourcePermissionItemStatus defines the observed state
              of DataSourcePermissionItem.
            properties:
              atProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) The UID of the datasource.
                      The UID of the datasource.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourcepermissions.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourcePermission
    listKind: DataSourcePermissionList
    plural: datasourcepermissions
    singular: datasourcepermission
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourcePermission is the Schema for the DataSourcePermissions
          API. Manages the entire set of permissions for a datasource. Permissions
          that aren't specified when applying this resource will be removed. HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/datasource_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourcePermissionSpec defines the desired state of DataSourcePermission
            properties:
              forProvider:
                properties:
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DataSourcePermissionStatus defines the observed state of
              DataSourcePermission.
            properties:
              atProvider:
                properties:
                  datasourceType:
                    description: |-
                      (String) The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                      The plugin type of the datasource (e.g. "prometheus"). If set, skips the lookup of the datasource type from the API.
                    type: string
                  datasourceUid:
                    description: |-
                      (String) UID of the datasource to apply permissions to.
                      UID of the datasource to apply permissions to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        builtInRole:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: Query, Edit or Admin (Admin can only be used with Grafana v10.3.0+).
                            Permission to associate with item. Options: `Query`, `Edit` or `Admin` (`Admin` can only be used with Grafana v10.3.0+).
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: keeperactivationv1beta1s.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: KeeperActivationV1Beta1
    listKind: KeeperActivationV1Beta1List
    plural: keeperactivationv1beta1s
    singular: keeperactivationv1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeeperActivationV1Beta1 is the Schema for the KeeperActivationV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeeperActivationV1Beta1Spec defines the desired state of
              KeeperActivationV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: KeeperActivationV1Beta1Status defines the observed state
              of KeeperActivationV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: keeperv1beta1s.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: KeeperV1Beta1
    listKind: KeeperV1Beta1List
    plural: keeperv1beta1s
    singular: keeperv1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeeperV1Beta1 is the Schema for the KeeperV1Beta1s API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KeeperV1Beta1Spec defines the desired state of KeeperV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: KeeperV1Beta1Status defines the observed state of KeeperV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      aws:
                        description: AWS Secrets Manager configuration.
                        properties:
                          assumeRole:
                            description: IAM role assumption configuration.
                            properties:
                              assumeRoleArn:
                                description: Assume role ARN.
                                type: string
                              externalId:
                                description: Assume role external ID.
                                type: string
                            type: object
                          region:
                            description: AWS region.
                            type: string
                        type: object
                      description:
                        description: Keeper description.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: reports.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Report
    listKind: ReportList
    plural: reports
    singular: report
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Report is the Schema for the Reports API. Note: This resource
          is available only with Grafana Enterprise 7.+. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-reports/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/reporting/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReportSpec defines the desired state of Report
            properties:
              forProvider:
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.recipients is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.recipients)
                || (has(self.initProvider) && has(self.initProvider.recipients))'
          status:
            description: ReportStatus defines the observed state of Report.
            properties:
              atProvider:
                properties:
                  dashboards:
                    description: |-
                      (Block List) List of dashboards to render into the report (see below for nested schema)
                      List of dashboards to render into the report
                    items:
                      properties:
                        reportVariables:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Add report variables to the dashboard. Values should be separated by commas.
                            Add report variables to the dashboard. Values should be separated by commas.
                          type: object
                          x-kubernetes-map-type: granular
                        timeRange:
                          description: |-
                            (Block List) Time range of the report. (see below for nested schema)
                            Time range of the report.
                          items:
                            properties:
                              from:
                                description: |-
                                  (String) Start of the time range.
                                  Start of the time range.
                                type: string
                              to:
                                description: |-
                                  (String) End of the time range.
                                  End of the time range.
                                type: string
                            type: object
                          type: array
                        uid:
                          description: |-
                            (String) Dashboard uid.
                            Dashboard uid.
                          type: string
                      type: object
                    type: array
                  formats:
                    description: |-
                      (Set of String) Specifies what kind of attachment to generate for the report. Allowed values: pdf, csv, image.
                      Specifies what kind of attachment to generate for the report. Allowed values: `pdf`, `csv`, `image`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) Generated identifier of the report.
                    type: string
                  includeDashboardLink:
                    description: |-
                      (Boolean) Whether to include a link to the dashboard in the report.
                      Whether to include a link to the dashboard in the report.
                    type: boolean
                  includeTableCsv:
                    description: |-
                      (Boolean) Whether to include a CSV file of table panel data.
                      Whether to include a CSV file of table panel data.
                    type: boolean
                  layout:
                    description: |-
                      (String) Layout of the report. Allowed values: simple, grid.
                      Layout of the report. Allowed values: `simple`, `grid`.
                    type: string
                  message:
                    description: |-
                      (String) Message to be sent in the report.
                      Message to be sent in the report.
                    type: string
                  name:
                    description: |-
                      (String) Name of the report.
                      Name of the report.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  orientation:
                    description: |-
                      (String) Orientation of the report. Allowed values: landscape, portrait.
                      Orientation of the report. Allowed values: `landscape`, `portrait`.
                    type: string
                  recipients:
                    description: |-
                      (List of String) List of recipients of the report.
                      List of recipients of the report.
                    items:
                      type: string
                    type: array
                  replyTo:
                    description: |-
                      to email address of the report.
                      Reply-to email address of the report.
                    type: string
                  schedule:
                    description: |-
                      (Block List)  Schedule of the report. (see below for nested schema)
                      (Required) Schedule of the report.
                    items:
                      properties:
                        customInterval:
                          description: |-
                            (String) Custom interval of the report.
                            Note: This field is only available when frequency is set to custom.
                            Custom interval of the report.
                            **Note:** This field is only available when frequency is set to `custom`.
                          type: string
                        endTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            End time of the report. If empty, the report will be sent indefinitely (according to frequency). Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        frequency:
                          description: |-
                            (String) Frequency of the report. Allowed values: never, once, hourly, daily, weekly, monthly, custom.
                            Frequency of the report. Allowed values: `never`, `once`, `hourly`, `daily`, `weekly`, `monthly`, `custom`.
                          type: string
                        lastDayOfMonth:
                          description: |-
                            (Boolean) Send the report on the last day of the month
                            Send the report on the last day of the month
                          type: boolean
                        startTime:
                          description: |-
                            01-02T15:04:05 format if you want to set a custom timezone
                            Start time of the report. If empty, the start date will be set to the creation time. Note that times will be saved as UTC in Grafana. Use 2006-01-02T15:04:05 format if you want to set a custom timezone
                          type: string
                        timezone:
                          description: |-
                            (String) Set the report time zone.
                            Set the report time zone.
                          type: string
                        workdaysOnly:
                          description: |-
                            (Boolean) Whether to send the report only on work days.
                            Whether to send the report only on work days.
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roleassignmentitems.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RoleAssignmentItem
    listKind: RoleAssignmentItemList
    plural: roleassignmentitems
    singular: roleassignmentitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RoleAssignmentItem is the Schema for the RoleAssignmentItems
          API. Manages a single assignment for a role. Conflicts with the "grafana_role_assignment"
          resource which manages the entire set of assignments for a role.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleAssignmentItemSpec defines the desired state of RoleAssignmentItem
            properties:
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                  userRef:
                    description: Reference to a User in oss to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  userSelector:
                    description: Selector for a User in oss to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                  userRef:
                    description: Reference to a User in oss to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  userSelector:
                    description: Selector for a User in oss to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RoleAssignmentItemStatus defines the observed state of RoleAssignmentItem.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  roleUid:
                    description: |-
                      (String) the role UID onto which to assign an actor
                      the role UID onto which to assign an actor
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) the service account onto which the role is to be assigned
                      the service account onto which the role is to be assigned
                    type: string
                  teamId:
                    description: |-
                      (String) the team onto which the role is to be assigned
                      the team onto which the role is to be assigned
                    type: string
                  userId:
                    description: |-
                      (String) the user onto which the role is to be assigned
                      the user onto which the role is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roleassignments.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RoleAssignment
    listKind: RoleAssignmentList
    plural: roleassignments
    singular: roleassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'RoleAssignment is the Schema for the RoleAssignments API. Manages
          the entire set of assignments for a role. Assignments that aren''t specified
          when applying this resource will be removed. Note: This resource is available
          only with Grafana Enterprise 9.2+. Official documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/access_control/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleAssignmentSpec defines the desired state of RoleAssignment
            properties:
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccountRefs:
                    description: References to ServiceAccount in oss to populate serviceAccounts.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceAccountSelector:
                    description: Selector for a list of ServiceAccount in oss to populate
                      serviceAccounts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamRefs:
                    description: References to Team in oss to populate teams.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  teamSelector:
                    description: Selector for a list of Team in oss to populate teams.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  userRefs:
                    description: References to User in oss to populate users.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  userSelector:
                    description: Selector for a list of User in oss to populate users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleRef:
                    description: Reference to a Role in enterprise to populate roleUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  roleSelector:
                    description: Selector for a Role in enterprise to populate roleUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccountRefs:
                    description: References to ServiceAccount in oss to populate serviceAccounts.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceAccountSelector:
                    description: Selector for a list of ServiceAccount in oss to populate
                      serviceAccounts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamRefs:
                    description: References to Team in oss to populate teams.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  teamSelector:
                    description: Selector for a list of Team in oss to populate teams.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  userRefs:
                    description: References to User in oss to populate users.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  userSelector:
                    description: Selector for a list of User in oss to populate users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RoleAssignmentStatus defines the observed state of RoleAssignment.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  roleUid:
                    description: |-
                      (String) Grafana RBAC role UID.
                      Grafana RBAC role UID.
                    type: string
                  serviceAccounts:
                    description: |-
                      (Set of String) IDs of service accounts that the role should be assigned to.
                      IDs of service accounts that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teams:
                    description: |-
                      (Set of String) IDs of teams that the role should be assigned to.
                      IDs of teams that the role should be assigned to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  users:
                    description: |-
                      (Set of Number) IDs of users that the role should be assigned to.
                      IDs of users that the role should be assigned to.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roles.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Role
    listKind: RoleList
    plural: roles
    singular: role
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Role is the Schema for the Roles API. Note: This resource is
          available only with Grafana Enterprise 8.+. Official documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/access_control/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RoleSpec defines the desired state of Role
            properties:
              forProvider:
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: RoleStatus defines the observed state of Role.
            properties:
              atProvider:
                properties:
                  autoIncrementVersion:
                    description: |-
                      (Boolean, Deprecated) Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                      Whether the role version should be incremented automatically on updates (and set to 1 on creation).
                    type: boolean
                  description:
                    description: |-
                      (String) Description of the role.
                      Description of the role.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name of the role. Available with Grafana 8.5+.
                      Display name of the role. Available with Grafana 8.5+.
                    type: string
                  global:
                    description: |-
                      (Boolean) Boolean to state whether the role is available across all organizations or not. Defaults to false.
                      Boolean to state whether the role is available across all organizations or not. Defaults to `false`.
                    type: boolean
                  group:
                    description: |-
                      (String) Group of the role. Available with Grafana 8.5+.
                      Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: |-
                      (Boolean) Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to false.
                      Boolean to state whether the role should be visible in the Grafana UI or not. Available with Grafana 8.5+. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the role
                      Name of the role
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) Specific set of actions granted by the role. (see below for nested schema)
                      Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: |-
                            (String) Specific action users granted with the role will be allowed to perform (for example: users:read)
                            Specific action users granted with the role will be allowed to perform (for example: `users:read`)
                          type: string
                        scope:
                          description: |-
                            (String) Scope to restrict the action to a set of resources (for example: users:* or roles:customrole1) Defaults to “.
                            Scope to restrict the action to a set of resources (for example: `users:*` or `roles:customrole1`) Defaults to “.
                          type: string
                      type: object
                    type: array
                  uid:
                    description: |-
                      (String) Unique identifier of the role. Used for assignments.
                      Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: |-
                      (Number, Deprecated) Version of the role. The server manages this automatically.
                      Version of the role. The server manages this automatically.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: scimconfigs.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ScimConfig
    listKind: ScimConfigList
    plural: scimconfigs
    singular: scimconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ScimConfig is the Schema for the ScimConfigs API. Note: Available
          in Grafana Enterprise https://grafana.com/docs/grafana/latest/introduction/grafana-enterprise/
          and Grafana Cloud https://grafana.com/docs/grafana-cloud/. Official documentation
          https://grafana.com/docs/grafana/latest/setup-grafana/configure-access/configure-scim-provisioning/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScimConfigSpec defines the desired state of ScimConfig
            properties:
              forProvider:
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enableGroupSync is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enableGroupSync)
                || (has(self.initProvider) && has(self.initProvider.enableGroupSync))'
            - message: spec.forProvider.enableUserSync is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enableUserSync)
                || (has(self.initProvider) && has(self.initProvider.enableUserSync))'
            - message: spec.forProvider.rejectNonProvisionedUsers is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rejectNonProvisionedUsers)
                || (has(self.initProvider) && has(self.initProvider.rejectNonProvisionedUsers))'
          status:
            description: ScimConfigStatus defines the observed state of ScimConfig.
            properties:
              atProvider:
                properties:
                  enableGroupSync:
                    description: |-
                      (Boolean) Whether group synchronization is enabled.
                      Whether group synchronization is enabled.
                    type: boolean
                  enableUserSync:
                    description: |-
                      (Boolean) Whether user synchronization is enabled.
                      Whether user synchronization is enabled.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  rejectNonProvisionedUsers:
                    description: |-
                      provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                      Whether to block non-provisioned user access to Grafana. Cloud Portal users will always be able to access Grafana, regardless of this setting.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: securevaluev1beta1s.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SecurevalueV1Beta1
    listKind: SecurevalueV1Beta1List
    plural: securevaluev1beta1s
    singular: securevaluev1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecurevalueV1Beta1 is the Schema for the SecurevalueV1Beta1s
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurevalueV1Beta1Spec defines the desired state of SecurevalueV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                      valueSecretRef:
                        description: Plaintext value to store. This value is write-only.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                      valueSecretRef:
                        description: Plaintext value to store. This value is write-only.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
          status:
            description: SecurevalueV1Beta1Status defines the observed state of SecurevalueV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                  metadata:
                    description: The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: The UID of the folder to save the resource in.
                          For example, it's supported for dashboards and folders.
                          To know if it's supported for the specific resource you're
                          using check the documentation.
                        type: string
                      uid:
                        description: The unique identifier of the resource.
                        type: string
                      url:
                        description: The full URL of the resource.
                        type: string
                      uuid:
                        description: The globally unique identifier of a resource,
                          used by the API for tracking.
                        type: string
                      version:
                        description: The version of the resource.
                        type: string
                    type: object
                  options:
                    description: Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: Set to true if you want to overwrite existing
                          resource with newer version, same resource title in folder
                          or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: The spec of the resource.
                    properties:
                      decrypters:
                        description: List of decrypters allowed to read this secure
                          value.
                        items:
                          type: string
                        type: array
                      description:
                        description: Secure value description.
                        type: string
                      ref:
                        description: Reference to an existing secret managed by the
                          keeper.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teamexternalgroups.enterprise.grafana.m.crossplane.io
spec:
  group: enterprise.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: TeamExternalGroup
    listKind: TeamExternalGroupList
    plural: teamexternalgroups
    singular: teamexternalgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamExternalGroup is the Schema for the TeamExternalGroups API.
          Equivalent to the the team_sync attribute of the grafana_team resource.
          Use one or the other to configure a team's external groups syncing config.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamExternalGroupSpec defines the desired state of TeamExternalGroup
            properties:
              forProvider:
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                  teamRef:
                    description: Reference to a Team in oss to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oss to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.groups is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.groups)
                || (has(self.initProvider) && has(self.initProvider.groups))'
          status:
            description: TeamExternalGroupStatus defines the observed state of TeamExternalGroup.
            properties:
              atProvider:
                properties:
                  groups:
                    description: |-
                      (Set of String) The team external groups list
                      The team external groups list
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  teamId:
                    description: |-
                      (String) The Team ID
                      The Team ID
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roles.enterprise.grafana.o.crossplane.io
spec:
  group: enterprise.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Role
    listKind: RoleList
    plural: roles
    singular: role
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Role is an observe-only resource backed by the grafana_role Terraform
          data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: RoleParameters defines the input parameters for the grafana_role
                  data source.
                properties:
                  name:
                    description: Name of the role
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: RoleObservation holds the observed (computed) fields
                  from the grafana_role data source.
                properties:
                  description:
                    description: Description of the role.
                    type: string
                  displayName:
                    description: Display name of the role. Available with Grafana
                      8.5+.
                    type: string
                  global:
                    description: Boolean to state whether the role is available across
                      all organizations or not.
                    type: boolean
                  group:
                    description: Group of the role. Available with Grafana 8.5+.
                    type: string
                  hidden:
                    description: Boolean to state whether the role should be visible
                      in the Grafana UI or not. Available with Grafana 8.5+.
                    type: boolean
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  permissions:
                    description: Specific set of actions granted by the role.
                    items:
                      properties:
                        action:
                          description: 'Specific action users granted with the role
                            will be allowed to perform (for example: `users:read`)'
                          type: string
                        scope:
                          description: 'Scope to restrict the action to a set of resources
                            (for example: `users:*` or `roles:customrole1`)'
                          type: string
                      type: object
                    type: array
                  uid:
                    description: Unique identifier of the role. Used for assignments.
                    type: string
                  version:
                    description: Version of the role. The server manages this automatically.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: collectors.fleetmanagement.grafana.crossplane.io
spec:
  group: fleetmanagement.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Collector
    listKind: CollectorList
    plural: collectors
    singular: collector
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Collector is the Schema for the Collectors API. Manages Grafana
          Fleet Management collectors. Official documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/API
          documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/api-reference/collector-api/Step-by-step
          guide https://grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CollectorSpec defines the desired state of Collector
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: CollectorStatus defines the observed state of Collector.
            properties:
              atProvider:
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  id:
                    description: (String) ID of the collector
                    type: string
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pipelines.fleetmanagement.grafana.crossplane.io
spec:
  group: fleetmanagement.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Pipeline
    listKind: PipelineList
    plural: pipelines
    singular: pipeline
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pipeline is the Schema for the Pipelines API. Manages Grafana
          Fleet Management pipelines. Official documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/API
          documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/api-reference/pipeline-api/Step-by-step
          guide https://grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PipelineSpec defines the desired state of Pipeline
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.contents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.contents)
                || (has(self.initProvider) && has(self.initProvider.contents))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PipelineStatus defines the observed state of Pipeline.
            properties:
              atProvider:
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  id:
                    description: assigned ID of the pipeline
                    type: string
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: collectors.fleetmanagement.grafana.m.crossplane.io
spec:
  group: fleetmanagement.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Collector
    listKind: CollectorList
    plural: collectors
    singular: collector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Collector is the Schema for the Collectors API. Manages Grafana
          Fleet Management collectors. Official documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/API
          documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/api-reference/collector-api/Step-by-step
          guide https://grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CollectorSpec defines the desired state of Collector
            properties:
              forProvider:
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: CollectorStatus defines the observed state of Collector.
            properties:
              atProvider:
                properties:
                  collectorType:
                    description: |-
                      (String) Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the collector. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                      Whether remote configuration for the collector is enabled or not. If the collector is disabled, it will receive empty configurations from the Fleet Management service
                    type: boolean
                  id:
                    description: (String) ID of the collector
                    type: string
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Remote attributes for the collector
                      Remote attributes for the collector
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pipelines.fleetmanagement.grafana.m.crossplane.io
spec:
  group: fleetmanagement.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Pipeline
    listKind: PipelineList
    plural: pipelines
    singular: pipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pipeline is the Schema for the Pipelines API. Manages Grafana
          Fleet Management pipelines. Official documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/API
          documentation https://grafana.com/docs/grafana-cloud/send-data/fleet-management/api-reference/pipeline-api/Step-by-step
          guide https://grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PipelineSpec defines the desired state of Pipeline
            properties:
              forProvider:
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.contents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.contents)
                || (has(self.initProvider) && has(self.initProvider.contents))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PipelineStatus defines the observed state of Pipeline.
            properties:
              atProvider:
                properties:
                  configType:
                    description: |-
                      (String) Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                      Type of the config. Must be one of: ALLOY, OTEL. Defaults to ALLOY if not specified.
                    type: string
                  contents:
                    description: |-
                      (String) Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                      Configuration contents of the pipeline to be used by collectors (can be Alloy config syntax or OTel YAML)
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether the pipeline is enabled for collectors
                      Whether the pipeline is enabled for collectors
                    type: boolean
                  id:
                    description: assigned ID of the pipeline
                    type: string
                  matchers:
                    description: |-
                      (List of String) Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                      Used to match against collectors and assign pipelines to them; follows the syntax of Prometheus Alertmanager matchers
                    items:
                      type: string
                    type: array
                  name:
                    description: |-
                      (String) Name of the pipeline which is the unique identifier for the pipeline
                      Name of the pipeline which is the unique identifier for the pipeline
                    type: string
                  terraformSourceNamespace:
                    description: If omitted, the namespace default is used. If omitted,
                      the namespace `default` is used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: collectors.fleetmanagement.grafana.o.crossplane.io
spec:
  group: fleetmanagement.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Collector
    listKind: CollectorList
    plural: collectors
    singular: collector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Collector is an observe-only resource backed by the grafana_fleet_management_collector
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: CollectorParameters defines the input parameters for
                  the grafana_fleet_management_collector data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: CollectorObservation holds the observed (computed) fields
                  from the grafana_fleet_management_collector data source.
                properties:
                  collectorType:
                    description: Type of the collector (ALLOY or OTEL)
                    type: string
                  enabled:
                    description: Whether remote configuration for the collector is
                      enabled or not. If the collector is disabled, it will receive
                      empty configurations from the Fleet Management service
                    type: boolean
                  localAttributes:
                    additionalProperties:
                      type: string
                    description: Local attributes for the collector
                    type: object
                  remoteAttributes:
                    additionalProperties:
                      type: string
                    description: Remote attributes for the collector
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: collectorsets.fleetmanagement.grafana.o.crossplane.io
spec:
  group: fleetmanagement.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: CollectorSet
    listKind: CollectorSetList
    plural: collectorsets
    singular: collectorset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CollectorSet is an observe-only resource backed by the grafana_fleet_management_collectors
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: CollectorSetParameters defines the input parameters for
                  the grafana_fleet_management_collectors data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: CollectorSetObservation holds the observed (computed)
                  fields from the grafana_fleet_management_collectors data source.
                properties:
                  collectors:
                    description: List of collectors
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: apps.frontendobservability.grafana.crossplane.io
spec:
  group: frontendobservability.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: App
    listKind: AppList
    plural: apps
    singular: app
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: App is the Schema for the Apps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AppSpec defines the desired state of App
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.allowedOrigins is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.allowedOrigins)
                || (has(self.initProvider) && has(self.initProvider.allowedOrigins))'
            - message: spec.forProvider.extraLogAttributes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.extraLogAttributes)
                || (has(self.initProvider) && has(self.initProvider.extraLogAttributes))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.settings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.settings)
                || (has(self.initProvider) && has(self.initProvider.settings))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AppStatus defines the observed state of App.
            properties:
              atProvider:
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  collectorEndpoint:
                    description: |-
                      (String) The collector URL Grafana Cloud Frontend Observability. Use this endpoint to send your Telemetry.
                      The collector URL Grafana Cloud Frontend Observability. Use this endpoint to send your Telemetry.
                    type: string
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: generated from Frontend Observability API.
                    type: string
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: apps.frontendobservability.grafana.m.crossplane.io
spec:
  group: frontendobservability.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: App
    listKind: AppList
    plural: apps
    singular: app
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: App is the Schema for the Apps API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AppSpec defines the desired state of App
            properties:
              forProvider:
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.allowedOrigins is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.allowedOrigins)
                || (has(self.initProvider) && has(self.initProvider.allowedOrigins))'
            - message: spec.forProvider.extraLogAttributes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.extraLogAttributes)
                || (has(self.initProvider) && has(self.initProvider.extraLogAttributes))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.settings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.settings)
                || (has(self.initProvider) && has(self.initProvider.settings))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: AppStatus defines the observed state of App.
            properties:
              atProvider:
                properties:
                  allowedOrigins:
                    description: |-
                      (List of String) A list of allowed origins for CORS.
                      A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  collectorEndpoint:
                    description: |-
                      (String) The collector URL Grafana Cloud Frontend Observability. Use this endpoint to send your Telemetry.
                      The collector URL Grafana Cloud Frontend Observability. Use this endpoint to send your Telemetry.
                    type: string
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) The extra attributes to append in each signal.
                      The extra attributes to append in each signal.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    description: generated from Frontend Observability API.
                    type: string
                  name:
                    description: |-
                      (String) The name of Frontend Observability App.
                      The name of Frontend Observability App.
                    type: string
                  settings:
                    additionalProperties:
                      type: string
                    description: |-
                      value settings of the Frontend Observability app. Available Settings: {combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}
                      The key-value settings of the Frontend Observability app. Available Settings: `{combineLabData=(0|1),geolocation.level=(0|1),geolocation.level=0-4,geolocation.country_denylist=<comma-separated-list-of-country-codes>}`
                    type: object
                    x-kubernetes-map-type: granular
                  stackId:
                    description: |-
                      (Number) The Stack ID of the Grafana Cloud instance.
                      The Stack ID of the Grafana Cloud instance.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: apps.frontendobservability.grafana.o.crossplane.io
spec:
  group: frontendobservability.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: App
    listKind: AppList
    plural: apps
    singular: app
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: App is an observe-only resource backed by the grafana_frontend_o11y_app
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: AppParameters defines the input parameters for the grafana_frontend_o11y_app
                  data source.
                properties:
                  name:
                    description: The name of the Frontend Observability App. Part
                      of the Terraform Resource ID.
                    type: string
                  stackID:
                    description: The Stack ID of the Grafana Cloud instance. Part
                      of the Terraform Resource ID.
                    format: int64
                    type: integer
                required:
                - name
                - stackID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: AppObservation holds the observed (computed) fields from
                  the grafana_frontend_o11y_app data source.
                properties:
                  allowedOrigins:
                    description: A list of allowed origins for CORS.
                    items:
                      type: string
                    type: array
                  collectorEndpoint:
                    description: The collector URL Grafana Cloud Frontend Observability.
                      Use this endpoint to send your Telemetry.
                    type: string
                  extraLogAttributes:
                    additionalProperties:
                      type: string
                    description: The extra attributes to append in each signal.
                    type: object
                  settings:
                    additionalProperties:
                      type: string
                    description: The settings of the Frontend Observability App.
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: providerconfigs.grafana.crossplane.io
spec:
  group: grafana.crossplane.io
  names:
    categories:
    - crossplane
    - provider
    - grafana
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.credentials.secretRef.name
      name: SECRET-NAME
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A ProviderConfig configures a Grafana provider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A ProviderConfigSpec defines the desired state of a ProviderConfig.
            properties:
              cloudApiUrl:
                description: Override the Grafana Cloud API URL from the credentials
                  reference attribute.
                type: string
              cloudProviderUrl:
                description: Override the Cloud Provider API from the credentials
                  reference attribute.
                type: string
              connectionsApiUrl:
                description: Override the Connections API from the credentials reference
                  attribute.
                type: string
              credentials:
                description: Credentials required to authenticate to this provider.
                properties:
                  env:
                    description: |-
                      Env is a reference to an environment variable that contains credentials
                      that must be used to connect to the provider.
                    properties:
                      name:
                        description: Name is the name of an environment variable.
                        type: string
                    required:
                    - name
                    type: object
                  fs:
                    description: |-
                      Fs is a reference to a filesystem location that contains credentials that
                      must be used to connect to the provider.
                    properties:
                      path:
                        description: Path is a filesystem path.
                        type: string
                    required:
                    - path
                    type: object
                  secretRef:
                    description: |-
                      A SecretRef is a reference to a secret key that contains the credentials
                      that must be used to connect to the provider.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  source:
                    description: Source of the provider credentials.
                    enum:
                    - None
                    - Secret
                    - InjectedIdentity
                    - Environment
                    - Filesystem
                    type: string
                required:
                - source
                type: object
              fleetManagementUrl:
                description: Override the FleetManagement API from the credentials
                  reference attribute.
                type: string
              oncallUrl:
                description: Override the OnCall API URL from the credentials reference
                  attribute.
                type: string
              orgId:
                description: Override the Grafana org ID from the credentials reference
                  attribute.
                type: integer
              smUrl:
                description: Override the Synthetic Monitoring API URL from the credentials
                  reference attribute.
                type: string
              stackId:
                description: Override the Grafana stack ID from the credentials reference
                  attribute.
                type: integer
              stackSecretRef:
                description: |-
                  StackSecretRef is an optional reference to a Secret containing Stack
                  connection details (e.g., from a grafana_cloud_stack resource's
                  writeConnectionSecretToRef). All keys in the Secret are merged into
                  the credential map. Key remapping is applied (e.g., oncall_api_url
                  becomes oncall_url, id becomes stack_id). Values from this secret
                  override the primary credentials secret and are in turn overridden by
                  explicit spec fields (url, oncallUrl, etc.).
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              url:
                description: Override the Grafana URL from the credentials reference
                  attribute.
                type: string
            required:
            - credentials
            type: object
          status:
            description: A ProviderConfigStatus reflects the observed state of a ProviderConfig.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: providerconfigusages.grafana.crossplane.io
spec:
  group: grafana.crossplane.io
  names:
    categories:
    - crossplane
    - provider
    - grafana
    kind: ProviderConfigUsage
    listKind: ProviderConfigUsageList
    plural: providerconfigusages
    singular: providerconfigusage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .providerConfigRef.name
      name: CONFIG-NAME
      type: string
    - jsonPath: .resourceRef.kind
      name: RESOURCE-KIND
      type: string
    - jsonPath: .resourceRef.name
      name: RESOURCE-NAME
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A ProviderConfigUsage indicates that a resource is using a ProviderConfig.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          providerConfigRef:
            description: ProviderConfigReference to the provider config being used.
            properties:
              name:
                description: Name of the referenced object.
                type: string
              policy:
                description: Policies for referencing.
                properties:
                  resolution:
                    default: Required
                    description: |-
                      Resolution specifies whether resolution of this reference is required.
                      The default is 'Required', which means the reconcile will fail if the
                      reference cannot be resolved. 'Optional' means this reference will be
                      a no-op if it cannot be resolved.
                    enum:
                    - Required
                    - Optional
                    type: string
                  resolve:
                    description: |-
                      Resolve specifies when this reference should be resolved. The default
                      is 'IfNotPresent', which will attempt to resolve the reference only when
                      the corresponding field is not present. Use 'Always' to resolve the
                      reference on every reconcile.
                    enum:
                    - Always
                    - IfNotPresent
                    type: string
                type: object
            required:
            - name
            type: object
          resourceRef:
            description: ResourceReference to the managed resource using the provider
              config.
            properties:
              apiVersion:
                description: APIVersion of the referenced object.
                type: string
              kind:
                description: Kind of the referenced object.
                type: string
              name:
                description: Name of the referenced object.
                type: string
              uid:
                description: UID of the referenced object.
                type: string
            required:
            - apiVersion
            - kind
            - name
            type: object
        required:
        - providerConfigRef
        - resourceRef
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterproviderconfigs.grafana.m.crossplane.io
spec:
  group: grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - provider
    - grafana
    kind: ClusterProviderConfig
    listKind: ClusterProviderConfigList
    plural: clusterproviderconfigs
    singular: clusterproviderconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.credentials.secretRef.name
      name: SECRET-NAME
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ProviderConfigSpec defines desired state for namespaced ProviderConfig.
              Same fields as legacy, but secret selectors resolve in the namespace of the MR when omitted.
              ESS alpha fields intentionally removed.
            properties:
              cloudApiUrl:
                type: string
              cloudProviderUrl:
                type: string
              connectionsApiUrl:
                type: string
              credentials:
                properties:
                  env:
                    description: |-
                      Env is a reference to an environment variable that contains credentials
                      that must be used to connect to the provider.
                    properties:
                      name:
                        description: Name is the name of an environment variable.
                        type: string
                    required:
                    - name
                    type: object
                  fs:
                    description: |-
                      Fs is a reference to a filesystem location that contains credentials that
                      must be used to connect to the provider.
                    properties:
                      path:
                        description: Path is a filesystem path.
                        type: string
                    required:
                    - path
                    type: object
                  secretRef:
                    description: |-
                      A SecretRef is a reference to a secret key that contains the credentials
                      that must be used to connect to the provider.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  source:
                    description: |-
                      A CredentialsSource is a source from which provider credentials may be
                      acquired.
                    enum:
                    - None
                    - Secret
                    - InjectedIdentity
                    - Environment
                    - Filesystem
                    type: string
                required:
                - source
                type: object
              fleetManagementUrl:
                type: string
              oncallUrl:
                type: string
              orgId:
                type: integer
              smUrl:
                type: string
              stackId:
                type: integer
              stackSecretRef:
                description: |-
                  StackSecretRef is an optional reference to a Secret containing Stack
                  connection details (e.g., from a grafana_cloud_stack resource's
                  writeConnectionSecretToRef). All keys in the Secret are merged into
                  the credential map. Key remapping is applied (e.g., oncall_api_url
                  becomes oncall_url, id becomes stack_id). Values from this secret
                  override the primary credentials secret and are in turn overridden by
                  explicit spec fields (url, oncallUrl, etc.).
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              url:
                type: string
            required:
            - credentials
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: providerconfigs.grafana.m.crossplane.io
spec:
  group: grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - provider
    - grafana
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.credentials.secretRef.name
      name: SECRET-NAME
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ProviderConfigSpec defines desired state for namespaced ProviderConfig.
              Same fields as legacy, but secret selectors resolve in the namespace of the MR when omitted.
              ESS alpha fields intentionally removed.
            properties:
              cloudApiUrl:
                type: string
              cloudProviderUrl:
                type: string
              connectionsApiUrl:
                type: string
              credentials:
                properties:
                  env:
                    description: |-
                      Env is a reference to an environment variable that contains credentials
                      that must be used to connect to the provider.
                    properties:
                      name:
                        description: Name is the name of an environment variable.
                        type: string
                    required:
                    - name
                    type: object
                  fs:
                    description: |-
                      Fs is a reference to a filesystem location that contains credentials that
                      must be used to connect to the provider.
                    properties:
                      path:
                        description: Path is a filesystem path.
                        type: string
                    required:
                    - path
                    type: object
                  secretRef:
                    description: |-
                      A SecretRef is a reference to a secret key that contains the credentials
                      that must be used to connect to the provider.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  source:
                    description: |-
                      A CredentialsSource is a source from which provider credentials may be
                      acquired.
                    enum:
                    - None
                    - Secret
                    - InjectedIdentity
                    - Environment
                    - Filesystem
                    type: string
                required:
                - source
                type: object
              fleetManagementUrl:
                type: string
              oncallUrl:
                type: string
              orgId:
                type: integer
              smUrl:
                type: string
              stackId:
                type: integer
              stackSecretRef:
                description: |-
                  StackSecretRef is an optional reference to a Secret containing Stack
                  connection details (e.g., from a grafana_cloud_stack resource's
                  writeConnectionSecretToRef). All keys in the Secret are merged into
                  the credential map. Key remapping is applied (e.g., oncall_api_url
                  becomes oncall_url, id becomes stack_id). Values from this secret
                  override the primary credentials secret and are in turn overridden by
                  explicit spec fields (url, oncallUrl, etc.).
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              url:
                type: string
            required:
            - credentials
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: providerconfigusages.grafana.m.crossplane.io
spec:
  group: grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - provider
    - grafana
    kind: ProviderConfigUsage
    listKind: ProviderConfigUsageList
    plural: providerconfigusages
    singular: providerconfigusage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .providerConfigRef.name
      name: CONFIG-NAME
      type: string
    - jsonPath: .resourceRef.kind
      name: RESOURCE-KIND
      type: string
    - jsonPath: .resourceRef.name
      name: RESOURCE-NAME
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          providerConfigRef:
            description: ProviderConfigReference to the provider config being used.
            properties:
              kind:
                description: Kind of the referenced object.
                type: string
              name:
                description: Name of the referenced object.
                type: string
            required:
            - kind
            - name
            type: object
          resourceRef:
            description: ResourceReference to the managed resource using the provider
              config.
            properties:
              apiVersion:
                description: APIVersion of the referenced object.
                type: string
              kind:
                description: Kind of the referenced object.
                type: string
              name:
                description: Name of the referenced object.
                type: string
              uid:
                description: UID of the referenced object.
                type: string
            required:
            - apiVersion
            - kind
            - name
            type: object
        required:
        - providerConfigRef
        - resourceRef
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: installations.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Installation
    listKind: InstallationList
    plural: installations
    singular: installation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Installation is the Schema for the Installations API. Sets up
          the k6 App on a Grafana Cloud instance and generates a token. Once a Grafana
          Cloud stack is created, a user can either use this resource or go into the
          UI to install k6. This resource cannot be imported but it can be used on
          an existing k6 App installation without issues. Note that this resource
          must be used on a provider configured with Grafana Cloud credentials. Official
          documentation https://grafana.com/docs/grafana-cloud/testing/k6/ Required
          access policy scopes: stacks:readstacks:writesubscriptions:readorgs:readstack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstallationSpec defines the desired state of Installation
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/account-management/authentication-and-permissions/access-policies/).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  grafanaSaTokenSecretRef:
                    description: |-
                      (String, Sensitive) The service account token.
                      The [service account](https://grafana.com/docs/grafana/latest/administration/service-accounts/) token.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/account-management/authentication-and-permissions/access-policies/).
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  grafanaSaTokenSecretRef:
                    description: |-
                      (String, Sensitive) The service account token.
                      The [service account](https://grafana.com/docs/grafana/latest/administration/service-accounts/) token.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                required:
                - cloudAccessPolicyTokenSecretRef
                - grafanaSaTokenSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudAccessPolicyTokenSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudAccessPolicyTokenSecretRef)'
            - message: spec.forProvider.grafanaSaTokenSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.grafanaSaTokenSecretRef)'
            - message: spec.forProvider.grafanaUser is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.grafanaUser)
                || (has(self.initProvider) && has(self.initProvider.grafanaUser))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: InstallationStatus defines the observed state of Installation.
            properties:
              atProvider:
                properties:
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  k6Organization:
                    description: |-
                      (String) The identifier of the k6 organization.
                      The identifier of the k6 organization.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loadtests.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LoadTest
    listKind: LoadTestList
    plural: loadtests
    singular: loadtest
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadTest is the Schema for the LoadTests API. Manages a k6 load
          test.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadTestSpec defines the desired state of LoadTest
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.script is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.script)
                || (has(self.initProvider) && has(self.initProvider.script))'
          status:
            description: LoadTestStatus defines the observed state of LoadTest.
            properties:
              atProvider:
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  created:
                    description: |-
                      (String) The date when the load test was created.
                      The date when the load test was created.
                    type: string
                  id:
                    description: (String) Numeric identifier of the load test.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                  updated:
                    description: |-
                      (String) The date when the load test was last updated.
                      The date when the load test was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectallowedloadzones.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProjectAllowedLoadZones
    listKind: ProjectAllowedLoadZonesList
    plural: projectallowedloadzones
    singular: projectallowedloadzones
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectAllowedLoadZones is the Schema for the ProjectAllowedLoadZoness
          API. Manages allowed private load zones for a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectAllowedLoadZonesSpec defines the desired state of
              ProjectAllowedLoadZones
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.allowedLoadZones is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.allowedLoadZones)
                || (has(self.initProvider) && has(self.initProvider.allowedLoadZones))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: ProjectAllowedLoadZonesStatus defines the observed state
              of ProjectAllowedLoadZones.
            properties:
              atProvider:
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  id:
                    description: (String) The identifier of the project. This is set
                      to the same as the project_id.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectlimits.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProjectLimits
    listKind: ProjectLimitsList
    plural: projectlimits
    singular: projectlimits
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectLimits is the Schema for the ProjectLimitss API. Manages
          limits for a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectLimitsSpec defines the desired state of ProjectLimits
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ProjectLimitsStatus defines the observed state of ProjectLimits.
            properties:
              atProvider:
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  id:
                    description: (String) The identifier of the project limits. This
                      is the same as the project_id.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projects.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the Projects API. Manages a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of Project
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ProjectStatus defines the observed state of Project.
            properties:
              atProvider:
                properties:
                  created:
                    description: |-
                      (String) The date when the project was created.
                      The date when the project was created.
                    type: string
                  grafanaFolderUid:
                    description: |-
                      (String) The Grafana folder uid.
                      The Grafana folder uid.
                    type: string
                  id:
                    description: (String) Numeric identifier of the project.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Use this project as default for running tests when no explicit project identifier is provided.
                      Use this project as default for running tests when no explicit project identifier is provided.
                    type: boolean
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                  updated:
                    description: |-
                      (String) The date when the project was last updated.
                      The date when the project was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.k6.grafana.crossplane.io
spec:
  group: k6.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the Schedules API. Manages a k6 schedule
          for automated test execution.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.loadTestId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loadTestId)
                || (has(self.initProvider) && has(self.initProvider.loadTestId))'
            - message: spec.forProvider.starts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.starts)
                || (has(self.initProvider) && has(self.initProvider.starts))'
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              atProvider:
                properties:
                  createdBy:
                    description: |-
                      (String) The email of the user who created the schedule.
                      The email of the user who created the schedule.
                    type: string
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  deactivated:
                    description: |-
                      (Boolean) Whether the schedule is deactivated.
                      Whether the schedule is deactivated.
                    type: boolean
                  id:
                    description: (String) Numeric identifier of the schedule.
                    type: string
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  nextRun:
                    description: |-
                      (String) The next scheduled execution time.
                      The next scheduled execution time.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: installations.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Installation
    listKind: InstallationList
    plural: installations
    singular: installation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Installation is the Schema for the Installations API. Sets up
          the k6 App on a Grafana Cloud instance and generates a token. Once a Grafana
          Cloud stack is created, a user can either use this resource or go into the
          UI to install k6. This resource cannot be imported but it can be used on
          an existing k6 App installation without issues. Note that this resource
          must be used on a provider configured with Grafana Cloud credentials. Official
          documentation https://grafana.com/docs/grafana-cloud/testing/k6/ Required
          access policy scopes: stacks:readstacks:writesubscriptions:readorgs:readstack-service-accounts:write'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstallationSpec defines the desired state of Installation
            properties:
              forProvider:
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/account-management/authentication-and-permissions/access-policies/).
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  grafanaSaTokenSecretRef:
                    description: |-
                      (String, Sensitive) The service account token.
                      The [service account](https://grafana.com/docs/grafana/latest/administration/service-accounts/) token.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudAccessPolicyTokenSecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/account-management/authentication-and-permissions/access-policies/).
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  grafanaSaTokenSecretRef:
                    description: |-
                      (String, Sensitive) The service account token.
                      The [service account](https://grafana.com/docs/grafana/latest/administration/service-accounts/) token.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                required:
                - cloudAccessPolicyTokenSecretRef
                - grafanaSaTokenSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudAccessPolicyTokenSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudAccessPolicyTokenSecretRef)'
            - message: spec.forProvider.grafanaSaTokenSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.grafanaSaTokenSecretRef)'
            - message: spec.forProvider.grafanaUser is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.grafanaUser)
                || (has(self.initProvider) && has(self.initProvider.grafanaUser))'
            - message: spec.forProvider.stackId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.stackId)
                || (has(self.initProvider) && has(self.initProvider.stackId))'
          status:
            description: InstallationStatus defines the observed state of Installation.
            properties:
              atProvider:
                properties:
                  grafanaUser:
                    description: |-
                      (String) The user to use for the installation.
                      The user to use for the installation.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  k6ApiUrl:
                    description: |-
                      (String) The Grafana Cloud k6 API url.
                      The Grafana Cloud k6 API url.
                    type: string
                  k6Organization:
                    description: |-
                      (String) The identifier of the k6 organization.
                      The identifier of the k6 organization.
                    type: string
                  stackId:
                    description: |-
                      (String) The identifier of the stack to install k6 on.
                      The identifier of the stack to install k6 on.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loadtests.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LoadTest
    listKind: LoadTestList
    plural: loadtests
    singular: loadtest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadTest is the Schema for the LoadTests API. Manages a k6 load
          test.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadTestSpec defines the desired state of LoadTest
            properties:
              forProvider:
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.script is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.script)
                || (has(self.initProvider) && has(self.initProvider.script))'
          status:
            description: LoadTestStatus defines the observed state of LoadTest.
            properties:
              atProvider:
                properties:
                  baselineTestRunId:
                    description: |-
                      (String) Identifier of a baseline test run used for results comparison.
                      Identifier of a baseline test run used for results comparison.
                    type: string
                  created:
                    description: |-
                      (String) The date when the load test was created.
                      The date when the load test was created.
                    type: string
                  id:
                    description: (String) Numeric identifier of the load test.
                    type: string
                  name:
                    description: |-
                      friendly identifier of the load test.
                      Human-friendly identifier of the load test.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project this load test belongs to.
                      The identifier of the project this load test belongs to.
                    type: string
                  script:
                    description: |-
                      (String) The k6 test script content. Can be provided inline or via the file() function.
                      The k6 test script content. Can be provided inline or via the `file()` function.
                    type: string
                  updated:
                    description: |-
                      (String) The date when the load test was last updated.
                      The date when the load test was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectallowedloadzones.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProjectAllowedLoadZones
    listKind: ProjectAllowedLoadZonesList
    plural: projectallowedloadzones
    singular: projectallowedloadzones
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectAllowedLoadZones is the Schema for the ProjectAllowedLoadZoness
          API. Manages allowed private load zones for a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectAllowedLoadZonesSpec defines the desired state of
              ProjectAllowedLoadZones
            properties:
              forProvider:
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.allowedLoadZones is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.allowedLoadZones)
                || (has(self.initProvider) && has(self.initProvider.allowedLoadZones))'
            - message: spec.forProvider.projectId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.projectId)
                || (has(self.initProvider) && has(self.initProvider.projectId))'
          status:
            description: ProjectAllowedLoadZonesStatus defines the observed state
              of ProjectAllowedLoadZones.
            properties:
              atProvider:
                properties:
                  allowedLoadZones:
                    description: |-
                      (List of String) List of allowed private k6 load zone IDs for this project.
                      List of allowed private k6 load zone IDs for this project.
                    items:
                      type: string
                    type: array
                  id:
                    description: (String) The identifier of the project. This is set
                      to the same as the project_id.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage private allowed load zones for.
                      The identifier of the project to manage private allowed load zones for.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectlimits.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ProjectLimits
    listKind: ProjectLimitsList
    plural: projectlimits
    singular: projectlimits
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectLimits is the Schema for the ProjectLimitss API. Manages
          limits for a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectLimitsSpec defines the desired state of ProjectLimits
            properties:
              forProvider:
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  projectRef:
                    description: Reference to a Project in k6 to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectSelector:
                    description: Selector for a Project in k6 to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ProjectLimitsStatus defines the observed state of ProjectLimits.
            properties:
              atProvider:
                properties:
                  durationMaxPerTest:
                    description: |-
                      (Number) Maximum duration of a test in seconds.
                      Maximum duration of a test in seconds.
                    type: number
                  id:
                    description: (String) The identifier of the project limits. This
                      is the same as the project_id.
                    type: string
                  projectId:
                    description: |-
                      (String) The identifier of the project to manage limits for.
                      The identifier of the project to manage limits for.
                    type: string
                  vuBrowserMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent browser virtual users (VUs) used in one test.
                      Maximum number of concurrent browser virtual users (VUs) used in one test.
                    type: number
                  vuMaxPerTest:
                    description: |-
                      (Number) Maximum number of concurrent virtual users (VUs) used in one test.
                      Maximum number of concurrent virtual users (VUs) used in one test.
                    type: number
                  vuhMaxPerMonth:
                    description: |-
                      (Number) Maximum amount of virtual user hours (VU/h) used per one calendar month.
                      Maximum amount of virtual user hours (VU/h) used per one calendar month.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projects.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the Projects API. Manages a k6 project.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of Project
            properties:
              forProvider:
                properties:
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ProjectStatus defines the observed state of Project.
            properties:
              atProvider:
                properties:
                  created:
                    description: |-
                      (String) The date when the project was created.
                      The date when the project was created.
                    type: string
                  grafanaFolderUid:
                    description: |-
                      (String) The Grafana folder uid.
                      The Grafana folder uid.
                    type: string
                  id:
                    description: (String) Numeric identifier of the project.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Use this project as default for running tests when no explicit project identifier is provided.
                      Use this project as default for running tests when no explicit project identifier is provided.
                    type: boolean
                  name:
                    description: |-
                      friendly identifier of the project.
                      Human-friendly identifier of the project.
                    type: string
                  updated:
                    description: |-
                      (String) The date when the project was last updated.
                      The date when the project was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.k6.grafana.m.crossplane.io
spec:
  group: k6.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the Schedules API. Manages a k6 schedule
          for automated test execution.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule
            properties:
              forProvider:
                properties:
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.loadTestId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loadTestId)
                || (has(self.initProvider) && has(self.initProvider.loadTestId))'
            - message: spec.forProvider.starts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.starts)
                || (has(self.initProvider) && has(self.initProvider.starts))'
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              atProvider:
                properties:
                  createdBy:
                    description: |-
                      (String) The email of the user who created the schedule.
                      The email of the user who created the schedule.
                    type: string
                  cron:
                    description: |-
                      (Block, Optional) The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The cron schedule to trigger the test periodically. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      schedule:
                        description: |-
                          (String) A cron expression with exactly 5 entries, or an alias. The allowed aliases are: @yearly, @annually, @monthly, @weekly, @daily, @hourly.
                          A cron expression with exactly 5 entries, or an alias. The allowed aliases are: `@yearly`, `@annually`, `@monthly`, `@weekly`, `@daily`, `@hourly`.
                        type: string
                      timezone:
                        description: |-
                          (String) The timezone of the cron expression. For example, UTC or Europe/London.
                          The timezone of the cron expression. For example, `UTC` or `Europe/London`.
                        type: string
                    type: object
                  deactivated:
                    description: |-
                      (Boolean) Whether the schedule is deactivated.
                      Whether the schedule is deactivated.
                    type: boolean
                  id:
                    description: (String) Numeric identifier of the schedule.
                    type: string
                  loadTestId:
                    description: |-
                      (String) The identifier of the load test to schedule.
                      The identifier of the load test to schedule.
                    type: string
                  nextRun:
                    description: |-
                      (String) The next scheduled execution time.
                      The next scheduled execution time.
                    type: string
                  recurrenceRule:
                    description: |-
                      (Block, Optional) The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of recurrence_rule and cron can be set. (see below for nested schema)
                      The schedule recurrence settings. If not specified, the test will run only once on the 'starts' date. Only one of `recurrence_rule` and `cron` can be set.
                    properties:
                      byday:
                        description: |-
                          (List of String) The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                          The weekdays when the 'WEEKLY' recurrence will be applied (e.g., ['MO', 'WE', 'FR']). Cannot be set for other frequencies.
                        items:
                          type: string
                        type: array
                      count:
                        description: |-
                          (Number) How many times the recurrence will repeat.
                          How many times the recurrence will repeat.
                        type: number
                      frequency:
                        description: |-
                          (String) The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                          The frequency of the schedule (HOURLY, DAILY, WEEKLY, MONTHLY, YEARLY).
                        type: string
                      interval:
                        description: |-
                          (Number) The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                          The interval between each frequency iteration (e.g., 2 = every 2 hours for HOURLY). Defaults to 1.
                        type: number
                      until:
                        description: |-
                          (String) The end time for the recurrence (RFC3339 format).
                          The end time for the recurrence (RFC3339 format).
                        type: string
                    type: object
                  starts:
                    description: |-
                      (String) The start time for the schedule (RFC3339 format).
                      The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loadtests.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: LoadTest
    listKind: LoadTestList
    plural: loadtests
    singular: loadtest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadTest is an observe-only resource backed by the grafana_k6_load_test
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: LoadTestParameters defines the input parameters for the
                  grafana_k6_load_test data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: LoadTestObservation holds the observed (computed) fields
                  from the grafana_k6_load_test data source.
                properties:
                  baselineTestRunID:
                    description: Identifier of a baseline test run used for results
                      comparison.
                    type: string
                  created:
                    description: The date when the load test was created.
                    type: string
                  name:
                    description: Human-friendly identifier of the load test.
                    type: string
                  projectID:
                    description: The identifier of the project this load test belongs
                      to.
                    type: string
                  script:
                    description: The k6 test script content.
                    type: string
                  updated:
                    description: The date when the load test was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loadtestsets.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: LoadTestSet
    listKind: LoadTestSetList
    plural: loadtestsets
    singular: loadtestset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadTestSet is an observe-only resource backed by the grafana_k6_load_tests
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: LoadTestSetParameters defines the input parameters for
                  the grafana_k6_load_tests data source.
                properties:
                  name:
                    description: Human-friendly identifier of the load test.
                    type: string
                  projectID:
                    description: The identifier of the project the load tests belong
                      to.
                    type: string
                required:
                - projectID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: LoadTestSetObservation holds the observed (computed)
                  fields from the grafana_k6_load_tests data source.
                properties:
                  loadTests:
                    items:
                      type: string
                    type: array
                  name:
                    description: Human-friendly identifier of the load test.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectallowedloadzones.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ProjectAllowedLoadZones
    listKind: ProjectAllowedLoadZonesList
    plural: projectallowedloadzones
    singular: projectallowedloadzones
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectAllowedLoadZones is an observe-only resource backed by
          the grafana_k6_project_allowed_load_zones Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProjectAllowedLoadZonesParameters defines the input parameters
                  for the grafana_k6_project_allowed_load_zones data source.
                properties:
                  projectID:
                    description: The identifier of the project to retrieve private
                      allowed load zones for.
                    type: string
                required:
                - projectID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProjectAllowedLoadZonesObservation holds the observed
                  (computed) fields from the grafana_k6_project_allowed_load_zones
                  data source.
                properties:
                  allowedLoadZones:
                    description: List of allowed private k6 load zone IDs for this
                      project.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectlimits.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ProjectLimits
    listKind: ProjectLimitsList
    plural: projectlimits
    singular: projectlimits
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectLimits is an observe-only resource backed by the grafana_k6_project_limits
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProjectLimitsParameters defines the input parameters
                  for the grafana_k6_project_limits data source.
                properties:
                  projectID:
                    description: The identifier of the project to get limits for.
                    type: string
                required:
                - projectID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProjectLimitsObservation holds the observed (computed)
                  fields from the grafana_k6_project_limits data source.
                properties:
                  durationMaxPerTest:
                    description: Maximum duration of a test in seconds.
                    type: string
                  vuBrowserMaxPerTest:
                    description: Maximum number of concurrent browser virtual users
                      (VUs) used in one test.
                    type: string
                  vuMaxPerTest:
                    description: Maximum number of concurrent virtual users (VUs)
                      used in one test.
                    type: string
                  vuhMaxPerMonth:
                    description: Maximum amount of virtual user hours (VU/h) used
                      per one calendar month.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projects.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is an observe-only resource backed by the grafana_k6_project
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProjectParameters defines the input parameters for the
                  grafana_k6_project data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProjectObservation holds the observed (computed) fields
                  from the grafana_k6_project data source.
                properties:
                  created:
                    description: The date when the project was created.
                    type: string
                  grafanaFolderUID:
                    description: The Grafana folder uid.
                    type: string
                  isDefault:
                    description: Whether this project is the default for running tests
                      when no explicit project identifier is provided.
                    type: boolean
                  name:
                    description: Human-friendly identifier of the project.
                    type: string
                  updated:
                    description: The date when the project was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: projectsets.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ProjectSet
    listKind: ProjectSetList
    plural: projectsets
    singular: projectset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProjectSet is an observe-only resource backed by the grafana_k6_projects
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProjectSetParameters defines the input parameters for
                  the grafana_k6_projects data source.
                properties:
                  name:
                    description: Human-friendly identifier of the project.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProjectSetObservation holds the observed (computed) fields
                  from the grafana_k6_projects data source.
                properties:
                  name:
                    description: Human-friendly identifier of the project.
                    type: string
                  projects:
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is an observe-only resource backed by the grafana_k6_schedule
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ScheduleParameters defines the input parameters for the
                  grafana_k6_schedule data source.
                properties:
                  loadTestID:
                    description: The identifier of the load test to retrieve the schedule
                      for.
                    type: string
                required:
                - loadTestID
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ScheduleObservation holds the observed (computed) fields
                  from the grafana_k6_schedule data source.
                properties:
                  createdBy:
                    description: The email of the user who created the schedule.
                    type: string
                  deactivated:
                    description: Whether the schedule is deactivated.
                    type: boolean
                  nextRun:
                    description: The next scheduled execution time.
                    type: string
                  starts:
                    description: The start time for the schedule (RFC3339 format).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedulesets.k6.grafana.o.crossplane.io
spec:
  group: k6.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ScheduleSet
    listKind: ScheduleSetList
    plural: schedulesets
    singular: scheduleset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScheduleSet is an observe-only resource backed by the grafana_k6_schedules
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ScheduleSetParameters defines the input parameters for
                  the grafana_k6_schedules data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ScheduleSetObservation holds the observed (computed)
                  fields from the grafana_k6_schedules data source.
                properties:
                  schedules:
                    description: List of k6 schedules.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alerts.ml.grafana.crossplane.io
spec:
  group: ml.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the Alerts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertSpec defines the desired state of Alert
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.title is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.title)
                || (has(self.initProvider) && has(self.initProvider.title))'
          status:
            description: AlertStatus defines the observed state of Alert.
            properties:
              atProvider:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  id:
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: holidays.ml.grafana.crossplane.io
spec:
  group: ml.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Holiday
    listKind: HolidayList
    plural: holidays
    singular: holiday
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Holiday is the Schema for the Holidays API. A holiday describes
          time periods where a time series is expected to behave differently to normal.
          To use a holiday in a job, use its id in the holidays attribute of a grafana_machine_learning_job:'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HolidaySpec defines the desired state of Holiday
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: HolidayStatus defines the observed state of Holiday.
            properties:
              atProvider:
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  id:
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: jobs.ml.grafana.crossplane.io
spec:
  group: ml.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Job
    listKind: JobList
    plural: jobs
    singular: job
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Job is the Schema for the Jobs API. A job defines the queries
          and model parameters for a machine learning task. See the Grafana Cloud
          docs https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/dynamic-alerting/forecasting/config/
          for more information on available hyperparameters for use in the hyper_params
          field.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JobSpec defines the desired state of Job
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceType)
                || (has(self.initProvider) && has(self.initProvider.datasourceType))'
            - message: spec.forProvider.metric is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metric)
                || (has(self.initProvider) && has(self.initProvider.metric))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.queryParams is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryParams)
                || (has(self.initProvider) && has(self.initProvider.queryParams))'
          status:
            description: JobStatus defines the observed state of Job.
            properties:
              atProvider:
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    type: string
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: outlierdetectors.ml.grafana.crossplane.io
spec:
  group: ml.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OutlierDetector
    listKind: OutlierDetectorList
    plural: outlierdetectors
    singular: outlierdetector
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OutlierDetector is the Schema for the OutlierDetectors API. An
          outlier detector monitors the results of a query and reports when its values
          are outside normal bands. The normal band is configured by choice of algorithm,
          its sensitivity and other configuration. Visit https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
          for more details.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OutlierDetectorSpec defines the desired state of OutlierDetector
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.algorithm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.algorithm)
                || (has(self.initProvider) && has(self.initProvider.algorithm))'
            - message: spec.forProvider.datasourceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceType)
                || (has(self.initProvider) && has(self.initProvider.datasourceType))'
            - message: spec.forProvider.metric is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metric)
                || (has(self.initProvider) && has(self.initProvider.metric))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.queryParams is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryParams)
                || (has(self.initProvider) && has(self.initProvider.queryParams))'
          status:
            description: OutlierDetectorStatus defines the observed state of OutlierDetector.
            properties:
              atProvider:
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  id:
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: alerts.ml.grafana.m.crossplane.io
spec:
  group: ml.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the Alerts API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertSpec defines the desired state of Alert
            properties:
              forProvider:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.title is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.title)
                || (has(self.initProvider) && has(self.initProvider.title))'
          status:
            description: AlertStatus defines the observed state of Alert.
            properties:
              atProvider:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  anomalyCondition:
                    description: The condition for when to consider a point as anomalous.
                    type: string
                  for:
                    description: How long values must be anomalous before firing an
                      alert.
                    type: string
                  id:
                    type: string
                  jobId:
                    description: The forecast this alert belongs to.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the alert generated in Grafana.
                    type: object
                    x-kubernetes-map-type: granular
                  noDataState:
                    description: How the alert should be processed when no data is
                      returned by the underlying series
                    type: string
                  outlierId:
                    description: The forecast this alert belongs to.
                    type: string
                  threshold:
                    description: The threshold of points over the window that need
                      to be anomalous to alert.
                    type: string
                  title:
                    description: The title of the alert.
                    type: string
                  window:
                    description: How much time to average values over
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: holidays.ml.grafana.m.crossplane.io
spec:
  group: ml.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Holiday
    listKind: HolidayList
    plural: holidays
    singular: holiday
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Holiday is the Schema for the Holidays API. A holiday describes
          time periods where a time series is expected to behave differently to normal.
          To use a holiday in a job, use its id in the holidays attribute of a grafana_machine_learning_job:'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HolidaySpec defines the desired state of Holiday
            properties:
              forProvider:
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: HolidayStatus defines the observed state of Holiday.
            properties:
              atProvider:
                properties:
                  customPeriods:
                    description: A list of custom periods for the holiday.
                    items:
                      properties:
                        endTime:
                          type: string
                        name:
                          description: The name of the custom period.
                          type: string
                        startTime:
                          type: string
                      type: object
                    type: array
                  description:
                    description: A description of the holiday.
                    type: string
                  icalTimezone:
                    description: The timezone to use for events in the iCal file pointed
                      to by ical_url.
                    type: string
                  icalUrl:
                    description: A URL to an iCal file containing all occurrences
                      of the holiday.
                    type: string
                  id:
                    type: string
                  name:
                    description: The name of the holiday.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: jobs.ml.grafana.m.crossplane.io
spec:
  group: ml.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Job
    listKind: JobList
    plural: jobs
    singular: job
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Job is the Schema for the Jobs API. A job defines the queries
          and model parameters for a machine learning task. See the Grafana Cloud
          docs https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/dynamic-alerting/forecasting/config/
          for more information on available hyperparameters for use in the hyper_params
          field.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JobSpec defines the desired state of Job
            properties:
              forProvider:
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.datasourceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceType)
                || (has(self.initProvider) && has(self.initProvider.datasourceType))'
            - message: spec.forProvider.metric is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metric)
                || (has(self.initProvider) && has(self.initProvider.metric))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.queryParams is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryParams)
                || (has(self.initProvider) && has(self.initProvider.queryParams))'
          status:
            description: JobStatus defines the observed state of Job.
            properties:
              atProvider:
                properties:
                  customLabels:
                    additionalProperties:
                      type: string
                    description: An object representing the custom labels added on
                      the forecast.
                    type: object
                    x-kubernetes-map-type: granular
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the job.
                    type: string
                  holidays:
                    description: A list of holiday IDs or names to take into account
                      when training the model.
                    items:
                      type: string
                    type: array
                  hyperParams:
                    additionalProperties:
                      type: string
                    description: The hyperparameters used to fine tune the algorithm.
                      See https://grafana.com/docs/grafana-cloud/alerting-and-irm/machine-learning/forecasts/models/
                      for the full list of available hyperparameters. Defaults to
                      `map[]`.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    type: string
                  interval:
                    description: The data interval in seconds to train the data on.
                      Defaults to `300`.
                    type: number
                  metric:
                    description: The metric used to query the job results.
                    type: string
                  name:
                    description: The name of the job.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                  trainingWindow:
                    description: The data interval in seconds to train the data on.
                      Defaults to `7776000`.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: outlierdetectors.ml.grafana.m.crossplane.io
spec:
  group: ml.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OutlierDetector
    listKind: OutlierDetectorList
    plural: outlierdetectors
    singular: outlierdetector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OutlierDetector is the Schema for the OutlierDetectors API. An
          outlier detector monitors the results of a query and reports when its values
          are outside normal bands. The normal band is configured by choice of algorithm,
          its sensitivity and other configuration. Visit https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
          for more details.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OutlierDetectorSpec defines the desired state of OutlierDetector
            properties:
              forProvider:
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  dataSourceRef:
                    description: Reference to a DataSource in oss to populate datasourceUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dataSourceSelector:
                    description: Selector for a DataSource in oss to populate datasourceUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.algorithm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.algorithm)
                || (has(self.initProvider) && has(self.initProvider.algorithm))'
            - message: spec.forProvider.datasourceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.datasourceType)
                || (has(self.initProvider) && has(self.initProvider.datasourceType))'
            - message: spec.forProvider.metric is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metric)
                || (has(self.initProvider) && has(self.initProvider.metric))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.queryParams is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryParams)
                || (has(self.initProvider) && has(self.initProvider.queryParams))'
          status:
            description: OutlierDetectorStatus defines the observed state of OutlierDetector.
            properties:
              atProvider:
                properties:
                  algorithm:
                    description: The algorithm to use and its configuration. See https://grafana.com/docs/grafana-cloud/machine-learning/outlier-detection/
                      for details.
                    items:
                      properties:
                        config:
                          description: For DBSCAN only, specify the configuration
                            map
                          items:
                            properties:
                              epsilon:
                                description: Specify the epsilon parameter (positive
                                  float)
                                type: number
                            type: object
                          type: array
                        name:
                          description: The name of the algorithm to use ('mad' or
                            'dbscan').
                          type: string
                        sensitivity:
                          description: Specify the sensitivity of the detector (in
                            range [0,1]).
                          type: number
                      type: object
                    type: array
                  datasourceType:
                    description: The type of datasource being queried. Currently allowed
                      values are prometheus, grafana-prometheus-datasource, grafana-amazonprometheus-datasource,
                      loki, grafana-loki-datasource, graphite, grafana-graphite-datasource,
                      grafana-datadog-datasource, postgres, grafana-postgresql-datasource,
                      doitintl-bigquery-datasource, grafana-bigquery-datasource, grafana-snowflake-datasource,
                      influxdb, grafana-influxdb-datasource, grafana-splunk-datasource,
                      elasticsearch, grafana-elasticsearch-datasource, and grafana-mongodb-datasource.
                    type: string
                  datasourceUid:
                    description: The uid of the datasource to query.
                    type: string
                  description:
                    description: A description of the outlier detector.
                    type: string
                  id:
                    type: string
                  interval:
                    description: The data interval in seconds to monitor. Defaults
                      to `300`.
                    type: number
                  metric:
                    description: The metric used to query the outlier detector results.
                    type: string
                  name:
                    description: The name of the outlier detector.
                    type: string
                  queryParams:
                    additionalProperties:
                      type: string
                    description: An object representing the query params to query
                      Grafana with.
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: escalationchains.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: EscalationChain
    listKind: EscalationChainList
    plural: escalationchains
    singular: escalationchain
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EscalationChain is the Schema for the EscalationChains API. HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/escalation_chains/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EscalationChainSpec defines the desired state of EscalationChain
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: EscalationChainStatus defines the observed state of EscalationChain.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: escalations.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Escalation
    listKind: EscalationList
    plural: escalations
    singular: escalation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Escalation is the Schema for the Escalations API. Official documentation
          https://grafana.com/docs/oncall/latest/configure/escalation-chains-and-routes/HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/escalation_policies/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EscalationSpec defines the desired state of Escalation
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  actionToTriggerRef:
                    description: Reference to a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  actionToTriggerSelector:
                    description: Selector for a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  groupToNotifyRef:
                    description: Reference to a UserGroup in oncall to populate groupToNotify.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupToNotifySelector:
                    description: Selector for a UserGroup in oncall to populate groupToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyOnCallFromScheduleRef:
                    description: Reference to a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  notifyOnCallFromScheduleSelector:
                    description: Selector for a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTimeRef:
                    description: References to User in oncall to populate personsToNotifyNextEachTime.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifyNextEachTimeSelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotifyNextEachTime.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  personsToNotifyRef:
                    description: References to User in oncall to populate personsToNotify.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifySelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  actionToTriggerRef:
                    description: Reference to a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  actionToTriggerSelector:
                    description: Selector for a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  groupToNotifyRef:
                    description: Reference to a UserGroup in oncall to populate groupToNotify.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupToNotifySelector:
                    description: Selector for a UserGroup in oncall to populate groupToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyOnCallFromScheduleRef:
                    description: Reference to a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  notifyOnCallFromScheduleSelector:
                    description: Selector for a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTimeRef:
                    description: References to User in oncall to populate personsToNotifyNextEachTime.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifyNextEachTimeSelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotifyNextEachTime.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  personsToNotifyRef:
                    description: References to User in oncall to populate personsToNotify.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifySelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.position is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.position)
                || (has(self.initProvider) && has(self.initProvider.position))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: EscalationStatus defines the observed state of Escalation.
            properties:
              atProvider:
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: integrations.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Integration
    listKind: IntegrationList
    plural: integrations
    singular: integration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Integration is the Schema for the Integrations API. Official
          documentation https://grafana.com/docs/oncall/latest/configure/integrations/HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IntegrationSpec defines the desired state of Integration
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        escalationChainRef:
                          description: Reference to a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        escalationChainSelector:
                          description: Selector for a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                              slackChannelRef:
                                description: Reference to a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              slackChannelSelector:
                                description: Selector for a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        escalationChainRef:
                          description: Reference to a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        escalationChainSelector:
                          description: Selector for a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                              slackChannelRef:
                                description: Reference to a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              slackChannelSelector:
                                description: Selector for a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.defaultRoute is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultRoute)
                || (has(self.initProvider) && has(self.initProvider.defaultRoute))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: IntegrationStatus defines the observed state of Integration.
            properties:
              atProvider:
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        id:
                          description: (String) The ID of this resource.
                          type: string
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  link:
                    description: |-
                      (String) The link for using in an integrated tool.
                      The link for using in an integrated tool.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: oncallshifts.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OnCallShift
    listKind: OnCallShiftList
    plural: oncallshifts
    singular: oncallshift
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OnCallShift is the Schema for the OnCallShifts API. HTTP API
          https://grafana.com/docs/oncall/latest/oncall-api-reference/on_call_shifts/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OnCallShiftSpec defines the desired state of OnCallShift
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  usersRef:
                    description: References to User in oncall to populate users.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  usersSelector:
                    description: Selector for a list of User in oncall to populate
                      users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  usersRef:
                    description: References to User in oncall to populate users.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  usersSelector:
                    description: Selector for a list of User in oncall to populate
                      users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.duration is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.duration)
                || (has(self.initProvider) && has(self.initProvider.duration))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.start is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.start)
                || (has(self.initProvider) && has(self.initProvider.start))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: OnCallShiftStatus defines the observed state of OnCallShift.
            properties:
              atProvider:
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: outgoingwebhooks.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OutgoingWebhook
    listKind: OutgoingWebhookList
    plural: outgoingwebhooks
    singular: outgoingwebhook
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OutgoingWebhook is the Schema for the OutgoingWebhooks API. HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/outgoing_webhooks/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OutgoingWebhookSpec defines the desired state of OutgoingWebhook
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used in Authorization header instead of user/password auth.
                      The auth data of the webhook. Used in Authorization header instead of user/password auth.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  integrationFilterRef:
                    description: References to Integration in oncall to populate integrationFilter.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  integrationFilterSelector:
                    description: Selector for a list of Integration in oncall to populate
                      integrationFilter.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used for Basic authentication
                      The auth data of the webhook. Used for Basic authentication
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used in Authorization header instead of user/password auth.
                      The auth data of the webhook. Used in Authorization header instead of user/password auth.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  integrationFilterRef:
                    description: References to Integration in oncall to populate integrationFilter.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  integrationFilterSelector:
                    description: Selector for a list of Integration in oncall to populate
                      integrationFilter.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used for Basic authentication
                      The auth data of the webhook. Used for Basic authentication
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: OutgoingWebhookStatus defines the observed state of OutgoingWebhook.
            properties:
              atProvider:
                properties:
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: routes.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route is the Schema for the Routes API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/routes/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RouteSpec defines the desired state of Route
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  integrationRef:
                    description: Reference to a Integration in oncall to populate
                      integrationId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  integrationSelector:
                    description: Selector for a Integration in oncall to populate
                      integrationId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  integrationRef:
                    description: Reference to a Integration in oncall to populate
                      integrationId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  integrationSelector:
                    description: Selector for a Integration in oncall to populate
                      integrationId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.position is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.position)
                || (has(self.initProvider) && has(self.initProvider.position))'
            - message: spec.forProvider.routingRegex is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.routingRegex)
                || (has(self.initProvider) && has(self.initProvider.routingRegex))'
          status:
            description: RouteStatus defines the observed state of Route.
            properties:
              atProvider:
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the Schedules API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/schedules/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  shiftsRef:
                    description: References to OnCallShift in oncall to populate shifts.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  shiftsSelector:
                    description: Selector for a list of OnCallShift in oncall to populate
                      shifts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        slackUserGroupRef:
                          description: Reference to a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackUserGroupSelector:
                          description: Selector for a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  shiftsRef:
                    description: References to OnCallShift in oncall to populate shifts.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  shiftsSelector:
                    description: Selector for a list of OnCallShift in oncall to populate
                      shifts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        slackUserGroupRef:
                          description: Reference to a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackUserGroupSelector:
                          description: Selector for a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              atProvider:
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: usernotificationrules.oncall.grafana.crossplane.io
spec:
  group: oncall.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: UserNotificationRule
    listKind: UserNotificationRuleList
    plural: usernotificationrules
    singular: usernotificationrule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'UserNotificationRule is the Schema for the UserNotificationRules
          API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/personal_notification_rules/
          Note: you must be running Grafana OnCall >= v1.8.0 to use this resource.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserNotificationRuleSpec defines the desired state of UserNotificationRule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  onCallUserRef:
                    description: Reference to a User in oncall to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  onCallUserSelector:
                    description: Selector for a User in oncall to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  onCallUserRef:
                    description: Reference to a User in oncall to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  onCallUserSelector:
                    description: Selector for a User in oncall to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: UserNotificationRuleStatus defines the observed state of
              UserNotificationRule.
            properties:
              atProvider:
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: escalationchains.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: EscalationChain
    listKind: EscalationChainList
    plural: escalationchains
    singular: escalationchain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EscalationChain is the Schema for the EscalationChains API. HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/escalation_chains/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EscalationChainSpec defines the desired state of EscalationChain
            properties:
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: EscalationChainStatus defines the observed state of EscalationChain.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the escalation chain.
                      The name of the escalation chain.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: escalations.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Escalation
    listKind: EscalationList
    plural: escalations
    singular: escalation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Escalation is the Schema for the Escalations API. Official documentation
          https://grafana.com/docs/oncall/latest/configure/escalation-chains-and-routes/HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/escalation_policies/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EscalationSpec defines the desired state of Escalation
            properties:
              forProvider:
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  actionToTriggerRef:
                    description: Reference to a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  actionToTriggerSelector:
                    description: Selector for a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  groupToNotifyRef:
                    description: Reference to a UserGroup in oncall to populate groupToNotify.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupToNotifySelector:
                    description: Selector for a UserGroup in oncall to populate groupToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyOnCallFromScheduleRef:
                    description: Reference to a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  notifyOnCallFromScheduleSelector:
                    description: Selector for a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTimeRef:
                    description: References to User in oncall to populate personsToNotifyNextEachTime.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifyNextEachTimeSelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotifyNextEachTime.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  personsToNotifyRef:
                    description: References to User in oncall to populate personsToNotify.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifySelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  actionToTriggerRef:
                    description: Reference to a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  actionToTriggerSelector:
                    description: Selector for a OutgoingWebhook in oncall to populate
                      actionToTrigger.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  groupToNotifyRef:
                    description: Reference to a UserGroup in oncall to populate groupToNotify.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupToNotifySelector:
                    description: Selector for a UserGroup in oncall to populate groupToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyOnCallFromScheduleRef:
                    description: Reference to a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  notifyOnCallFromScheduleSelector:
                    description: Selector for a Schedule in oncall to populate notifyOnCallFromSchedule.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTimeRef:
                    description: References to User in oncall to populate personsToNotifyNextEachTime.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifyNextEachTimeSelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotifyNextEachTime.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  personsToNotifyRef:
                    description: References to User in oncall to populate personsToNotify.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  personsToNotifySelector:
                    description: Selector for a list of User in oncall to populate
                      personsToNotify.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate notifyToTeamMembers.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.position is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.position)
                || (has(self.initProvider) && has(self.initProvider.position))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: EscalationStatus defines the observed state of Escalation.
            properties:
              atProvider:
                properties:
                  actionToTrigger:
                    description: |-
                      (String) The ID of an Action for trigger_webhook type step.
                      The ID of an Action for trigger_webhook type step.
                    type: string
                  duration:
                    description: |-
                      86400) seconds
                      The duration of delay for wait type step. (60-86400) seconds
                    type: number
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  groupToNotify:
                    description: |-
                      (String) The ID of a User Group for notify_user_group type step.
                      The ID of a User Group for notify_user_group type step.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  important:
                    description: |-
                      (Boolean) Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                      Will activate "important" personal notification rules. Actual for steps: notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, notify_user_group and notify_team_members
                    type: boolean
                  notifyIfTimeFrom:
                    description: |-
                      (String) The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                      The beginning of the time interval for notify_if_time_from_to type step in UTC (for example 08:00:00Z).
                    type: string
                  notifyIfTimeTo:
                    description: |-
                      (String) The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                      The end of the time interval for notify_if_time_from_to type step in UTC (for example 18:00:00Z).
                    type: string
                  notifyOnCallFromSchedule:
                    description: |-
                      (String) ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                      ID of a Schedule for notify_on_call_from_schedule or notify_next_on_call_from_schedule type step.
                    type: string
                  notifyToTeamMembers:
                    description: |-
                      (String) The ID of a Team for a notify_team_members type step.
                      The ID of a Team for a notify_team_members type step.
                    type: string
                  numAlertsInWindow:
                    description: |-
                      (Number) Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                      Number of alerts that must occur within the time window to continue escalation for notify_if_num_alerts_in_window type step.
                    type: number
                  numMinutesInWindow:
                    description: |-
                      (Number) Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                      Time window in minutes to count alerts for notify_if_num_alerts_in_window type step.
                    type: number
                  personsToNotify:
                    description: |-
                      (Set of String) The list of ID's of users for notify_persons type step.
                      The list of ID's of users for notify_persons type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  personsToNotifyNextEachTime:
                    description: |-
                      (Set of String) The list of ID's of users for notify_person_next_each_time type step.
                      The list of ID's of users for notify_person_next_each_time type step.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  position:
                    description: |-
                      (Number) The position of the escalation step (starts from 0).
                      The position of the escalation step (starts from 0).
                    type: number
                  severity:
                    description: |-
                      (String) The severity of the incident for declare_incident type step.
                      The severity of the incident for declare_incident type step.
                    type: string
                  type:
                    description: |-
                      (String) The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                      The type of escalation policy. Can be wait, notify_persons, notify_person_next_each_time, notify_on_call_from_schedule, notify_next_on_call_from_schedule, trigger_webhook, notify_user_group, resolve, notify_whole_channel, notify_if_time_from_to, notify_if_num_alerts_in_window, repeat_escalation, notify_team_members, declare_incident
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: integrations.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Integration
    listKind: IntegrationList
    plural: integrations
    singular: integration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Integration is the Schema for the Integrations API. Official
          documentation https://grafana.com/docs/oncall/latest/configure/integrations/HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IntegrationSpec defines the desired state of Integration
            properties:
              forProvider:
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        escalationChainRef:
                          description: Reference to a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        escalationChainSelector:
                          description: Selector for a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                              slackChannelRef:
                                description: Reference to a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              slackChannelSelector:
                                description: Selector for a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        escalationChainRef:
                          description: Reference to a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        escalationChainSelector:
                          description: Selector for a EscalationChain in oncall to
                            populate escalationChainId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                              slackChannelRef:
                                description: Reference to a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              slackChannelSelector:
                                description: Selector for a SlackChannel in oncall
                                  to populate channelId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.defaultRoute is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultRoute)
                || (has(self.initProvider) && has(self.initProvider.defaultRoute))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: IntegrationStatus defines the observed state of Integration.
            properties:
              atProvider:
                properties:
                  defaultRoute:
                    description: |-
                      (Block List, Min: 1, Max: 1) The Default route for all alerts from the given integration (see below for nested schema)
                      The Default route for all alerts from the given integration
                    items:
                      properties:
                        escalationChainId:
                          description: |-
                            (String) The ID of the escalation chain.
                            The ID of the escalation chain.
                          type: string
                        id:
                          description: (String) The ID of this resource.
                          type: string
                        msteams:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            MS teams-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in MS teams. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                                type: string
                            type: object
                          type: array
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Slack-specific settings for a route.
                          items:
                            properties:
                              channelId:
                                description: |-
                                  (String) Slack channel id. Alerts will be directed to this channel in Slack.
                                  Slack channel id. Alerts will be directed to this channel in Slack.
                                type: string
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Slack. Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Telegram-specific settings for a route.
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) Enable notification in MS teams. Defaults to true.
                                  Enable notification in Telegram. Defaults to `true`.
                                type: boolean
                              id:
                                description: |-
                                  (String) The ID of this resource.
                                  Telegram channel id. Alerts will be directed to this channel in Telegram.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dynamicLabels:
                    description: |-
                      to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for dynamic labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  labels:
                    description: |-
                      to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the grafana_oncall_label datasource).
                      A list of string-to-string mappings for static labels. Each map must include one key named "key" and one key named "value" (using the `grafana_oncall_label` datasource).
                    items:
                      additionalProperties:
                        type: string
                      type: object
                    type: array
                  link:
                    description: |-
                      (String) The link for using in an integrated tool.
                      The link for using in an integrated tool.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service integration.
                      The name of the service integration.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  templates:
                    description: |-
                      (Block List, Max: 1) Jinja2 templates for Alert payload. An empty templates block will be ignored. (see below for nested schema)
                      Jinja2 templates for Alert payload. An empty templates block will be ignored.
                    items:
                      properties:
                        acknowledgeSignal:
                          description: |-
                            (String) Template for sending a signal to acknowledge the Incident.
                            Template for sending a signal to acknowledge the Incident.
                          type: string
                        email:
                          description: |-
                            (Block List, Max: 1) Templates for Email. (see below for nested schema)
                            Templates for Email.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        groupingKey:
                          description: |-
                            (String) Template for the key by which alerts are grouped.
                            Template for the key by which alerts are grouped.
                          type: string
                        microsoftTeams:
                          description: |-
                            (Block List, Max: 1) Templates for Microsoft Teams. NOTE: Microsoft Teams templates are only available on Grafana Cloud. (see below for nested schema)
                            Templates for Microsoft Teams. **NOTE**: Microsoft Teams templates are only available on Grafana Cloud.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        mobileApp:
                          description: |-
                            (Block List, Max: 1) Templates for Mobile app push notifications. (see below for nested schema)
                            Templates for Mobile app push notifications.
                          items:
                            properties:
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        phoneCall:
                          description: |-
                            (Block List, Max: 1) Templates for Phone Call. (see below for nested schema)
                            Templates for Phone Call.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        resolveSignal:
                          description: |-
                            (String) Template for sending a signal to resolve the Incident.
                            Template for sending a signal to resolve the Incident.
                          type: string
                        slack:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Slack.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sms:
                          description: |-
                            (Block List, Max: 1) Templates for SMS. (see below for nested schema)
                            Templates for SMS.
                          items:
                            properties:
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        sourceLink:
                          description: |-
                            (String) Template for a source link.
                            Template for a source link.
                          type: string
                        telegram:
                          description: |-
                            specific settings for a route. (see below for nested schema)
                            Templates for Telegram.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                        web:
                          description: |-
                            (Block List, Max: 1) Templates for Web. (see below for nested schema)
                            Templates for Web.
                          items:
                            properties:
                              imageUrl:
                                description: |-
                                  (String) Template for Alert image url.
                                  Template for Alert image url.
                                type: string
                              message:
                                description: |-
                                  (String) Template for Alert message.
                                  Template for Alert message.
                                type: string
                              title:
                                description: |-
                                  (String) Template for Alert title.
                                  Template for Alert title.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  type:
                    description: |-
                      (String) The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                      The type of integration. Can be grafana, grafana_alerting, webhook, alertmanager, kapacitor, fabric, newrelic, datadog, pagerduty, pingdom, elastalert, amazon_sns, curler, sentry, formatted_webhook, stackdriver, uptimerobot, sentry, zabbix, prtg, inbound_email, direct_paging, jira, zendesk.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: oncallshifts.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OnCallShift
    listKind: OnCallShiftList
    plural: oncallshifts
    singular: oncallshift
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OnCallShift is the Schema for the OnCallShifts API. HTTP API
          https://grafana.com/docs/oncall/latest/oncall-api-reference/on_call_shifts/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OnCallShiftSpec defines the desired state of OnCallShift
            properties:
              forProvider:
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  usersRef:
                    description: References to User in oncall to populate users.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  usersSelector:
                    description: Selector for a list of User in oncall to populate
                      users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  usersRef:
                    description: References to User in oncall to populate users.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  usersSelector:
                    description: Selector for a list of User in oncall to populate
                      users.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.duration is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.duration)
                || (has(self.initProvider) && has(self.initProvider.duration))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.start is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.start)
                || (has(self.initProvider) && has(self.initProvider.start))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: OnCallShiftStatus defines the observed state of OnCallShift.
            properties:
              atProvider:
                properties:
                  byDay:
                    description: |-
                      (Set of String) This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      This parameter takes a list of days in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  byMonth:
                    description: |-
                      (Set of Number) This parameter takes a list of months. Valid values are 1 to 12
                      This parameter takes a list of months. Valid values are 1 to 12
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  byMonthday:
                    description: |-
                      31 to -1
                      This parameter takes a list of days of the month.  Valid values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  duration:
                    description: |-
                      (Number) The duration of the event.
                      The duration of the event.
                    type: number
                  frequency:
                    description: |-
                      (String) The frequency of the event. Can be hourly, daily, weekly, monthly
                      The frequency of the event. Can be hourly, daily, weekly, monthly
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interval:
                    description: |-
                      (Number) The positive integer representing at which intervals the recurrence rule repeats.
                      The positive integer representing at which intervals the recurrence rule repeats.
                    type: number
                  level:
                    description: |-
                      (Number) The priority level. The higher the value, the higher the priority.
                      The priority level. The higher the value, the higher the priority.
                    type: number
                  name:
                    description: |-
                      (String) The shift's name.
                      The shift's name.
                    type: string
                  rollingUsers:
                    description: |-
                      call users (for rolling_users event type)
                      The list of lists with on-call users (for rolling_users event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: |-
                      call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The start time of the on-call shift. This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: |-
                      call rotation starts.
                      The index of the list of users in rolling_users, from which on-call rotation starts.
                    type: number
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  timeZone:
                    description: |-
                      (String) The shift's timezone.  Overrides schedule's timezone.
                      The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: |-
                      (String) The shift's type. Can be rolling_users, recurrent_event, single_event
                      The shift's type. Can be rolling_users, recurrent_event, single_event
                    type: string
                  until:
                    description: |-
                      call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                      The end time of recurrent on-call shifts (endless if null). This parameter takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  users:
                    description: |-
                      call users (for single_event and recurrent_event event type).
                      The list of on-call users (for single_event and recurrent_event event type).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  weekStart:
                    description: |-
                      (String) Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                      Start day of the week in iCal format. Can be MO, TU, WE, TH, FR, SA, SU
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: outgoingwebhooks.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OutgoingWebhook
    listKind: OutgoingWebhookList
    plural: outgoingwebhooks
    singular: outgoingwebhook
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OutgoingWebhook is the Schema for the OutgoingWebhooks API. HTTP
          API https://grafana.com/docs/oncall/latest/oncall-api-reference/outgoing_webhooks/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OutgoingWebhookSpec defines the desired state of OutgoingWebhook
            properties:
              forProvider:
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used in Authorization header instead of user/password auth.
                      The auth data of the webhook. Used in Authorization header instead of user/password auth.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  integrationFilterRef:
                    description: References to Integration in oncall to populate integrationFilter.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  integrationFilterSelector:
                    description: Selector for a list of Integration in oncall to populate
                      integrationFilter.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used for Basic authentication
                      The auth data of the webhook. Used for Basic authentication
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authorizationHeaderSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used in Authorization header instead of user/password auth.
                      The auth data of the webhook. Used in Authorization header instead of user/password auth.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  integrationFilterRef:
                    description: References to Integration in oncall to populate integrationFilter.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  integrationFilterSelector:
                    description: Selector for a list of Integration in oncall to populate
                      integrationFilter.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The auth data of the webhook. Used for Basic authentication
                      The auth data of the webhook. Used for Basic authentication
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: OutgoingWebhookStatus defines the observed state of OutgoingWebhook.
            properties:
              atProvider:
                properties:
                  data:
                    description: |-
                      (String) The data of the webhook.
                      The data of the webhook.
                    type: string
                  forwardWholePayload:
                    description: |-
                      (Boolean) Toggle to send the entire webhook payload instead of using the values in the Data field.
                      Toggle to send the entire webhook payload instead of using the values in the Data field.
                    type: boolean
                  headers:
                    description: |-
                      (String) Headers to add to the outgoing webhook request.
                      Headers to add to the outgoing webhook request.
                    type: string
                  httpMethod:
                    description: |-
                      (String) The HTTP method used in the request made by the outgoing webhook. Defaults to POST.
                      The HTTP method used in the request made by the outgoing webhook. Defaults to `POST`.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  integrationFilter:
                    description: |-
                      (List of String) Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                      Restricts the outgoing webhook to only trigger if the event came from a selected integration. If no integrations are selected the outgoing webhook will trigger for any integration.
                    items:
                      type: string
                    type: array
                  isWebhookEnabled:
                    description: |-
                      (Boolean) Controls whether the outgoing webhook will trigger or is ignored. Defaults to true.
                      Controls whether the outgoing webhook will trigger or is ignored. Defaults to `true`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the outgoing webhook.
                      The name of the outgoing webhook.
                    type: string
                  preset:
                    description: |-
                      (String) The preset of the outgoing webhook. Possible values are: simple_webhook, advanced_webhook, grafana_sift, grafana_assistant, incident_webhook. If no preset is set, the default preset is advanced_webhook.
                      The preset of the outgoing webhook. Possible values are: `simple_webhook`, `advanced_webhook`, `grafana_sift`, `grafana_assistant`, `incident_webhook`. If no preset is set, the default preset is `advanced_webhook`.
                    type: string
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  triggerTemplate:
                    description: |-
                      (String) A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                      A template used to dynamically determine whether the webhook should execute based on the content of the payload.
                    type: string
                  triggerType:
                    description: |-
                      (String) The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: escalation, alert group created, status change, acknowledge, resolve, silence, unsilence, unresolve, unacknowledge, resolution note added, personal notification; for incident webhooks: incident declared, incident changed, incident resolved. Defaults to escalation.
                      The type of event that will cause this outgoing webhook to execute. The events available will depend on the preset used. For alert group webhooks, the possible triggers are: `escalation`, `alert group created`, `status change`, `acknowledge`, `resolve`, `silence`, `unsilence`, `unresolve`, `unacknowledge`, `resolution note added`, `personal notification`; for incident webhooks: `incident declared`, `incident changed`, `incident resolved`. Defaults to `escalation`.
                    type: string
                  url:
                    description: |-
                      (String) The webhook URL. Required when not using a preset that controls this field.
                      The webhook URL. Required when not using a preset that controls this field.
                    type: string
                  user:
                    description: |-
                      (String) Username to use when making the outgoing webhook request.
                      Username to use when making the outgoing webhook request.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: routes.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route is the Schema for the Routes API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/routes/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RouteSpec defines the desired state of Route
            properties:
              forProvider:
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  integrationRef:
                    description: Reference to a Integration in oncall to populate
                      integrationId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  integrationSelector:
                    description: Selector for a Integration in oncall to populate
                      integrationId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  escalationChainRef:
                    description: Reference to a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationChainSelector:
                    description: Selector for a EscalationChain in oncall to populate
                      escalationChainId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  integrationRef:
                    description: Reference to a Integration in oncall to populate
                      integrationId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  integrationSelector:
                    description: Selector for a Integration in oncall to populate
                      integrationId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.position is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.position)
                || (has(self.initProvider) && has(self.initProvider.position))'
            - message: spec.forProvider.routingRegex is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.routingRegex)
                || (has(self.initProvider) && has(self.initProvider.routingRegex))'
          status:
            description: RouteStatus defines the observed state of Route.
            properties:
              atProvider:
                properties:
                  escalationChainId:
                    description: |-
                      (String) The ID of the escalation chain.
                      The ID of the escalation chain.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  integrationId:
                    description: |-
                      (String) The ID of the integration.
                      The ID of the integration.
                    type: string
                  msteams:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      MS teams-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in MS teams. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            MS teams channel id. Alerts will be directed to this channel in Microsoft teams.
                          type: string
                      type: object
                    type: array
                  position:
                    description: |-
                      (Number) The position of the route (starts from 0).
                      The position of the route (starts from 0).
                    type: number
                  routingRegex:
                    description: |-
                      (String) Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                      Python Regex query. Route is chosen for an alert if there is a match inside the alert payload.
                    type: string
                  routingType:
                    description: |-
                      (String) The type of route. Can be jinja2, regex Defaults to regex.
                      The type of route. Can be jinja2, regex Defaults to `regex`.
                    type: string
                  slack:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Slack-specific settings for a route.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Alerts will be directed to this channel in Slack.
                            Slack channel id. Alerts will be directed to this channel in Slack.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Slack. Defaults to `true`.
                          type: boolean
                      type: object
                    type: array
                  telegram:
                    description: |-
                      specific settings for a route. (see below for nested schema)
                      Telegram-specific settings for a route.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Enable notification in MS teams. Defaults to true.
                            Enable notification in Telegram. Defaults to `true`.
                          type: boolean
                        id:
                          description: |-
                            (String) The ID of this resource.
                            Telegram channel id. Alerts will be directed to this channel in Telegram.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the Schedules API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/schedules/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of Schedule
            properties:
              forProvider:
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  shiftsRef:
                    description: References to OnCallShift in oncall to populate shifts.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  shiftsSelector:
                    description: Selector for a list of OnCallShift in oncall to populate
                      shifts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        slackUserGroupRef:
                          description: Reference to a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackUserGroupSelector:
                          description: Selector for a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  shiftsRef:
                    description: References to OnCallShift in oncall to populate shifts.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  shiftsSelector:
                    description: Selector for a list of OnCallShift in oncall to populate
                      shifts.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        slackChannelRef:
                          description: Reference to a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackChannelSelector:
                          description: Selector for a SlackChannel in oncall to populate
                            channelId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        slackUserGroupRef:
                          description: Reference to a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        slackUserGroupSelector:
                          description: Selector for a UserGroup in oncall to populate
                            userGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  teamRef:
                    description: Reference to a Team in oncall to populate teamId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  teamSelector:
                    description: Selector for a Team in oncall to populate teamId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ScheduleStatus defines the observed state of Schedule.
            properties:
              atProvider:
                properties:
                  enableWebOverrides:
                    description: |-
                      (Boolean) Enable overrides via web UI (it will ignore ical_url_overrides).
                      Enable overrides via web UI (it will ignore ical_url_overrides).
                    type: boolean
                  icalUrlOverrides:
                    description: |-
                      (String) The URL of external iCal calendar which override primary events.
                      The URL of external iCal calendar which override primary events.
                    type: string
                  icalUrlPrimary:
                    description: |-
                      (String) The URL of the external calendar iCal file.
                      The URL of the external calendar iCal file.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The schedule's name.
                      The schedule's name.
                    type: string
                  shifts:
                    description: |-
                      call shifts.
                      The list of ID's of on-call shifts.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  slack:
                    description: |-
                      specific settings for a schedule. (see below for nested schema)
                      The Slack-specific settings for a schedule.
                    items:
                      properties:
                        channelId:
                          description: |-
                            (String) Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                            Slack channel id. Reminder about schedule shifts will be directed to this channel in Slack.
                          type: string
                        userGroupId:
                          description: |-
                            call users change.
                            Slack user group id. Members of user group will be updated when on-call users change.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (String) The ID of the OnCall team (using the grafana_oncall_team datasource).
                      The ID of the OnCall team (using the `grafana_oncall_team` datasource).
                    type: string
                  timeZone:
                    description: |-
                      (String) The schedule's time zone.
                      The schedule's time zone.
                    type: string
                  type:
                    description: |-
                      (String) The schedule's type. Valid values are ical, calendar, web.
                      The schedule's type. Valid values are `ical`, `calendar`, `web`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: usernotificationrules.oncall.grafana.m.crossplane.io
spec:
  group: oncall.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: UserNotificationRule
    listKind: UserNotificationRuleList
    plural: usernotificationrules
    singular: usernotificationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'UserNotificationRule is the Schema for the UserNotificationRules
          API. HTTP API https://grafana.com/docs/oncall/latest/oncall-api-reference/personal_notification_rules/
          Note: you must be running Grafana OnCall >= v1.8.0 to use this resource.'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserNotificationRuleSpec defines the desired state of UserNotificationRule
            properties:
              forProvider:
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  onCallUserRef:
                    description: Reference to a User in oncall to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  onCallUserSelector:
                    description: Selector for a User in oncall to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  onCallUserRef:
                    description: Reference to a User in oncall to populate userId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  onCallUserSelector:
                    description: Selector for a User in oncall to populate userId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: UserNotificationRuleStatus defines the observed state of
              UserNotificationRule.
            properties:
              atProvider:
                properties:
                  duration:
                    description: |-
                      (Number) A time in seconds to wait (when type=wait). Can be 60, 300, 900, 1800, 3600
                      A time in seconds to wait (when `type=wait`). Can be 60, 300, 900, 1800, 3600
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  important:
                    description: |-
                      (Boolean) Boolean value which indicates if a rule is “important”
                      Boolean value which indicates if a rule is “important”
                    type: boolean
                  position:
                    description: |-
                      (Number) Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                      Personal notification rules execute one after another starting from position=0. A new escalation policy created with a position of an existing escalation policy will move the old one (and all following) down on the list.
                    type: number
                  type:
                    description: |-
                      (String) The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: notify_by_msteams is only available for Grafana Cloud customers.
                      The type of notification rule. Can be wait, notify_by_slack, notify_by_msteams, notify_by_sms, notify_by_phone_call, notify_by_telegram, notify_by_email, notify_by_mobile_app, notify_by_mobile_app_critical. NOTE: `notify_by_msteams` is only available for Grafana Cloud customers.
                    type: string
                  userId:
                    description: |-
                      (String) User ID
                      User ID
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: escalationchains.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: EscalationChain
    listKind: EscalationChainList
    plural: escalationchains
    singular: escalationchain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EscalationChain is an observe-only resource backed by the grafana_oncall_escalation_chain
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: EscalationChainParameters defines the input parameters
                  for the grafana_oncall_escalation_chain data source.
                properties:
                  name:
                    description: The escalation chain name.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: EscalationChainObservation holds the observed (computed)
                  fields from the grafana_oncall_escalation_chain data source.
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: integrations.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Integration
    listKind: IntegrationList
    plural: integrations
    singular: integration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Integration is an observe-only resource backed by the grafana_oncall_integration
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: IntegrationParameters defines the input parameters for
                  the grafana_oncall_integration data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: IntegrationObservation holds the observed (computed)
                  fields from the grafana_oncall_integration data source.
                properties:
                  inboundEmail:
                    description: The inbound email for the integration. Only available
                      for integration type `inbound_email`.
                    type: string
                  link:
                    description: The link for the integration.
                    type: string
                  name:
                    description: The integration name.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: labels.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Label
    listKind: LabelList
    plural: labels
    singular: label
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Label is an observe-only resource backed by the grafana_oncall_label
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: LabelParameters defines the input parameters for the
                  grafana_oncall_label data source.
                properties:
                  key:
                    description: The key for the label.
                    type: string
                  value:
                    description: The value of the label.
                    type: string
                required:
                - key
                - value
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: LabelObservation holds the observed (computed) fields
                  from the grafana_oncall_label data source.
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: outgoingwebhooks.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: OutgoingWebhook
    listKind: OutgoingWebhookList
    plural: outgoingwebhooks
    singular: outgoingwebhook
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OutgoingWebhook is an observe-only resource backed by the grafana_oncall_outgoing_webhook
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: OutgoingWebhookParameters defines the input parameters
                  for the grafana_oncall_outgoing_webhook data source.
                properties:
                  name:
                    description: The outgoing webhook name.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: OutgoingWebhookObservation holds the observed (computed)
                  fields from the grafana_oncall_outgoing_webhook data source.
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: schedules.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is an observe-only resource backed by the grafana_oncall_schedule
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ScheduleParameters defines the input parameters for the
                  grafana_oncall_schedule data source.
                properties:
                  name:
                    description: The schedule name.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ScheduleObservation holds the observed (computed) fields
                  from the grafana_oncall_schedule data source.
                properties:
                  type:
                    description: The schedule type.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: slackchannels.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: SlackChannel
    listKind: SlackChannelList
    plural: slackchannels
    singular: slackchannel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SlackChannel is an observe-only resource backed by the grafana_oncall_slack_channel
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: SlackChannelParameters defines the input parameters for
                  the grafana_oncall_slack_channel data source.
                properties:
                  name:
                    description: The Slack channel name.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: SlackChannelObservation holds the observed (computed)
                  fields from the grafana_oncall_slack_channel data source.
                properties:
                  slackID:
                    description: The Slack ID of the channel.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teams.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Team
    listKind: TeamList
    plural: teams
    singular: team
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Team is an observe-only resource backed by the grafana_oncall_team
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: TeamParameters defines the input parameters for the grafana_oncall_team
                  data source.
                properties:
                  name:
                    description: The team name.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: TeamObservation holds the observed (computed) fields
                  from the grafana_oncall_team data source.
                properties:
                  avatarURL:
                    type: string
                  email:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: usergroups.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: UserGroup
    listKind: UserGroupList
    plural: usergroups
    singular: usergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserGroup is an observe-only resource backed by the grafana_oncall_user_group
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: UserGroupParameters defines the input parameters for
                  the grafana_oncall_user_group data source.
                properties:
                  slackHandle:
                    type: string
                required:
                - slackHandle
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: UserGroupObservation holds the observed (computed) fields
                  from the grafana_oncall_user_group data source.
                properties:
                  slackID:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is an observe-only resource backed by the grafana_oncall_user
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: UserParameters defines the input parameters for the grafana_oncall_user
                  data source.
                properties:
                  username:
                    description: The username of the user.
                    type: string
                required:
                - username
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: UserObservation holds the observed (computed) fields
                  from the grafana_oncall_user data source.
                properties:
                  email:
                    description: The email of the user.
                    type: string
                  role:
                    description: The role of the user.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: usersets.oncall.grafana.o.crossplane.io
spec:
  group: oncall.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: UserSet
    listKind: UserSetList
    plural: usersets
    singular: userset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserSet is an observe-only resource backed by the grafana_oncall_users
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: UserSetParameters defines the input parameters for the
                  grafana_oncall_users data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: UserSetObservation holds the observed (computed) fields
                  from the grafana_oncall_users data source.
                properties:
                  users:
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: annotations.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Annotation
    listKind: AnnotationList
    plural: annotations
    singular: annotation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Annotation is the Schema for the Annotations API. Manages Grafana
          annotations. Official documentation https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/annotate-visualizations/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/annotations/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnnotationSpec defines the desired state of Annotation
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.text is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.text)
                || (has(self.initProvider) && has(self.initProvider.text))'
          status:
            description: AnnotationStatus defines the observed state of Annotation.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: connectionv0alpha1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ConnectionV0Alpha1
    listKind: ConnectionV0Alpha1List
    plural: connectionv0alpha1s
    singular: connectionv0alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConnectionV0Alpha1 is the Schema for the ConnectionV0Alpha1s
          API. Manages Grafana Git Sync connections used by repositories for provider
          authentication.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionV0Alpha1Spec defines the desired state of ConnectionV0Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: ConnectionV0Alpha1Status defines the observed state of ConnectionV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpermissionitems.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPermissionItem
    listKind: DashboardPermissionItemList
    plural: dashboardpermissionitems
    singular: dashboardpermissionitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardPermissionItem is the Schema for the DashboardPermissionItems
          API. Manages a single permission item for a dashboard. Conflicts with the
          "grafana_dashboard_permission" resource which manages the entire set of
          permissions for a dashboard.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPermissionItemSpec defines the desired state of
              DashboardPermissionItem
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dashboardUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dashboardUid)
                || (has(self.initProvider) && has(self.initProvider.dashboardUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: DashboardPermissionItemStatus defines the observed state
              of DashboardPermissionItem.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpermissions.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPermission
    listKind: DashboardPermissionList
    plural: dashboardpermissions
    singular: dashboardpermission
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardPermission is the Schema for the DashboardPermissions
          API. Manages the entire set of permissions for a dashboard. Permissions
          that aren't specified when applying this resource will be removed. Official
          documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/dashboard_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPermissionSpec defines the desired state of DashboardPermission
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DashboardPermissionStatus defines the observed state of DashboardPermission.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpublics.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPublic
    listKind: DashboardPublicList
    plural: dashboardpublics
    singular: dashboardpublic
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'DashboardPublic is the Schema for the DashboardPublics API.
          Manages Grafana public dashboards. Note: This resource is available only
          with Grafana 10.2+. Official documentation https://grafana.com/docs/grafana/latest/dashboards/share-dashboards-panels/shared-dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/dashboard_public/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPublicSpec defines the desired state of DashboardPublic
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DashboardPublicStatus defines the observed state of DashboardPublic.
            properties:
              atProvider:
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboards.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Dashboard
    listKind: DashboardList
    plural: dashboards
    singular: dashboard
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dashboard is the Schema for the Dashboards API. Manages Grafana
          dashboards. Official documentation https://grafana.com/docs/grafana/latest/dashboards/HTTP
          API (legacy API, recommended for Grafana 12 or earlier) https://grafana.com/docs/grafana/v11.6/developers/http_api/dashboard/HTTP
          API (new Kubernetes-style API, recommended for Grafana 13 and later) https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardSpec defines the desired state of Dashboard
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  folderRef:
                    description: Reference to a Folder in oss to populate folder.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folder.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  folderRef:
                    description: Reference to a Folder in oss to populate folder.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folder.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.configJson is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.configJson)
                || (has(self.initProvider) && has(self.initProvider.configJson))'
          status:
            description: DashboardStatus defines the observed state of Dashboard.
            properties:
              atProvider:
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  dashboardId:
                    description: |-
                      (Number) The numeric ID of the dashboard computed by Grafana.
                      The numeric ID of the dashboard computed by Grafana.
                    type: number
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a dashboard. The uid allows having consistent URLs for accessing dashboards and when syncing dashboards between multiple Grafana installs.
                      The unique identifier of a dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a dashboard. The uid allows having consistent URLs for accessing dashboards and when syncing dashboards between multiple Grafana installs.
                    type: string
                  url:
                    description: |-
                      (String) The full URL of the dashboard.
                      The full URL of the dashboard.
                    type: string
                  version:
                    description: |-
                      (Number) Whenever you save a version of your dashboard, a copy of that version is saved so that previous versions of your dashboard are not lost.
                      Whenever you save a version of your dashboard, a copy of that version is saved so that previous versions of your dashboard are not lost.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv1beta1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV1Beta1
    listKind: DashboardV1Beta1List
    plural: dashboardv1beta1s
    singular: dashboardv1beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV1Beta1 is the Schema for the DashboardV1Beta1s API.
          Manages Grafana dashboards using the new Grafana APIs. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/HTTP API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV1Beta1Spec defines the desired state of DashboardV1Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV1Beta1Status defines the observed state of DashboardV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv2beta1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV2Beta1
    listKind: DashboardV2Beta1List
    plural: dashboardv2beta1s
    singular: dashboardv2beta1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV2Beta1 is the Schema for the DashboardV2Beta1s API.
          Manages Grafana dashboards using the v2beta1 (Dynamic Dashboards) schema.
          Official documentation https://grafana.com/docs/grafana/latest/dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV2Beta1Spec defines the desired state of DashboardV2Beta1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV2Beta1Status defines the observed state of DashboardV2Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv2s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV2
    listKind: DashboardV2List
    plural: dashboardv2s
    singular: dashboardv2
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV2 is the Schema for the DashboardV2s API. Manages Grafana
          dashboards using the v2 (Dynamic Dashboards) schema. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/HTTP API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV2Spec defines the desired state of DashboardV2
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV2Status defines the observed state of DashboardV2.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourceconfigs.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceConfig
    listKind: DataSourceConfigList
    plural: datasourceconfigs
    singular: datasourceconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourceConfig is the Schema for the DataSourceConfigs API.
          Official documentation https://grafana.com/docs/grafana/latest/datasources/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/data_source/
          The required arguments for this resource vary depending on the type of data
          source selected (via the 'type' argument). Use this resource for configuring
          multiple datasources, when that configuration (json_data_encoded field)
          requires circular references like in the example below. When using the
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceConfigSpec defines the desired state of DataSourceConfig
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  httpHeadersSecretRef:
                    description: |-
                      (Map of String, Sensitive) Custom HTTP headers
                      Custom HTTP headers
                    properties:
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  httpHeadersSecretRef:
                    additionalProperties:
                      type: string
                    type: object
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DataSourceConfigStatus defines the observed state of DataSourceConfig.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasources.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    singular: datasource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSource is the Schema for the DataSources API. Official documentation
          https://grafana.com/docs/grafana/latest/datasources/HTTP API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/data_source/
          The required arguments for this resource vary depending on the type of data
          source selected (via the 'type' argument).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceSpec defines the desired state of DataSource
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  httpHeadersSecretRef:
                    description: |-
                      (Map of String, Sensitive) Custom HTTP headers
                      Custom HTTP headers
                    properties:
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  httpHeadersSecretRef:
                    additionalProperties:
                      type: string
                    type: object
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: DataSourceStatus defines the observed state of DataSource.
            properties:
              atProvider:
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folderpermissionitems.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: FolderPermissionItem
    listKind: FolderPermissionItemList
    plural: folderpermissionitems
    singular: folderpermissionitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FolderPermissionItem is the Schema for the FolderPermissionItems
          API. Manages a single permission item for a folder. Conflicts with the "grafana_folder_permission"
          resource which manages the entire set of permissions for a folder.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderPermissionItemSpec defines the desired state of FolderPermissionItem
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.folderUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.folderUid)
                || (has(self.initProvider) && has(self.initProvider.folderUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: FolderPermissionItemStatus defines the observed state of
              FolderPermissionItem.
            properties:
              atProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folderpermissions.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: FolderPermission
    listKind: FolderPermissionList
    plural: folderpermissions
    singular: folderpermission
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FolderPermission is the Schema for the FolderPermissions API.
          Manages the entire set of permissions for a folder. Permissions that aren't
          specified when applying this resource will be removed. Official documentation
          https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/folder_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderPermissionSpec defines the desired state of FolderPermission
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: FolderPermissionStatus defines the observed state of FolderPermission.
            properties:
              atProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folders.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Folder
    listKind: FolderList
    plural: folders
    singular: folder
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Folder is the Schema for the Folders API. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/manage-dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/folder/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderSpec defines the desired state of Folder
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate parentFolderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate parentFolderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate parentFolderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate parentFolderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.title is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.title)
                || (has(self.initProvider) && has(self.initProvider.title))'
          status:
            description: FolderStatus defines the observed state of Folder.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                  url:
                    description: |-
                      (String) The full URL of the folder.
                      The full URL of the folder.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: librarypanels.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LibraryPanel
    listKind: LibraryPanelList
    plural: librarypanels
    singular: librarypanel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LibraryPanel is the Schema for the LibraryPanels API. Manages
          Grafana library panels. Official documentation https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/manage-library-panels/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/library_element/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LibraryPanelSpec defines the desired state of LibraryPanel
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.modelJson is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelJson)
                || (has(self.initProvider) && has(self.initProvider.modelJson))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: LibraryPanelStatus defines the observed state of LibraryPanel.
            properties:
              atProvider:
                properties:
                  created:
                    description: |-
                      (String) Timestamp when the library panel was created.
                      Timestamp when the library panel was created.
                    type: string
                  dashboardIds:
                    description: |-
                      (List of Number) Numerical IDs of Grafana dashboards containing the library panel.
                      Numerical IDs of Grafana dashboards containing the library panel.
                    items:
                      type: number
                    type: array
                  description:
                    description: |-
                      (String) Description of the library panel.
                      Description of the library panel.
                    type: string
                  folderName:
                    description: |-
                      (String) Name of the folder containing the library panel.
                      Name of the folder containing the library panel.
                    type: string
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  panelId:
                    description: |-
                      (Number) The numeric ID of the library panel computed by Grafana.
                      The numeric ID of the library panel computed by Grafana.
                    type: number
                  type:
                    description: |-
                      (String) Type of the library panel (eg. text).
                      Type of the library panel (eg. text).
                    type: string
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                  updated:
                    description: |-
                      (String) Timestamp when the library panel was last modified.
                      Timestamp when the library panel was last modified.
                    type: string
                  version:
                    description: |-
                      (Number) Version of the library panel.
                      Version of the library panel.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizationpreferences.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OrganizationPreferences
    listKind: OrganizationPreferencesList
    plural: organizationpreferences
    singular: organizationpreferences
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrganizationPreferences is the Schema for the OrganizationPreferencess
          API. Official documentation https://grafana.com/docs/grafana/latest/administration/organization-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/preferences/#get-current-org-prefs
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationPreferencesSpec defines the desired state of
              OrganizationPreferences
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: OrganizationPreferencesStatus defines the observed state
              of OrganizationPreferences.
            properties:
              atProvider:
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizations.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Organization
    listKind: OrganizationList
    plural: organizations
    singular: organization
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Organization is the Schema for the Organizations API. Official
          documentation https://grafana.com/docs/grafana/latest/administration/organization-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/org/
          This resource represents an instance-scoped resource and uses Grafana's
          admin APIs. It does not work with API tokens or service accounts which are
          org-scoped. You must use basic auth. This resource is also not compatible
          with Grafana Cloud, as it does not allow basic auth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationSpec defines the desired state of Organization
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: OrganizationStatus defines the observed state of Organization.
            properties:
              atProvider:
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  orgId:
                    description: |-
                      (Number) The organization id assigned to this organization by Grafana.
                      The organization id assigned to this organization by Grafana.
                    type: number
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlists.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Playlist
    listKind: PlaylistList
    plural: playlists
    singular: playlist
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Playlist is the Schema for the Playlists API. Manages Grafana
          playlists. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/playlist/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistSpec defines the desired state of Playlist
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.interval is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.interval)
                || (has(self.initProvider) && has(self.initProvider.interval))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PlaylistStatus defines the observed state of Playlist.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        id:
                          description: (String) The ID of this resource.
                          type: string
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlistv0alpha1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PlaylistV0Alpha1
    listKind: PlaylistV0Alpha1List
    plural: playlistv0alpha1s
    singular: playlistv0alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlaylistV0Alpha1 is the Schema for the PlaylistV0Alpha1s API.
          Manages Grafana playlists using the new Grafana APIs. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/apis/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistV0Alpha1Spec defines the desired state of PlaylistV0Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: PlaylistV0Alpha1Status defines the observed state of PlaylistV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlistv1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PlaylistV1
    listKind: PlaylistV1List
    plural: playlistv1s
    singular: playlistv1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlaylistV1 is the Schema for the PlaylistV1s API. Manages Grafana
          playlists using the new Grafana APIs. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/apis/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistV1Spec defines the desired state of PlaylistV1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: PlaylistV1Status defines the observed state of PlaylistV1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: repositoryv0alpha1s.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RepositoryV0Alpha1
    listKind: RepositoryV0Alpha1List
    plural: repositoryv0alpha1s
    singular: repositoryv0alpha1
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RepositoryV0Alpha1 is the Schema for the RepositoryV0Alpha1s
          API. Manages Grafana Git Sync repositories for provisioning dashboards and
          related resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositoryV0Alpha1Spec defines the desired state of RepositoryV0Alpha1
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: RepositoryV0Alpha1Status defines the observed state of RepositoryV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: resources.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Resource
    listKind: ResourceList
    plural: resources
    singular: resource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Resource is the Schema for the Resources API.  The resource
          is still experimental; diffing semantics are subject to change - feedback
          welcome in https://github. This resource accepts a Kubernetes-style manifest
          as the single source of truth for the resource definition. Only namespaced
          App Platform kinds are supported. The provider autodiscovers the namespace
          from /bootdata on every operation. If autodiscovery does not find a cloud
          stack namespace, the provider falls back to the explicit stack_id and then
          org_id provider settings. Top-level manifest fields are limited to apiVersion,
          kind, metadata, spec, and the ignored status field. If metadata.namespace
          is configured, it must match the provider-selected namespace. Inside manifest.metadata,
          both Kubernetes name and uid are accepted as input aliases for the object
          identifier.11 or later. Reads refresh managed drift from the API. Import
          format:'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceSpec defines the desired state of Resource
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ResourceStatus defines the observed state of Resource.
            properties:
              atProvider:
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  id:
                    description: (String) The API resource UUID assigned by Grafana.
                      This is not used for import; import uses the object name stored
                      in metadata.uid.
                    type: string
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountpermissionitems.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountPermissionItem
    listKind: ServiceAccountPermissionItemList
    plural: serviceaccountpermissionitems
    singular: serviceaccountpermissionitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceAccountPermissionItem is the Schema for the ServiceAccountPermissionItems
          API. Manages a single permission item for a service account. Conflicts with
          the "grafana_service_account_permission" resource which manages the entire
          set of permissions for a service account. Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/#manage-users-and-teams-permissions-for-a-service-account-in-grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountPermissionItemSpec defines the desired state
              of ServiceAccountPermissionItem
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
          status:
            description: ServiceAccountPermissionItemStatus defines the observed state
              of ServiceAccountPermissionItem.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountpermissions.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountPermission
    listKind: ServiceAccountPermissionList
    plural: serviceaccountpermissions
    singular: serviceaccountpermission
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountPermission is the Schema for the ServiceAccountPermissions
          API. Manages the entire set of permissions for a service account. Permissions
          that aren''t specified when applying this resource will be removed. Note:
          This resource is available from Grafana 9.2.4 onwards. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/#manage-users-and-teams-permissions-for-a-service-account-in-grafana'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountPermissionSpec defines the desired state of
              ServiceAccountPermission
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ServiceAccountPermissionStatus defines the observed state
              of ServiceAccountPermission.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountrotatingtokens.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountRotatingToken
    listKind: ServiceAccountRotatingTokenList
    plural: serviceaccountrotatingtokens
    singular: serviceaccountrotatingtoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountRotatingToken is the Schema for the ServiceAccountRotatingTokens
          API. Note: This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountRotatingTokenSpec defines the desired state
              of ServiceAccountRotatingToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.earlyRotationWindowSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindowSeconds)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindowSeconds))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.secondsToLive is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.secondsToLive)
                || (has(self.initProvider) && has(self.initProvider.secondsToLive))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
          status:
            description: ServiceAccountRotatingTokenStatus defines the observed state
              of ServiceAccountRotatingToken.
            properties:
              atProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      and will have characters appended to it to make the name unique.
                      The name of the service account token. It will start with `<name_prefix>-` and will have characters appended to it to make the name unique.
                    type: string
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the service account token is expired or within the period to be early rotated.
                      Signals that the service account token is expired or within the period to be early rotated.
                    type: boolean
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccounts.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccount
    listKind: ServiceAccountList
    plural: serviceaccounts
    singular: serviceaccount
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccount is the Schema for the ServiceAccounts API. Note:
          This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountSpec defines the desired state of ServiceAccount
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
          status:
            description: ServiceAccountStatus defines the observed state of ServiceAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccounttokens.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountToken
    listKind: ServiceAccountTokenList
    plural: serviceaccounttokens
    singular: serviceaccounttoken
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountToken is the Schema for the ServiceAccountTokens
          API. Note: This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountTokenSpec defines the desired state of ServiceAccountToken
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServiceAccountTokenStatus defines the observed state of ServiceAccountToken.
            properties:
              atProvider:
                properties:
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ssosettings.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SsoSettings
    listKind: SsoSettingsList
    plural: ssosettings
    singular: ssosettings
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SsoSettings is the Schema for the SsoSettingss API. Manages Grafana
          SSO Settings for OAuth2, SAML and LDAP. Support for LDAP is currently in
          preview, it will be available in Grafana starting with v11.3. Official documentation
          https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/sso-settings/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SsoSettingsSpec defines the desired state of SsoSettings
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    bindPasswordSecretRef:
                                      description: |-
                                        (String, Sensitive) The search user bind password.
                                        The search user bind password.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) The path to the client private key.
                                        The path to the client private key.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    clientKeyValueSecretRef:
                                      description: |-
                                        (String, Sensitive) The Base64 encoded value of the client private key.
                                        The Base64 encoded value of the client private key.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecretSecretRef:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        certificateSecretRef:
                          description: |-
                            encoded string for the SP X.509 certificate.
                            Base64-encoded string for the SP X.509 certificate.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        privateKeySecretRef:
                          description: |-
                            encoded string for the SP private key.
                            Base64-encoded string for the SP private key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    bindPasswordSecretRef:
                                      description: |-
                                        (String, Sensitive) The search user bind password.
                                        The search user bind password.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) The path to the client private key.
                                        The path to the client private key.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    clientKeyValueSecretRef:
                                      description: |-
                                        (String, Sensitive) The Base64 encoded value of the client private key.
                                        The Base64 encoded value of the client private key.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecretSecretRef:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        certificateSecretRef:
                          description: |-
                            encoded string for the SP X.509 certificate.
                            Base64-encoded string for the SP X.509 certificate.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        privateKeySecretRef:
                          description: |-
                            encoded string for the SP private key.
                            Base64-encoded string for the SP private key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.providerName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.providerName)
                || (has(self.initProvider) && has(self.initProvider.providerName))'
          status:
            description: SsoSettingsStatus defines the observed state of SsoSettings.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teams.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Team
    listKind: TeamList
    plural: teams
    singular: team
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Team is the Schema for the Teams API. Official documentation
          https://grafana.com/docs/grafana/latest/administration/team-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamSpec defines the desired state of Team
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  memberRefs:
                    description: References to User in oss to populate members.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  memberSelector:
                    description: Selector for a list of User in oss to populate members.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  memberRefs:
                    description: References to User in oss to populate members.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  memberSelector:
                    description: Selector for a list of User in oss to populate members.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: TeamStatus defines the observed state of Team.
            properties:
              atProvider:
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (Number) The team id assigned to this team by Grafana.
                      The team id assigned to this team by Grafana.
                    type: number
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  teamUid:
                    description: |-
                      (String) The team uid assigned to this team by Grafana.
                      The team uid assigned to this team by Grafana.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.oss.grafana.crossplane.io
spec:
  group: oss.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the Users API. Official documentation
          https://grafana.com/docs/grafana/latest/administration/user-management/server-user-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/user/
          This resource represents an instance-scoped resource and uses Grafana's
          admin APIs. It does not work with API tokens or service accounts which are
          org-scoped. You must use basic auth. This resource is also not compatible
          with Grafana Cloud, as it does not allow basic auth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password for the Grafana user.
                      The password for the Grafana user.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password for the Grafana user.
                      The password for the Grafana user.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - passwordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.email is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.email)
                || (has(self.initProvider) && has(self.initProvider.email))'
            - message: spec.forProvider.passwordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.passwordSecretRef)'
          status:
            description: UserStatus defines the observed state of User.
            properties:
              atProvider:
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  userId:
                    description: |-
                      (Number) The numerical ID of the Grafana user.
                      The numerical ID of the Grafana user.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: annotations.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Annotation
    listKind: AnnotationList
    plural: annotations
    singular: annotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Annotation is the Schema for the Annotations API. Manages Grafana
          annotations. Official documentation https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/annotate-visualizations/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/annotations/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnnotationSpec defines the desired state of Annotation
            properties:
              forProvider:
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.text is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.text)
                || (has(self.initProvider) && has(self.initProvider.text))'
          status:
            description: AnnotationStatus defines the observed state of Annotation.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard on which to create the annotation.
                      The UID of the dashboard on which to create the annotation.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  panelId:
                    description: |-
                      (Number) The ID of the dashboard panel on which to create the annotation.
                      The ID of the dashboard panel on which to create the annotation.
                    type: number
                  tags:
                    description: |-
                      (Set of String) The tags to associate with the annotation.
                      The tags to associate with the annotation.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  text:
                    description: |-
                      (String) The text to associate with the annotation.
                      The text to associate with the annotation.
                    type: string
                  time:
                    description: |-
                      formatted time string indicating the annotation's time.
                      The RFC 3339-formatted time string indicating the annotation's time.
                    type: string
                  timeEnd:
                    description: |-
                      formatted time string indicating the annotation's end time.
                      The RFC 3339-formatted time string indicating the annotation's end time.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: connectionv0alpha1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ConnectionV0Alpha1
    listKind: ConnectionV0Alpha1List
    plural: connectionv0alpha1s
    singular: connectionv0alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConnectionV0Alpha1 is the Schema for the ConnectionV0Alpha1s
          API. Manages Grafana Git Sync connections used by repositories for provider
          authentication.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionV0Alpha1Spec defines the desired state of ConnectionV0Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: ConnectionV0Alpha1Status defines the observed state of ConnectionV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      privateKey:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Private key for GitHub App authentication.
                          Private key for GitHub App authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Access token.
                          Access token.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      description:
                        description: |-
                          (String) Connection description.
                          Connection description.
                        type: string
                      github:
                        description: |-
                          (Block, Optional) GitHub App configuration. (see below for nested schema)
                          GitHub App configuration.
                        properties:
                          appId:
                            description: |-
                              (String) GitHub App ID.
                              GitHub App ID.
                            type: string
                          installationId:
                            description: |-
                              (String) GitHub App installation ID.
                              GitHub App installation ID.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Connection provider type.
                          Connection provider type.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          Provider URL.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpermissionitems.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPermissionItem
    listKind: DashboardPermissionItemList
    plural: dashboardpermissionitems
    singular: dashboardpermissionitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardPermissionItem is the Schema for the DashboardPermissionItems
          API. Manages a single permission item for a dashboard. Conflicts with the
          "grafana_dashboard_permission" resource which manages the entire set of
          permissions for a dashboard.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPermissionItemSpec defines the desired state of
              DashboardPermissionItem
            properties:
              forProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dashboardUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dashboardUid)
                || (has(self.initProvider) && has(self.initProvider.dashboardUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: DashboardPermissionItemStatus defines the observed state
              of DashboardPermissionItem.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) The UID of the dashboard.
                      The UID of the dashboard.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpermissions.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPermission
    listKind: DashboardPermissionList
    plural: dashboardpermissions
    singular: dashboardpermission
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardPermission is the Schema for the DashboardPermissions
          API. Manages the entire set of permissions for a dashboard. Permissions
          that aren't specified when applying this resource will be removed. Official
          documentation https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/dashboard_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPermissionSpec defines the desired state of DashboardPermission
            properties:
              forProvider:
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DashboardPermissionStatus defines the observed state of DashboardPermission.
            properties:
              atProvider:
                properties:
                  dashboardUid:
                    description: |-
                      (String) UID of the dashboard to apply permissions to.
                      UID of the dashboard to apply permissions to.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardpublics.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardPublic
    listKind: DashboardPublicList
    plural: dashboardpublics
    singular: dashboardpublic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'DashboardPublic is the Schema for the DashboardPublics API.
          Manages Grafana public dashboards. Note: This resource is available only
          with Grafana 10.2+. Official documentation https://grafana.com/docs/grafana/latest/dashboards/share-dashboards-panels/shared-dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/dashboard_public/'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardPublicSpec defines the desired state of DashboardPublic
            properties:
              forProvider:
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardRef:
                    description: Reference to a Dashboard in oss to populate dashboardUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  dashboardSelector:
                    description: Selector for a Dashboard in oss to populate dashboardUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DashboardPublicStatus defines the observed state of DashboardPublic.
            properties:
              atProvider:
                properties:
                  accessToken:
                    description: |-
                      (String) A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                      A public unique identifier of a public dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                  annotationsEnabled:
                    description: |-
                      (Boolean) Set to true to show annotations. The default value is false.
                      Set to `true` to show annotations. The default value is `false`.
                    type: boolean
                  dashboardUid:
                    description: |-
                      (String) The unique identifier of the original dashboard.
                      The unique identifier of the original dashboard.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isEnabled:
                    description: |-
                      (Boolean) Set to true to enable the public dashboard. The default value is false.
                      Set to `true` to enable the public dashboard. The default value is `false`.
                    type: boolean
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  share:
                    description: |-
                      (String) Set the share mode. The default value is public.
                      Set the share mode. The default value is `public`.
                    type: string
                  timeSelectionEnabled:
                    description: |-
                      (Boolean) Set to true to enable the time picker in the public dashboard. The default value is false.
                      Set to `true` to enable the time picker in the public dashboard. The default value is `false`.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                      The unique identifier of a public dashboard. It's automatically generated if not provided when creating a public dashboard.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboards.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Dashboard
    listKind: DashboardList
    plural: dashboards
    singular: dashboard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dashboard is the Schema for the Dashboards API. Manages Grafana
          dashboards. Official documentation https://grafana.com/docs/grafana/latest/dashboards/HTTP
          API (legacy API, recommended for Grafana 12 or earlier) https://grafana.com/docs/grafana/v11.6/developers/http_api/dashboard/HTTP
          API (new Kubernetes-style API, recommended for Grafana 13 and later) https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardSpec defines the desired state of Dashboard
            properties:
              forProvider:
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  folderRef:
                    description: Reference to a Folder in oss to populate folder.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folder.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  folderRef:
                    description: Reference to a Folder in oss to populate folder.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folder.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.configJson is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.configJson)
                || (has(self.initProvider) && has(self.initProvider.configJson))'
          status:
            description: DashboardStatus defines the observed state of Dashboard.
            properties:
              atProvider:
                properties:
                  configJson:
                    description: |-
                      (String) The complete dashboard model JSON.
                      The complete dashboard model JSON.

                      Starting with Grafana v13, use the resource corresponding to your dashboard's API version for Kubernetes-style dashboards.

                      If you decide to use this legacy resource with a Kubernetes-style dashboard definition:
                      - In Grafana v12, provide the "spec" field of the dashboard definition.
                      - In Grafana v13 and later, provide the full Kubernetes-style dashboard JSON (including "apiVersion", "kind", "metadata", and "spec").
                    type: string
                  dashboardId:
                    description: |-
                      (Number) The numeric ID of the dashboard computed by Grafana.
                      The numeric ID of the dashboard computed by Grafana.
                    type: number
                  folder:
                    description: |-
                      (String) The id or UID of the folder to save the dashboard in.
                      The id or UID of the folder to save the dashboard in.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  message:
                    description: |-
                      (String) Set a commit message for the version history.
                      Set a commit message for the version history.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  overwrite:
                    description: |-
                      (Boolean) Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                      Set to true if you want to overwrite existing dashboard with newer version, same dashboard title in folder or same dashboard uid.
                    type: boolean
                  uid:
                    description: |-
                      (String) The unique identifier of a dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a dashboard. The uid allows having consistent URLs for accessing dashboards and when syncing dashboards between multiple Grafana installs.
                      The unique identifier of a dashboard. This is used to construct its URL. It's automatically generated if not provided when creating a dashboard. The uid allows having consistent URLs for accessing dashboards and when syncing dashboards between multiple Grafana installs.
                    type: string
                  url:
                    description: |-
                      (String) The full URL of the dashboard.
                      The full URL of the dashboard.
                    type: string
                  version:
                    description: |-
                      (Number) Whenever you save a version of your dashboard, a copy of that version is saved so that previous versions of your dashboard are not lost.
                      Whenever you save a version of your dashboard, a copy of that version is saved so that previous versions of your dashboard are not lost.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv1beta1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV1Beta1
    listKind: DashboardV1Beta1List
    plural: dashboardv1beta1s
    singular: dashboardv1beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV1Beta1 is the Schema for the DashboardV1Beta1s API.
          Manages Grafana dashboards using the new Grafana APIs. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/HTTP API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV1Beta1Spec defines the desired state of DashboardV1Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV1Beta1Status defines the observed state of DashboardV1Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard spec.
                          The JSON representation of the dashboard spec.
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv2beta1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV2Beta1
    listKind: DashboardV2Beta1List
    plural: dashboardv2beta1s
    singular: dashboardv2beta1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV2Beta1 is the Schema for the DashboardV2Beta1s API.
          Manages Grafana dashboards using the v2beta1 (Dynamic Dashboards) schema.
          Official documentation https://grafana.com/docs/grafana/latest/dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV2Beta1Spec defines the desired state of DashboardV2Beta1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV2Beta1Status defines the observed state of DashboardV2Beta1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2beta1 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardv2s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DashboardV2
    listKind: DashboardV2List
    plural: dashboardv2s
    singular: dashboardv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardV2 is the Schema for the DashboardV2s API. Manages Grafana
          dashboards using the v2 (Dynamic Dashboards) schema. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/HTTP API https://grafana.com/docs/grafana/latest/developers/http_api/dashboard/#new-dashboard-apis
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DashboardV2Spec defines the desired state of DashboardV2
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: DashboardV2Status defines the observed state of DashboardV2.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      allowUiUpdates:
                        description: |-
                          (Boolean) Set to true to allow editing the resource from the Grafana UI.
                          Set to true to allow editing the resource from the Grafana UI.
                        type: boolean
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      json:
                        description: |-
                          (String) The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                          The JSON representation of the dashboard v2 spec. Must be the spec object only — not the full Kubernetes envelope. Use: json = jsonencode(jsondecode(file("dashboard.json")).spec)
                        type: string
                      tags:
                        description: |-
                          (List of String) The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                          The tags of the dashboard. If not set, the tags will be derived from the JSON spec.
                        items:
                          type: string
                        type: array
                      title:
                        description: |-
                          (String) The title of the dashboard. If not set, the title will be derived from the JSON spec.
                          The title of the dashboard. If not set, the title will be derived from the JSON spec.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasourceconfigs.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSourceConfig
    listKind: DataSourceConfigList
    plural: datasourceconfigs
    singular: datasourceconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSourceConfig is the Schema for the DataSourceConfigs API.
          Official documentation https://grafana.com/docs/grafana/latest/datasources/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/data_source/
          The required arguments for this resource vary depending on the type of data
          source selected (via the 'type' argument). Use this resource for configuring
          multiple datasources, when that configuration (json_data_encoded field)
          requires circular references like in the example below. When using the
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceConfigSpec defines the desired state of DataSourceConfig
            properties:
              forProvider:
                properties:
                  httpHeadersSecretRef:
                    description: |-
                      (Map of String, Sensitive) Custom HTTP headers
                      Custom HTTP headers
                    properties:
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - name
                    type: object
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  httpHeadersSecretRef:
                    additionalProperties:
                      type: string
                    type: object
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DataSourceConfigStatus defines the observed state of DataSourceConfig.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasources.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    singular: datasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSource is the Schema for the DataSources API. Official documentation
          https://grafana.com/docs/grafana/latest/datasources/HTTP API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/data_source/
          The required arguments for this resource vary depending on the type of data
          source selected (via the 'type' argument).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSourceSpec defines the desired state of DataSource
            properties:
              forProvider:
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  httpHeadersSecretRef:
                    description: |-
                      (Map of String, Sensitive) Custom HTTP headers
                      Custom HTTP headers
                    properties:
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - name
                    type: object
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  httpHeadersSecretRef:
                    additionalProperties:
                      type: string
                    type: object
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  secureJsonDataEncodedSecretRef:
                    description: |-
                      (String, Sensitive) Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the secure json data. This attribute can be used to pass secure configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: DataSourceStatus defines the observed state of DataSource.
            properties:
              atProvider:
                properties:
                  accessMode:
                    description: |-
                      (String) The method by which Grafana will access the data source: proxy or direct. Defaults to proxy.
                      The method by which Grafana will access the data source: `proxy` or `direct`. Defaults to `proxy`.
                    type: string
                  basicAuthEnabled:
                    description: |-
                      (Boolean) Whether to enable basic auth for the data source. Defaults to false.
                      Whether to enable basic auth for the data source. Defaults to `false`.
                    type: boolean
                  basicAuthUsername:
                    description: |-
                      (String) Basic auth username. Defaults to “.
                      Basic auth username. Defaults to “.
                    type: string
                  databaseName:
                    description: |-
                      (String)  The name of the database to use on the selected data source server. Defaults to “.
                      (Required by some data source types) The name of the database to use on the selected data source server. Defaults to “.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Whether to set the data source as default. This should only be true to a single data source. Defaults to false.
                      Whether to set the data source as default. This should only be `true` to a single data source. Defaults to `false`.
                    type: boolean
                  jsonDataEncoded:
                    description: |-
                      (String) Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                      Serialized JSON string containing the json data. This attribute can be used to pass configuration options to the data source. To figure out what options a datasource has available, see its docs or inspect the network data when saving it from the Grafana UI. Note that keys in this map are usually camelCased.
                    type: string
                  name:
                    description: |-
                      (String) A unique name for the data source.
                      A unique name for the data source.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  privateDataSourceConnectNetworkId:
                    description: |-
                      (String) (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                      (Can only be used with data sources in Grafana Cloud) The ID of the Private Data source Connect network to use with this data source. Defaults to “.
                    type: string
                  type:
                    description: |-
                      (String) The data source type. Must be one of the supported data source keywords.
                      The data source type. Must be one of the supported data source keywords.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier. If unset, this will be automatically generated.
                      Unique identifier. If unset, this will be automatically generated.
                    type: string
                  url:
                    description: |-
                      (String) The URL for the data source. The type of URL required varies depending on the chosen data source type.
                      The URL for the data source. The type of URL required varies depending on the chosen data source type.
                    type: string
                  username:
                    description: |-
                      (String)  The username to use to authenticate to the data source. Defaults to “.
                      (Required by some data source types) The username to use to authenticate to the data source. Defaults to “.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folderpermissionitems.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: FolderPermissionItem
    listKind: FolderPermissionItemList
    plural: folderpermissionitems
    singular: folderpermissionitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FolderPermissionItem is the Schema for the FolderPermissionItems
          API. Manages a single permission item for a folder. Conflicts with the "grafana_folder_permission"
          resource which manages the entire set of permissions for a folder.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderPermissionItemSpec defines the desired state of FolderPermissionItem
            properties:
              forProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.folderUid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.folderUid)
                || (has(self.initProvider) && has(self.initProvider.folderUid))'
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
          status:
            description: FolderPermissionItemStatus defines the observed state of
              FolderPermissionItem.
            properties:
              atProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  role:
                    description: |-
                      (String) the role onto which the permission is to be assigned
                      the role onto which the permission is to be assigned
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folderpermissions.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: FolderPermission
    listKind: FolderPermissionList
    plural: folderpermissions
    singular: folderpermission
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FolderPermission is the Schema for the FolderPermissions API.
          Manages the entire set of permissions for a folder. Permissions that aren't
          specified when applying this resource will be removed. Official documentation
          https://grafana.com/docs/grafana/latest/administration/roles-and-permissions/access-control/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/folder_permissions/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderPermissionSpec defines the desired state of FolderPermission
            properties:
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: FolderPermissionStatus defines the observed state of FolderPermission.
            properties:
              atProvider:
                properties:
                  folderUid:
                    description: |-
                      (String) The UID of the folder.
                      The UID of the folder.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Options: View, Edit, Admin.
                            Permission to associate with item. Options: View, Edit, Admin.
                          type: string
                        role:
                          description: |-
                            (String) Name of the basic role to manage permissions for. Options: Viewer, Editor or Admin.
                            Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for.
                            ID of the team to manage permissions for.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for.
                            ID of the user or service account to manage permissions for.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folders.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Folder
    listKind: FolderList
    plural: folders
    singular: folder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Folder is the Schema for the Folders API. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/manage-dashboards/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/folder/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FolderSpec defines the desired state of Folder
            properties:
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate parentFolderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate parentFolderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate parentFolderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate parentFolderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.title is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.title)
                || (has(self.initProvider) && has(self.initProvider.title))'
          status:
            description: FolderStatus defines the observed state of Folder.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  parentFolderUid:
                    description: |-
                      (String) The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                      The uid of the parent folder. If set, the folder will be nested. If not set, the folder will be created in the root folder. Note: This requires the nestedFolders feature flag to be enabled on your Grafana instance.
                    type: string
                  preventDestroyIfNotEmpty:
                    description: |-
                      (Boolean) Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to false.
                      Prevent deletion of the folder if it is not empty (contains dashboards or alert rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
                    type: boolean
                  title:
                    description: |-
                      (String) The title of the folder.
                      The title of the folder.
                    type: string
                  uid:
                    description: |-
                      (String) Unique identifier.
                      Unique identifier.
                    type: string
                  url:
                    description: |-
                      (String) The full URL of the folder.
                      The full URL of the folder.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: librarypanels.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: LibraryPanel
    listKind: LibraryPanelList
    plural: librarypanels
    singular: librarypanel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LibraryPanel is the Schema for the LibraryPanels API. Manages
          Grafana library panels. Official documentation https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/manage-library-panels/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/library_element/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LibraryPanelSpec defines the desired state of LibraryPanel
            properties:
              forProvider:
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  folderRef:
                    description: Reference to a Folder in oss to populate folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: Selector for a Folder in oss to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.modelJson is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelJson)
                || (has(self.initProvider) && has(self.initProvider.modelJson))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: LibraryPanelStatus defines the observed state of LibraryPanel.
            properties:
              atProvider:
                properties:
                  created:
                    description: |-
                      (String) Timestamp when the library panel was created.
                      Timestamp when the library panel was created.
                    type: string
                  dashboardIds:
                    description: |-
                      (List of Number) Numerical IDs of Grafana dashboards containing the library panel.
                      Numerical IDs of Grafana dashboards containing the library panel.
                    items:
                      type: number
                    type: array
                  description:
                    description: |-
                      (String) Description of the library panel.
                      Description of the library panel.
                    type: string
                  folderName:
                    description: |-
                      (String) Name of the folder containing the library panel.
                      Name of the folder containing the library panel.
                    type: string
                  folderUid:
                    description: |-
                      (String) Unique ID (UID) of the folder containing the library panel.
                      Unique ID (UID) of the folder containing the library panel.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  modelJson:
                    description: |-
                      (String) The JSON model for the library panel.
                      The JSON model for the library panel.
                    type: string
                  name:
                    description: |-
                      (String) Name of the library panel.
                      Name of the library panel.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  panelId:
                    description: |-
                      (Number) The numeric ID of the library panel computed by Grafana.
                      The numeric ID of the library panel computed by Grafana.
                    type: number
                  type:
                    description: |-
                      (String) Type of the library panel (eg. text).
                      Type of the library panel (eg. text).
                    type: string
                  uid:
                    description: |-
                      (String) The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                      The unique identifier (UID) of a library panel uniquely identifies library panels between multiple Grafana installs. It’s automatically generated unless you specify it during library panel creation.The UID provides consistent URLs for accessing library panels and when syncing library panels between multiple Grafana installs.
                    type: string
                  updated:
                    description: |-
                      (String) Timestamp when the library panel was last modified.
                      Timestamp when the library panel was last modified.
                    type: string
                  version:
                    description: |-
                      (Number) Version of the library panel.
                      Version of the library panel.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizationpreferences.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: OrganizationPreferences
    listKind: OrganizationPreferencesList
    plural: organizationpreferences
    singular: organizationpreferences
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrganizationPreferences is the Schema for the OrganizationPreferencess
          API. Official documentation https://grafana.com/docs/grafana/latest/administration/organization-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/preferences/#get-current-org-prefs
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationPreferencesSpec defines the desired state of
              OrganizationPreferences
            properties:
              forProvider:
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: OrganizationPreferencesStatus defines the observed state
              of OrganizationPreferences.
            properties:
              atProvider:
                properties:
                  homeDashboardUid:
                    description: |-
                      (String) The Organization home dashboard UID. This is only available in Grafana 9.0+.
                      The Organization home dashboard UID. This is only available in Grafana 9.0+.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  theme:
                    description: |-
                      (String) The Organization theme. Any string value is supported, including custom themes. Common values are light, dark, system, or an empty string for the default.
                      The Organization theme. Any string value is supported, including custom themes. Common values are `light`, `dark`, `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: |-
                      (String) The Organization timezone. Any string value is supported, including IANA timezone names. Common values are utc, browser, or an empty string for the default.
                      The Organization timezone. Any string value is supported, including IANA timezone names. Common values are `utc`, `browser`, or an empty string for the default.
                    type: string
                  weekStart:
                    description: |-
                      (String) The Organization week start day. Available values are sunday, monday, saturday, or an empty string for the default. Defaults to “.
                      The Organization week start day. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default. Defaults to “.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizations.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Organization
    listKind: OrganizationList
    plural: organizations
    singular: organization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Organization is the Schema for the Organizations API. Official
          documentation https://grafana.com/docs/grafana/latest/administration/organization-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/org/
          This resource represents an instance-scoped resource and uses Grafana's
          admin APIs. It does not work with API tokens or service accounts which are
          org-scoped. You must use basic auth. This resource is also not compatible
          with Grafana Cloud, as it does not allow basic auth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OrganizationSpec defines the desired state of Organization
            properties:
              forProvider:
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: OrganizationStatus defines the observed state of Organization.
            properties:
              atProvider:
                properties:
                  adminUser:
                    description: |-
                      (String) The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to admin.
                      The login name of the configured default admin user for the Grafana
                      installation. If unset, this value defaults to admin, the Grafana default.
                      Defaults to `admin`.
                    type: string
                  admins:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given admin
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  createUsers:
                    description: |-
                      (Boolean) Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to true.
                      Whether or not to create Grafana users specified in the organization's
                      membership if they don't already exist in Grafana. If unspecified, this
                      parameter defaults to true, creating placeholder users with the name, login,
                      and email set to the email of the user, and a random password. Setting this
                      option to false will cause an error to be thrown for any users that do not
                      already exist in Grafana.
                      Defaults to `true`.
                    type: boolean
                  editors:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given editor
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana organization created.
                      The display name for the Grafana organization created.
                    type: string
                  orgId:
                    description: |-
                      (Number) The organization id assigned to this organization by Grafana.
                      The organization id assigned to this organization by Grafana.
                    type: number
                  usersWithoutAccess:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                      A list of email addresses corresponding to users who should be given none access to the organization.
                      Note: users specified here must already exist in Grafana, unless 'create_users' is
                      set to true. This feature is only available in Grafana 10.2+.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  viewers:
                    description: |-
                      (Set of String) A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                      A list of email addresses corresponding to users who should be given viewer
                      access to the organization. Note: users specified here must already exist in
                      Grafana unless 'create_users' is set to true.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlists.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Playlist
    listKind: PlaylistList
    plural: playlists
    singular: playlist
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Playlist is the Schema for the Playlists API. Manages Grafana
          playlists. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/playlist/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistSpec defines the desired state of Playlist
            properties:
              forProvider:
                properties:
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.interval is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.interval)
                || (has(self.initProvider) && has(self.initProvider.interval))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: PlaylistStatus defines the observed state of Playlist.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  interval:
                    description: (String)
                    type: string
                  item:
                    description: (Block Set) (see below for nested schema)
                    items:
                      properties:
                        id:
                          description: (String) The ID of this resource.
                          type: string
                        order:
                          description: (Number)
                          type: number
                        type:
                          description: (String)
                          type: string
                        value:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the playlist.
                      The name of the playlist.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlistv0alpha1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PlaylistV0Alpha1
    listKind: PlaylistV0Alpha1List
    plural: playlistv0alpha1s
    singular: playlistv0alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlaylistV0Alpha1 is the Schema for the PlaylistV0Alpha1s API.
          Manages Grafana playlists using the new Grafana APIs. Official documentation
          https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/apis/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistV0Alpha1Spec defines the desired state of PlaylistV0Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: PlaylistV0Alpha1Status defines the observed state of PlaylistV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: playlistv1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: PlaylistV1
    listKind: PlaylistV1List
    plural: playlistv1s
    singular: playlistv1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlaylistV1 is the Schema for the PlaylistV1s API. Manages Grafana
          playlists using the new Grafana APIs. Official documentation https://grafana.com/docs/grafana/latest/dashboards/create-manage-playlists/HTTP
          API https://grafana.com/docs/grafana/latest/developers/http_api/apis/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlaylistV1Spec defines the desired state of PlaylistV1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: PlaylistV1Status defines the observed state of PlaylistV1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      interval:
                        description: |-
                          (String) The interval of the playlist.
                          The interval of the playlist.
                        type: string
                      items:
                        description: |-
                          (List of Object) The items of the playlist. (see below for nested schema)
                          The items of the playlist.
                        items:
                          properties:
                            type:
                              description: (String)
                              type: string
                            value:
                              description: (String)
                              type: string
                          type: object
                        type: array
                      title:
                        description: |-
                          (String) The title of the playlist.
                          The title of the playlist.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: repositoryv0alpha1s.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: RepositoryV0Alpha1
    listKind: RepositoryV0Alpha1List
    plural: repositoryv0alpha1s
    singular: repositoryv0alpha1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RepositoryV0Alpha1 is the Schema for the RepositoryV0Alpha1s
          API. Manages Grafana Git Sync repositories for provisioning dashboards and
          related resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositoryV0Alpha1Spec defines the desired state of RepositoryV0Alpha1
            properties:
              forProvider:
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadata is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadata)
                || (has(self.initProvider) && has(self.initProvider.metadata))'
            - message: spec.forProvider.spec is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spec)
                || (has(self.initProvider) && has(self.initProvider.spec))'
          status:
            description: RepositoryV0Alpha1Status defines the observed state of RepositoryV0Alpha1.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of the resource derived from UUID.
                    type: string
                  metadata:
                    description: |-
                      (Block, Optional) The metadata of the resource. (see below for nested schema)
                      The metadata of the resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          (Map of String) Annotations of the resource.
                          Annotations of the resource.
                        type: object
                        x-kubernetes-map-type: granular
                      folderUid:
                        description: |-
                          (String) The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                          The UID of the folder to save the resource in. For example, it's supported for dashboards and folders. To know if it's supported for the specific resource you're using check the documentation.
                        type: string
                      uid:
                        description: |-
                          (String) The unique identifier of the resource.
                          The unique identifier of the resource.
                        type: string
                      url:
                        description: |-
                          (String) The full URL of the resource.
                          The full URL of the resource.
                        type: string
                      uuid:
                        description: |-
                          (String) The globally unique identifier of a resource, used by the API for tracking.
                          The globally unique identifier of a resource, used by the API for tracking.
                        type: string
                      version:
                        description: |-
                          (String) The version of the resource.
                          The version of the resource.
                        type: string
                    type: object
                  options:
                    description: |-
                      (Block, Optional) Options for applying the resource. (see below for nested schema)
                      Options for applying the resource.
                    properties:
                      managerIdentity:
                        description: Override the identity stamped on this resource's
                          manager metadata.
                        type: string
                      overwrite:
                        description: |-
                          (Boolean) Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                          Set to true if you want to overwrite existing resource with newer version, same resource title in folder or same resource uid.
                        type: boolean
                    type: object
                  secure:
                    description: |-
                      (see below for nested schema)
                      Sensitive credentials.
                    properties:
                      token:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Token for repository authentication.
                          Token for repository authentication.
                        type: object
                        x-kubernetes-map-type: granular
                      webhookSecret:
                        additionalProperties:
                          type: string
                        description: |-
                          only) Webhook secret.
                          Webhook secret.
                        type: object
                        x-kubernetes-map-type: granular
                    type: object
                  secureVersion:
                    description: |-
                      application of secure values.
                      Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
                    type: number
                  spec:
                    description: |-
                      (Block, Optional) The spec of the resource. (see below for nested schema)
                      The spec of the resource.
                    properties:
                      bitbucket:
                        description: |-
                          (Block, Optional) Bitbucket repository configuration. (see below for nested schema)
                          Bitbucket repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      connection:
                        description: |-
                          (Block, Optional) Connection resource reference. (see below for nested schema)
                          Connection resource reference.
                        properties:
                          name:
                            description: |-
                              (String) Connection resource name.
                              Connection resource name.
                            type: string
                        type: object
                      description:
                        description: |-
                          (String) Repository description.
                          Repository description.
                        type: string
                      git:
                        description: |-
                          (Block, Optional) Generic git repository configuration. (see below for nested schema)
                          Generic git repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          tokenUser:
                            description: |-
                              (String) Username for PAT auth.
                              Username for PAT auth.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      github:
                        description: |-
                          (Block, Optional) GitHub repository configuration. (see below for nested schema)
                          GitHub repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          generateDashboardPreviews:
                            description: |-
                              (Boolean) Whether to generate dashboard previews.
                              Whether to generate dashboard previews.
                            type: boolean
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      gitlab:
                        description: |-
                          (Block, Optional) GitLab repository configuration. (see below for nested schema)
                          GitLab repository configuration.
                        properties:
                          branch:
                            description: |-
                              (String) Branch to sync.
                              Branch to sync.
                            type: string
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Optional subdirectory path.
                            type: string
                          url:
                            description: |-
                              (String) The full URL of the resource.
                              Repository URL.
                            type: string
                        type: object
                      local:
                        description: |-
                          (Block, Optional) Local filesystem repository configuration. (see below for nested schema)
                          Local filesystem repository configuration.
                        properties:
                          path:
                            description: |-
                              (String) Optional subdirectory path.
                              Filesystem path.
                            type: string
                        type: object
                      sync:
                        description: |-
                          (Block, Optional) Sync configuration. (see below for nested schema)
                          Sync configuration.
                        properties:
                          enabled:
                            description: |-
                              (Boolean) Whether sync is enabled.
                              Whether sync is enabled.
                            type: boolean
                          intervalSeconds:
                            description: |-
                              (Number) Sync interval in seconds.
                              Sync interval in seconds.
                            type: number
                          target:
                            description: |-
                              (String) Sync target: instance or folder.
                              Sync target: instance or folder.
                            type: string
                        type: object
                      title:
                        description: |-
                          (String) Display name shown in the UI.
                          Display name shown in the UI.
                        type: string
                      type:
                        description: |-
                          (String) Repository provider type: local, github, git, bitbucket, or gitlab.
                          Repository provider type: local, github, git, bitbucket, or gitlab.
                        type: string
                      webhook:
                        description: |-
                          (Block, Optional) Webhook delivery configuration. (see below for nested schema)
                          Webhook delivery configuration.
                        properties:
                          baseUrl:
                            description: |-
                              (String) Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                              Optional public webhook base URL override used when incoming webhook delivery must target a different host than the Grafana UI URL.
                            type: string
                        type: object
                      workflows:
                        description: |-
                          (List of String) Allowed change workflows: write, branch.
                          Allowed change workflows: write, branch.
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: resources.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Resource
    listKind: ResourceList
    plural: resources
    singular: resource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Resource is the Schema for the Resources API.  The resource
          is still experimental; diffing semantics are subject to change - feedback
          welcome in https://github. This resource accepts a Kubernetes-style manifest
          as the single source of truth for the resource definition. Only namespaced
          App Platform kinds are supported. The provider autodiscovers the namespace
          from /bootdata on every operation. If autodiscovery does not find a cloud
          stack namespace, the provider falls back to the explicit stack_id and then
          org_id provider settings. Top-level manifest fields are limited to apiVersion,
          kind, metadata, spec, and the ignored status field. If metadata.namespace
          is configured, it must match the provider-selected namespace. Inside manifest.metadata,
          both Kubernetes name and uid are accepted as input aliases for the object
          identifier.11 or later. Reads refresh managed drift from the API. Import
          format:'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceSpec defines the desired state of Resource
            properties:
              forProvider:
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ResourceStatus defines the observed state of Resource.
            properties:
              atProvider:
                properties:
                  allowUiUpdates:
                    description: |-
                      managed resources are locked from UI edits unless you opt in. Set to true to allow UI modifications; not supported by all resources.
                      Whether the resource can be edited from the Grafana UI. Set to `true` to allow UI modifications; not supported by all resources.
                    type: boolean
                  id:
                    description: (String) The API resource UUID assigned by Grafana.
                      This is not used for import; import uses the object name stored
                      in metadata.uid.
                    type: string
                  managerIdentity:
                    description: Override the identity stamped on this resource's
                      manager metadata.
                    type: string
                  manifest:
                    description: |-
                      style manifest, typically from yamldecode(file(...)) or jsondecode(file(...)). Must contain apiVersion, kind, metadata (with name or uid), and spec. If you start from an exported manifest, remove noisy server-managed metadata such as resourceVersion, generation, and managedFields, or import the resource first and use the normalized state shape. If metadata.namespace is set, it must match the namespace selected from provider org_id or stack_id / autodiscovery. Top-level manifest fields are limited to apiVersion, kind, metadata, spec, and the ignored status field. The secure field must not be set here; use the top-level secure argument instead.
                      Kubernetes-style manifest, typically from `yamldecode(file(...))` or `jsondecode(file(...))`. Must contain `apiVersion`, `kind`, `metadata` (with `name` or `uid`), and `spec`. If you start from an exported manifest, remove noisy server-managed metadata such as `resourceVersion`, `generation`, and `managedFields`, or import the resource first and use the normalized state shape. If `metadata.namespace` is set, it must match the namespace selected from provider `org_id` or `stack_id` / autodiscovery. Top-level manifest fields are limited to `apiVersion`, `kind`, `metadata`, `spec`, and the ignored `status` field. The `secure` field must not be set here; use the top-level `secure` argument instead.
                    x-kubernetes-preserve-unknown-fields: true
                  secure:
                    description: |-
                      only) Write-only secure values map. Each key must contain exactly one of create or name; empty objects are invalid.
                      Write-only secure values map. Each key must contain exactly one of `create` or `name`; empty objects are invalid.
                    x-kubernetes-preserve-unknown-fields: true
                  secureVersion:
                    description: apply secure values.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountpermissionitems.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountPermissionItem
    listKind: ServiceAccountPermissionItemList
    plural: serviceaccountpermissionitems
    singular: serviceaccountpermissionitem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceAccountPermissionItem is the Schema for the ServiceAccountPermissionItems
          API. Manages a single permission item for a service account. Conflicts with
          the "grafana_service_account_permission" resource which manages the entire
          set of permissions for a service account. Official documentation https://grafana.com/docs/grafana/latest/administration/service-accounts/#manage-users-and-teams-permissions-for-a-service-account-in-grafana
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountPermissionItemSpec defines the desired state
              of ServiceAccountPermissionItem
            properties:
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.permission is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permission)
                || (has(self.initProvider) && has(self.initProvider.permission))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
          status:
            description: ServiceAccountPermissionItemStatus defines the observed state
              of ServiceAccountPermissionItem.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  permission:
                    description: |-
                      (String) the permission to be assigned
                      the permission to be assigned
                    type: string
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account.
                      The ID of the service account.
                    type: string
                  team:
                    description: |-
                      (String) the team onto which the permission is to be assigned
                      the team onto which the permission is to be assigned
                    type: string
                  user:
                    description: |-
                      (String) the user or service account onto which the permission is to be assigned
                      the user or service account onto which the permission is to be assigned
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountpermissions.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountPermission
    listKind: ServiceAccountPermissionList
    plural: serviceaccountpermissions
    singular: serviceaccountpermission
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountPermission is the Schema for the ServiceAccountPermissions
          API. Manages the entire set of permissions for a service account. Permissions
          that aren''t specified when applying this resource will be removed. Note:
          This resource is available from Grafana 9.2.4 onwards. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/#manage-users-and-teams-permissions-for-a-service-account-in-grafana'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountPermissionSpec defines the desired state of
              ServiceAccountPermission
            properties:
              forProvider:
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        teamRef:
                          description: Reference to a Team in oss to populate teamId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        teamSelector:
                          description: Selector for a Team in oss to populate teamId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                        userRef:
                          description: Reference to a User in oss to populate userId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        userSelector:
                          description: Selector for a User in oss to populate userId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ServiceAccountPermissionStatus defines the observed state
              of ServiceAccountPermission.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  permissions:
                    description: |-
                      (Block Set) The permission items to add/update. Items that are omitted from the list will be removed. (see below for nested schema)
                      The permission items to add/update. Items that are omitted from the list will be removed.
                    items:
                      properties:
                        permission:
                          description: |-
                            (String) Permission to associate with item. Must be one of View, Edit, or Admin.
                            Permission to associate with item. Must be one of `View`, `Edit`, or `Admin`.
                          type: string
                        teamId:
                          description: |-
                            (String) ID of the team to manage permissions for. Defaults to 0.
                            ID of the team to manage permissions for. Defaults to `0`.
                          type: string
                        userId:
                          description: |-
                            (String) ID of the user or service account to manage permissions for. Defaults to 0.
                            ID of the user or service account to manage permissions for. Defaults to `0`.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: |-
                      (String) The id of the service account.
                      The id of the service account.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccountrotatingtokens.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountRotatingToken
    listKind: ServiceAccountRotatingTokenList
    plural: serviceaccountrotatingtokens
    singular: serviceaccountrotatingtoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountRotatingToken is the Schema for the ServiceAccountRotatingTokens
          API. Note: This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountRotatingTokenSpec defines the desired state
              of ServiceAccountRotatingToken
            properties:
              forProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.earlyRotationWindowSeconds is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.earlyRotationWindowSeconds)
                || (has(self.initProvider) && has(self.initProvider.earlyRotationWindowSeconds))'
            - message: spec.forProvider.namePrefix is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.namePrefix)
                || (has(self.initProvider) && has(self.initProvider.namePrefix))'
            - message: spec.forProvider.secondsToLive is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.secondsToLive)
                || (has(self.initProvider) && has(self.initProvider.secondsToLive))'
            - message: spec.forProvider.serviceAccountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountId)
                || (has(self.initProvider) && has(self.initProvider.serviceAccountId))'
          status:
            description: ServiceAccountRotatingTokenStatus defines the observed state
              of ServiceAccountRotatingToken.
            properties:
              atProvider:
                properties:
                  deleteOnDestroy:
                    description: Use it with lifecycle { create_before_destroy = true
                      } to make sure that the new token is created before the old
                      one is deleted. Defaults to false. Use it with `lifecycle {
                      create_before_destroy = true }` to make sure that the new token
                      is created before the old one is deleted. Defaults to `false`.
                    type: boolean
                  earlyRotationWindowSeconds:
                    description: |-
                      (Number) Duration of the time window before expiring where the token can be rotated, in seconds.
                      Duration of the time window before expiring where the token can be rotated, in seconds.
                    type: number
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      and will have characters appended to it to make the name unique.
                      The name of the service account token. It will start with `<name_prefix>-` and will have characters appended to it to make the name unique.
                    type: string
                  namePrefix:
                    description: |-
                      <additional_characters>.
                      Prefix for the name of the service account tokens created by this resource. The actual name will be stored in the computed field `name`, which will be in the format `<name_prefix>-<additional_characters>`.
                    type: string
                  readyForRotation:
                    description: |-
                      (Boolean) Signals that the service account token is expired or within the period to be early rotated.
                      Signals that the service account token is expired or within the period to be early rotated.
                    type: boolean
                  secondsToLive:
                    description: |-
                      (Number) The token expiration in seconds.
                      The token expiration in seconds.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccounts.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccount
    listKind: ServiceAccountList
    plural: serviceaccounts
    singular: serviceaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccount is the Schema for the ServiceAccounts API. Note:
          This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountSpec defines the desired state of ServiceAccount
            properties:
              forProvider:
                properties:
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.role is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.role)
                || (has(self.initProvider) && has(self.initProvider.role))'
          status:
            description: ServiceAccountStatus defines the observed state of ServiceAccount.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isDisabled:
                    description: |-
                      (Boolean) The disabled status for the service account. Defaults to false.
                      The disabled status for the service account. Defaults to `false`.
                    type: boolean
                  name:
                    description: |-
                      (String) The name of the service account.
                      The name of the service account.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the Org ID defined in the provider block will be used.
                      The Organization ID. If not set, the Org ID defined in the provider block will be used.
                    type: string
                  role:
                    description: |-
                      (String) The basic role of the service account in the organization.
                      The basic role of the service account in the organization.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccounttokens.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: ServiceAccountToken
    listKind: ServiceAccountTokenList
    plural: serviceaccounttokens
    singular: serviceaccounttoken
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'ServiceAccountToken is the Schema for the ServiceAccountTokens
          API. Note: This resource is available only with Grafana 9.1+. Official documentation
          https://grafana.com/docs/grafana/latest/administration/service-accounts/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/serviceaccount/#service-account-api'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceAccountTokenSpec defines the desired state of ServiceAccountToken
            properties:
              forProvider:
                properties:
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                  serviceAccountRef:
                    description: Reference to a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountSelector:
                    description: Selector for a ServiceAccount in oss to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServiceAccountTokenStatus defines the observed state of ServiceAccountToken.
            properties:
              atProvider:
                properties:
                  expiration:
                    description: |-
                      (String) The expiration date of the service account token.
                      The expiration date of the service account token.
                    type: string
                  hasExpired:
                    description: |-
                      (Boolean) The status of the service account token.
                      The status of the service account token.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) The name of the service account token.
                      The name of the service account token.
                    type: string
                  secondsToLive:
                    description: |-
                      (Number) The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless api_key_max_seconds_to_live configuration option is set) the key will never expire.
                      The key expiration in seconds. It is optional. If it is a positive number an expiration date for the key is set. If it is null, zero or is omitted completely (unless `api_key_max_seconds_to_live` configuration option is set) the key will never expire.
                    type: number
                  serviceAccountId:
                    description: |-
                      (String) The ID of the service account to which the token belongs.
                      The ID of the service account to which the token belongs.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ssosettings.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SsoSettings
    listKind: SsoSettingsList
    plural: ssosettings
    singular: ssosettings
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SsoSettings is the Schema for the SsoSettingss API. Manages Grafana
          SSO Settings for OAuth2, SAML and LDAP. Support for LDAP is currently in
          preview, it will be available in Grafana starting with v11.3. Official documentation
          https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/sso-settings/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SsoSettingsSpec defines the desired state of SsoSettings
            properties:
              forProvider:
                properties:
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    bindPasswordSecretRef:
                                      description: |-
                                        (String, Sensitive) The search user bind password.
                                        The search user bind password.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) The path to the client private key.
                                        The path to the client private key.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    clientKeyValueSecretRef:
                                      description: |-
                                        (String, Sensitive) The Base64 encoded value of the client private key.
                                        The Base64 encoded value of the client private key.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecretSecretRef:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        certificateSecretRef:
                          description: |-
                            encoded string for the SP X.509 certificate.
                            Base64-encoded string for the SP X.509 certificate.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        privateKeySecretRef:
                          description: |-
                            encoded string for the SP private key.
                            Base64-encoded string for the SP private key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    bindPasswordSecretRef:
                                      description: |-
                                        (String, Sensitive) The search user bind password.
                                        The search user bind password.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) The path to the client private key.
                                        The path to the client private key.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    clientKeyValueSecretRef:
                                      description: |-
                                        (String, Sensitive) The Base64 encoded value of the client private key.
                                        The Base64 encoded value of the client private key.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecretSecretRef:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        certificateSecretRef:
                          description: |-
                            encoded string for the SP X.509 certificate.
                            Base64-encoded string for the SP X.509 certificate.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        privateKeySecretRef:
                          description: |-
                            encoded string for the SP private key.
                            Base64-encoded string for the SP private key.
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.providerName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.providerName)
                || (has(self.initProvider) && has(self.initProvider.providerName))'
          status:
            description: SsoSettingsStatus defines the observed state of SsoSettings.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ldapSettings:
                    description: |-
                      (Block Set, Max: 1) The LDAP settings set. Required for the ldap provider. (see below for nested schema)
                      The LDAP settings set. Required for the ldap provider.
                    items:
                      properties:
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                          type: boolean
                        config:
                          description: |-
                            (Block List, Min: 1, Max: 1) The LDAP configuration. (see below for nested schema)
                            The LDAP configuration.
                          items:
                            properties:
                              servers:
                                description: |-
                                  (Block List, Min: 1) The LDAP servers configuration. (see below for nested schema)
                                  The LDAP servers configuration.
                                items:
                                  properties:
                                    attributes:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        (Map of String) The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                        The LDAP server attributes. The following attributes can be configured: email, member_of, name, surname, username.
                                      type: object
                                      x-kubernetes-map-type: granular
                                    bindDn:
                                      description: |-
                                        (String) The search user bind DN.
                                        The search user bind DN.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) The path to the client certificate.
                                        The path to the client certificate.
                                      type: string
                                    clientCertValue:
                                      description: |-
                                        (String) The Base64 encoded value of the client certificate.
                                        The Base64 encoded value of the client certificate.
                                      type: string
                                    groupMappings:
                                      description: |-
                                        (Block List) For mapping an LDAP group to a Grafana organization and role. (see below for nested schema)
                                        For mapping an LDAP group to a Grafana organization and role.
                                      items:
                                        properties:
                                          grafanaAdmin:
                                            description: |-
                                              (Boolean) If set to true, it makes the user of group_dn Grafana server admin.
                                              If set to true, it makes the user of group_dn Grafana server admin.
                                            type: boolean
                                          groupDn:
                                            description: |-
                                              (String) LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                              LDAP distinguished name (DN) of LDAP group. If you want to match all (or no LDAP groups) then you can use wildcard ("*").
                                            type: string
                                          orgId:
                                            description: |-
                                              (Number) The Grafana organization database id.
                                              The Grafana organization database id.
                                            type: number
                                          orgRole:
                                            description: |-
                                              (String) Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                              Assign users of group_dn the organization role Admin, Editor, or Viewer.
                                            type: string
                                        type: object
                                      type: array
                                    groupSearchBaseDns:
                                      description: |-
                                        (List of String) An array of the base DNs to search through for groups. Typically uses ou=groups.
                                        An array of the base DNs to search through for groups. Typically uses ou=groups.
                                      items:
                                        type: string
                                      type: array
                                    groupSearchFilter:
                                      description: |-
                                        (String) Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                        Group search filter, to retrieve the groups of which the user is a member (only set if memberOf attribute is not available).
                                      type: string
                                    groupSearchFilterUserAttribute:
                                      description: |-
                                        (String) The %s in the search filter will be replaced with the attribute defined in this field.
                                        The %s in the search filter will be replaced with the attribute defined in this field.
                                      type: string
                                    host:
                                      description: |-
                                        (String) The LDAP server host.
                                        The LDAP server host.
                                      type: string
                                    minTlsVersion:
                                      description: |-
                                        (String) Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                        Minimum TLS version allowed. Accepted values are: TLS1.2, TLS1.3.
                                      type: string
                                    port:
                                      description: |-
                                        (Number) The LDAP server port.
                                        The LDAP server port.
                                      type: number
                                    rootCaCert:
                                      description: |-
                                        (String) The path to the root CA certificate.
                                        The path to the root CA certificate.
                                      type: string
                                    rootCaCertValue:
                                      description: |-
                                        (List of String) The Base64 encoded values of the root CA certificates.
                                        The Base64 encoded values of the root CA certificates.
                                      items:
                                        type: string
                                      type: array
                                    searchBaseDns:
                                      description: |-
                                        (List of String) An array of base DNs to search through.
                                        An array of base DNs to search through.
                                      items:
                                        type: string
                                      type: array
                                    searchFilter:
                                      description: |-
                                        (String) The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                        The user search filter, for example "(cn=%s)" or "(sAMAccountName=%s)" or "(uid=%s)".
                                      type: string
                                    sslSkipVerify:
                                      description: |-
                                        (Boolean) If set to true, the SSL cert validation will be skipped.
                                        If set to true, the SSL cert validation will be skipped.
                                      type: boolean
                                    startTls:
                                      description: |-
                                        (Boolean) If set to true, use LDAP with STARTTLS instead of LDAPS.
                                        If set to true, use LDAP with STARTTLS instead of LDAPS.
                                      type: boolean
                                    timeout:
                                      description: |-
                                        (Number) The timeout in seconds for connecting to the LDAP host.
                                        The timeout in seconds for connecting to the LDAP host.
                                      type: number
                                    tlsCiphers:
                                      description: |-
                                        (List of String) Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                        Accepted TLS ciphers. For a complete list of supported ciphers, refer to: https://go.dev/src/crypto/tls/cipher_suites.go.
                                      items:
                                        type: string
                                      type: array
                                    useSsl:
                                      description: |-
                                        (Boolean) Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                        Set to true if LDAP server should use an encrypted TLS connection (either with STARTTLS or LDAPS).
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for LDAP. Defaults to `true`.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from LDAP.
                          type: boolean
                      type: object
                    type: array
                  oauth2Settings:
                    description: |-
                      (Block Set, Max: 1) The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers. (see below for nested schema)
                      The OAuth2 settings set. Required for github, gitlab, google, azuread, okta, generic_oauth providers.
                    items:
                      properties:
                        allowAssignGrafanaAdmin:
                          description: |-
                            (Boolean) If enabled, it will automatically sync the Grafana server administrator role.
                            If enabled, it will automatically sync the Grafana server administrator role.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            If not enabled, only existing Grafana users can log in using OAuth.
                          type: boolean
                        allowedDomains:
                          description: |-
                            or space-separated domains. The user should belong to at least one domain to log in.
                            List of comma- or space-separated domains. The user should belong to at least one domain to log in.
                          type: string
                        allowedGroups:
                          description: |-
                            or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                            List of comma- or space-separated groups. The user should be a member of at least one group to log in. For Generic OAuth, if you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. The user should be a member of at least one organization to log in.
                          type: string
                        apiUrl:
                          description: |-
                            (String) The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                            The user information endpoint of your OAuth2 provider. Required for okta and generic_oauth providers.
                          type: string
                        authStyle:
                          description: |-
                            (String) It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                            It determines how client_id and client_secret are sent to Oauth2 provider. Possible values are AutoDetect, InParams, InHeader. Default is AutoDetect.
                          type: string
                        authUrl:
                          description: |-
                            (String) The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The authorization endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Log in automatically, skipping the login screen.
                          type: boolean
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        custom:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) Custom fields to configure for OAuth2 such as the force_use_graph_api field.
                            Custom fields to configure for OAuth2 such as the [force_use_graph_api](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-authentication/azuread/#force-fetching-groups-from-microsoft-graph-api) field.
                          type: object
                          x-kubernetes-map-type: granular
                        defineAllowedGroups:
                          description: |-
                            (Boolean) Define allowed groups.
                            Define allowed groups.
                          type: boolean
                        defineAllowedTeamsIds:
                          description: |-
                            (Boolean) Define allowed teams ids.
                            Define allowed teams ids.
                          type: boolean
                        emailAttributeName:
                          description: |-
                            (String) Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                            Name of the key to use for user email lookup within the attributes map of OAuth2 ID token. Only applicable to Generic OAuth.
                          type: string
                        emailAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                            JMESPath expression to use for user email lookup from the user information. Only applicable to Generic OAuth.
                          type: string
                        emptyScopes:
                          description: |-
                            (Boolean) If enabled, no scopes will be sent to the OAuth2 provider.
                            If enabled, no scopes will be sent to the OAuth2 provider.
                          type: boolean
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for the specified provider. Defaults to `true`.
                          type: boolean
                        groupsAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                            JMESPath expression to use for user group lookup. If you configure allowed_groups, you must also configure groups_attribute_path.
                          type: string
                        idTokenAttributeName:
                          description: |-
                            (String) The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                            The name of the key used to extract the ID token from the returned OAuth2 token. Only applicable to Generic OAuth.
                          type: string
                        loginAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                            JMESPath expression to use for user login lookup from the user ID token. Only applicable to Generic OAuth.
                          type: string
                        loginPrompt:
                          description: |-
                            (String) Indicates the type of user interaction when the user logs in with the IdP. Available values are login, consent and select_account.
                            Indicates the type of user interaction when the user logs in with the IdP. Available values are `login`, `consent` and `select_account`.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Helpful if you use more than one identity providers or SSO protocols.
                          type: string
                        nameAttributePath:
                          description: |-
                            (String) JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                            JMESPath expression to use for user name lookup from the user ID token. This name will be used as the user’s display name. Only applicable to Generic OAuth.
                          type: string
                        orgAttributePath:
                          description: |-
                            (String) JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                            JMESPath expression to use for the organization mapping lookup from the user ID token. The extracted list will be used for the organization mapping (to match "Organization" in the "org_mapping"). Only applicable to Generic OAuth and Okta.
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                          type: string
                        roleAttributePath:
                          description: |-
                            (String) JMESPath expression to use for Grafana role lookup.
                            JMESPath expression to use for Grafana role lookup.
                          type: string
                        roleAttributeStrict:
                          description: |-
                            (Boolean) If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                            If enabled, denies user login if the Grafana role cannot be extracted using Role attribute path.
                          type: boolean
                        scopes:
                          description: |-
                            or space-separated OAuth2 scopes.
                            List of comma- or space-separated OAuth2 scopes.
                          type: string
                        signoutRedirectUrl:
                          description: |-
                            (String) The URL to redirect the user to after signing out from Grafana.
                            The URL to redirect the user to after signing out from Grafana.
                          type: string
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        teamIds:
                          description: |-
                            (String) String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                            String list of Team Ids. If set, the user must be a member of one of the given teams to log in. If you configure team_ids, you must also configure teams_url and team_ids_attribute_path.
                          type: string
                        teamIdsAttributePath:
                          description: |-
                            (String) The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                            The JMESPath expression to use for Grafana Team Id lookup within the results returned by the teams_url endpoint. Only applicable to Generic OAuth.
                          type: string
                        teamsUrl:
                          description: |-
                            (String) The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                            The URL used to query for Team Ids. If not set, the default value is /teams. If you configure teams_url, you must also configure team_ids_attribute_path. Only applicable to Generic OAuth.
                          type: string
                        tlsClientCa:
                          description: |-
                            (String) The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                            The path to the trusted certificate authority list. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientCert:
                          description: |-
                            (String) The path to the certificate. Is not applicable on Grafana Cloud.
                            The path to the certificate. Is not applicable on Grafana Cloud.
                          type: string
                        tlsClientKey:
                          description: |-
                            (String) The path to the key. Is not applicable on Grafana Cloud.
                            The path to the key. Is not applicable on Grafana Cloud.
                          type: string
                        tlsSkipVerifyInsecure:
                          description: |-
                            in-the-middle attacks.
                            If enabled, the client accepts any certificate presented by the server and any host name in that certificate. You should only use this for testing, because this mode leaves SSL/TLS susceptible to man-in-the-middle attacks.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                          type: string
                        usePkce:
                          description: |-
                            (Boolean) If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                            If enabled, Grafana will use Proof Key for Code Exchange (PKCE) with the OAuth2 Authorization Code Grant.
                          type: boolean
                        useRefreshToken:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                            If enabled, Grafana will fetch a new access token using the refresh token provided by the OAuth2 provider.
                          type: boolean
                      type: object
                    type: array
                  providerName:
                    description: |-
                      (String) The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                      The name of the SSO provider. Supported values: github, gitlab, google, azuread, okta, generic_oauth, saml, ldap.
                    type: string
                  samlSettings:
                    description: |-
                      (Block Set, Max: 1) The SAML settings set. Required for the saml provider. (see below for nested schema)
                      The SAML settings set. Required for the saml provider.
                    items:
                      properties:
                        allowIdpInitiated:
                          description: |-
                            initiated login is allowed.
                            Whether SAML IdP-initiated login is allowed.
                          type: boolean
                        allowSignUp:
                          description: |-
                            (Boolean) Whether to allow new Grafana user creation through LDAP login. If set to false, then only existing Grafana users can log in with LDAP.
                            Whether to allow new Grafana user creation through SAML login. If set to false, then only existing Grafana users can log in with SAML.
                          type: boolean
                        allowedOrganizations:
                          description: |-
                            or space-separated organizations. The user should be a member of at least one organization to log in.
                            List of comma- or space-separated organizations. User should be a member of at least one organization to log in.
                          type: string
                        assertionAttributeEmail:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user email.
                            Friendly name or name of the attribute within the SAML assertion to use as the user email.
                          type: string
                        assertionAttributeExternalUid:
                          description: |-
                            (String) Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                            Friendly name of the attribute within the SAML assertion to use as the external user ID. Only used for SCIM provisioned users.
                          type: string
                        assertionAttributeGroups:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                            Friendly name or name of the attribute within the SAML assertion to use as the user groups.
                          type: string
                        assertionAttributeLogin:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                            Friendly name or name of the attribute within the SAML assertion to use as the user login handle.
                          type: string
                        assertionAttributeName:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                            Friendly name or name of the attribute within the SAML assertion to use as the user name. Alternatively, this can be a template with variables that match the names of attributes within the SAML assertion.
                          type: string
                        assertionAttributeOrg:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                            Friendly name or name of the attribute within the SAML assertion to use as the user organization.
                          type: string
                        assertionAttributeRole:
                          description: |-
                            (String) Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                            Friendly name or name of the attribute within the SAML assertion to use as the user roles.
                          type: string
                        autoLogin:
                          description: |-
                            (Boolean) Log in automatically, skipping the login screen.
                            Whether SAML auto login is enabled.
                          type: boolean
                        certificatePath:
                          description: |-
                            (String) Path for the SP X.509 certificate.
                            Path for the SP X.509 certificate.
                          type: string
                        clientId:
                          description: |-
                            (String) The client Id of your OAuth2 app.
                            The client Id of your OAuth2 app.
                          type: string
                        clientSecret:
                          description: |-
                            (String, Sensitive) The client secret of your OAuth2 app.
                            The client secret of your OAuth2 app.
                          type: string
                        enabled:
                          description: |-
                            (Boolean) Define whether this configuration is enabled for LDAP. Defaults to true.
                            Define whether this configuration is enabled for SAML. Defaults to `true`.
                          type: boolean
                        entityId:
                          description: |-
                            (String) The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                            The entity ID is a globally unique identifier for the service provider. It is used to identify the service provider to the identity provider. Defaults to the URL of the Grafana instance if not set.
                          type: string
                        forceUseGraphApi:
                          description: |-
                            (Boolean) If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                            If enabled, Grafana will fetch groups from Microsoft Graph API instead of using the groups claim from the ID token.
                          type: boolean
                        idpMetadata:
                          description: |-
                            encoded string for the IdP SAML metadata XML.
                            Base64-encoded string for the IdP SAML metadata XML.
                          type: string
                        idpMetadataPath:
                          description: |-
                            (String) Path for the IdP SAML metadata XML.
                            Path for the IdP SAML metadata XML.
                          type: string
                        idpMetadataUrl:
                          description: |-
                            (String) URL for the IdP SAML metadata XML.
                            URL for the IdP SAML metadata XML.
                          type: string
                        maxIssueDelay:
                          description: |-
                            (String) Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                            Duration, since the IdP issued a response and the SP is allowed to process it. For example: 90s, 1h.
                          type: string
                        metadataValidDuration:
                          description: |-
                            (String) Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                            Duration, for how long the SP metadata is valid. For example: 48h, 5d.
                          type: string
                        name:
                          description: |-
                            (String) Helpful if you use more than one identity providers or SSO protocols.
                            Name used to refer to the SAML authentication.
                          type: string
                        nameIdFormat:
                          description: |-
                            format:transient
                            The Name ID Format to request within the SAML assertion. Defaults to urn:oasis:names:tc:SAML:2.0:nameid-format:transient
                          type: string
                        orgMapping:
                          description: |-
                            or space-separated Organization:OrgIdOrOrgName:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: None, Viewer, Editor or Admin.
                            List of comma- or space-separated Organization:OrgId:Role mappings. Organization can be * meaning “All users”. Role is optional and can have the following values: Viewer, Editor or Admin.
                          type: string
                        privateKeyPath:
                          description: |-
                            (String) Path for the SP private key.
                            Path for the SP private key.
                          type: string
                        relayState:
                          description: |-
                            initiated login. Should match relay state configured in IdP.
                            Relay state for IdP-initiated login. Should match relay state configured in IdP.
                          type: string
                        roleValuesAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Admin role.
                            List of comma- or space-separated roles which will be mapped into the Admin role.
                          type: string
                        roleValuesEditor:
                          description: |-
                            or space-separated roles which will be mapped into the Editor role.
                            List of comma- or space-separated roles which will be mapped into the Editor role.
                          type: string
                        roleValuesGrafanaAdmin:
                          description: |-
                            or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                            List of comma- or space-separated roles which will be mapped into the Grafana Admin (Super Admin) role.
                          type: string
                        roleValuesNone:
                          description: |-
                            or space-separated roles which will be mapped into the None role.
                            List of comma- or space-separated roles which will be mapped into the None role.
                          type: string
                        roleValuesViewer:
                          description: |-
                            or space-separated roles which will be mapped into the Viewer role.
                            List of comma- or space-separated roles which will be mapped into the Viewer role.
                          type: string
                        signatureAlgorithm:
                          description: |-
                            sha1, rsa-sha256, rsa-sha512.
                            Signature algorithm used for signing requests to the IdP. Supported values are rsa-sha1, rsa-sha256, rsa-sha512.
                          type: string
                        singleLogout:
                          description: |-
                            (Boolean) Whether SAML Single Logout is enabled.
                            Whether SAML Single Logout is enabled.
                          type: boolean
                        skipOrgRoleSync:
                          description: |-
                            (Boolean) Prevent synchronizing users’ organization roles from LDAP.
                            Prevent synchronizing users’ organization roles from your IdP.
                          type: boolean
                        tokenUrl:
                          description: |-
                            (String) The token endpoint of your OAuth2 provider. Required for azuread, okta and generic_oauth providers.
                            The token endpoint of your OAuth2 provider. Required for Azure AD providers.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teams.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Team
    listKind: TeamList
    plural: teams
    singular: team
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Team is the Schema for the Teams API. Official documentation
          https://grafana.com/docs/grafana/latest/administration/team-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamSpec defines the desired state of Team
            properties:
              forProvider:
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  memberRefs:
                    description: References to User in oss to populate members.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  memberSelector:
                    description: Selector for a list of User in oss to populate members.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  memberRefs:
                    description: References to User in oss to populate members.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  memberSelector:
                    description: Selector for a list of User in oss to populate members.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  organizationRef:
                    description: Reference to a Organization in oss to populate orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  organizationSelector:
                    description: Selector for a Organization in oss to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: TeamStatus defines the observed state of Team.
            properties:
              atProvider:
                properties:
                  email:
                    description: |-
                      (String) An email address for the team.
                      An email address for the team.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ignoreExternallySyncedMembers:
                    description: |-
                      (Boolean) Ignores team members that have been added to team by Team Sync. Team Sync can be provisioned using grafana_team_external_group resource.
                      Ignores team members that have been added to team by [Team Sync](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/). Team Sync can be provisioned using [grafana_team_external_group resource](https://registry.io/providers/grafana/grafana/latest/docs/resources/team_external_group).
                    type: boolean
                  members:
                    description: |-
                      (Set of String) A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                      A set of email addresses corresponding to users who should be given membership to the team. Note: users specified here must already exist in Grafana.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: |-
                      (String) The display name for the Grafana team created.
                      The display name for the Grafana team created.
                    type: string
                  orgId:
                    description: |-
                      (String) The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                      The Organization ID. If not set, the default organization is used for basic authentication, or the one that owns your service account for token authentication.
                    type: string
                  preferences:
                    description: (Block List) (see below for nested schema)
                    items:
                      properties:
                        homeDashboardUid:
                          description: |-
                            (String) The UID of the dashboard to display when a team member logs in.
                            The UID of the dashboard to display when a team member logs in.
                          type: string
                        theme:
                          description: |-
                            (String) The default theme for this team. Available themes are light, dark, system, or an empty string for the default theme.
                            The default theme for this team. Available themes are `light`, `dark`, `system`, or an empty string for the default theme.
                          type: string
                        timezone:
                          description: |-
                            (String) The default timezone for this team. Available values are utc, browser, or an empty string for the default.
                            The default timezone for this team. Available values are `utc`, `browser`, or an empty string for the default.
                          type: string
                        weekStart:
                          description: |-
                            (String) The default week start day for this team. Available values are sunday, monday, saturday, or an empty string for the default.
                            The default week start day for this team. Available values are `sunday`, `monday`, `saturday`, or an empty string for the default.
                          type: string
                      type: object
                    type: array
                  teamId:
                    description: |-
                      (Number) The team id assigned to this team by Grafana.
                      The team id assigned to this team by Grafana.
                    type: number
                  teamSync:
                    description: |-
                      (Block List) Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      Sync external auth provider groups with this Grafana team. Only available in Grafana Enterprise.
                      * [Official documentation](https://grafana.com/docs/grafana/latest/setup-grafana/configure-security/configure-team-sync/)
                      * [HTTP API](https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/team_sync/)
                    items:
                      properties:
                        groups:
                          description: (Set of String)
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  teamUid:
                    description: |-
                      (String) The team uid assigned to this team by Grafana.
                      The team uid assigned to this team by Grafana.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.oss.grafana.m.crossplane.io
spec:
  group: oss.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the Users API. Official documentation
          https://grafana.com/docs/grafana/latest/administration/user-management/server-user-management/HTTP
          API https://grafana.com/docs/grafana/latest/developer-resources/api-reference/http-api/api-legacy/user/
          This resource represents an instance-scoped resource and uses Grafana's
          admin APIs. It does not work with API tokens or service accounts which are
          org-scoped. You must use basic auth. This resource is also not compatible
          with Grafana Cloud, as it does not allow basic auth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              forProvider:
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password for the Grafana user.
                      The password for the Grafana user.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password for the Grafana user.
                      The password for the Grafana user.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - passwordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.email is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.email)
                || (has(self.initProvider) && has(self.initProvider.email))'
            - message: spec.forProvider.passwordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.passwordSecretRef)'
          status:
            description: UserStatus defines the observed state of User.
            properties:
              atProvider:
                properties:
                  email:
                    description: |-
                      (String) The email address of the Grafana user.
                      The email address of the Grafana user.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  isAdmin:
                    description: |-
                      (Boolean) Whether to make user an admin. Defaults to false.
                      Whether to make user an admin. Defaults to `false`.
                    type: boolean
                  login:
                    description: |-
                      (String) The username for the Grafana user.
                      The username for the Grafana user.
                    type: string
                  name:
                    description: |-
                      (String) The display name for the Grafana user.
                      The display name for the Grafana user.
                    type: string
                  userId:
                    description: |-
                      (Number) The numerical ID of the Grafana user.
                      The numerical ID of the Grafana user.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboards.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Dashboard
    listKind: DashboardList
    plural: dashboards
    singular: dashboard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dashboard is an observe-only resource backed by the grafana_dashboard
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: DashboardParameters defines the input parameters for
                  the grafana_dashboard data source.
                properties:
                  dashboardID:
                    description: The numerical ID of the Grafana dashboard. Specify
                      either this or `uid`.
                    format: int64
                    type: integer
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  uid:
                    description: The uid of the Grafana dashboard. Specify either
                      this or `dashboard_id`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: DashboardObservation holds the observed (computed) fields
                  from the grafana_dashboard data source.
                properties:
                  configJSON:
                    description: The complete dashboard model JSON.
                    type: string
                  dashboardID:
                    description: The numerical ID of the Grafana dashboard. Specify
                      either this or `uid`.
                    format: int64
                    type: integer
                  folderUID:
                    description: The UID of the folder where the Grafana dashboard
                      is found.
                    type: string
                  isStarred:
                    description: Whether or not the Grafana dashboard is starred.
                      Starred Dashboards will show up on your own Home Dashboard by
                      default, and are a convenient way to mark Dashboards that you’re
                      interested in.
                    type: boolean
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  slug:
                    description: URL slug of the dashboard (deprecated).
                    type: string
                  title:
                    description: The title of the Grafana dashboard.
                    type: string
                  uid:
                    description: The uid of the Grafana dashboard. Specify either
                      this or `dashboard_id`.
                    type: string
                  url:
                    description: The full URL of the dashboard.
                    type: string
                  version:
                    description: The numerical version of the Grafana dashboard.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dashboardsets.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: DashboardSet
    listKind: DashboardSetList
    plural: dashboardsets
    singular: dashboardset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DashboardSet is an observe-only resource backed by the grafana_dashboards
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: DashboardSetParameters defines the input parameters for
                  the grafana_dashboards data source.
                properties:
                  folderUIDs:
                    description: UIDs of Grafana folders containing dashboards. Specify
                      to filter for dashboards by folder (eg. `["General"]` for General
                      folder), or leave blank to get all dashboards in all folders.
                    items:
                      type: string
                    type: array
                  limit:
                    description: Maximum number of dashboard search results to return.
                    format: int64
                    type: integer
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  tags:
                    description: List of string Grafana dashboard tags to search for,
                      eg. `["prod"]`. Used only as search input, i.e., attribute value
                      will remain unchanged.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: DashboardSetObservation holds the observed (computed)
                  fields from the grafana_dashboards data source.
                properties:
                  dashboards:
                    items:
                      properties:
                        folderTitle:
                          type: string
                        title:
                          type: string
                        uid:
                          type: string
                      type: object
                    type: array
                  folderUIDs:
                    description: UIDs of Grafana folders containing dashboards. Specify
                      to filter for dashboards by folder (eg. `["General"]` for General
                      folder), or leave blank to get all dashboards in all folders.
                    items:
                      type: string
                    type: array
                  limit:
                    description: Maximum number of dashboard search results to return.
                    format: int64
                    type: integer
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  tags:
                    description: List of string Grafana dashboard tags to search for,
                      eg. `["prod"]`. Used only as search input, i.e., attribute value
                      will remain unchanged.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: datasources.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: DataSource
    listKind: DataSourceList
    plural: datasources
    singular: datasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataSource is an observe-only resource backed by the grafana_data_source
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: DataSourceParameters defines the input parameters for
                  the grafana_data_source data source.
                properties:
                  name:
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  uid:
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: DataSourceObservation holds the observed (computed) fields
                  from the grafana_data_source data source.
                properties:
                  accessMode:
                    description: 'The method by which Grafana will access the data
                      source: `proxy` or `direct`.'
                    type: string
                  basicAuthEnabled:
                    description: Whether to enable basic auth for the data source.
                    type: boolean
                  basicAuthUsername:
                    description: Basic auth username.
                    type: string
                  databaseName:
                    description: (Required by some data source types) The name of
                      the database to use on the selected data source server.
                    type: string
                  isDefault:
                    description: Whether to set the data source as default. This should
                      only be `true` to a single data source.
                    type: boolean
                  jsonDataEncoded:
                    description: Serialized JSON string containing the json data.
                      This attribute can be used to pass configuration options to
                      the data source. To figure out what options a datasource has
                      available, see its docs or inspect the network data when saving
                      it from the Grafana UI. Note that keys in this map are usually
                      camelCased.
                    type: string
                  name:
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  privateDataSourceConnectNetworkID:
                    description: (Can only be used with data sources in Grafana Cloud)
                      The ID of the Private Data source Connect network to use with
                      this data source.
                    type: string
                  type:
                    description: The data source type. Must be one of the supported
                      data source keywords.
                    type: string
                  uid:
                    type: string
                  url:
                    description: The URL for the data source. The type of URL required
                      varies depending on the chosen data source type.
                    type: string
                  username:
                    description: (Required by some data source types) The username
                      to use to authenticate to the data source.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: folders.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Folder
    listKind: FolderList
    plural: folders
    singular: folder
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Folder is an observe-only resource backed by the grafana_folder
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: FolderParameters defines the input parameters for the
                  grafana_folder data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  title:
                    description: The title of the folder. If not set, only the uid
                      is used to find the folder.
                    type: string
                  uid:
                    description: The uid of the folder. If not set, only the title
                      of the folder is used to find the folder.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: FolderObservation holds the observed (computed) fields
                  from the grafana_folder data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  parentFolderUID:
                    description: 'The uid of the parent folder. If set, the folder
                      will be nested. If not set, the folder will be created in the
                      root folder. Note: This requires the nestedFolders feature flag
                      to be enabled on your Grafana instance.'
                    type: string
                  title:
                    description: The title of the folder. If not set, only the uid
                      is used to find the folder.
                    type: string
                  uid:
                    description: The uid of the folder. If not set, only the title
                      of the folder is used to find the folder.
                    type: string
                  url:
                    description: The full URL of the folder.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: foldersets.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: FolderSet
    listKind: FolderSetList
    plural: foldersets
    singular: folderset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FolderSet is an observe-only resource backed by the grafana_folders
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: FolderSetParameters defines the input parameters for
                  the grafana_folders data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: FolderSetObservation holds the observed (computed) fields
                  from the grafana_folders data source.
                properties:
                  folders:
                    description: The Grafana instance's folders.
                    items:
                      properties:
                        id:
                          description: The folder ID.
                          format: int64
                          type: integer
                        title:
                          description: The folder title.
                          type: string
                        uid:
                          description: The folder's unique identifier.
                          type: string
                        url:
                          description: The folder's URL
                          type: string
                      type: object
                    type: array
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: librarypanels.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: LibraryPanel
    listKind: LibraryPanelList
    plural: librarypanels
    singular: librarypanel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LibraryPanel is an observe-only resource backed by the grafana_library_panel
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: LibraryPanelParameters defines the input parameters for
                  the grafana_library_panel data source.
                properties:
                  name:
                    description: Name of the library panel.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  uid:
                    description: The unique identifier (UID) of the library panel.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: LibraryPanelObservation holds the observed (computed)
                  fields from the grafana_library_panel data source.
                properties:
                  created:
                    description: Timestamp when the library panel was created.
                    type: string
                  dashboardIDs:
                    description: Numerical IDs of Grafana dashboards containing the
                      library panel.
                    items:
                      format: int64
                      type: integer
                    type: array
                  description:
                    description: Description of the library panel.
                    type: string
                  folderName:
                    description: Name of the folder containing the library panel.
                    type: string
                  folderUID:
                    description: Unique ID (UID) of the folder containing the library
                      panel.
                    type: string
                  modelJSON:
                    description: The JSON model for the library panel.
                    type: string
                  name:
                    description: Name of the library panel.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  panelID:
                    description: The numeric ID of the library panel computed by Grafana.
                    format: int64
                    type: integer
                  type:
                    description: Type of the library panel (eg. text).
                    type: string
                  uid:
                    description: The unique identifier (UID) of the library panel.
                    type: string
                  updated:
                    description: Timestamp when the library panel was last modified.
                    type: string
                  version:
                    description: Version of the library panel.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: librarypanelsets.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: LibraryPanelSet
    listKind: LibraryPanelSetList
    plural: librarypanelsets
    singular: librarypanelset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LibraryPanelSet is an observe-only resource backed by the grafana_library_panels
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: LibraryPanelSetParameters defines the input parameters
                  for the grafana_library_panels data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the default organization
                      is used for basic authentication, or the one that owns your
                      service account for token authentication.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: LibraryPanelSetObservation holds the observed (computed)
                  fields from the grafana_library_panels data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the default organization
                      is used for basic authentication, or the one that owns your
                      service account for token authentication.
                    type: string
                  panels:
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizationpreferences.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: OrganizationPreferences
    listKind: OrganizationPreferencesList
    plural: organizationpreferences
    singular: organizationpreferences
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrganizationPreferences is an observe-only resource backed by
          the grafana_organization_preferences Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: OrganizationPreferencesParameters defines the input parameters
                  for the grafana_organization_preferences data source.
                properties:
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: OrganizationPreferencesObservation holds the observed
                  (computed) fields from the grafana_organization_preferences data
                  source.
                properties:
                  homeDashboardUID:
                    description: The Organization home dashboard UID. This is only
                      available in Grafana 9.0+.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  theme:
                    description: The Organization theme. Any string value is supported,
                      including custom themes. Common values are `light`, `dark`,
                      `system`, or an empty string for the default.
                    type: string
                  timezone:
                    description: The Organization timezone. Any string value is supported,
                      including IANA timezone names. Common values are `utc`, `browser`,
                      or an empty string for the default.
                    type: string
                  weekStart:
                    description: The Organization week start day. Available values
                      are `sunday`, `monday`, `saturday`, or an empty string for the
                      default.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizations.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Organization
    listKind: OrganizationList
    plural: organizations
    singular: organization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Organization is an observe-only resource backed by the grafana_organization
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: OrganizationParameters defines the input parameters for
                  the grafana_organization data source.
                properties:
                  name:
                    description: The name of the Organization.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: OrganizationObservation holds the observed (computed)
                  fields from the grafana_organization data source.
                properties:
                  admins:
                    description: A list of email addresses corresponding to users
                      given admin access to the organization.
                    items:
                      type: string
                    type: array
                  editors:
                    description: A list of email addresses corresponding to users
                      given editor access to the organization.
                    items:
                      type: string
                    type: array
                  viewers:
                    description: A list of email addresses corresponding to users
                      given viewer access to the organization.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: organizationusers.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: OrganizationUser
    listKind: OrganizationUserList
    plural: organizationusers
    singular: organizationuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrganizationUser is an observe-only resource backed by the grafana_organization_user
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: OrganizationUserParameters defines the input parameters
                  for the grafana_organization_user data source.
                properties:
                  email:
                    description: The email address of the Grafana user.
                    type: string
                  login:
                    description: The username of the Grafana user.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: OrganizationUserObservation holds the observed (computed)
                  fields from the grafana_organization_user data source.
                properties:
                  email:
                    description: The email address of the Grafana user.
                    type: string
                  login:
                    description: The username of the Grafana user.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  userID:
                    description: The numerical ID of the Grafana user.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceaccounts.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ServiceAccount
    listKind: ServiceAccountList
    plural: serviceaccounts
    singular: serviceaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceAccount is an observe-only resource backed by the grafana_service_account
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ServiceAccountParameters defines the input parameters
                  for the grafana_service_account data source.
                properties:
                  name:
                    description: The name of the Service Account.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ServiceAccountObservation holds the observed (computed)
                  fields from the grafana_service_account data source.
                properties:
                  isDisabled:
                    description: The disabled status for the service account.
                    type: boolean
                  orgID:
                    description: The Organization ID. If not set, the Org ID defined
                      in the provider block will be used.
                    type: string
                  role:
                    description: The basic role of the service account in the organization.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: teams.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Team
    listKind: TeamList
    plural: teams
    singular: team
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Team is an observe-only resource backed by the grafana_team Terraform
          data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: TeamParameters defines the input parameters for the grafana_team
                  data source.
                properties:
                  name:
                    description: The name of the Grafana team.
                    type: string
                  orgID:
                    description: The Organization ID. If not set, the default organization
                      is used for basic authentication, or the one that owns your
                      service account for token authentication.
                    type: string
                  readTeamSync:
                    description: Whether to read the team sync settings. This is only
                      available in Grafana Enterprise.
                    type: boolean
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: TeamObservation holds the observed (computed) fields
                  from the grafana_team data source.
                properties:
                  email:
                    description: An email address for the team.
                    type: string
                  members:
                    description: A set of email addresses corresponding to users who
                      are members of the team.
                    items:
                      type: string
                    type: array
                  orgID:
                    description: The Organization ID. If not set, the default organization
                      is used for basic authentication, or the one that owns your
                      service account for token authentication.
                    type: string
                  readTeamSync:
                    description: Whether to read the team sync settings. This is only
                      available in Grafana Enterprise.
                    type: boolean
                  teamID:
                    description: The team id assigned to this team by Grafana.
                    format: int64
                    type: integer
                  teamUID:
                    description: The team uid assigned to this team by Grafana.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: users.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is an observe-only resource backed by the grafana_user Terraform
          data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: UserParameters defines the input parameters for the grafana_user
                  data source.
                properties:
                  email:
                    description: The email address of the Grafana user.
                    type: string
                  login:
                    description: The username for the Grafana user.
                    type: string
                  userID:
                    description: The numerical ID of the Grafana user.
                    format: int64
                    type: integer
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: UserObservation holds the observed (computed) fields
                  from the grafana_user data source.
                properties:
                  email:
                    description: The email address of the Grafana user.
                    type: string
                  isAdmin:
                    description: Whether the user is an admin.
                    type: boolean
                  login:
                    description: The username for the Grafana user.
                    type: string
                  name:
                    description: The display name for the Grafana user.
                    type: string
                  userID:
                    description: The numerical ID of the Grafana user.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: usersets.oss.grafana.o.crossplane.io
spec:
  group: oss.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: UserSet
    listKind: UserSetList
    plural: usersets
    singular: userset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserSet is an observe-only resource backed by the grafana_users
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: UserSetParameters defines the input parameters for the
                  grafana_users data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: UserSetObservation holds the observed (computed) fields
                  from the grafana_users data source.
                properties:
                  users:
                    description: The Grafana instance's users.
                    items:
                      properties:
                        email:
                          description: The user's email.
                          type: string
                        id:
                          description: The user ID.
                          format: int64
                          type: integer
                        isAdmin:
                          description: Whether the user is admin or not.
                          type: boolean
                        login:
                          description: The user's login.
                          type: string
                        name:
                          description: The user's name.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: slos.slo.grafana.crossplane.io
spec:
  group: slo.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SLO
    listKind: SLOList
    plural: slos
    singular: slo
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SLO is the Schema for the SLOs API. Resource manages Grafana
          SLOs (Service Level Objectives). Official documentation https://grafana.com/docs/grafana-cloud/alerting-and-irm/slo/API
          documentation https://grafana.com/docs/grafana-cloud/alerting-and-irm/slo/api/Additional
          Information On Alerting Rule Annotations and Labels https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/#templating/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SLOSpec defines the desired state of SLO
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        ref:
                          description: Reference to a DataSource in oss to populate
                            uid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        selector:
                          description: Selector for a DataSource in oss to populate
                            uid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        ref:
                          description: Reference to a DataSource in oss to populate
                            uid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        selector:
                          description: Selector for a DataSource in oss to populate
                            uid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.description is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.description)
                || (has(self.initProvider) && has(self.initProvider.description))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SLOStatus defines the observed state of SLO.
            properties:
              atProvider:
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  id:
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: slos.slo.grafana.m.crossplane.io
spec:
  group: slo.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: SLO
    listKind: SLOList
    plural: slos
    singular: slo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SLO is the Schema for the SLOs API. Resource manages Grafana
          SLOs (Service Level Objectives). Official documentation https://grafana.com/docs/grafana-cloud/alerting-and-irm/slo/API
          documentation https://grafana.com/docs/grafana-cloud/alerting-and-irm/slo/api/Additional
          Information On Alerting Rule Annotations and Labels https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/#templating/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SLOSpec defines the desired state of SLO
            properties:
              forProvider:
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        ref:
                          description: Reference to a DataSource in oss to populate
                            uid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        selector:
                          description: Selector for a DataSource in oss to populate
                            uid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        ref:
                          description: Reference to a DataSource in oss to populate
                            uid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        selector:
                          description: Selector for a DataSource in oss to populate
                            uid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.description is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.description)
                || (has(self.initProvider) && has(self.initProvider.description))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SLOStatus defines the observed state of SLO.
            properties:
              atProvider:
                properties:
                  alerting:
                    description: |-
                      term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating. (see below for nested schema)
                      Configures the alerting rules that will be generated for each
                      time window associated with the SLO. Grafana SLOs can generate
                      alerts when the short-term error budget burn is very high, the
                      long-term error budget burn rate is high, or when the remaining
                      error budget is below a certain threshold. Annotations and Labels support templating.
                    items:
                      properties:
                        advancedOptions:
                          description: |-
                            (Block List) Advanced Options for Alert Rules (see below for nested schema)
                            Advanced Options for Alert Rules
                          items:
                            properties:
                              minFailures:
                                description: |-
                                  (Number) Minimum number of failed events to trigger an alert
                                  Minimum number of failed events to trigger an alert
                                type: number
                            type: object
                          type: array
                        annotation:
                          description: |-
                            (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                            Annotations will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        fastburn:
                          description: |-
                            (Block List) Alerting Rules generated for Fast Burn alerts (see below for nested schema)
                            Alerting Rules generated for Fast Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Fast Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        label:
                          description: |-
                            "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                            Labels will be attached to all alerts generated by any of these rules.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) Key for filtering and identification
                                  Key for filtering and identification
                                type: string
                              value:
                                description: |-
                                  (String) Templatable value
                                  Templatable value
                                type: string
                            type: object
                          type: array
                        slowburn:
                          description: |-
                            (Block List) Alerting Rules generated for Slow Burn alerts (see below for nested schema)
                            Alerting Rules generated for Slow Burn alerts
                          items:
                            properties:
                              annotation:
                                description: |-
                                  (Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
                                  Annotations to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                              enrichment:
                                description: |-
                                  (Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
                                  Enrichments to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    type:
                                      description: |-
                                        (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                        Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                                      type: string
                                  type: object
                                type: array
                              label:
                                description: |-
                                  "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                                  Labels to attach only to Slow Burn alerts.
                                items:
                                  properties:
                                    key:
                                      description: |-
                                        (String) Key for filtering and identification
                                        Key for filtering and identification
                                      type: string
                                    value:
                                      description: |-
                                        (String) Templatable value
                                        Templatable value
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  description:
                    description: |-
                      text field that can provide more context to an SLO.
                      Description is a free-text field that can provide more context to an SLO.
                    type: string
                  destinationDatasource:
                    description: |-
                      (Block List) Required. Destination Datasource sets the datasource defined for an SLO. (see below for nested schema)
                      **Required.** Destination Datasource sets the datasource defined for an SLO.
                    items:
                      properties:
                        uid:
                          description: |-
                            (String) UID for the Datasource
                            UID for the Datasource
                          type: string
                      type: object
                    type: array
                  folderUid:
                    description: |-
                      empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                      UID for the SLO folder. Must be non-empty if set; omit the attribute entirely to associate the SLO with the default Grafana SLO folder.
                    type: string
                  id:
                    type: string
                  label:
                    description: |-
                      "^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
                      Additional labels that will be attached to all metrics generated from the query. These labels are useful for grouping SLOs in dashboard views that you create by hand. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key for filtering and identification
                            Key for filtering and identification
                          type: string
                        value:
                          description: |-
                            (String) Templatable value
                            Templatable value
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name should be a short description of your indicator. Consider names like "API Availability"
                      Name should be a short description of your indicator. Consider names like "API Availability"
                    type: string
                  objectives:
                    description: |-
                      (Block List) Required. Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget. (see below for nested schema)
                      **Required.** Over each rolling time window, the remaining error budget will be calculated, and separate alerts can be generated for each time window based on the SLO burn rate or remaining error budget.
                    items:
                      properties:
                        value:
                          description: |-
                            (String) Templatable value
                            Value between 0 and 1. If the value of the query is above the objective, the SLO is met.
                          type: number
                        window:
                          description: |-
                            parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                            A Prometheus-parsable time duration string like 24h, 60m. This is the time window the objective is measured over.
                          type: string
                      type: object
                    type: array
                  query:
                    description: |-
                      (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                      **Required.** Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported.
                    items:
                      properties:
                        freeform:
                          description: |-
                            (Block List) Freeform query configuration. (see below for nested schema)
                            Freeform query configuration.
                          items:
                            properties:
                              query:
                                description: |-
                                  (Block List) Required. Query describes the indicator that will be measured against the objective. Freeform Query types are currently supported. (see below for nested schema)
                                  Freeform Query Field - valid promQl
                                type: string
                            type: object
                          type: array
                        grafanaQueries:
                          description: |-
                            (Block List) Array for holding a set of grafana queries (see below for nested schema)
                            Array for holding a set of grafana queries
                          items:
                            properties:
                              grafanaQueries:
                                description: |-
                                  (Block List) Array for holding a set of grafana queries (see below for nested schema)
                                  Query Object - Array of Grafana Query JSON objects
                                type: string
                            type: object
                          type: array
                        ratio:
                          description: |-
                            (Block List) Ratio query configuration. (see below for nested schema)
                            Ratio query configuration.
                          items:
                            properties:
                              groupByLabels:
                                description: |-
                                  label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                  Defines Group By Labels used for per-label alerting. These appear as variables on SLO dashboards to enable filtering and aggregation. Labels must adhere to Prometheus label name schema - "^[a-zA-Z_][a-zA-Z0-9_]*$"
                                items:
                                  type: string
                                type: array
                              successMetric:
                                description: |-
                                  (String) Counter metric for success events (numerator)
                                  Counter metric for success events (numerator)
                                type: string
                              totalMetric:
                                description: |-
                                  (String) Metric for total events (denominator)
                                  Metric for total events (denominator)
                                type: string
                            type: object
                          type: array
                        type:
                          description: |-
                            (String) Type of the alert enrichment. Currently only "assistantInvestigation" is supported.
                            Query type must be one of: "freeform", "query", "ratio", "grafana_queries" or "threshold"
                          type: string
                      type: object
                    type: array
                  searchExpression:
                    description: |-
                      empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                      The name of a search expression in Grafana Asserts. Must be non-empty if set; omit the attribute entirely to leave it unset. This is used in the SLO UI to open the Asserts RCA workbench and in alerts to link to the RCA workbench.
                    type: string
                  uuid:
                    description: |-
                      (String) UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                      UUID for the SLO. Custom UUIDs can be set. If not provided, a random UUID will be generated by the API.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: slos.slo.grafana.o.crossplane.io
spec:
  group: slo.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: SLOs
    listKind: SLOsList
    plural: slos
    singular: slos
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SLOs is an observe-only resource backed by the grafana_slos Terraform
          data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: SLOsParameters defines the input parameters for the grafana_slos
                  data source.
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: SLOsObservation holds the observed (computed) fields
                  from the grafana_slos data source.
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: checkalerts.sm.grafana.crossplane.io
spec:
  group: sm.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CheckAlerts
    listKind: CheckAlertsList
    plural: checkalerts
    singular: checkalerts
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CheckAlerts is the Schema for the CheckAlertss API. Manages alerts
          for a check in Grafana Synthetic Monitoring. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/configure-per-check-alerts/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CheckAlertsSpec defines the desired state of CheckAlerts
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  checkRef:
                    description: Reference to a Check in sm to populate checkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  checkSelector:
                    description: Selector for a Check in sm to populate checkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  checkRef:
                    description: Reference to a Check in sm to populate checkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  checkSelector:
                    description: Selector for a Check in sm to populate checkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.alerts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alerts)
                || (has(self.initProvider) && has(self.initProvider.alerts))'
          status:
            description: CheckAlertsStatus defines the observed state of CheckAlerts.
            properties:
              atProvider:
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: checks.sm.grafana.crossplane.io
spec:
  group: sm.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Check
    listKind: CheckList
    plural: checks
    singular: check
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Check is the Schema for the Checks API. Synthetic Monitoring
          checks are tests that run on selected probes at defined intervals and report
          metrics and logs back to your Grafana Cloud account. The target for checks
          can be a domain name, a server, or a website, depending on what information
          you would like to gather about your endpoint. You can define multiple checks
          for a single endpoint to check different capabilities. Official documentation
          https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CheckSpec defines the desired state of Check
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    passwordSecretRef:
                                      description: |-
                                        (String, Sensitive) Basic auth password.
                                        Basic auth password.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  type: object
                                type: array
                              bearerTokenSecretRef:
                                description: |-
                                  (String, Sensitive) Token for use with bearer authorization header.
                                  Token for use with bearer authorization header.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    passwordSecretRef:
                                      description: |-
                                        (String, Sensitive) Basic auth password.
                                        Basic auth password.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  required:
                                  - passwordSecretRef
                                  type: object
                                type: array
                              bearerTokenSecretRef:
                                description: |-
                                  (String, Sensitive) Token for use with bearer authorization header.
                                  Token for use with bearer authorization header.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                        namespace:
                                          description: Namespace of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      - namespace
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.job is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.job)
                || (has(self.initProvider) && has(self.initProvider.job))'
            - message: spec.forProvider.probes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.probes)
                || (has(self.initProvider) && has(self.initProvider.probes))'
            - message: spec.forProvider.settings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.settings)
                || (has(self.initProvider) && has(self.initProvider.settings))'
            - message: spec.forProvider.target is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.target)
                || (has(self.initProvider) && has(self.initProvider.target))'
          status:
            description: CheckStatus defines the observed state of Check.
            properties:
              atProvider:
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  id:
                    description: (String) The ID of the check.
                    type: string
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  type: object
                                type: array
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  tenantId:
                    description: |-
                      (Number) The tenant ID of the check.
                      The tenant ID of the check.
                    type: number
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: installations.sm.grafana.crossplane.io
spec:
  group: sm.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Installation
    listKind: InstallationList
    plural: installations
    singular: installation
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Installation is the Schema for the Installations API. Sets up
          Synthetic Monitoring on a Grafana cloud stack and generates a token. Once
          a Grafana Cloud stack is created, a user can either use this resource or
          go into the UI to install synthetic monitoring. This resource cannot be
          imported but it can be used on an existing Synthetic Monitoring installation
          without issues. Note that this resource must be used on a provider configured
          with Grafana Cloud credentials. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/API
          documentation https://github.com/grafana/synthetic-monitoring-api-go-client/blob/main/docs/API.md#apiv1registerinstall
          Required access policy scopes: stacks:read'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstallationSpec defines the desired state of Installation
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  metricsPublisherKeySecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy with the following scopes: stacks:read, metrics:write, logs:write, traces:write. This is used to publish metrics and logs to Grafana Cloud stack.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/) with the following scopes: `stacks:read`, `metrics:write`, `logs:write`, `traces:write`. This is used to publish metrics and logs to Grafana Cloud stack.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  metricsPublisherKeySecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy with the following scopes: stacks:read, metrics:write, logs:write, traces:write. This is used to publish metrics and logs to Grafana Cloud stack.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/) with the following scopes: `stacks:read`, `metrics:write`, `logs:write`, `traces:write`. This is used to publish metrics and logs to Grafana Cloud stack.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                required:
                - metricsPublisherKeySecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metricsPublisherKeySecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metricsPublisherKeySecretRef)'
          status:
            description: InstallationStatus defines the observed state of Installation.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  smAccessToken:
                    description: |-
                      (String) Generated token to access the SM API.
                      Generated token to access the SM API.
                    type: string
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: probes.sm.grafana.crossplane.io
spec:
  group: sm.grafana.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Probe
    listKind: ProbeList
    plural: probes
    singular: probe
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Probe is the Schema for the Probes API. Besides the public probes
          run by Grafana Labs, you can also install your own private probes. These
          are only accessible to you and only write data to your Grafana Cloud account.
          Private probes are instances of the open source Grafana Synthetic Monitoring
          Agent. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProbeSpec defines the desired state of Probe
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.latitude is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.latitude)
                || (has(self.initProvider) && has(self.initProvider.latitude))'
            - message: spec.forProvider.longitude is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.longitude)
                || (has(self.initProvider) && has(self.initProvider.longitude))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: ProbeStatus defines the observed state of Probe.
            properties:
              atProvider:
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of the probe.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                  tenantId:
                    description: |-
                      (Number) The tenant ID of the probe.
                      The tenant ID of the probe.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: checkalerts.sm.grafana.m.crossplane.io
spec:
  group: sm.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: CheckAlerts
    listKind: CheckAlertsList
    plural: checkalerts
    singular: checkalerts
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CheckAlerts is the Schema for the CheckAlertss API. Manages alerts
          for a check in Grafana Synthetic Monitoring. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/configure-per-check-alerts/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CheckAlertsSpec defines the desired state of CheckAlerts
            properties:
              forProvider:
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  checkRef:
                    description: Reference to a Check in sm to populate checkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  checkSelector:
                    description: Selector for a Check in sm to populate checkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  checkRef:
                    description: Reference to a Check in sm to populate checkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  checkSelector:
                    description: Selector for a Check in sm to populate checkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.alerts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alerts)
                || (has(self.initProvider) && has(self.initProvider.alerts))'
          status:
            description: CheckAlertsStatus defines the observed state of CheckAlerts.
            properties:
              atProvider:
                properties:
                  alerts:
                    description: |-
                      (Set of Object) List of alerts for the check. (see below for nested schema)
                      List of alerts for the check.
                    items:
                      properties:
                        name:
                          description: (String)
                          type: string
                        period:
                          description: (String)
                          type: string
                        runbookUrl:
                          description: (String)
                          type: string
                        threshold:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  checkId:
                    description: |-
                      (Number) The ID of the check to manage alerts for.
                      The ID of the check to manage alerts for.
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: checks.sm.grafana.m.crossplane.io
spec:
  group: sm.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Check
    listKind: CheckList
    plural: checks
    singular: check
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Check is the Schema for the Checks API. Synthetic Monitoring
          checks are tests that run on selected probes at defined intervals and report
          metrics and logs back to your Grafana Cloud account. The target for checks
          can be a domain name, a server, or a website, depending on what information
          you would like to gather about your endpoint. You can define multiple checks
          for a single endpoint to check different capabilities. Official documentation
          https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CheckSpec defines the desired state of Check
            properties:
              forProvider:
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    passwordSecretRef:
                                      description: |-
                                        (String, Sensitive) Basic auth password.
                                        Basic auth password.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  type: object
                                type: array
                              bearerTokenSecretRef:
                                description: |-
                                  (String, Sensitive) Token for use with bearer authorization header.
                                  Token for use with bearer authorization header.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    passwordSecretRef:
                                      description: |-
                                        (String, Sensitive) Basic auth password.
                                        Basic auth password.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  required:
                                  - passwordSecretRef
                                  type: object
                                type: array
                              bearerTokenSecretRef:
                                description: |-
                                  (String, Sensitive) Token for use with bearer authorization header.
                                  Token for use with bearer authorization header.
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    clientKeySecretRef:
                                      description: |-
                                        (String, Sensitive) Client key in PEM format.
                                        Client key in PEM format.
                                      properties:
                                        key:
                                          type: string
                                        name:
                                          description: Name of the secret.
                                          type: string
                                      required:
                                      - key
                                      - name
                                      type: object
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.job is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.job)
                || (has(self.initProvider) && has(self.initProvider.job))'
            - message: spec.forProvider.probes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.probes)
                || (has(self.initProvider) && has(self.initProvider.probes))'
            - message: spec.forProvider.settings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.settings)
                || (has(self.initProvider) && has(self.initProvider.settings))'
            - message: spec.forProvider.target is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.target)
                || (has(self.initProvider) && has(self.initProvider.target))'
          status:
            description: CheckStatus defines the observed state of Check.
            properties:
              atProvider:
                properties:
                  alertSensitivity:
                    description: |-
                      (String) Can be set to none, low, medium, or high to correspond to the check alert levels. Defaults to none.
                      Can be set to `none`, `low`, `medium`, or `high` to correspond to the check [alert levels](https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/configure-alerts/synthetic-monitoring-alerting/). Defaults to `none`.
                    type: string
                  basicMetricsOnly:
                    description: |-
                      (Boolean) Metrics are reduced by default. Set this to false if you'd like to publish all metrics. We maintain a full list of metrics collected for each. Defaults to true.
                      Metrics are reduced by default. Set this to `false` if you'd like to publish all metrics. We maintain a [full list of metrics](https://github.com/grafana/synthetic-monitoring-agent/tree/main/internal/scraper/testdata) collected for each. Defaults to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether to enable the check. Defaults to true.
                      Whether to enable the check. Defaults to `true`.
                    type: boolean
                  frequency:
                    description: |-
                      (Number) How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to 60000.
                      How often the check runs in milliseconds (the value is not truly a "frequency" but a "period"). The minimum acceptable value is 1 second (1000 ms), and the maximum is 1 hour (3600000 ms). Defaults to `60000`.
                    type: number
                  id:
                    description: (String) The ID of the check.
                    type: string
                  job:
                    description: |-
                      (String) Name used for job label.
                      Name used for job label.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels that can be specified per check is 5. These are applied, along with the probe-specific labels, to the outgoing metrics. The names and values of the labels cannot be empty, and the maximum length is 32 bytes.
                    type: object
                    x-kubernetes-map-type: granular
                  probeNames:
                    description: |-
                      CUSTOM FIELD: List of probe names to resolve to probe IDs.
                      If set, this will override the Probes field with the resolved probe IDs on every reconciliation.
                      This field is excluded from Terraform via tf:"-" tag.
                    items:
                      type: string
                    type: array
                  probes:
                    description: |-
                      (Set of Number) List of probe location IDs where this target will be checked from.
                      List of probe location IDs where this target will be checked from.
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  settings:
                    description: |-
                      (Block Set, Min: 1, Max: 1) Check settings. Should contain exactly one nested block. (see below for nested schema)
                      Check settings. Should contain exactly one nested block.
                    items:
                      properties:
                        browser:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/. (see below for nested schema)
                            Settings for browser check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6-browser/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        dns:
                          description: |-
                            (Block Set, Max: 1) Settings for DNS check. The target must be a valid hostname (or IP address for PTR records). (see below for nested schema)
                            Settings for DNS check. The target must be a valid hostname (or IP address for `PTR` records).
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              port:
                                description: |-
                                  (Number) Port to target. Defaults to 53.
                                  Port to target. Defaults to `53`.
                                type: number
                              protocol:
                                description: |-
                                  (String) TCP or UDP. Defaults to UDP.
                                  `TCP` or `UDP`. Defaults to `UDP`.
                                type: string
                              recordType:
                                description: |-
                                  (String) One of ANY, A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT. Defaults to A.
                                  One of `ANY`, `A`, `AAAA`, `CNAME`, `MX`, `NS`, `PTR`, `SOA`, `SRV`, `TXT`. Defaults to `A`.
                                type: string
                              server:
                                description: |-
                                  (String) DNS server address to target. Defaults to 8.8.8.8.
                                  DNS server address to target. Defaults to `8.8.8.8`.
                                type: string
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              validRCodes:
                                description: |-
                                  (Set of String) List of valid response codes. Options include NOERROR, BADALG, BADMODE, BADKEY, BADCOOKIE, BADNAME, BADSIG, BADTIME, BADTRUNC, BADVERS, FORMERR, NOTIMP, NOTAUTH, NOTZONE, NXDOMAIN, NXRRSET, REFUSED, SERVFAIL, YXDOMAIN, YXRRSET.
                                  List of valid response codes. Options include `NOERROR`, `BADALG`, `BADMODE`, `BADKEY`, `BADCOOKIE`, `BADNAME`, `BADSIG`, `BADTIME`, `BADTRUNC`, `BADVERS`, `FORMERR`, `NOTIMP`, `NOTAUTH`, `NOTZONE`, `NXDOMAIN`, `NXRRSET`, `REFUSED`, `SERVFAIL`, `YXDOMAIN`, `YXRRSET`.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validateAdditionalRrs:
                                description: |-
                                  (Block Set) Validate additional matches. (see below for nested schema)
                                  Validate additional matches.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAnswerRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response answer. (see below for nested schema)
                                  Validate response answer.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              validateAuthorityRrs:
                                description: |-
                                  (Block Set, Max: 1) Validate response authority. (see below for nested schema)
                                  Validate response authority.
                                items:
                                  properties:
                                    failIfMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value matches regex.
                                        Fail if value matches regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    failIfNotMatchesRegexp:
                                      description: |-
                                        (Set of String) Fail if value does not match regex.
                                        Fail if value does not match regex.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                        grpc:
                          description: |-
                            (Block Set, Max: 1) Settings for gRPC Health check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for gRPC Health check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              service:
                                description: |-
                                  (String) gRPC service.
                                  gRPC service.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        http:
                          description: |-
                            (Block Set, Max: 1) Settings for HTTP check. The target must be a URL (http or https). (see below for nested schema)
                            Settings for HTTP check. The target must be a URL (http or https).
                          items:
                            properties:
                              basicAuth:
                                description: |-
                                  (Block Set, Max: 1) Basic auth settings. (see below for nested schema)
                                  Basic auth settings.
                                items:
                                  properties:
                                    username:
                                      description: |-
                                        (String) Basic auth username.
                                        Basic auth username.
                                      type: string
                                  type: object
                                type: array
                              body:
                                description: |-
                                  (String) The body of the HTTP request used in probe.
                                  The body of the HTTP request used in probe.
                                type: string
                              cacheBustingQueryParamName:
                                description: |-
                                  (String) The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                  The name of the query parameter used to prevent the server from using a cached response. Each probe will assign a random value to this parameter each time a request is made.
                                type: string
                              compression:
                                description: |-
                                  (String) Check fails if the response body is not compressed using this compression algorithm. One of none, identity, br, gzip, deflate.
                                  Check fails if the response body is not compressed using this compression algorithm. One of `none`, `identity`, `br`, `gzip`, `deflate`.
                                type: string
                              failIfBodyMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any match the response body, the check will fail.
                                  List of regexes. If any match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfBodyNotMatchesRegexp:
                                description: |-
                                  (Set of String) List of regexes. If any do not match the response body, the check will fail.
                                  List of regexes. If any do not match the response body, the check will fail.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              failIfHeaderMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers match. (see below for nested schema)
                                  Check fails if headers match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfHeaderNotMatchesRegexp:
                                description: |-
                                  (Block Set) Check fails if headers do not match. (see below for nested schema)
                                  Check fails if headers do not match.
                                items:
                                  properties:
                                    allowMissing:
                                      description: |-
                                        (Boolean) Allow header to be missing from responses. Defaults to false.
                                        Allow header to be missing from responses. Defaults to `false`.
                                      type: boolean
                                    header:
                                      description: |-
                                        (String) Header name.
                                        Header name.
                                      type: string
                                    regexp:
                                      description: |-
                                        (String) Regex that header value should match.
                                        Regex that header value should match.
                                      type: string
                                  type: object
                                type: array
                              failIfNotSsl:
                                description: |-
                                  (Boolean) Fail if SSL is not present. Defaults to false.
                                  Fail if SSL is not present. Defaults to `false`.
                                type: boolean
                              failIfSsl:
                                description: |-
                                  (Boolean) Fail if SSL is present. Defaults to false.
                                  Fail if SSL is present. Defaults to `false`.
                                type: boolean
                              headers:
                                description: |-
                                  (Set of String) The HTTP headers set for the probe.
                                  The HTTP headers set for the probe.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              method:
                                description: |-
                                  (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                  Request method. One of `GET`, `CONNECT`, `DELETE`, `HEAD`, `OPTIONS`, `POST`, `PUT`, `TRACE` Defaults to `GET`.
                                type: string
                              noFollowRedirects:
                                description: |-
                                  (Boolean) Do not follow redirects. Defaults to false.
                                  Do not follow redirects. Defaults to `false`.
                                type: boolean
                              proxyConnectHeaders:
                                description: |-
                                  (Set of String) The HTTP headers sent to the proxy URL
                                  The HTTP headers sent to the proxy URL
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              proxyUrl:
                                description: |-
                                  (String) Proxy URL.
                                  Proxy URL.
                                type: string
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                              validHttpVersions:
                                description: |-
                                  (Set of String) List of valid HTTP versions. Options include HTTP/1.0, HTTP/1.1, HTTP/2.0
                                  List of valid HTTP versions. Options include `HTTP/1.0`, `HTTP/1.1`, `HTTP/2.0`
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              validStatusCodes:
                                description: |-
                                  (Set of Number) Accepted status codes. If unset, defaults to 2xx.
                                  Accepted status codes. If unset, defaults to 2xx.
                                items:
                                  type: number
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        multihttp:
                          description: |-
                            (Block Set, Max: 1) Settings for MultiHTTP check. The target must be a URL (http or https) (see below for nested schema)
                            Settings for MultiHTTP check. The target must be a URL (http or https)
                          items:
                            properties:
                              entries:
                                description: (Block List) (see below for nested schema)
                                items:
                                  properties:
                                    assertions:
                                      description: |-
                                        (Block List) Assertions to make on the request response (see below for nested schema)
                                        Assertions to make on the request response
                                      items:
                                        properties:
                                          condition:
                                            description: |-
                                              (String) The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                              The condition of the assertion: NOT_CONTAINS, EQUALS, STARTS_WITH, ENDS_WITH, TYPE_OF, CONTAINS
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression of the assertion. Should start with $.
                                            type: string
                                          subject:
                                            description: |-
                                              (String) The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                              The subject of the assertion: RESPONSE_HEADERS, HTTP_STATUS_CODE, RESPONSE_BODY
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                            type: string
                                          value:
                                            description: |-
                                              (String) The value of the assertion
                                              The value of the assertion
                                            type: string
                                        type: object
                                      type: array
                                    request:
                                      description: |-
                                        (Block Set, Max: 1) An individual MultiHTTP request (see below for nested schema)
                                        An individual MultiHTTP request
                                      items:
                                        properties:
                                          body:
                                            description: |-
                                              (String) The body of the HTTP request used in probe.
                                              The body of the HTTP request used in probe.
                                            items:
                                              properties:
                                                contentEncoding:
                                                  description: |-
                                                    (String) The content encoding of the body
                                                    The content encoding of the body
                                                  type: string
                                                contentType:
                                                  description: |-
                                                    (String) The content type of the body
                                                    The content type of the body
                                                  type: string
                                                payload:
                                                  description: |-
                                                    (String) The body payload
                                                    The body payload
                                                  type: string
                                              type: object
                                            type: array
                                          headers:
                                            description: |-
                                              (Set of String) The HTTP headers set for the probe.
                                              The headers to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the header to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the header to send
                                                  type: string
                                              type: object
                                            type: array
                                          method:
                                            description: |-
                                              (String) Request method. One of GET, CONNECT, DELETE, HEAD, OPTIONS, POST, PUT, TRACE Defaults to GET.
                                              The HTTP method to use
                                            type: string
                                          queryFields:
                                            description: |-
                                              (Block Set) Query fields to send with the request (see below for nested schema)
                                              Query fields to send with the request
                                            items:
                                              properties:
                                                name:
                                                  description: |-
                                                    (String) Name of the header to send
                                                    Name of the query field to send
                                                  type: string
                                                value:
                                                  description: |-
                                                    (String) The value of the assertion
                                                    Value of the query field to send
                                                  type: string
                                              type: object
                                            type: array
                                          url:
                                            description: |-
                                              (String) The URL for the request
                                              The URL for the request
                                            type: string
                                        type: object
                                      type: array
                                    variables:
                                      description: |-
                                        (Block List) Variables to extract from the request response (see below for nested schema)
                                        Variables to extract from the request response
                                      items:
                                        properties:
                                          attribute:
                                            description: |-
                                              (String) The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                              The attribute to use when finding the variable value. Only used when type is CSS_SELECTOR
                                            type: string
                                          expression:
                                            description: |-
                                              (String) The expression of the assertion. Should start with $.
                                              The expression to when finding the variable. Should start with $. Only use when type is JSON_PATH or REGEX
                                            type: string
                                          name:
                                            description: |-
                                              (String) Name of the header to send
                                              The name of the variable to extract
                                            type: string
                                          type:
                                            description: |-
                                              (String) The type of assertion to make: TEXT, JSON_PATH_VALUE, JSON_PATH_ASSERTION, REGEX_ASSERTION
                                              The method of finding the variable value to extract. JSON_PATH, REGEX, CSS_SELECTOR
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        ping:
                          description: |-
                            (Block Set, Max: 1) Settings for ping (ICMP) check. The target must be a valid hostname or IP address. (see below for nested schema)
                            Settings for ping (ICMP) check. The target must be a valid hostname or IP address.
                          items:
                            properties:
                              dontFragment:
                                description: |-
                                  bit in the IP-header. Only works with ipV4. Defaults to false.
                                  Set the DF-bit in the IP-header. Only works with ipV4. Defaults to `false`.
                                type: boolean
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              payloadSize:
                                description: |-
                                  (Number) Payload size. Defaults to 0.
                                  Payload size. Defaults to `0`.
                                type: number
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                            type: object
                          type: array
                        scripted:
                          description: |-
                            cloud/testing/synthetic-monitoring/create-checks/checks/k6/. (see below for nested schema)
                            Settings for scripted check. See https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/create-checks/checks/k6/.
                          items:
                            properties:
                              script:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        tcp:
                          description: |-
                            (Block Set, Max: 1) Settings for TCP check. The target must be of the form <host>:<port>, where the host portion must be a valid hostname or IP address. (see below for nested schema)
                            Settings for TCP check. The target must be of the form `<host>:<port>`, where the host portion must be a valid hostname or IP address.
                          items:
                            properties:
                              ipVersion:
                                description: |-
                                  (String) Options are V4, V6, Any. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The Any value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to V4.
                                  Options are `V4`, `V6`, `Any`. Specifies whether the corresponding check will be performed using IPv4 or IPv6. The `Any` value indicates that IPv6 should be used, falling back to IPv4 if that's not available. Defaults to `V4`.
                                type: string
                              queryResponse:
                                description: |-
                                  (Block Set) The query sent in the TCP probe and the expected associated response. (see below for nested schema)
                                  The query sent in the TCP probe and the expected associated response.
                                items:
                                  properties:
                                    expect:
                                      description: |-
                                        (String) Response to expect.
                                        Response to expect.
                                      type: string
                                    send:
                                      description: |-
                                        (String) Data to send.
                                        Data to send.
                                      type: string
                                    startTls:
                                      description: |-
                                        (Boolean) Upgrade TCP connection to TLS. Defaults to false.
                                        Upgrade TCP connection to TLS. Defaults to `false`.
                                      type: boolean
                                  type: object
                                type: array
                              sourceIpAddress:
                                description: |-
                                  (String) Source IP address.
                                  Source IP address.
                                type: string
                              tls:
                                description: |-
                                  (Boolean) Whether or not TLS is used when the connection is initiated. Defaults to false.
                                  Whether or not TLS is used when the connection is initiated. Defaults to `false`.
                                type: boolean
                              tlsConfig:
                                description: |-
                                  (Block Set, Max: 1) TLS config. (see below for nested schema)
                                  TLS config.
                                items:
                                  properties:
                                    caCert:
                                      description: |-
                                        (String) CA certificate in PEM format.
                                        CA certificate in PEM format.
                                      type: string
                                    clientCert:
                                      description: |-
                                        (String) Client certificate in PEM format.
                                        Client certificate in PEM format.
                                      type: string
                                    insecureSkipVerify:
                                      description: |-
                                        (Boolean) Disable target certificate validation. Defaults to false.
                                        Disable target certificate validation. Defaults to `false`.
                                      type: boolean
                                    serverName:
                                      description: |-
                                        (String) Used to verify the hostname for the targets.
                                        Used to verify the hostname for the targets.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        traceroute:
                          description: |-
                            (Block Set, Max: 1) Settings for traceroute check. The target must be a valid hostname or IP address (see below for nested schema)
                            Settings for traceroute check. The target must be a valid hostname or IP address
                          items:
                            properties:
                              maxHops:
                                description: |-
                                  (Number) Maximum TTL for the trace Defaults to 64.
                                  Maximum TTL for the trace Defaults to `64`.
                                type: number
                              maxUnknownHops:
                                description: |-
                                  (Number) Maximum number of hosts to travers that give no response Defaults to 15.
                                  Maximum number of hosts to travers that give no response Defaults to `15`.
                                type: number
                              ptrLookup:
                                description: |-
                                  (Boolean) Reverse lookup hostnames from IP addresses Defaults to true.
                                  Reverse lookup hostnames from IP addresses Defaults to `true`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  target:
                    description: |-
                      (String) Hostname to ping.
                      Hostname to ping.
                    type: string
                  tenantId:
                    description: |-
                      (Number) The tenant ID of the check.
                      The tenant ID of the check.
                    type: number
                  timeout:
                    description: |-
                      (Number) Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to 3000.
                      Specifies the maximum running time for the check in milliseconds. The minimum acceptable value is 1 second (1000 ms), and the maximum 180 seconds (180000 ms). Defaults to `3000`.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: installations.sm.grafana.m.crossplane.io
spec:
  group: sm.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Installation
    listKind: InstallationList
    plural: installations
    singular: installation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: 'Installation is the Schema for the Installations API. Sets up
          Synthetic Monitoring on a Grafana cloud stack and generates a token. Once
          a Grafana Cloud stack is created, a user can either use this resource or
          go into the UI to install synthetic monitoring. This resource cannot be
          imported but it can be used on an existing Synthetic Monitoring installation
          without issues. Note that this resource must be used on a provider configured
          with Grafana Cloud credentials. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/API
          documentation https://github.com/grafana/synthetic-monitoring-api-go-client/blob/main/docs/API.md#apiv1registerinstall
          Required access policy scopes: stacks:read'
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstallationSpec defines the desired state of Installation
            properties:
              forProvider:
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  metricsPublisherKeySecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy with the following scopes: stacks:read, metrics:write, logs:write, traces:write. This is used to publish metrics and logs to Grafana Cloud stack.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/) with the following scopes: `stacks:read`, `metrics:write`, `logs:write`, `traces:write`. This is used to publish metrics and logs to Grafana Cloud stack.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudStackRef:
                    description: Reference to a Stack in cloud to populate stackId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudStackSelector:
                    description: Selector for a Stack in cloud to populate stackId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  metricsPublisherKeySecretRef:
                    description: |-
                      (String, Sensitive) The Grafana Cloud access policy with the following scopes: stacks:read, metrics:write, logs:write, traces:write. This is used to publish metrics and logs to Grafana Cloud stack.
                      The [Grafana Cloud access policy](https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/) with the following scopes: `stacks:read`, `metrics:write`, `logs:write`, `traces:write`. This is used to publish metrics and logs to Grafana Cloud stack.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                required:
                - metricsPublisherKeySecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metricsPublisherKeySecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metricsPublisherKeySecretRef)'
          status:
            description: InstallationStatus defines the observed state of Installation.
            properties:
              atProvider:
                properties:
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  smAccessToken:
                    description: |-
                      (String) Generated token to access the SM API.
                      Generated token to access the SM API.
                    type: string
                  stackId:
                    description: |-
                      (String) The ID or slug of the stack to install SM on.
                      The ID or slug of the stack to install SM on.
                    type: string
                  stackSmApiUrl:
                    description: |-
                      cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                      The URL of the SM API to install SM on. This depends on the stack region, find the list of API URLs here: https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/#probe-api-server-url. A static mapping exists in the provider but it may not contain all the regions. If it does contain the stack's region, this field is computed automatically and readable.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: probes.sm.grafana.m.crossplane.io
spec:
  group: sm.grafana.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafana
    kind: Probe
    listKind: ProbeList
    plural: probes
    singular: probe
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Probe is the Schema for the Probes API. Besides the public probes
          run by Grafana Labs, you can also install your own private probes. These
          are only accessible to you and only write data to your Grafana Cloud account.
          Private probes are instances of the open source Grafana Synthetic Monitoring
          Agent. Official documentation https://grafana.com/docs/grafana-cloud/testing/synthetic-monitoring/set-up/set-up-private-probes/
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProbeSpec defines the desired state of Probe
            properties:
              forProvider:
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.latitude is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.latitude)
                || (has(self.initProvider) && has(self.initProvider.latitude))'
            - message: spec.forProvider.longitude is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.longitude)
                || (has(self.initProvider) && has(self.initProvider.longitude))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: ProbeStatus defines the observed state of Probe.
            properties:
              atProvider:
                properties:
                  disableBrowserChecks:
                    description: |-
                      (Boolean) Disables browser checks for this probe. Defaults to false.
                      Disables browser checks for this probe. Defaults to `false`.
                    type: boolean
                  disableScriptedChecks:
                    description: |-
                      (Boolean) Disables scripted checks for this probe. Defaults to false.
                      Disables scripted checks for this probe. Defaults to `false`.
                    type: boolean
                  id:
                    description: (String) The ID of the probe.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                      Custom labels to be included with collected metrics and logs. The maximum number of labels for private probes is 3.
                    type: object
                    x-kubernetes-map-type: granular
                  latitude:
                    description: |-
                      (Number) Latitude coordinates.
                      Latitude coordinates.
                    type: number
                  longitude:
                    description: |-
                      (Number) Longitude coordinates.
                      Longitude coordinates.
                    type: number
                  name:
                    description: |-
                      (String) Name of the probe.
                      Name of the probe.
                    type: string
                  public:
                    description: |-
                      (Boolean) Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to true. Defaults to false.
                      Public probes are run by Grafana Labs and can be used by all users. Only Grafana Labs managed public probes will be set to `true`. Defaults to `false`.
                    type: boolean
                  region:
                    description: |-
                      (String) Region of the probe.
                      Region of the probe.
                    type: string
                  tenantId:
                    description: |-
                      (Number) The tenant ID of the probe.
                      The tenant ID of the probe.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: probes.sm.grafana.o.crossplane.io
spec:
  group: sm.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: Probe
    listKind: ProbeList
    plural: probes
    singular: probe
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Probe is an observe-only resource backed by the grafana_synthetic_monitoring_probe
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProbeParameters defines the input parameters for the
                  grafana_synthetic_monitoring_probe data source.
                properties:
                  name:
                    description: Name of the probe.
                    type: string
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProbeObservation holds the observed (computed) fields
                  from the grafana_synthetic_monitoring_probe data source.
                properties:
                  disableBrowserChecks:
                    description: Disables browser checks for this probe.
                    type: boolean
                  disableScriptedChecks:
                    description: Disables scripted checks for this probe.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: Custom labels to be included with collected metrics
                      and logs.
                    type: object
                  latitude:
                    description: Latitude coordinates.
                    type: number
                  longitude:
                    description: Longitude coordinates.
                    type: number
                  public:
                    description: Public probes are run by Grafana Labs and can be
                      used by all users. Only Grafana Labs managed public probes will
                      be set to `true`.
                    type: boolean
                  region:
                    description: Region of the probe.
                    type: string
                  tenantID:
                    description: The tenant ID of the probe.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: probesets.sm.grafana.o.crossplane.io
spec:
  group: sm.grafana.o.crossplane.io
  names:
    categories:
    - crossplane
    kind: ProbeSet
    listKind: ProbeSetList
    plural: probesets
    singular: probeset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProbeSet is an observe-only resource backed by the grafana_synthetic_monitoring_probes
          Terraform data source.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              forProvider:
                description: ProbeSetParameters defines the input parameters for the
                  grafana_synthetic_monitoring_probes data source.
                properties:
                  filterDeprecated:
                    description: If true, only probes that are not deprecated will
                      be returned. Defaults to `true`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProbeSetObservation holds the observed (computed) fields
                  from the grafana_synthetic_monitoring_probes data source.
                properties:
                  filterDeprecated:
                    description: If true, only probes that are not deprecated will
                      be returned. Defaults to `true`.
                    type: boolean
                  probes:
                    additionalProperties:
                      type: string
                    description: Map of probes with their names as keys and IDs as
                      values.
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

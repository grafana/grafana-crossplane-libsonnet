# cloud.stack



## Index

* [`fn new(name, namespace, cloudProviderConfigName, secretName="<name>-providerconfig-token")`](#fn-new)

## Fields

### fn new

```jsonnet
new(name, namespace, cloudProviderConfigName, secretName="<name>-providerconfig-token")
```

PARAMETERS:

* **name** (`string`)
* **namespace** (`string`)
* **cloudProviderConfigName** (`string`)
* **secretName** (`string`)
   - default value: `"<name>-providerconfig-token"`

`new` creates a new Grafana Cloud Stack.
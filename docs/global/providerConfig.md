# global.providerConfig



## Index

* [`fn new(name, secretName, secretNamespace, secretKey)`](#fn-new)

## Fields

### fn new

```jsonnet
new(name, secretName, secretNamespace, secretKey)
```

PARAMETERS:

* **name** (`string`)
* **secretName** (`string`)
* **secretNamespace** (`string`)
* **secretKey** (`string`)

`new` creates a ProviderConfig.
# slowburn



## Subpackages

* [annotation](annotation.md)
* [enrichment](enrichment.md)
* [label](label.md)

## Index

* [`fn withAnnotation(value)`](#fn-withannotation)
* [`fn withAnnotationMixin(value)`](#fn-withannotationmixin)
* [`fn withEnrichment(value)`](#fn-withenrichment)
* [`fn withEnrichmentMixin(value)`](#fn-withenrichmentmixin)
* [`fn withLabel(value)`](#fn-withlabel)
* [`fn withLabelMixin(value)`](#fn-withlabelmixin)

## Fields

### fn withAnnotation

```jsonnet
withAnnotation(value)
```

PARAMETERS:

* **value** (`array`)

(Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
Annotations to attach only to Slow Burn alerts.
### fn withAnnotationMixin

```jsonnet
withAnnotationMixin(value)
```

PARAMETERS:

* **value** (`array`)

(Block List) Annotations will be attached to all alerts generated by any of these rules. (see below for nested schema)
Annotations to attach only to Slow Burn alerts.
### fn withEnrichment

```jsonnet
withEnrichment(value)
```

PARAMETERS:

* **value** (`array`)

(Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
Enrichments to attach only to Slow Burn alerts.
### fn withEnrichmentMixin

```jsonnet
withEnrichmentMixin(value)
```

PARAMETERS:

* **value** (`array`)

(Block List) Enrichments to attach only to Fast Burn alerts. (see below for nested schema)
Enrichments to attach only to Slow Burn alerts.
### fn withLabel

```jsonnet
withLabel(value)
```

PARAMETERS:

* **value** (`array`)

"^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
Labels to attach only to Slow Burn alerts.
### fn withLabelMixin

```jsonnet
withLabelMixin(value)
```

PARAMETERS:

* **value** (`array`)

"^[a-zA-Z_][a-zA-Z0-9_]*$" (see below for nested schema)
Labels to attach only to Slow Burn alerts.